// Phase 14.4 Wave 0 stub — filled in by plan 04
import { test, expect } from "../support/fixtures";

test.describe("Phase 14.4 / now-playing chip", () => {
  test.fixme("home renders chip with 'X listening now' text when play count >= 3", async ({ page }) => {
    // TODO: implement in plan 04
  });
  test.fixme("home suppresses chip when play count < 3", async ({ page }) => {
    // TODO: implement in plan 04
  });
  test.fixme("chip is absent on /songs catalog route", async ({ page }) => {
    // TODO: implement in plan 04
  });
  test.fixme("chip is absent on /songs/[slug] route", async ({ page }) => {
    // TODO: implement in plan 04
  });
});
