---
phase: 11.6-beginner-focused-practice-redesign
plan: 12
subsystem: tooling
tags: [unsplash-api, idempotent, rate-limit, gap-closure, ships-tooling-only, vocab-images]

# Dependency graph
requires:
  - phase: 11.4-personal-vocabulary-canon
    provides: vocabularyItems.image_url column + URL_PATTERN regex export from 19-curate-vocab-images.ts
  - phase: 11.6-02-comfyui-revert
    provides: ComfyUI revert (Task 1 of 11.6-02 v2 already shipped — this plan closes Tasks 2-3)
provides:
  - scripts/seed/19c-fetch-vocab-images.ts — idempotent Unsplash auto-search fetcher (full imageable catalog)
  - npm run fetch-images — wired entry point invoking the new script
  - .env.example documents UNSPLASH_ACCESS_KEY with registration instructions
  - public/vocab-images/ confirmed absent (SPEC-REQ-9 acceptance literal)
affects: [11.6-13 operator overnight batch run consumes this tooling]

# Tech tracking
tech-stack:
  added: [Unsplash Search Photos API integration via native fetch]
  patterns:
    - "Idempotent batch fetcher with per-row commit (no transaction wrapper)"
    - "In-process Map dedup keyed by english meaning to suppress duplicate API calls"
    - "Rate-limit retry loop reading X-Ratelimit-Reset + 5s buffer (max 3 attempts)"
    - "Single-source-of-truth import of URL_PATTERN regex from sibling seed script"

key-files:
  created:
    - scripts/seed/19c-fetch-vocab-images.ts (283 lines)
  modified:
    - package.json (added fetch-images npm script)
    - .env.example (added UNSPLASH_ACCESS_KEY block with registration link)

key-decisions:
  - "Did NOT remove public/vocab-images/ — directory was already absent on disk; rmdir step was a no-op (SPEC-REQ-9 already satisfied)"
  - "Placed fetch-images npm script entry next to seed:enrich-vocab (logical grouping with other vocab-pipeline scripts)"
  - "Used the exact runbook message verbatim from the plan to ensure a future operator gets the documented copy when UNSPLASH_ACCESS_KEY is unset"

patterns-established:
  - "Idempotent API-fetcher seed scripts: WHERE column IS NULL skip gate at SQL level + per-row UPDATE for crash-safe resumability"
  - "Cross-script regex sharing via export const + .js import (avoids drift between curator and fetcher)"

requirements-completed: [SPEC-REQ-8, SPEC-REQ-9]

# Metrics
duration: 3min
completed: 2026-05-02
---

# Phase 11.6 Plan 12: Unsplash Auto-Search Vocab Image Fetcher Summary

**Shipped the never-delivered Tasks 2-3 of Plan 11.6-02 v2: idempotent Unsplash auto-search fetcher (`scripts/seed/19c-fetch-vocab-images.ts`, 283 lines), wired `npm run fetch-images`, documented `UNSPLASH_ACCESS_KEY` in `.env.example`, and confirmed `public/vocab-images/` is absent. Operator can now invoke the overnight batch in Plan 11.6-13.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-05-02T18:01:59Z
- **Completed:** 2026-05-02T18:04:52Z
- **Tasks:** 2 / 2
- **Files modified:** 3 (1 created, 2 edited)

## Accomplishments

- Created `scripts/seed/19c-fetch-vocab-images.ts` (283 lines): idempotent Unsplash Search Photos fetcher with rate-limit retry, in-process meaning_en dedup, per-row commit, content_filter=high NSFW guard, URL_PATTERN validation gate.
- Wired `npm run fetch-images` to invoke the script via tsx + tsconfig.scripts.json.
- Documented `UNSPLASH_ACCESS_KEY` in `.env.example` with the full registration runbook (free demo tier, 50 req/hr, https://unsplash.com/developers).
- Verified all 6 plan-level verification commands pass (file exists, tsc clean, npm script wired, JSON valid, env doc present, public/vocab-images/ absent).
- Confirmed end-to-end fail-fast behaviour: with `DATABASE_URL` set and `UNSPLASH_ACCESS_KEY=""`, the script emits the exact runbook message and exits 1.

## Task Commits

Each task was committed atomically:

1. **Task 1: Create scripts/seed/19c-fetch-vocab-images.ts** — `af2c4e1` (feat)
2. **Task 2: Wire fetch-images npm script + .env.example doc + confirm dir absent** — `65ab2c7` (chore)

## Files Created/Modified

- `scripts/seed/19c-fetch-vocab-images.ts` — created — Idempotent Unsplash auto-search fetcher: builds worklist via raw SQL (POS allowlist + image_url IS NULL), runs an in-process meaning_en dedup Map, calls `https://api.unsplash.com/search/photos?...&content_filter=high` per row, validates returned URL against the imported `URL_PATTERN` regex, persists via `db.update().set({image_url}).where(...)` with no transaction wrapper, and handles HTTP 429 by sleeping until `X-Ratelimit-Reset + 5s` (max 3 attempts). Exports `runFetch` and `runVerify`.
- `package.json` — modified — Added `"fetch-images": "tsx --tsconfig tsconfig.scripts.json scripts/seed/19c-fetch-vocab-images.ts"` next to `seed:enrich-vocab`.
- `.env.example` — modified — Appended a 5-line block documenting `UNSPLASH_ACCESS_KEY`, the registration link, and the demo-tier rate limit.

## Verification Results

All 6 plan-level verification commands PASS:

| # | Check | Result |
|---|-------|--------|
| 1 | `test -f scripts/seed/19c-fetch-vocab-images.ts` | PASS |
| 2 | `npx tsc --noEmit -p tsconfig.scripts.json` (no 19c errors) | PASS |
| 3 | `grep "fetch-images" package.json && JSON.parse(...)` valid | PASS |
| 4 | `grep "UNSPLASH_ACCESS_KEY" .env.example` | PASS |
| 5 | `! test -d public/vocab-images` | PASS |
| 6 | `npm run fetch-images` emits exact runbook fail-fast | PASS |

Additional pattern checks (per Task 1 acceptance criteria):

| Check | Result |
|-------|--------|
| `URL_PATTERN` imported (not redefined) | PASS |
| `content_filter=high` present in API call | PASS |
| `x-ratelimit-reset` 429 handler present | PASS |
| `meaningEnCache` dedup Map present | PASS |
| No `db.transaction(...)` or `BEGIN` (no transaction wrapper) | PASS |
| `image_url` snake_case column reference | PASS |
| `export async function runFetch` present | PASS |
| `export async function runVerify` present | PASS |
| File length ≥ 150 lines | PASS (283 lines) |

### Exact Runbook Message

When `UNSPLASH_ACCESS_KEY` is unset (with `DATABASE_URL` set so the upstream gate passes), `npm run fetch-images` emits:

```
Error: UNSPLASH_ACCESS_KEY is not set. Register a free app at https://unsplash.com/developers, copy the Access Key from the app dashboard, and add it to .env.local as UNSPLASH_ACCESS_KEY=...
    at getAccessKey (.../scripts/seed/19c-fetch-vocab-images.ts:55:11)
    at runFetch (.../scripts/seed/19c-fetch-vocab-images.ts:137:21)
    ...
```

This is the verbatim runbook copy specified in the plan.

### Confirmation Snapshot

- `public/vocab-images/` does **not** exist on disk (was already absent before Task 2; rmdir was a no-op).
- `.env.example` now has `UNSPLASH_ACCESS_KEY=` documented with registration link.
- `.gitignore` contains no `public/vocab-images` reference (no re-introduction).
- `npm run fetch-images` is wired and bootstraps cleanly through dotenv → drizzle → getAccessKey gate.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking step skipped] Task 2 Step 3 (`rmdir public/vocab-images`) was a no-op**

- **Found during:** Task 2 setup (pre-Step-3 sanity check)
- **Issue:** The plan's Step 3 instructs `rmdir public/vocab-images` to satisfy SPEC-REQ-9. On disk inspection, `public/vocab-images/` already does not exist (likely cleaned earlier in the worktree base or never created since Plan 11.6-02 v2 Task 1 ComfyUI revert).
- **Fix:** Did not run `rmdir` (which would have failed with "directory does not exist"). Instead verified the postcondition (`! test -d public/vocab-images` returns 0) which is the actual SPEC-REQ-9 acceptance criterion.
- **Files modified:** none (no-op)
- **Commit:** N/A — observation only; the postcondition is satisfied either way.

### Auth Gates

None during this plan. Note: `UNSPLASH_ACCESS_KEY` is **not** required to complete Plan 11.6-12 — the plan only ships the tooling. The operator registers an Unsplash app in parallel and runs the actual batch in Plan 11.6-13.

## Plan 11.6-13 Pointer

This plan ships only the tooling. The full-catalog populate (~1,000 imageable rows, ~22-28 hr unattended at demo-tier 50 req/hr) is **Plan 11.6-13** (operator-driven). Pre-flight for 11.6-13:

1. Register Unsplash app at https://unsplash.com/developers (instant approval for demo tier).
2. Copy the Access Key into `.env.local` as `UNSPLASH_ACCESS_KEY=...`.
3. Run `npm run fetch-images` (bash) — script auto-resumes if interrupted; re-running on a fully-populated catalog produces 0 API calls (image_url IS NULL skip gate).
4. After completion, run `npm run fetch-images -- --verify` (or invoke `runVerify` directly) to confirm `populated > 0, missing == 0, invalid == 0`.

## Self-Check: PASSED

- File present: `scripts/seed/19c-fetch-vocab-images.ts` — FOUND
- Commit `af2c4e1` (Task 1: feat) — FOUND in git log
- Commit `65ab2c7` (Task 2: chore) — FOUND in git log
- All 6 plan-level verification commands — PASS
- All Task 1 grep/pattern acceptance checks — PASS
- All Task 2 grep/test acceptance checks — PASS
- No stubs introduced (CLI tool, no UI surface)
- No new threat surface beyond the plan's documented `<threat_model>` (T-11.6-12-01..05)
