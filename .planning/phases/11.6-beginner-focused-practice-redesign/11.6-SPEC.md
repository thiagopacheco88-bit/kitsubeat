# Phase 11.6: Beginner-Focused Practice Redesign — Specification

**Created:** 2026-05-01
**Ambiguity score:** 0.18 (gate: ≤ 0.20)
**Requirements:** 18 locked

## Goal

Restructure the song-page practice flow into three parallel beginner-focused tracks (Vocabulary, Grammar, Kanji) — with romaji + translation as the teaching surface, ComfyUI-batched mnemonic images, dual FSRS cards per word, JLPT-ordered lag-tested scheduling, per-verse "dominated" state with inline animation, and an Advanced Drills entry gated behind ≥80% per-track progress.

## Background

The exercise engine is feature-complete (7 exercise types, 3-star mastery, FSRS persistence, /review cross-song SRS queue, per-song completion %), but the practice surface is misaligned for beginners:

- **Furigana is taught** in the standard Vocab + Grammar exercise flow (via [src/lib/fsrs/tier.ts](src/lib/fsrs/tier.ts) tier-based reveal). Beginners need romaji + translation as the primary signal — furigana is display-only ornament.
- **One FSRS card per word** in `user_vocab_mastery` — there is no separation between "I know what this word means" and "I can produce it from kanji/kana". Mastery in one collapses both.
- **Exercise scheduling is encounter-order** — `buildQuestions` in [src/lib/exercises/generator.ts](src/lib/exercises/generator.ts) emits questions per-vocab in vocab-list order, and an introduced word can be tested at the next position. No JLPT ordering, no lag.
- **Advanced Drills is per-exercise-type free-quota gated** (10 listening + 3 advanced drills) rather than per-song-progress gated.
- **No Kanji track** — `kanji_breakdown` ships in the FeedbackPanel accordion (Phase 08.3) but has no dedicated drill mode.
- **No per-verse mastery surface** — the song page shows aggregate completion % only; users cannot see which specific verses they have or have not "owned".
- **No ComfyUI image generation** — Phase 11.4 wires `vocabulary_items.image_url` infra and ships 50 hand-curated Unsplash CC0 images for concrete top-frequency vocab; the remaining ~1,360 words have no image.

This phase rebuilds the practice loop around the beginner romaji-first mental model, splits per-word mastery into two FSRS cards, adds a Kanji-only drill track with a mnemonic image per word, lands a verse-by-verse "domination" state with an inline animation, and replaces the per-quota Advanced Drills gate with a three-ring per-song-progress gate.

## Requirements

1. **Three-track ExerciseTab structure**: ExerciseTab presents Vocabulary, Grammar, and Kanji as the three primary mode cards, each with a Short / Long toggle.
   - Current: ExerciseTab shows three cards — standard exercises, Listening Drills (10-song free quota), Advanced Drills (3-song free quota); no track separation; no length toggle.
   - Target: ExerciseTab renders Vocabulary, Grammar, Kanji cards (in that order) plus an Advanced Drills card below them. Each track card has a Short/Long toggle. Today's per-exercise-type free-quota gating is removed.
   - Acceptance: Playwright loads `/songs/[slug]` exercise tab and asserts (a) four mode cards visible: Vocabulary, Grammar, Kanji, Advanced Drills; (b) each track card exposes a Short/Long toggle control; (c) no card references the strings "Listening Drills" or the previous mode names; (d) clicking Vocab/Grammar/Kanji starts a session of the corresponding track.

2. **Vocabulary + Grammar tracks teach romaji, not furigana**: In Vocab and Grammar exercise prompts, romaji + translation are the highlighted teaching signal; furigana is shown as an unobtrusive display ornament only (no tier-based reveal-as-progress mechanic in these tracks).
   - Current: Vocab and Grammar prompts use [src/lib/fsrs/tier.ts](src/lib/fsrs/tier.ts) which gates furigana → kanji-only based on FSRS state, treating furigana removal as a teaching milestone.
   - Target: Vocab and Grammar tracks render `kanji + small-furigana + romaji` consistently, with romaji as the visual emphasis. Tier-based reveal is disabled for these tracks. Furigana stays present (small, dim) at all FSRS states; correct answer reveals translation if not visible.
   - Acceptance: Render Vocab and Grammar question cards at 3 different FSRS states (new, learning, review); assert romaji is present and visually emphasized in all three; assert furigana is present in all three; assert no tier-class transition logic fires for these tracks.

3. **Kanji track is per-song and drills kanji-bearing vocab**: New Kanji track that drills only the song's vocab items containing at least one kanji, using the mnemonic image + kanji_breakdown as the teaching surface.
   - Current: No Kanji-only drill mode exists; `kanji_breakdown` is shown only in the FeedbackPanel "More" accordion (Phase 08.3).
   - Target: Clicking the Kanji card builds a session of questions drawn from this song's vocabulary_items where `surface` contains at least one kanji codepoint. Each question shows the mnemonic image + kanji_breakdown + meaning, asks the user to produce the kana reading (romaji-typed input).
   - Acceptance: For a test song with N kanji-bearing vocab, the Kanji session emits exactly N questions (short) or up to 2N (long); zero questions reference non-kanji-bearing words; FeedbackPanel shows the same image rendered on Vocab track for the same word (proves shared `image_url`).

4. **Dual FSRS cards per word**: Each vocabulary_item carries two FSRS cards — `romaji_meaning` (driven by Vocab track) and `kanji_kana` (driven by Kanji track) — tracked independently in `user_vocab_mastery`.
   - Current: `user_vocab_mastery` has one row per (user_id, vocab_item_id); `recordVocabAnswer` updates a single FSRS scalar set.
   - Target: `user_vocab_mastery` carries a `card_kind` discriminator (`romaji_meaning` | `kanji_kana`); primary key becomes (user_id, vocab_item_id, card_kind); `recordVocabAnswer` accepts a `cardKind` argument and routes to the correct row. Existing rows are wiped and recreated under `card_kind = 'romaji_meaning'` (per Q1.3 (b)).
   - Acceptance: Vitest integration test inserts a Vocab answer (correct) and a Kanji answer (incorrect) for the same vocab_item_id, asserts two distinct rows exist with diverged FSRS state; existing pre-migration rows are gone post-migration (`SELECT count(*) FROM user_vocab_mastery WHERE card_kind IS NULL` returns 0).

5. **JLPT-ordered question queue**: Within a session, questions are sorted N5 → N4 → N3 → N2 → N1, with `jlpt_level IS NULL` items emitted last.
   - Current: `buildQuestions` emits questions in vocab-list order then sentence_order then per-grammar-point — no JLPT sort.
   - Target: Final question array is stable-sorted by `jlpt_level` ascending (N5 first), with nulls last. Sort is preserved across track types.
   - Acceptance: Vitest unit test feeds a mixed-JLPT vocab list to `buildQuestions`; asserts the emitted question array's `jlpt_level` values are monotonically non-decreasing with nulls at the tail.

6. **Lag-tested scheduling with SRS interleave**: Within a session, a vocab item introduced (LearnCard) at index N is never tested before index N+3; new introductions are interleaved with same-track SRS-due review items when both are present.
   - Current: A LearnCard at index N is followed by the question for the same vocab at index N+1.
   - Target: After a LearnCard at position N, the same vocab's first question appears at position ≥ N+3; if review-queue items are due in this track, they interleave with new introductions rather than appearing as a separate block.
   - Acceptance: Vitest integration test builds a session with 3 new vocab + 7 SRS-due reviews; asserts (a) for every introduced vocab item, the index gap between LearnCard and first question ≥3; (b) the resulting sequence does not contain a contiguous block of 3 new followed by 7 reviews — at least one review appears within the first 5 items.

7. **Romaji-typing exercise variant**: A typing-input exercise type exists where the user types the romaji reading of a kanji/kana surface and the answer is checked via [src/lib/exercises/romaji-normalize.ts](src/lib/exercises/romaji-normalize.ts) tolerance (e.g., `benkyou` / `benkyō` / `benkyō` all accepted).
   - Current: GrammarWriteCard uses `romaji-normalize` for grammar typing; no equivalent for vocab/kanji.
   - Target: A `vocab_typed` exercise type exists in the generator union; renders an input field; submission goes through the existing `normalizeRomaji` comparator; used by Kanji track (always) and Advanced Drills (some questions).
   - Acceptance: Vitest accepts inputs `benkyou`, `benkyō`, `benkyo-` (long-vowel hyphen), `BENKYOU` (case) for the answer 「勉強」 reading "benkyou"; rejects `benku` (truncated), `benkyu` (mistyped). Playwright e2e: type romaji into a Kanji track question, see correct feedback.

8. **ComfyUI mnemonic image batch — full catalog**: A local batch script generates a mnemonic image for every vocab item in the catalog (~1,409) using ComfyUI on the user's GPU and populates `vocabulary_items.image_url`.
   - Current: 50 Unsplash CC0 images populated by Phase 11.4-03 (only for top-frequency concrete vocab); 1,359 vocab items have `image_url IS NULL`.
   - Target: A new script (e.g. `scripts/seed/14-comfyui-batch-images.ts`) drives a local ComfyUI API workflow over every row in `vocabulary_items` where `image_url IS NULL`, writes PNG to `public/vocab-images/<vocab_item_id>.png`, sets `image_url = '/vocab-images/<vocab_item_id>.png'`. Idempotent (re-runnable; skips populated rows). Style prompt template uses `meaning` + `surface` to generate flat illustration on white background, no text.
   - Acceptance: After overnight batch, `SELECT count(*) FROM vocabulary_items WHERE image_url IS NULL` returns 0; sample of 10 random images render in LearnCard without 404; re-running the script makes 0 new ComfyUI calls.

9. **Image storage path**: Mnemonic images are served from `public/vocab-images/` and the directory is gitignored; the ComfyUI batch script is the source of truth and is re-runnable.
   - Current: `public/vocab-images/` does not exist; only `image_url` column exists.
   - Target: `public/vocab-images/` directory is created by the batch script on first run; `.gitignore` excludes it; URLs in `vocabulary_items.image_url` use the `/vocab-images/<vocab_item_id>.png` form for items generated by ComfyUI (Unsplash URLs from Phase 11.4 remain unchanged).
   - Acceptance: `git check-ignore public/vocab-images/sample.png` returns 0 (ignored); Next.js dev server returns 200 for any URL stored in `image_url` for a generated row.

10. **Per-song advanced drills unlock = ≥80% per track**: Advanced Drills is gated by per-song progress: ≥80% of vocab items answered correctly ≥1 time in the romaji-meaning card AND ≥80% of grammar items answered correctly ≥1 time AND ≥80% of kanji-bearing items answered correctly ≥1 time in the kanji-kana card.
   - Current: Advanced Drills is gated by free-quota counters in `user_exercise_song_counters` (3-song free quota).
   - Target: New per-song progress query computes the ≥80%-correct ratio per track; Advanced Drills button is disabled (with tooltip showing missing %) until all three pass; existing free-quota gating logic is removed.
   - Acceptance: Vitest integration test: with a song's 10 vocab + 5 grammar + 6 kanji items, simulate user answering 8/10 vocab correctly, 4/5 grammar, 5/6 kanji — assert Advanced Drills unlocks. Reduce kanji to 4/6 — assert Advanced Drills locks again.

11. **Three-ring unlock UI**: Song page renders three progress rings (Vocab / Grammar / Kanji) above the Advanced Drills card; ring fills proportionally to its track's per-song progress %; ring is "closed" (filled + colored) at 100%.
   - Current: No per-track progress ring exists on the song page.
   - Target: On `/songs/[slug]`, three SVG rings render above the Advanced Drills card with labels "Vocab", "Grammar", "Kanji"; each fills to `(items_correct_at_least_once / total_items_in_track) × 100%`; Advanced Drills button visually locked (greyed) when any ring < 80%, unlocked when all three ≥ 80%.
   - Acceptance: Playwright at three states (0/0/0%, 80/80/79%, 80/80/80%) asserts ring fill levels match expected and Advanced Drills button is `disabled=true` for the first two and `disabled=false` for the third.

12. **Advanced Drills content**: When unlocked, Advanced Drills runs a mixed-track session that includes Vocab, Grammar, and Kanji content; furigana is visible in this track (matching the higher-progress mental model); the session includes romaji-typing variants alongside multiple-choice.
   - Current: Advanced Drills today is the union of Grammar Conjugation + Listening Drill + Sentence Order with no display-mode change.
   - Target: Advanced Drills draws questions across all three tracks weighted 1:1:1; furigana is rendered on every question (not stripped); ≥30% of vocab/kanji questions are romaji-typed (production), the rest multiple-choice; Listening Drill + Sentence Order continue to be reachable from this entry.
   - Acceptance: Vitest builds a 30-question Advanced Drills session for a song where all three tracks have ≥10 items; asserts roughly 1:1:1 distribution (each track ±20%); asserts ≥9 of vocab+kanji questions are typed; asserts furigana attribute is present on all rendered questions.

13. **Per-verse domination state**: For each verse in a song, "dominated" = every applicable item (vocab, grammar, kanji) in that verse has been answered correctly at least once in the corresponding card; for tracks with zero applicable items in a given verse, that track auto-passes for that verse.
   - Current: No per-verse mastery state exists; the song page shows only aggregate completion %.
   - Target: A `user_verse_domination` table (or a derived query) tracks `(user_id, song_version_id, verse_number, dominated_at)` where `dominated_at` is set the moment the last required item in the verse is answered correctly across the three tracks.
   - Acceptance: Vitest integration test: a verse with 3 vocab + 1 grammar + 2 kanji items requires 6 correct answers across the right cards before `dominated_at` is set; the 6th correct answer triggers the write; the 7th (a re-answer) does not change `dominated_at`. A verse with 2 vocab + 0 grammar + 0 kanji becomes dominated after 2 correct vocab answers.

14. **Verse domination UI surfaces**: Song page header shows `🌟 X/Y verses` counter; lyrics view shows a yellow star next to each dominated verse; song catalog cards show `XX% dominated` per song.
   - Current: Song page shows aggregate exercise completion %; no per-verse symbol; song catalog cards show stars + bonus badge.
   - Target: (a) Song page header counter near the song title with star symbol + "X/Y verses"; (b) lyrics-view verse rows render a yellow star icon (⭐) at the start of any dominated verse; (c) song catalog `SongCard` renders an additional line under existing stars: `42% dominated` (rounded to nearest %).
   - Acceptance: Playwright at song page asserts (a) header counter displays `X/Y verses` matching DB state; (b) star icons render on exactly the dominated verses (count matches X); (c) catalog page card shows `% dominated` line matching DB state for the same song.

15. **Verse domination animation**: When the answer that tips a verse to "dominated" is submitted, an inline animation fires once (particle burst + "Verse dominated!" overlay); the animation does NOT re-fire on subsequent revisits of the same (user, verse).
   - Current: No verse-level animation exists; star unlocks fire confetti at session-end only.
   - Target: After a correct answer is recorded, server returns a `versesDominatedNow: number[]` flag indicating which verses tipped; the client triggers an inline animation overlay above/around the verse text in the FeedbackPanel for each one; the animation hooks the same canvas-confetti package already in deps.
   - Acceptance: Vitest unit test: simulate the tipping answer for verse 3 — assert the server response includes `versesDominatedNow: [3]`; simulate a re-answer of the same verse's items — assert response includes `versesDominatedNow: []`. Playwright: see the overlay render exactly once per (user, verse) across a session and a reload.

16. **All-kana song handling**: For a song where zero vocab items contain kanji, the Kanji track card is hidden from ExerciseTab; the kanji ring auto-passes (treated as 100%) for the per-song unlock gate; affected verses do not require any kanji answers to dominate.
   - Current: All catalog content has at least some kanji historically, but the gate logic is undefined for the all-kana edge case.
   - Target: ExerciseTab branches on `song has any kanji-bearing vocab` — if false, Kanji card is not rendered; the unlock gate's kanji clause auto-passes; per-verse domination requires only vocab + grammar items.
   - Acceptance: Vitest test with a song fixture where every vocab `surface` is hiragana-only — assert (a) Kanji card not present in ExerciseTab; (b) `getAdvancedDrillUnlock` returns true when only Vocab + Grammar each ≥80%; (c) verse domination triggers without kanji answers.

17. **/review queue extends to dual cards**: The cross-song /review SRS queue includes both `romaji_meaning` and `kanji_kana` due cards; daily new-card cap counts both card types together.
   - Current: /review queue ([src/lib/review/queue-builder.ts](src/lib/review/queue-builder.ts)) reads single-card `user_vocab_mastery` rows.
   - Target: Queue builder reads both card_kind values; review session UI exposes the card kind on the question (different question shape for `kanji_kana` vs `romaji_meaning`); daily new-card cap counts intros from both kinds against the same per-day budget.
   - Acceptance: Vitest test: with two due cards (one of each kind) for the same vocab_item_id, queue builder returns both as separate ReviewQueueItems; recording a new-card answer of either kind decrements the same `review_new_today` counter.

18. **/vocabulary dashboard extends to dual progress**: Each word row on the /vocabulary dashboard shows mastery for both card kinds (romaji-meaning and kanji-kana) instead of a single mastery indicator.
   - Current: /vocabulary dashboard ([src/app/vocabulary/page.tsx](src/app/vocabulary/page.tsx)) shows one mastery state per word, grouped by tier.
   - Target: Each word row renders two mastery indicators (e.g., two pills or two FSRS-state badges), one per card_kind; tier grouping is computed against the romaji-meaning card (the foundational track).
   - Acceptance: Render dashboard for a user with diverged card states (word A: romaji=Review, kanji=New; word B: both Review) — assert both indicators visible and distinct per word; tier grouping reflects romaji-meaning state.

## Boundaries

**In scope:**
- ExerciseTab UI restructure into Vocab/Grammar/Kanji + Advanced Drills, each with Short/Long toggle
- Furigana-as-teaching removed from Vocab + Grammar; display-only furigana retained
- Kanji track (per-song; drills kanji-bearing vocab; uses mnemonic image + kanji_breakdown)
- Dual FSRS cards per word (`card_kind`: `romaji_meaning` | `kanji_kana`); existing rows wiped
- Romaji-typing exercise variant (`vocab_typed`)
- ComfyUI batch image generation for the full ~1,409 vocab catalog → `public/vocab-images/`
- JLPT-ordered question queue (N5 → N1, nulls last)
- Lag-tested intra-session scheduling (intro→test gap ≥3) + SRS interleave
- Per-song Advanced Drills unlock at ≥80% across all three tracks; three-ring UI
- Advanced Drills mixed-track content with furigana visible + romaji typing variants
- Per-verse domination state (`user_verse_domination` or equivalent)
- Verse counter on song page header, star badge in lyrics view, % dominated on song card
- Inline once-per-(user, verse) "verse dominated" animation
- All-kana song special case (Kanji card hidden, kanji ring auto-passes)
- /review queue extension to dual cards (kanji-kana cards enter the queue; daily cap shared)
- /vocabulary dashboard dual progress display
- Removal of today's per-exercise-type free quotas (`user_exercise_song_counters` deprecated for `listening_drill` / `advanced_drill` families)

**Out of scope:**
- Phase 12 gamification surfaces (curated path, XP curve, streak rules, level-up takeover) — XP still earned from any track session, but this phase does not touch XP math, level curves, or path ordering
- /kana hiragana/katakana trainer — different scope (kana ≠ kanji); no changes
- Native kanji-writing input (handwriting / IME) — explicitly limited to romaji-typing per Q2 in initial discussion
- Catalog-wide kanji drill (cross-song) — Q2.1 chose per-song-only; cross-song kanji review rides on /review extension (R17), not a separate /kanji route
- Backwards compatibility shim for legacy single-card `user_vocab_mastery` rows — Q1.3 chose wipe-and-restart
- Image regeneration / curation tooling — ComfyUI batch is one-shot; if an image is bad the user manually swaps the file (no in-app retry UI)
- Bonus mastery badge re-derivation (today: ex5 + ex7) — Stars/bonus logic continues to read from existing accuracy columns; this phase does not change `deriveStars` / `deriveBonusBadge`
- Phase 11.5 Admin Lyrics Editor interactions — no integration; admin lyrics workflow is independent
- Listening Drill content gating change — Listening Drill remains reachable from Advanced Drills; its free-quota for non-Advanced-Drills entry is removed (covered by R10 gate change)
- Animation framework swap — verse-dominated animation reuses `canvas-confetti` already in deps; no new motion library

## Constraints

- **Local-only image generation** — ComfyUI runs on the user's GPU; no paid API or cloud render service. Aligned with project preference (no API credit spend).
- **Image storage in `public/vocab-images/`** — gitignored, regeneratable from the batch script. Estimated ~140MB at 1,409 × ~100KB; serves via Next.js static asset path.
- **FSRS scalar set extension** — `user_vocab_mastery` schema migration adds `card_kind` enum + composite primary key (user_id, vocab_item_id, card_kind); migration is destructive (existing rows wiped per Q1.3).
- **Bundle budget preservation** — Phase 13 set 50KB gzipped budget on `/songs/[slug]`; new ExerciseTab structure must not breach. Three-ring SVG, per-verse star icons, and animation overlay are additive and must fit within remaining headroom.
- **canvas-confetti reuse** — verse-dominated animation must not introduce a new motion/animation dependency; reuse existing package.
- **Romaji normalization compatibility** — `vocab_typed` reuses [src/lib/exercises/romaji-normalize.ts](src/lib/exercises/romaji-normalize.ts) verbatim; no new normalization rules added in this phase.
- **/review queue daily cap shared across card kinds** — `users.review_new_today` counts both new card_kinds against the same per-day budget; this preserves Phase 11's premium-gating semantics without introducing per-kind quotas.
- **All-kana detection at song level** — computed from `vocab_global` materialized view query; no schema column needed.

## Acceptance Criteria

- [ ] ExerciseTab shows four cards (Vocab, Grammar, Kanji, Advanced Drills) with Short/Long toggle on the three tracks
- [ ] Vocab and Grammar prompts render romaji emphasized + furigana display-only at all FSRS states (no tier-reveal in these tracks)
- [ ] Kanji track session emits questions only for kanji-bearing vocab from the song; uses shared `image_url` with Vocab track
- [ ] `user_vocab_mastery` has `card_kind` discriminator; pre-migration rows are gone; `recordVocabAnswer` routes by card_kind
- [ ] `buildQuestions` output is JLPT-sorted (N5 → N1, nulls last)
- [ ] In a session with new + SRS-due items, every introduced vocab has intro→test gap ≥3 and reviews are interleaved (not block-after-block)
- [ ] `vocab_typed` exercise type accepts long-vowel variants and case-insensitive input via `romaji-normalize`
- [ ] After ComfyUI batch, every row in `vocabulary_items` has a non-null `image_url`; re-runs are no-ops
- [ ] `public/vocab-images/` is gitignored and served by Next.js dev server
- [ ] Advanced Drills button is locked until ≥80% per track; locks again if any track regresses
- [ ] Three-ring UI fills proportional to per-track progress and renders a "locked/unlocked" affordance on the Advanced Drills button
- [ ] Advanced Drills session mixes 1:1:1 across tracks (±20%), shows furigana, includes ≥30% typed questions
- [ ] `user_verse_domination` (or equivalent) records the tipping moment per (user, verse); idempotent re-answers do not update `dominated_at`
- [ ] Song page header shows `🌟 X/Y verses`; lyrics view shows ⭐ next to dominated verses; catalog card shows `% dominated`
- [ ] "Verse dominated" inline animation fires once per (user, verse) on the tipping answer; no re-fire on revisit
- [ ] All-kana song hides Kanji card, kanji ring auto-passes, advanced drills unlock without kanji answers
- [ ] /review queue returns both `romaji_meaning` and `kanji_kana` due cards; daily new-card cap counts both kinds together
- [ ] /vocabulary dashboard shows dual progress (romaji + kanji) per word; tier grouping uses romaji-meaning state

## Ambiguity Report

| Dimension          | Score | Min  | Status | Notes                                         |
|--------------------|-------|------|--------|-----------------------------------------------|
| Goal Clarity       | 0.85  | 0.75 | ✓      | Concrete renames, tracks, gates, and UI surfaces |
| Boundary Clarity   | 0.85  | 0.70 | ✓      | Adjacent surfaces (Phase 12, /kana, /review, /vocabulary) explicitly classified |
| Constraint Clarity | 0.75  | 0.65 | ✓      | Local ComfyUI, gitignored storage, FSRS migration destructive |
| Acceptance Criteria| 0.80  | 0.70 | ✓      | 18 falsifiable checks; lag-test and animation testable in CI |
| **Ambiguity**      | 0.18  | ≤0.20| ✓      |                                               |

## Interview Log

| Round | Perspective     | Question summary                                              | Decision locked                                                                              |
|-------|-----------------|---------------------------------------------------------------|----------------------------------------------------------------------------------------------|
| 0     | Pre-spec scope  | Discussion across multiple turns: track structure, no-furigana, kanji track, dual cards, romaji typing, JLPT order, lag-test, 80% per-song unlock, three-ring, per-verse dominated, image gen | Established the 14 redesign decisions; user confirmed "go full on recommendations" for thresholds; ComfyUI on user's GPU; star symbol for dominated verses |
| 1     | Researcher / Boundary | Q1.1 Replace existing modes vs. layer on top? Q1.2 Image gen scope (full / kanji-bearing / capped / on-demand)? Q1.3 FSRS migration (preserve / wipe / shim)?  | (a) Replace all existing modes; (a) Full ~1,409 catalog ComfyUI batch (overnight, local, no token spend); (b) Wipe and restart FSRS state |
| 2     | Simplifier / Boundary | Q2.1 Kanji track per-song / catalog-wide / both? Q2.2 Image storage (public/ / Vercel Blob / Supabase)? Q2.3 Lag-test acceptance (strict / soft / strict + interleave)? | (a) Per-song; (a) `public/vocab-images/` gitignored; (c) Strict intro→test gap ≥3 + SRS interleave |
| 3     | Boundary Keeper / Acceptance | Q3.1 Adjacent surfaces (Phase 12 untouched / /vocabulary extended / /kana untouched / /review extended)? Q3.2 All-kana song handling (hide / disable / show)? Q3.3 Animation acceptance (once per (user,verse) / every time / per session)? | All (a): Phase 12 untouched, /vocabulary extended, /kana untouched, /review extended; (a) Hide Kanji card; (a) Once per (user, verse), persisted via dominated_at |

---

*Phase: 11.6-beginner-focused-practice-redesign*
*Spec created: 2026-05-01*
*Next step: /gsd-discuss-phase 11.6 — implementation decisions (schema migration shape, ComfyUI workflow JSON, three-ring SVG component, etc.)*
