---
phase: 11.6-beginner-focused-practice-redesign
verified: 2026-05-02T18:00:00Z
status: gaps_found
score: 16/18 spec requirements verified
overrides_applied: 0
gaps:
  - truth: "scripts/seed/19c-fetch-vocab-images.ts exists, is idempotent, handles 429, per-row commit, dedup by meaning_en"
    status: failed
    reason: "Script was planned in Plan 11.6-02 v2 but never created. Only the ComfyUI revert (Task 1) executed. Tasks 2 and 3 did not ship."
    artifacts:
      - path: "scripts/seed/19c-fetch-vocab-images.ts"
        issue: "File does not exist"
      - path: "package.json"
        issue: "No 'fetch-images' npm script entry"
      - path: ".env.example"
        issue: "No UNSPLASH_ACCESS_KEY documentation"
      - path: "public/vocab-images/"
        issue: "Empty directory exists; SPEC-REQ-9 requires it NOT to exist"
    missing:
      - "Create scripts/seed/19c-fetch-vocab-images.ts per Plan 11.6-02 v2 Task 2 spec"
      - "Add 'fetch-images' npm script to package.json"
      - "Add UNSPLASH_ACCESS_KEY documentation to .env.example"
      - "Remove empty public/vocab-images/ directory (SPEC-REQ-9 acceptance: does NOT exist)"
      - "Run overnight batch (Task 3, operator-driven) to populate vocabulary_items.image_url"

  - truth: "After full curation pass, every vocabulary_items row for imageable POS has non-null image_url"
    status: failed
    reason: "SPEC-REQ-8 full-catalog population cannot be run because 19c-fetch-vocab-images.ts does not exist. Pre-existing 50 rows from Phase 11.4 are the only populated images."
    artifacts:
      - path: "scripts/seed/19c-fetch-vocab-images.ts"
        issue: "Missing — batch cannot be executed"
    missing:
      - "Script delivery (19c) is the prerequisite; then operator runs 'npm run fetch-images' overnight"
      - "Verify: SELECT count(*) FROM vocabulary_items WHERE part_of_speech IN ('noun','verb','i_adjective','na_adjective') AND image_url IS NULL returns 0 after batch"

human_verification:
  - test: "ExerciseTab four-card layout renders correctly for a kanji song vs. an all-kana song"
    expected: "Kanji song shows 4 cards (Vocabulary, Grammar, Kanji, Advanced Drills); all-kana song shows 3 cards (Kanji card hidden)"
    why_human: "SSR conditional depends on song content; Playwright e2e tests cover this but require a running dev server with seeded data"

  - test: "Vocab and Grammar exercise prompts visually emphasize romaji at all FSRS states"
    expected: "romaji is visually prominent (larger or bolder); furigana is small and dimmed; no tier-reveal progression fires"
    why_human: "Visual styling verification cannot be asserted from code structure alone; requires rendering a question at new/learning/review FSRS states"

  - test: "VerseDominatedAnimation fires exactly once per (user, verse) and does not re-fire on revisit"
    expected: "canvas-confetti burst + text overlay on tipping answer; no animation on re-answer; no animation on page reload"
    why_human: "Once-per-(user,verse) idempotency depends on server returning empty versesDominatedNow on revisit — the code is wired correctly but visual confirmation requires live session"

  - test: "Three-ring UI fills proportionally and Advanced Drills button locks/unlocks correctly"
    expected: "At 0/0/0% all rings empty, button disabled; at 80/80/79% ring fills but button still disabled; at 80/80/80% all rings at threshold, button enabled (gold gradient)"
    why_human: "SVG stroke-dasharray computation and button color transition require visual verification with seeded progress data"

  - test: "SongCard shows '% dominated' line in catalog"
    expected: "Below the stars/bonus row, a text line 'NN% dominated' appears in muted small text when user has dominated at least one verse"
    why_human: "Data-flow from getAllSongs versesDominatedPct subquery through SongCard conditional render requires a user session with dominated verses"
---

# Phase 11.6: Beginner-Focused Practice Redesign — Verification Report

**Phase Goal:** Restructure the song-page practice flow into three parallel beginner-focused tracks (Vocabulary, Grammar, Kanji) with romaji + translation as the teaching surface, mnemonic images (Unsplash post-pivot), dual FSRS cards per word (romaji_meaning + kanji_kana), JLPT-ordered lag-tested scheduling, per-verse "dominated" state with inline animation, and Advanced Drills entry gated behind >=80% per-track progress.
**Authoritative spec:** 11.6-SPEC.md (18 locked requirements; R8/R9 post-pivot to Unsplash API)
**Verified:** 2026-05-02T18:00:00Z
**Status:** GAPS FOUND
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (by SPEC requirement)

| # | SPEC-REQ | Truth | Status | Evidence |
|---|----------|-------|--------|----------|
| 1 | REQ-1 | ExerciseTab shows 4 mode cards: Vocabulary, Grammar, Kanji, Advanced Drills with Short/Long toggle on tracks | VERIFIED | `ExerciseTab.tsx` renders `TrackCard` for vocab/grammar/kanji, each with Short/Long radio pair; Advanced Drills card below |
| 2 | REQ-2 | Vocab and Grammar prompts render romaji-emphasized + furigana display-only (tier.ts bypassed) | VERIFIED | `tier.ts::tierFor` returns TIER_NEW when `trackKind === 'vocab' \|\| trackKind === 'grammar'`; ExerciseTab passes trackKind |
| 3 | REQ-3 | Kanji track drills only kanji-bearing vocab; uses shared image_url; `vocab_typed` exercise type | VERIFIED | `generator.ts` filters via `hasKanji`; `ExerciseType` union includes `vocab_typed`; `VocabTypedCard` renders image + kanji |
| 4 | REQ-4 | Dual FSRS cards per word (`romaji_meaning` \| `kanji_kana`); existing rows wiped; `recordVocabAnswer` routes by cardKind | VERIFIED | Migration 0017 has `DELETE FROM user_vocab_mastery` + `card_kind` enum + composite unique; `exercises.ts` upsert keys on `(user_id, vocab_item_id, card_kind)` |
| 5 | REQ-5 | `buildQuestions` output is JLPT-sorted (N5 to N1, nulls last) | VERIFIED | `generator.ts` imports `sortByJlpt` from `scheduler.ts`; `JLPT_RANK` sorts N5→N1 with nulls last |
| 6 | REQ-6 | Lag-test: intro→test gap >=3; SRS reviews interleaved with new intros | VERIFIED | `scheduler.ts::buildSessionSequence` enforces minIntroToTestGap; `generator.ts` wraps output through scheduler |
| 7 | REQ-7 | `vocab_typed` exercise accepts long-vowel variants and case-insensitive input via `romaji-normalize` | VERIFIED | `VocabTypedCard.tsx` imports `romajiEquals` from `romaji-normalize.ts`; applied on submit |
| 8 | REQ-8 | After full curation pass, every imageable POS row has non-null `image_url`; re-runs are no-ops | FAILED | `scripts/seed/19c-fetch-vocab-images.ts` does not exist; batch cannot be run |
| 9 | REQ-9 | All `image_url` values match Unsplash CDN regex; `public/vocab-images/` does NOT exist; ComfyUI artifacts reverted | PARTIAL | ComfyUI revert complete (script/workflow/gitignore/npm/env all clean); `public/vocab-images/` directory EXISTS (empty — should be removed); `19c` script and `UNSPLASH_ACCESS_KEY` in .env.example are missing |
| 10 | REQ-10 | Advanced Drills locked until >=80% per track; computed from `user_song_progress` track pct columns | VERIFIED | `exercises.ts` sets `advanced_drills_unlocked_at` when all three track_pcts >= 80; ExerciseTab reads `advancedDrillsUnlocked` prop; button disabled accordingly |
| 11 | REQ-11 | Three SVG rings render above Advanced Drills card, fill proportional to track pct | VERIFIED | `TrackProgressRings.tsx` (88 lines); wired in `ExerciseTab.tsx`; SSR threads `track_pct` from `user_song_progress` via `page.tsx` |
| 12 | REQ-12 | Advanced Drills mixed-track session 1:1:1 (+/-20%), furigana visible, >=30% typed | VERIFIED | `generator.ts` TRACK_TYPES maps `advanced_drills` to mixed types; `buildQuestions` enforces distribution |
| 13 | REQ-13 | `user_verse_domination` records tipping moment; idempotent re-answers do not update `dominated_at` | VERIFIED | Migration 0017 creates table with composite PK; `exercises.ts` uses `INSERT ... ON CONFLICT DO NOTHING RETURNING verse_number` |
| 14 | REQ-14 | Song header shows X/Y verses counter; lyrics view shows star on dominated verses; SongCard shows % dominated | VERIFIED | `SongContent.tsx` renders `X/Y verses` with `VerseStarIcon`; `VerseBlock.tsx` renders star when `isDominated`; `SongCard.tsx` renders `% dominated` line from `versesDominatedPct` |
| 15 | REQ-15 | Verse domination animation fires once per (user, verse) on tipping answer; no re-fire | VERIFIED | `VerseDominatedAnimation.tsx` (108 lines) subscribes to `useExerciseSession.versesDominatedNow`; server returns empty array on revisit (ON CONFLICT DO NOTHING) |
| 16 | REQ-16 | All-kana song hides Kanji card; kanji ring auto-passes; verse domination without kanji answers | VERIFIED | `ExerciseTab.tsx` conditionally renders Kanji card on `hasKanjiBearingVocab` prop; `page.tsx` SSR computes via `hasKanji(entry.surface)` |
| 17 | REQ-17 | /review queue returns both card_kinds; daily new-card cap shared | VERIFIED | `queue-builder.ts` `DueCardInput` and `ReviewQueueItem` carry `card_kind`; `getDueReviewQueue` SELECTs `card_kind`; `ReviewSession.tsx` dispatches kanji_kana to `VocabTypedCard` |
| 18 | REQ-18 | /vocabulary dashboard shows dual progress (romaji + kanji) per word; tier grouping uses romaji_meaning | VERIFIED | `getVocabularyDashboard` uses `MAX(CASE WHEN card_kind = 'romaji_meaning')` aggregation; `VocabularyList.tsx` renders dual `MasteryBadge` components per row |

**Score: 16/18 requirements verified** (REQ-8 FAILED, REQ-9 PARTIAL)

---

### Deferred Items

None — the two gaps (REQ-8, REQ-9 partial) are not addressed in any later milestone phase. No later phase claims this image-pipeline work.

---

### Required Artifacts

| Artifact | Plan | Status | Details |
|----------|------|--------|---------|
| `drizzle/0017_dual_card_kind_and_verse_domination.sql` | 01 | VERIFIED | EXISTS; contains `DELETE FROM user_vocab_mastery`, BEGIN/COMMIT, composite PK, user_verse_domination table, 4 track_pct columns |
| `src/lib/db/schema.ts` | 01 | VERIFIED | `cardKindEnum`, `userVerseDomination`, `card_kind` on `userVocabMastery`, `vocab_track_pct` + `grammar_track_pct` + `kanji_track_pct` + `advanced_drills_unlocked_at` on `userSongProgress` |
| `tests/integration/setup-card-kind.ts` | 01 | VERIFIED | EXISTS; exports `seedDualCardFixtures` |
| 5 Wave 0 RED integration test stubs | 01 | VERIFIED | All 5 files exist with substantive assertions (212-277 lines each) |
| `scripts/seed/19c-fetch-vocab-images.ts` | 02v2 | MISSING | Was never created; only ComfyUI revert (Task 1) executed |
| `package.json` `fetch-images` script | 02v2 | MISSING | No `fetch-images` entry in scripts object |
| `.env.example` UNSPLASH_ACCESS_KEY | 02v2 | MISSING | File has only DATABASE_URL + CLERK vars; no Unsplash section |
| `public/vocab-images/` directory | 02v2 | SHOULD NOT EXIST | Empty directory present; SPEC-REQ-9 requires it to not exist |
| `src/lib/exercises/kanji.ts` | 03 | VERIFIED | EXISTS; exports `isKanji`, `kanjiSet`, `hasKanji`; 03b script imports from it |
| `src/lib/exercises/scheduler.ts` | 03 | VERIFIED | EXISTS; exports `buildSessionSequence`, `sortByJlpt`, `JLPT_RANK`, `SessionItem`, `SequencedItem` |
| Unit tests (lag-test, jlpt-sort, kanji-track-filter) | 03 | VERIFIED | All 3 test files exist in `src/lib/exercises/__tests__/` |
| `src/lib/exercises/generator.ts` | 04 | VERIFIED | `vocab_typed` in `ExerciseType`, `TrackKind`, `buildQuestions(trackKind, lengthMode)`, imports `buildSessionSequence` + `hasKanji` |
| `src/lib/exercises/__tests__/vocab-typed.test.ts` | 04 | VERIFIED | EXISTS |
| `src/lib/exercises/__tests__/advanced-drills-distribution.test.ts` | 04 | VERIFIED | EXISTS |
| `src/app/actions/exercises.ts` | 05 | VERIFIED | `recordVocabAnswer` accepts `cardKind`, Zod-validated, CTE recomputes track pcts, verse domination atomic insert, returns `versesDominatedNow` |
| `src/stores/exerciseSession.ts` | 05 | VERIFIED | `versesDominatedNow` slice with setter/clearer; NOT persisted |
| `tests/integration/verses-dominated-now-flag.test.ts` | 05 | VERIFIED | EXISTS; 494 lines |
| `src/app/songs/[slug]/components/ExerciseTab.tsx` | 06 | VERIFIED | 4 mode cards, Short/Long toggle, all-kana conditional, calls `buildQuestions` with trackKind+lengthMode |
| `src/lib/fsrs/tier.ts` | 06 | VERIFIED | `tierFor` returns TIER_NEW for `trackKind === 'vocab' \|\| 'grammar'` |
| `src/app/songs/[slug]/components/TrackProgressRings.tsx` | 07 | VERIFIED | 88 lines; SVG rings with prop-driven progress; imported by ExerciseTab |
| `src/app/songs/[slug]/page.tsx` | 07 | VERIFIED | SSR reads `track_pct` columns, threads to ExerciseTab and TrackProgressRings |
| `src/app/songs/[slug]/components/VerseStarIcon.tsx` | 08 | VERIFIED | 46 lines |
| `src/app/songs/[slug]/components/VerseDominatedAnimation.tsx` | 08 | VERIFIED | 108 lines; canvas-confetti dynamic import; subscribes to `versesDominatedNow` |
| `src/app/globals.css` | 08 | VERIFIED | `@keyframes verse-dominated-pulse` with `prefers-reduced-motion` fallback |
| `src/lib/db/queries.ts` getDominatedVerses + versesDominatedPct | 08 | VERIFIED | `getDominatedVerses` function exists; `getAllSongs` extended with `verses_dominated_pct` subquery |
| `src/app/songs/[slug]/components/VocabTypedCard.tsx` | 09 | VERIFIED | 173 lines; imports `romajiEquals`; renders image + kanji prompt; `spellCheck=false` |
| `src/app/songs/[slug]/components/LearnCard.tsx` | 09 | VERIFIED | `trackKind` prop; KanjiBreakdownSection lifted out of accordion when `trackKind === 'kanji'` |
| `src/app/songs/[slug]/components/ExerciseSession.tsx` | 09 | VERIFIED | Switch case `vocab_typed` → `<VocabTypedCard />`; dispatches `cardKind` to `recordVocabAnswer` |
| `src/lib/review/queue-builder.ts` | 10 | VERIFIED | `DueCardInput.card_kind`, `ReviewQueueItem.card_kind`; routes `kanji_kana` → `vocab_typed` |
| `src/app/actions/review.ts` | 10 | VERIFIED | `recordReviewAnswer` accepts `cardKind`; Zod-validated; passes through to `recordVocabAnswer` |
| `src/lib/db/queries.ts` getDueReviewQueue | 10 | VERIFIED | SELECTs `m.card_kind` from `user_vocab_mastery` |
| `src/app/review/ReviewSession.tsx` | 10 | VERIFIED | Imports `VocabTypedCard`; dispatches `kanji_kana` cards to `VocabTypedCard` |
| `src/lib/db/queries.ts` getVocabularyDashboard | 11 | VERIFIED | `MAX(CASE WHEN card_kind = 'romaji_meaning')` + `MAX(CASE WHEN card_kind = 'kanji_kana')` aggregation |
| `src/app/vocabulary/VocabularyList.tsx` | 11 | VERIFIED | Dual `MasteryBadge` components per word; tier grouping uses `romaji_state` |

---

### Key Link Verification

| From | To | Via | Status |
|------|----|-----|--------|
| `ExerciseTab.tsx` | `generator.ts::buildQuestions` | `handleStart(trackKind, lengthMode)` | WIRED |
| `page.tsx` (SSR) | `kanji.ts::hasKanji` | reduce over `song.vocab` | WIRED |
| `exercises.ts::recordVocabAnswer` | `user_vocab_mastery` | drizzle upsert on `(user_id, vocab_item_id, card_kind)` | WIRED |
| `exercises.ts::recordVocabAnswer` | `user_verse_domination` | `INSERT ... ON CONFLICT DO NOTHING RETURNING` | WIRED |
| `exercises.ts::recordVocabAnswer` | `user_song_progress` track_pct | single CTE recompute | WIRED |
| `exerciseSession.ts::versesDominatedNow` | `FeedbackPanel.tsx` via `VerseDominatedAnimation` | `useExerciseSession` selector | WIRED |
| `VerseBlock.tsx` | `dominatedVerseNumbers` prop | `props.dominatedVerseNumbers.includes(verse.verse_number)` | WIRED |
| `SongCard.tsx` | `song.verses_dominated_pct` | `getAllSongs` subquery | WIRED |
| `VocabTypedCard.tsx` | `romaji-normalize.ts::romajiEquals` | import + on submit | WIRED |
| `ExerciseSession.tsx` | `VocabTypedCard.tsx` | switch case `vocab_typed` | WIRED |
| `queue-builder.ts` | `ReviewSession.tsx` | `ReviewQueueItem.card_kind` | WIRED |
| `review.ts::recordReviewAnswer` | `exercises.ts::recordVocabAnswer` | passthrough `cardKind` | WIRED |
| `vocabulary/VocabularyList.tsx` | `getVocabularyDashboard` | dual-card aggregation rows | WIRED |
| `19c-fetch-vocab-images.ts` | `vocabulary_items.image_url` | drizzle update per row | NOT WIRED (script missing) |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `TrackProgressRings.tsx` | `vocab/grammar/kanji` props | `user_song_progress.{vocab,grammar,kanji}_track_pct` via SSR in `page.tsx` | CTE recompute on every answer in `exercises.ts` | FLOWING |
| `SongContent.tsx` verse counter | `dominatedVerseNumbers.length / totalVerses` | `getDominatedVerses` query + `user_verse_domination` table | DB write on tipping answer; SSR per page load | FLOWING |
| `SongCard.tsx` % dominated | `versesDominatedPctNum` | `getAllSongs` subquery over `user_verse_domination` | DB-computed subquery | FLOWING |
| `VocabularyList.tsx` dual badges | `row.romaji_state`, `row.kanji_state` | `getVocabularyDashboard` GROUP BY aggregation | Real DB query with MAX(CASE WHEN card_kind) | FLOWING |
| `VocabTypedCard.tsx` | `question.image_url` | `vocabulary_items.image_url` (Phase 11.4 50 rows + future 19c batch) | Only 50 rows populated; rest NULL (19c not run) | STATIC for most vocab |

---

### Behavioral Spot-Checks

| Behavior | Check | Status |
|----------|-------|--------|
| `kanji.ts` exports all three functions | `grep -n "export function" src/lib/exercises/kanji.ts` returns `isKanji`, `kanjiSet`, `hasKanji` | PASS |
| `scheduler.ts` exports `buildSessionSequence` | `grep "export function buildSessionSequence" src/lib/exercises/scheduler.ts` | PASS |
| `generator.ts` includes `vocab_typed` in union | `grep "vocab_typed" src/lib/exercises/generator.ts` returns multiple matches | PASS |
| `recordVocabAnswer` accepts `cardKind` | `grep "cardKind" src/app/actions/exercises.ts` returns Zod-validated `CardKind` at boundary | PASS |
| `19c-fetch-vocab-images.ts` exists | `test -f scripts/seed/19c-fetch-vocab-images.ts` | FAIL |
| ComfyUI revert complete | `test ! -f scripts/seed/20-comfyui-batch-images.ts && ! grep -q gen-image package.json` | PASS |
| Migration 0017 has DELETE FROM | `grep "DELETE FROM" drizzle/0017_*.sql` | PASS |
| `userVerseDomination` in schema.ts | `grep "userVerseDomination" src/lib/db/schema.ts` | PASS |

---

### Requirements Coverage

REQUIREMENTS.md does not contain SPEC-REQ-1..18 entries — these requirements live entirely within `11.6-SPEC.md` as a self-contained locked specification. No cross-reference to the project REQUIREMENTS.md is possible; the ROADMAP.md references them by name (`SPEC-REQ-1` through `SPEC-REQ-18`). All 18 are verified directly against the codebase above.

| SPEC-REQ | Plan(s) | Status |
|----------|---------|--------|
| REQ-1 (four-card ExerciseTab) | 06 | SATISFIED |
| REQ-2 (romaji-emphasized, tier bypass) | 06 | SATISFIED |
| REQ-3 (Kanji track, kanji-bearing filter) | 03, 04, 09 | SATISFIED |
| REQ-4 (dual FSRS cards, card_kind) | 01, 05 | SATISFIED |
| REQ-5 (JLPT-sorted question queue) | 03, 04 | SATISFIED |
| REQ-6 (lag-tested scheduling, SRS interleave) | 03, 04 | SATISFIED |
| REQ-7 (vocab_typed romaji input) | 04, 09 | SATISFIED |
| REQ-8 (full catalog image population) | 02v2 | BLOCKED — 19c script missing |
| REQ-9 (Unsplash CDN only; ComfyUI reverted) | 02v2 | PARTIAL — revert complete; 19c script + env + npm script missing; empty dir exists |
| REQ-10 (>=80% per-track unlock) | 01, 05 | SATISFIED |
| REQ-11 (three-ring UI) | 07 | SATISFIED |
| REQ-12 (Advanced Drills 1:1:1 mix) | 04, 07 | SATISFIED |
| REQ-13 (user_verse_domination idempotent) | 01, 05 | SATISFIED |
| REQ-14 (verse counter + star + % dominated) | 08 | SATISFIED |
| REQ-15 (once-per-(user,verse) animation) | 05, 08 | SATISFIED |
| REQ-16 (all-kana song edge case) | 01, 05, 06 | SATISFIED |
| REQ-17 (/review dual-card queue) | 10 | SATISFIED |
| REQ-18 (/vocabulary dual progress) | 11 | SATISFIED |

---

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `public/vocab-images/` directory | Empty directory should not exist (SPEC-REQ-9 acceptance) | Warning | Does not affect runtime behavior; contradicts spec acceptance criteria |
| `src/app/actions/exercises.ts` L464 | `card_kind: "romaji_meaning"` hardcoded in `saveSessionResults` batch path (separate from `recordVocabAnswer`) | Warning | The batch-write path `saveSessionResults` still hardcodes `romaji_meaning`; only the interactive `recordVocabAnswer` was extended. No blocker for Phase 11.6 plans but could cause Kanji track answers to route to wrong card in batch mode |

---

### Human Verification Required

**Note:** The five items below are purely visual/behavioral checks. All automated artifact and wiring checks passed for the 16 verified requirements.

#### 1. Four-Card ExerciseTab vs. All-Kana Song

**Test:** Navigate to a song with kanji-bearing vocab (e.g., any standard song) and a song where all vocab surfaces are hiragana-only.
**Expected:** Kanji song shows 4 cards (Vocabulary, Grammar, Kanji, Advanced Drills); all-kana song shows 3 cards (Kanji card absent).
**Why human:** Requires running dev server with seeded DB content; Playwright e2e tests exist but need live server.

#### 2. Romaji-Emphasized Rendering in Vocab/Grammar Tracks

**Test:** Start a Vocabulary track session on any song at different FSRS states (new word, returning word, review-due word).
**Expected:** Romaji reading is visually prominent in each question card; furigana is shown small/dimmed; no tier-reveal animation fires.
**Why human:** Visual hierarchy (font size, color, opacity) cannot be asserted from code alone.

#### 3. Verse Domination Animation Once-Per Occurrence

**Test:** Answer all required items in a verse correctly. Watch for canvas-confetti + overlay. Then re-answer one item from the same verse.
**Expected:** Animation fires on the tipping answer; no animation fires on re-answer; animation does not fire on page reload.
**Why human:** Requires a live session with a user account and verse state tracking; visual confirmation required.

#### 4. Three-Ring Lock/Unlock UI Transition

**Test:** With DB state at 80/80/79% track pcts, verify Advanced Drills button is disabled with tooltip. Answer one more kanji item to tip to 80%. Verify button becomes gold and enabled.
**Expected:** Rings fill proportionally; button toggles from gray (disabled) to gold gradient (enabled) on the tipping answer without page reload.
**Why human:** Requires seeded progress data and visual verification of the color transition and tooltip text.

#### 5. SongCard "% dominated" Line in Catalog

**Test:** On the `/songs` catalog page, find a song where the current user has dominated at least one verse.
**Expected:** A line reading `NN% dominated` appears below the star/bonus badge row on that song's card, in small muted text.
**Why human:** Requires user session with actual verse domination data; the conditional render (`showDominatedPct`) requires `versesDominatedPctNum > 0`.

---

### Gaps Summary

**Two requirements are blocked by incomplete Plan 11.6-02 v2 execution.**

Plan 11.6-02 v2 had three tasks:
- Task 1 (revert ComfyUI): **COMPLETE** — all 5 artifacts cleaned (`20-comfyui-batch-images.ts` deleted, `comfyui-workflows/` deleted, `.gitignore` entry removed, `gen-image` npm script removed, `COMFYUI_*` env vars removed from `.env.example`)
- Task 2 (create `19c-fetch-vocab-images.ts`): **NOT DONE** — script does not exist; `fetch-images` npm script missing; `UNSPLASH_ACCESS_KEY` not documented in `.env.example`
- Task 3 (operator overnight batch run): **NOT DONE** — depends on Task 2

The 11.6-02-SUMMARY.md that exists is the **v1 (ComfyUI) summary** marked as superseded. No v2 summary was written. The gap strongly suggests Task 2 was intended but not executed in the current session.

Additionally, `public/vocab-images/` is an empty directory that per SPEC-REQ-9 should not exist. This is cosmetic but violates the acceptance criteria literally.

All remaining 16 SPEC requirements (REQ-1 through REQ-7, REQ-10 through REQ-18) are **VERIFIED** with substantive, wired, data-flowing implementations. The phase core — dual FSRS cards, verse domination, three-track ExerciseTab, lag-test scheduler, three-ring UI, verse animation, /review extension, /vocabulary dual progress — is fully delivered.

---

_Verified: 2026-05-02T18:00:00Z_
_Verifier: Claude (gsd-verifier)_
