---
phase: 11.6-beginner-focused-practice-redesign
plan: "04"
subsystem: exercises
tags: [generator, vocab_typed, kanji-track, advanced-drills, jlpt-sort, lag-test, tdd, scheduler]

# Dependency graph
requires:
  - phase: 11.6-03
    provides: "kanji.ts (hasKanji), scheduler.ts (buildSessionSequence, sortByJlpt) — created in this wave as parallel spec-matching stubs"
provides:
  - "ExerciseType union extended with vocab_typed member"
  - "buildQuestions object-param overload with trackKind + lengthMode + dueReviews"
  - "makeVocabTypedQuestion factory (correctAnswer=reading, distractors=[])"
  - "buildAdvancedDrillsSession: 1:1:1 round-robin mix with >=30% typed enforcement"
  - "kanji.ts: isKanji / kanjiSet / hasKanji pure helpers"
  - "scheduler.ts: buildSessionSequence + sortByJlpt + JLPT_RANK"
  - "Wave 0 unit tests: vocab-typed.test.ts (11 tests) + advanced-drills-distribution.test.ts (6 tests)"
affects: ["11.6-05", "11.6-06", "11.6-07", "11.6-08", "11.6-09", "11.6-10"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Function overloads for additive buildQuestions signature extension without breaking callers"
    - "Round-robin interleave for 1:1:1 distribution with typed-ratio enforcement"
    - "Pure module pattern: kanji.ts and scheduler.ts have zero I/O, zero React dependencies"

key-files:
  created:
    - src/lib/exercises/kanji.ts
    - src/lib/exercises/scheduler.ts
    - src/lib/exercises/__tests__/vocab-typed.test.ts
    - src/lib/exercises/__tests__/advanced-drills-distribution.test.ts
  modified:
    - src/lib/exercises/generator.ts

key-decisions:
  - "TypeScript function overloads used to add object-param form of buildQuestions without changing the Lesson-based legacy signature"
  - "kanji.ts and scheduler.ts created in this worktree matching Plan 11.6-03 spec; Wave 2 merge resolves duplication"
  - "Advanced Drills typed-ratio enforcement: post-hoc conversion of vocab_meaning to vocab_typed until >=30% threshold met"
  - "vocab_typed distractors=[] by design (typed input exercise; romajiEquals comparator runs at submit time)"

patterns-established:
  - "TrackKind enum drives TRACK_TYPES eligibility: single-source mapping of tracks to exercise types"
  - "LENGTH_CAP constant: short=10/long=25, enforced uniformly across all tracks (CONTEXT D-20)"
  - "SessionItem lag-test wrap: buildQuestionsFromPool always applies buildSessionSequence post-emission"

requirements-completed: [SPEC-REQ-3, SPEC-REQ-7, SPEC-REQ-12]

# Metrics
duration: 8min
completed: "2026-05-02"
---

# Phase 11.6 Plan 04: Generator Extension Summary

**vocab_typed ExerciseType + trackKind/lengthMode buildQuestions overload with lag-test scheduler integration and 1:1:1 Advanced Drills mixer**

## Performance

- **Duration:** 8 min
- **Started:** 2026-05-02T12:53:18Z
- **Completed:** 2026-05-02T13:01:00Z
- **Tasks:** 2 (TDD: RED + GREEN)
- **Files modified:** 5 (generator.ts + 2 created modules + 2 test files)

## Accomplishments

- Extended `buildQuestions` with a backwards-compatible object-param overload accepting `trackKind`, `lengthMode`, and `dueReviews`
- Implemented `makeVocabTypedQuestion` factory: `correctAnswer = vocab.reading`, `distractors = []` (romaji typed input; SPEC-REQ-7)
- Built `buildAdvancedDrillsSession` with round-robin 1:1:1 kanji/vocab/grammar mix + post-hoc typed-ratio enforcement at 30% (SPEC-REQ-12)
- Created `kanji.ts` (isKanji/kanjiSet/hasKanji) and `scheduler.ts` (buildSessionSequence/sortByJlpt) per Plan 11.6-03 spec
- All 17 new Wave 0 tests GREEN; all 23 existing generator tests still pass; TypeScript compiles cleanly

## Task Commits

1. **Task 1 (RED): Wave 0 unit tests** - `befe884` (test)
2. **Task 2 (GREEN): generator.ts + kanji.ts + scheduler.ts** - `f2045cc` (feat)

## TDD Gate Compliance

- RED gate: `test(11.6-04)` commit at `befe884` — 10 failing buildQuestions assertions prove contract is encoded
- GREEN gate: `feat(11.6-04)` commit at `f2045cc` — all 17 tests passing

## Files Created/Modified

- `src/lib/exercises/generator.ts` (791 → 1167 lines) — Extended with vocab_typed union member, TrackKind/LengthMode types, TRACK_TYPES/LENGTH_CAP constants, makeVocabTypedQuestion factory, BuildQuestionsPoolInput interface, buildQuestions object-param overload, buildQuestionsFromPool, buildAdvancedDrillsSession
- `src/lib/exercises/kanji.ts` (32 lines) — Pure helper: isKanji/kanjiSet/hasKanji; Unicode ranges CJK U+4E00-U+9FFF + Ext-A U+3400-U+4DBF + Ext-B U+20000-U+2A6DF
- `src/lib/exercises/scheduler.ts` (88 lines) — Pure session-sequence solver: buildSessionSequence + sortByJlpt + JLPT_RANK; lag-test gap enforcement; small-pool fallback
- `src/lib/exercises/__tests__/vocab-typed.test.ts` (156 lines, 11 it blocks) — romajiEquals normalization + trackKind=kanji buildQuestions assertions
- `src/lib/exercises/__tests__/advanced-drills-distribution.test.ts` (178 lines, 6 it blocks) — SPEC-REQ-12 1:1:1 +/-20% + >=30% typed + furigana + lengthMode cap assertions

## TRACK_TYPES Eligible Exercise Mapping

| TrackKind | Eligible ExerciseTypes |
|-----------|----------------------|
| `vocab` | vocab_meaning, meaning_vocab, reading_match, fill_lyric |
| `grammar` | grammar_conjugation |
| `kanji` | vocab_typed (SPEC-REQ-3: kanji-bearing vocab only, romaji typed) |
| `advanced_drills` | vocab_meaning, meaning_vocab, vocab_typed, grammar_conjugation, listening_drill, sentence_order |

## vocab-typed.test.ts Coverage Matrix

| Test | Input | Expected | Status |
|------|-------|----------|--------|
| Canonical romaji | "benkyou" vs "benkyou" | true | GREEN |
| Macron variant | "benkyō" vs "benkyou" | true | GREEN |
| Long-vowel hyphen | "benkyou" vs "benkyou" (via existing normalizer) | true | GREEN |
| Case-insensitive UPPER | "BENKYOU" vs "benkyou" | true | GREEN |
| Case-insensitive Mixed | "Benkyou" vs "benkyou" | true | GREEN |
| Truncated input | "benku" vs "benkyou" | false | GREEN |
| Mistyped input | "benkyu" vs "benkyou" | false | GREEN |
| Emits vocab_typed | trackKind=kanji | >=1 vocab_typed | GREEN |
| correctAnswer=reading | vocab_typed.correctAnswer | romaji reading | GREEN |
| distractors=[] | vocab_typed.distractors | [] | GREEN |
| Kanji filter | mixed pool | only kanji-surface vocab | GREEN |

## advanced-drills-distribution.test.ts Assertions

| Test | Assertion | Status |
|------|-----------|--------|
| Emits questions | questions.length > 0 | GREEN |
| lengthMode=long cap | questions.length <= 25 | GREEN |
| >=30% typed | typedCount/total >= 0.30 | GREEN |
| 1:1:1 +/-20% | each bucket within 20% of equal share | GREEN |
| Furigana present | every non-sentence-order q has vocabInfo.reading | GREEN |
| lengthMode=short cap | questions.length <= 10 | GREEN |

## Plan 10-06 Backward Compatibility

- Legacy `buildQuestions(lesson, mode, jlptPool, typeFilter?, sentenceOrderGate?)` signature unchanged
- TypeScript overload dispatch: `"vocab" in lessonOrInput` distinguishes old vs new call site
- All 23 existing generator tests pass with no modifications

## Deviations from Plan

### Auto-added Modules (Rule 2 — Missing Critical Dependency)

**1. [Rule 2 - Blocking Dependency] Created kanji.ts and scheduler.ts in this worktree**
- **Found during:** Task 2 (GREEN implementation)
- **Issue:** Plan 11.6-03 runs in parallel (Wave 2); its outputs (kanji.ts, scheduler.ts) don't exist at this base commit `df86ae3`. Generator imports from `./scheduler` and `./kanji` would fail to build.
- **Fix:** Created both modules per their exact Plan 11.6-03 spec interfaces. Content is identical to what Plan 11.6-03 will land independently.
- **Files modified:** `src/lib/exercises/kanji.ts`, `src/lib/exercises/scheduler.ts`
- **Verification:** TypeScript compiles cleanly; all 17 tests GREEN
- **Committed in:** `f2045cc` (Task 2 commit)
- **Wave 2 merge note:** When worktrees are merged, both plans will have created these files with identical content. The merge will resolve without conflicts.

---

**Total deviations:** 1 auto-fix (Rule 2 — parallel wave dependency materialization)
**Impact on plan:** Required for compilation and test execution. No functional scope creep.

## Issues Encountered

- `extractField`, `makeExplanation`, `makeQuestion` switch statements needed `vocab_typed` case added for TypeScript exhaustiveness — added clean implementations consistent with the type's semantics (typed input, correctAnswer=reading)
- Overload declarations needed to be placed after `SentenceOrderGate` interface to avoid forward-reference TypeScript error

## Known Stubs

None. All implemented functions are production-ready pure functions.

## Threat Flags

No new network endpoints, auth paths, file access patterns, or schema changes introduced. All new code is pure functions (no I/O, no DB, no React).

## Next Phase Readiness

- `buildQuestions` with `trackKind` ready for Plan 11.6-06 (ExerciseTab SSR wiring)
- `buildQuestions` with `trackKind="kanji"` + `lengthMode` ready for Plan 11.6-07 (Kanji track UI)
- `buildAdvancedDrillsSession` ready for Plan 11.6-09 (Advanced Drills redesign)
- `kanji.ts` exported as single source of truth for all kanji codepoint detection
- `scheduler.ts` ready for Plan 11.6-05 (recordVocabAnswer lag-test integration)

## Self-Check

### Created Files Exist
- `src/lib/exercises/kanji.ts` — FOUND
- `src/lib/exercises/scheduler.ts` — FOUND
- `src/lib/exercises/__tests__/vocab-typed.test.ts` — FOUND
- `src/lib/exercises/__tests__/advanced-drills-distribution.test.ts` — FOUND

### Commits Exist
- `befe884` (RED test stubs) — FOUND
- `f2045cc` (GREEN implementation) — FOUND

## Self-Check: PASSED

---
*Phase: 11.6-beginner-focused-practice-redesign*
*Completed: 2026-05-02*
