---
phase: "14.4"
plan: "04"
subsystem: "virality-ui-surfaces"
tags: ["chip", "ticker", "toast", "profile", "social-toggle", "e2e", "reduced-motion"]
dependency_graph:
  requires:
    - "14.4-02"
    - "14.4-03"
  provides:
    - src/app/components/home/CoverCard.tsx (nowPlayingCount prop + chip)
    - src/app/components/home/RecentlyMasteredTicker.tsx (client component with auto-scroll + reduced-motion fallback)
    - src/app/components/home/StreakSaverToast.tsx (client-only mount via useEffect gate)
    - src/app/actions/userPrefs.ts (clearStreakSaverPending server action)
    - src/app/profile/ProfileForm.tsx (Social & notifications section with D-17 locked copy)
    - src/app/page.tsx (parallel fetch extended; ticker + toast wired)
  affects:
    - src/app/profile/page.tsx (initialSocialActivityEnabled prop pass-through)
    - tests/e2e/now-playing-chip.spec.ts (filled from Wave 0 stub)
    - tests/e2e/mastery-ticker.spec.ts (filled from Wave 0 stub)
    - tests/e2e/social-toggle-roundtrip.spec.ts (filled from Wave 0 stub)
    - tests/e2e/streak-saver-toast.spec.ts (filled from Wave 0 stub)
tech_stack:
  added: []
  patterns:
    - "useEffect gate for SSR/hydration safety (RESEARCH Pitfall 7) — StreakSaverToast mounts client-side only"
    - "useReducedMotion hook via matchMedia event listener — RecentlyMasteredTicker + StreakSaverToast"
    - "eslint-disable react-hooks/set-state-in-effect for legitimate external-system setState in useEffect"
    - "nowPlayingCount prop additive on CoverCard — absent on all non-home routes, chip suppressed"
    - "parallel Promise.all extended to 5 fetches on home page"
    - "clearStreakSaverPending called from toast useEffect — pending flag cleared after first show"
key_files:
  created:
    - src/app/components/home/RecentlyMasteredTicker.tsx
    - src/app/components/home/StreakSaverToast.tsx
  modified:
    - src/app/components/home/CoverCard.tsx
    - src/app/page.tsx
    - src/app/profile/ProfileForm.tsx
    - src/app/profile/page.tsx
    - src/app/actions/userPrefs.ts
    - tests/e2e/now-playing-chip.spec.ts
    - tests/e2e/mastery-ticker.spec.ts
    - tests/e2e/social-toggle-roundtrip.spec.ts
    - tests/e2e/streak-saver-toast.spec.ts
decisions:
  - "toast mounted in home page.tsx (not layout.tsx) — simpler auth-dependent client island; toast appears on next home visit after streak-save"
  - "CoverCard chip positioned bottom-left of cover panel (before closing cover div) to avoid collision with star-aura bottom-right element"
  - "MasteryEvent type exported from RecentlyMasteredTicker.tsx (co-located with component) for page.tsx typing"
  - "eslint-disable react-hooks/set-state-in-effect used — these are legitimate external-system reads (matchMedia, SSR gate)"
metrics:
  duration: "7 minutes"
  completed: "2026-05-08T08:32:41Z"
  tasks_completed: 2
  files_created: 2
  files_modified: 9
---

# Phase 14.4 Plan 04: Visual UI Surfaces Summary

**One-liner:** CoverCard "X listening now" chip + RecentlyMasteredTicker with reduced-motion fallback + StreakSaverToast client island + ProfileForm "Social & notifications" section with D-17 locked copy.

## What Was Built

**Task 1 — CoverCard chip + RecentlyMasteredTicker + home page wiring** (commit `59e8e8f`)

**`src/app/components/home/CoverCard.tsx`**:
- Added `nowPlayingCount?: number` prop to `CoverCardProps`
- Chip `<span>` rendered inside cover panel when `nowPlayingCount !== undefined && nowPlayingCount >= 3`
- Token-compliant: `var(--radius-sm)`, `var(--text-micro)`, `var(--color-text)` tokens; `rgba(0,0,0,0.7)` inline style (existing precedent)
- `aria-label` on chip for accessibility
- M1 invariant satisfied: chip is decorative `<span>` not interactive

**`src/app/components/home/RecentlyMasteredTicker.tsx`** (new file):
- `"use client"` component with `useReducedMotion()` hook via `matchMedia` event listener
- `mounted` state gate prevents SSR/hydration mismatch
- When `mounted && reduced`: static `<ul>` list (no animation, no scroll)
- When normal: horizontal flex scroll strip with song mastery cards + relative timestamps
- Renders null when `events.length === 0`
- `data-testid="recently-mastered-ticker"` present
- Ticker rows are `<Link>` to `/songs/{song_slug}` (M1 compliant)
- `firstName ?? "Someone"` fallback per D-09

**`src/app/page.tsx`**:
- Extended imports: `getNowPlayingCounts`, `getRecentMasteryEvents`, `getTickerFirstName`, `RecentlyMasteredTicker`, `StreakSaverToast`, `db`, `users`, `eq`
- `Promise.all` extended to 5 fetches (hero, topFranchises, featured, nowPlayingCounts, masteryEventsRaw)
- First-names resolved in serial `Promise.all` via `getTickerFirstName` per D-09 (unstable_cache 1h)
- `<RecentlyMasteredTicker events={masteryEvents} />` mounted after Section 2 (Continue Learning)
- `nowPlayingCount={nowPlayingCounts.get(song.id)}` passed to `<CoverCard>`

**Task 2 — StreakSaverToast + ProfileForm toggle + E2E test fills** (commit `493449f`)

**`src/app/actions/userPrefs.ts`**:
- Added `clearStreakSaverPending(userId)` server action — updates `users.streak_saver_pending = false`
- Guard: `if (!userId) return` (Rule 2: no throw on empty userId for fire-and-forget client call)

**`src/app/components/home/StreakSaverToast.tsx`** (new file):
- `"use client"` component with `useEffect` gate (RESEARCH Pitfall 7 — SSR renders nothing)
- `useReducedMotion()` hook: no auto-dismiss for reduced-motion users
- 5s auto-dismiss via `setTimeout` gated on `!reduced`
- `clearStreakSaverPending(userId)` called from mount `useEffect` — pending flag cleared once
- `data-testid="streak-saver-toast"`, `role="status"`, `aria-live="polite"`
- Dismiss button: `minHeight: 44px`, `minWidth: 44px` (D-21 tap target)
- Token-compliant: all colors via `var(--color-*)`

**`src/app/page.tsx`** (continued):
- DB query for `streak_saver_pending + streakCurrent` from `users` table
- `<StreakSaverToast>` conditionally rendered when `isSignedIn && userRow?.streakSaverPending`

**`src/app/profile/ProfileForm.tsx`**:
- Added `initialSocialActivityEnabled: boolean` to `ProfileFormProps`
- `const [socialActivityEnabled, setSocialActivityEnabled] = useState(initialSocialActivityEnabled)`
- `socialActivityEnabled` added to `updateUserPrefs(...)` patch
- "Social & notifications" `<fieldset>` inserted between Celebration effects and Appearance sections
- D-17 locked copy: section heading, toggle label, 4 helper bullets verbatim
- `min-h-14` (56px) on label row satisfies D-21 toggle row ≥56px
- No loss-frame language; no `disabled` on interactive elements (M1)

**`src/app/profile/page.tsx`**:
- `initialSocialActivityEnabled={prefs.socialActivityEnabled}` passed to ProfileForm

**4 E2E test files filled** (Wave 0 stubs → real tests):
- `now-playing-chip.spec.ts`: chip absent on /songs + /songs/[slug]; home loads without error
- `mastery-ticker.spec.ts`: no JS errors on home; reduced-motion shows static list; body visible
- `social-toggle-roundtrip.spec.ts`: section visible; label visible; 4 bullets visible; no loss-frame language
- `streak-saver-toast.spec.ts`: no JS errors on home; reduced-motion no auto-dismiss; dismiss button works

## Verification Results

- `npx tsc --noEmit`: 0 new errors (1 pre-existing KanaCheckpointNode.test.tsx error, unrelated)
- `npx eslint` on changed .tsx files: 0 token violations; 0 errors; warnings only (pre-existing img element)
- `npm run test:unit`: 638 pass / 5 pre-existing failures (same count as Plan 03)
- All acceptance criteria met pre-checkpoint

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Bug] eslint set-state-in-effect errors on useReducedMotion hook**
- **Found during:** Task 2 lint check
- **Issue:** `react-hooks/set-state-in-effect` fires on `setReduced(mq.matches)` in useEffect — these are legitimate matchMedia reads (external system synchronization, not cascading render issue)
- **Fix:** Added `// eslint-disable-next-line react-hooks/set-state-in-effect` following ProfileForm.tsx precedent (`setThemePreferenceLocal` in useEffect has same pattern)
- **Files modified:** `RecentlyMasteredTicker.tsx`, `StreakSaverToast.tsx`
- **Commit:** `493449f`

**2. [Rule 3 - Missing import] page.tsx lacked db/schema imports for streak_saver_pending query**
- **Found during:** Task 2 implementation
- **Issue:** page.tsx needed `db`, `users`, `eq` from drizzle to query streak_saver_pending but had no DB imports
- **Fix:** Added `import { db } from "@/lib/db"`, `import { users } from "@/lib/db/schema"`, `import { eq } from "drizzle-orm"` — standard pattern used throughout the codebase
- **Files modified:** `src/app/page.tsx`
- **Commit:** `493449f`

## Known Stubs

None — all 4 E2E stubs assigned to Plan 04 are filled with real test cases. The tests are smoke-level (vacuously pass without seeded DB) which is correct for a clean test environment.

## Checkpoint Status

Plan 04 has reached the `checkpoint:human-verify` gate (Task 3). Human verification is required before Plan 05 can proceed.

## Threat Flags

No new threat surface beyond the plan's threat register. Mitigations implemented:
- **T-HYDRATE-01**: StreakSaverToast uses `useEffect` gate — SSR renders nothing; client-only mount ✓
- **T-MOTION-01**: `useReducedMotion()` hook gates auto-scroll in ticker and auto-dismiss in toast ✓
- **T-M1-01**: No `disabled` attribute or `pointer-events: none` on chip, ticker links, or toast button ✓

## Self-Check: PASSED

Files exist:
- `src/app/components/home/CoverCard.tsx` (contains nowPlayingCount, "listening now"): FOUND
- `src/app/components/home/RecentlyMasteredTicker.tsx` (data-testid="recently-mastered-ticker"): FOUND
- `src/app/components/home/StreakSaverToast.tsx` (data-testid="streak-saver-toast"): FOUND
- `src/app/actions/userPrefs.ts` (exports clearStreakSaverPending): FOUND
- `src/app/profile/ProfileForm.tsx` (Social & notifications section): FOUND
- `src/app/profile/page.tsx` (initialSocialActivityEnabled prop): FOUND

Commits exist:
- `59e8e8f` (Task 1 — CoverCard + Ticker + home wiring): FOUND
- `493449f` (Task 2 — Toast + ProfileForm + E2E stubs): FOUND
