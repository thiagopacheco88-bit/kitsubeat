---
phase: "14.4"
plan: "05"
type: execute
wave: 5
depends_on:
  - "14.4-04"
files_modified: []
autonomous: false
requirements:
  - REQ-1
  - REQ-2
  - REQ-3
  - REQ-4
  - REQ-5
  - REQ-6
  - REQ-7
  - REQ-8

must_haves:
  truths:
    - "Migration 0018 applied to the target database (local dev DB)"
    - "Full test suite passes: unit + integration + E2E"
    - "npm run build exits 0 with no errors"
    - "npm run lint exits 0 with no ESLint violations"
    - "Bundle delta for / is within +10 KB soft cap"
    - "All 16 SPEC acceptance criteria met"
    - "Vercel plan tier confirmed (Pro required for hourly cron)"
  artifacts:
    - path: drizzle/0018_virality_engagement.sql
      provides: "Applied migration confirmed via schema_migrations table"
      contains: "social_activity_enabled"
    - path: .planning/phases/14.4-virality-engagement/14.4-05-SUMMARY.md
      provides: "Phase completion record"
  key_links:
    - from: scripts/apply-migrations.ts
      to: drizzle/0018_virality_engagement.sql
      via: "tsx scripts/apply-migrations.ts auto-discovers and applies 0018"
      pattern: "apply-migrations"
---

<objective>
Apply the database migration to the local development database, run the complete test suite, verify the bundle delta, confirm the Vercel plan tier for the hourly cron, and perform a final copy review for loss-frame language across all Phase 14.4 deliverables.

Purpose: This plan is the phase gate. No code is written — it is the verification pass that confirms all previous plans delivered what the SPEC requires. The schema push is a BLOCKING step that must complete before the phase is considered done.

Output:
- Migration 0018 applied to local dev DB
- Full test suite green
- Build passing
- Bundle delta within target
- Vercel plan pre-flight confirmation
- Phase summary
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.4-virality-engagement/14.4-SPEC.md
@.planning/phases/14.4-virality-engagement/14.4-VALIDATION.md
@.planning/phases/14.4-virality-engagement/14.4-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Apply migration 0018 to local dev database</name>
  <files></files>

  <read_first>
    - scripts/apply-migrations.ts (lines 1-50 — confirm invocation pattern; auto-discovers all .sql sorted alphabetically)
    - drizzle/0018_virality_engagement.sql (verify it exists and contains expected DDL before running)
    - .env.local (to confirm DATABASE_URL is set — do not print the value)
  </read_first>

  <action>
Run the migration apply script to apply 0018 to the local development database:

```bash
cd C:/Users/thiag/velora-projects/kitsubeat && npx tsx scripts/apply-migrations.ts
```

The script auto-discovers all .sql files in drizzle/ sorted alphabetically. It checks the `schema_migrations` table to skip already-applied files. Migration 0018 should appear as newly applied in the output.

If the script exits with an error (e.g., column already exists), check whether 0018 was already applied. If `schema_migrations` contains the filename, the migration was applied on a previous attempt — this is safe to proceed.

After the script completes, verify the migration was applied by checking the schema_migrations table indirectly:
```bash
cd C:/Users/thiag/velora-projects/kitsubeat && npx tsx -e "
  import { config } from 'dotenv';
  import { join } from 'path';
  config({ path: join(process.cwd(), '.env.local') });
  const { Client } = await import('@neondatabase/serverless');
  const c = new Client(process.env.DATABASE_URL);
  await c.connect();
  const { rows } = await c.query(\"SELECT filename FROM schema_migrations WHERE filename = '0018_virality_engagement.sql'\");
  console.log('Applied:', rows.length > 0 ? 'YES' : 'NO');
  await c.end();
" 2>&1
```

If it prints "Applied: YES", the migration is live. Proceed to Task 2.

If DATABASE_URL is not set in .env.local, create a checkpoint for the user to supply the connection string before proceeding.
  </action>

  <verify>
    <automated>cd C:/Users/thiag/velora-projects/kitsubeat && npx tsx scripts/apply-migrations.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsx scripts/apply-migrations.ts` exits 0
    - Output contains "0018_virality_engagement.sql" (either "applied" or "already applied")
    - No error messages about missing tables or constraint violations
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    Migration 0018 needs to be verified against the production/staging database as well if you want to ensure the schema changes are live before the next deploy. The apply-migrations script runs automatically on `vercel-build`, so the migration will also apply on Vercel deploy.
  </what-built>

  <how-to-verify>
    **Vercel Plan Tier Pre-Flight (REQUIRED before deploying vercel.json):**

    1. Run `vercel projects ls` or check https://vercel.com/dashboard
    2. Confirm the KitsuBeat project is on **Vercel Pro** plan
    3. If on **Hobby plan**: The `vercel.json` with `"schedule": "0 * * * *"` (hourly) will cause deployment to FAIL with "Hobby accounts are limited to daily cron jobs"
       - Option A: Upgrade to Vercel Pro ($20/mo) before deploying
       - Option B: Remove the daily-reminder cron entry from vercel.json for now, keeping only the weekly recap (which runs once/day and is allowed on Hobby). The daily-reminder route will still exist as code but won't be triggered automatically.

    **Resend Account Pre-Flight:**

    4. Confirm Resend account exists and plan tier:
       - Free tier: 3,000 emails/month + 100/day limit
       - At 10% opt-in rate (~500 users), estimated 1,550 emails/week → 6,200/month — **requires Paid plan ($20/mo for 50K emails/month)**
       - If still on free tier for development: `RESEND_API_KEY` can remain unset locally (routes run in dry-run mode automatically per D-05)

    5. Confirm the sender domain is verified in Resend Dashboard:
       - If `kitsubeat.app` domain is verified: use `noreply@kitsubeat.app` as FROM address (already in the code)
       - If NOT verified: the emails will fail to deliver. Either verify the domain first, or change the FROM address to `onboarding@resend.dev` (Resend's shared domain, works immediately but FROM shows "KitsuBeat via Resend")

    **Environment Variables to Set in Vercel:**

    6. In Vercel Dashboard → Project → Settings → Environment Variables, add:
       - `CRON_SECRET` = (random 32-char string, e.g., run `openssl rand -hex 16`)
       - `RESEND_API_KEY` = (from Resend Dashboard → API Keys)

    Type "confirmed" when you have verified the Vercel plan tier, or describe any blockers.
  </how-to-verify>

  <resume-signal>Type "confirmed Pro" or "confirmed Hobby - removed daily cron" or describe blockers</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Full test suite + build + lint + bundle delta</name>
  <files></files>

  <read_first>
    - .planning/phases/14.4-virality-engagement/14.4-VALIDATION.md (sampling strategy and acceptance criteria to verify)
    - .planning/phases/14.4-virality-engagement/14.4-SPEC.md (16 acceptance criteria to check off)
  </read_first>

  <action>
Run the complete verification sequence in order:

**Step 1 — Capture pre-baseline for comparison (if not done in Plan 01)**

If the bundle baseline was captured in Plan 01 summary, use that value. Otherwise:
```bash
cd C:/Users/thiag/velora-projects/kitsubeat && npm run build 2>&1 | grep -E "Route \(/\)|First Load JS" | head -5
```

**Step 2 — Unit tests**

```bash
cd C:/Users/thiag/velora-projects/kitsubeat && npm run test:unit 2>&1
```

Expected: All unit tests pass. Key tests to verify specifically:
- `src/lib/streak/saver.test.ts` — 7 grant/consume cases green
- `src/lib/emails/dailyReminder.test.ts` — subject anti-loss-frame check green
- `src/lib/emails/weeklyRecap.test.ts` — 4-section order check green
- `src/lib/emails/send.test.ts` — dry-run path green
- `src/lib/cron/auth.test.ts` — 401/200 paths green

**Step 3 — Integration tests**

```bash
cd C:/Users/thiag/velora-projects/kitsubeat && npm run test:integration 2>&1
```

Expected: All integration tests pass (or skip if TEST_DATABASE_URL not set). Key tests:
- `email-sent-log-idempotency` — composite PK ON CONFLICT behavior green
- `streak-saver-grant-consume` — DB column verification green
- `activity-events-emission` — ON CONFLICT DO NOTHING green
- `now-playing-counts` — opt-in filter green

**Step 4 — Lint check (ESLint token compliance)**

```bash
cd C:/Users/thiag/velora-projects/kitsubeat && npm run lint 2>&1
```

Expected: 0 violations from `kitsubeat-tokens/no-raw-tokens` on new files.

**Step 5 — TypeScript check**

```bash
cd C:/Users/thiag/velora-projects/kitsubeat && npx tsc --noEmit 2>&1
```

Expected: 0 errors.

**Step 6 — Build + bundle delta**

```bash
cd C:/Users/thiag/velora-projects/kitsubeat && npm run build 2>&1
```

Expected:
- Build exits 0 (no errors)
- `/` route First Load JS delta ≤ +10 KB vs pre-baseline (soft cap D-22)
- `/songs/[slug]` route unchanged (still within 50 KB hard gate from Phase 13)

**Step 7 — Manual copy review (automated grep)**

Search for loss-frame language across all Phase 14.4 deliverables:

```bash
cd C:/Users/thiag/velora-projects/kitsubeat && grep -rn "don't lose\|you'll fail\|you failed\|miss out\|ends in" src/lib/emails/ src/app/components/home/StreakSaverToast.tsx src/app/profile/ProfileForm.tsx 2>&1
```

Expected: 0 matches (per D-17 + SPEC AC #17).

**Step 8 — SPEC acceptance criteria checklist**

Review all 16 SPEC acceptance criteria and confirm each is met:

| # | Criterion | Status |
|---|-----------|--------|
| 1 | Migration adds `users.social_activity_enabled boolean NOT NULL DEFAULT false` | verify: migration file contains it |
| 2 | Migration adds `users.streak_saver_token integer NOT NULL DEFAULT 0 CHECK(0,1)` | verify: migration file |
| 3 | Migration adds `activity_events` with UNIQUE(user_id, song_id) | verify: migration file |
| 4 | Migration adds `email_sent_log` with composite PK | verify: migration file |
| 5 | Home SongCard renders chip ≥3, suppressed otherwise | verify: CoverCard.tsx nowPlayingCount prop + threshold check |
| 6 | Home mastery ticker renders last 10 opt-in events | verify: RecentlyMasteredTicker + getRecentMasteryEvents |
| 7 | Ticker auto-scrolls; prefers-reduced-motion → static list | verify: RecentlyMasteredTicker useReducedMotion |
| 8 | Settings has "Social & notifications" toggle | verify: ProfileForm.tsx section heading |
| 9 | Helper text has 4 bullets | verify: ProfileForm.tsx 4 <li> elements present |
| 10 | /api/cron/daily-reminder: 401 without CRON_SECRET, ≤1 email/user/day at 19:00 | verify: assertCronSecret + idempotency check |
| 11 | /api/cron/weekly-recap: 401 without CRON_SECRET, ≤1 email/user/week Sunday 18:00 UTC | verify: assertCronSecret + email_sent_log |
| 12 | Weekly recap body has 4 sections in fixed order | verify: weeklyRecap.ts render function |
| 13 | Streak-saver: granted at first streak=7, consumed on reset, re-grantable | verify: saver.ts + test results |
| 14 | Streak-saver toast renders once after consume, reduced-motion = static banner | verify: StreakSaverToast useReducedMotion |
| 15 | All email paths dry-run when RESEND_API_KEY absent | verify: send.ts + cron routes |
| 16 | Home bundle ≤ +10 KB vs Phase 14.2 baseline | verify: build output delta |
| 17 | Zero loss-frame language in emails/settings/toast | verify: grep from Step 7 |

Record any failing criteria as items for gap closure.

**Step 9 — E2E smoke run (optional, if Playwright browser available)**

```bash
cd C:/Users/thiag/velora-projects/kitsubeat && npx playwright test --project=chromium tests/e2e/cron-auth.spec.ts tests/e2e/social-toggle-roundtrip.spec.ts 2>&1 | tail -30
```

If browser is not available in the current environment, skip and note for human verification.
  </action>

  <verify>
    <automated>cd C:/Users/thiag/velora-projects/kitsubeat && npm run test:unit && npm run lint && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - `npm run test:unit` exits 0
    - `npm run lint` exits 0
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
    - grep for loss-frame terms returns 0 matches across email/settings/toast files
    - Build output shows `/` route First Load JS delta ≤ +10 KB vs baseline
    - `npx tsx scripts/apply-migrations.ts` exits 0 with 0018 applied
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 14.4 Virality & Engagement is complete. All deliverables:

    **REQ-1 (Now Playing):** CoverCard on home renders "{N} listening now" chip when ≥3 distinct learners in last 30 min. Chip absent on /songs, /path, /songs/[slug].

    **REQ-2 (Mastery Ticker):** RecentlyMasteredTicker shows last 10 song-mastered events from opt-in users with Clerk first-name. Auto-scrolls; static list under prefers-reduced-motion.

    **REQ-3 (Opt-in Toggle):** users.social_activity_enabled defaults false. Toggle added to /profile under "Social & notifications".

    **REQ-4 (Daily Reminder):** /api/cron/daily-reminder route — hourly, CRON_SECRET-gated, timezone-aware 19:00 filter, email_sent_log idempotency, dry-run when RESEND_API_KEY absent.

    **REQ-5 (Weekly Recap):** /api/cron/weekly-recap route — Sunday 18:00 UTC, 4 fixed sections, per-week idempotency.

    **REQ-6 (Streak-Saver):** streak_saver_token column, grant at 7-day milestone, auto-consume on reset, StreakSaverToast on next session.

    **REQ-7 (Settings Copy):** "Social & notifications" section with 4 enumerated helper bullets. Zero loss-frame language.

    **REQ-8 (Infra):** resend installed, CRON_SECRET + RESEND_API_KEY env vars documented, email_sent_log table, vercel.json created.

    Migration 0018 applied. All unit/integration tests pass. Build and lint clean.
  </what-built>

  <how-to-verify>
    1. Visit http://localhost:7000 — home page renders without errors
    2. Check browser DevTools console — no JavaScript errors
    3. Visit /profile — "Social & notifications" section visible with toggle and 4 bullet points
    4. Confirm no bullet contains "don't lose", "miss out", "ends in", or loss-frame language
    5. Test toggle: enable "Social & notifications" → save → refresh → toggle should still be ON
    6. Run `curl -X GET http://localhost:7000/api/cron/daily-reminder` → should return `{"status":401}` or `401 Unauthorized`
    7. With CRON_SECRET set in .env.local, run: `curl -H "Authorization: Bearer {your-secret}" http://localhost:7000/api/cron/daily-reminder` → should return 200 with `dry_run: true` (since RESEND_API_KEY not set locally)
    8. Bundle delta acceptable (check build output from Task 2)
    9. Confirm Vercel plan tier decision was made in earlier checkpoint
  </how-to-verify>

  <resume-signal>Type "phase-complete" to finalize. Or describe any issues for gap closure planning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev → Neon DB | Migration script uses DATABASE_URL from .env.local |
| Vercel deploy → DB | apply-migrations.ts runs in vercel-build; same migration applied to production DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-MIGR-01 | Tampering | 0018 migration idempotency | mitigate | IF NOT EXISTS guards on all DDL; BEGIN/COMMIT atomicity; schema_migrations tracks applied files |
| T-ENV-01 | Information Disclosure | CRON_SECRET in env | accept | High-entropy random string; set only in Vercel env vars (never in .env.local committed to git) |
</threat_model>

<verification>
Phase 14.4 is complete when:
1. Migration 0018 applied successfully
2. All unit tests pass
3. Integration tests pass (or skipped with TEST_DATABASE_URL absent)
4. Build exits 0 with no errors
5. Lint exits 0
6. Bundle delta on `/` ≤ +10 KB
7. Zero loss-frame terms in email/settings/toast content
8. Human approval on final checkpoint
</verification>

<success_criteria>
- Migration 0018 applied to dev DB
- `npm run test:unit` green
- `npm run build` green
- `npm run lint` green
- Bundle delta on `/` within soft cap
- Human verification confirms all 8 REQs are working
- Vercel plan tier confirmed for deploy
</success_criteria>

<output>
After completion, create `.planning/phases/14.4-virality-engagement/14.4-05-SUMMARY.md`
</output>
