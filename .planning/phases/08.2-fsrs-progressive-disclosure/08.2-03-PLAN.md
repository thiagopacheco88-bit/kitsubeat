---
phase: 08.2-fsrs-progressive-disclosure
plan: 03
type: execute
wave: 3
depends_on: ["08.2-01", "08.2-02"]
files_modified:
  - src/app/songs/[slug]/components/ExerciseTab.tsx
  - src/app/songs/[slug]/components/ExerciseSession.tsx
  - src/app/songs/[slug]/components/QuestionCard.tsx
  - src/app/songs/[slug]/components/FeedbackPanel.tsx
  - src/app/songs/[slug]/components/SessionSummary.tsx
  - src/app/songs/[slug]/components/MasteryDetailPopover.tsx
  - src/app/songs/[slug]/components/TierText.tsx
  - src/stores/exerciseSession.ts
  - src/lib/exercises/generator.ts
autonomous: true
requirements:
  - EXER-08
  - EXER-09
  - EXER-10
  - DATA-02

must_haves:
  truths:
    - "On 'Start session', ExerciseTab fetches tiers for every question's vocabItemId in ONE call to /api/exercises/vocab-tiers and seeds the session store"
    - "QuestionCard renders every vocab-surface prompt and every kanji-bearing answer option through TierText, applying the per-vocab tier (with the leak overrides documented in Task 2)"
    - "Reading-match prompts and meaning→vocab answer options NEVER show furigana/romaji on the answer-revealing surface (exercise-semantic override beats tier)"
    - "FeedbackPanel ALWAYS renders the target vocab block at Tier 1 (kanji + furigana + romaji + meaning), regardless of stored tier"
    - "Each answered question fires recordVocabAnswer({ userId, vocabItemId: question.vocabItemId, songVersionId, exerciseType: question.type, correct, responseTimeMs }) exactly once before the user advances"
    - "Distractor vocab IDs are NEVER passed to recordVocabAnswer — only the question's target"
    - "A 'Reveal reading' button is visible while the question is unanswered; tapping it (a) toggles the prompt to Tier 1 view inline and (b) flags the next answer so recordVocabAnswer is called with revealedReading: true (rated Again)"
    - "Tier in the session store is updated optimistically after the recordVocabAnswer response so a re-encounter of the same word later in the session shows the new tier (mid-session 'flicker' is acceptable per CONTEXT)"
    - "FeedbackPanel and SessionSummary expose a tap-to-inspect mastery details popover (reps, correct %, state, next-due) backed by /api/exercises/vocab-mastery/[vocabItemId]"
    - "Player lyrics (LyricsPanel) and the song-page VocabularySection are NOT modified — tier rendering is exercise-flow-only"
  artifacts:
    - path: "src/app/songs/[slug]/components/TierText.tsx"
      provides: "<TierText vocab={VocabInfo} tier={Tier} mode='prompt'|'option'|'feedback' /> — single render component honoring tier + leak override"
      exports: ["default"]
      min_lines: 40
    - path: "src/app/songs/[slug]/components/MasteryDetailPopover.tsx"
      provides: "Tap-to-inspect mastery detail panel (reps, correct %, state, due) — fetches /api/exercises/vocab-mastery/[id] on open"
      exports: ["default"]
    - path: "src/stores/exerciseSession.ts"
      provides: "Adds tiers: Record<vocabItemId, Tier> + setTiers + setTier(id, tier) actions; tiers persist with the rest of the session state"
      contains: "tiers"
    - path: "src/app/songs/[slug]/components/ExerciseTab.tsx"
      provides: "Prefetches tiers between buildQuestions and startSession"
      contains: "vocab-tiers"
    - path: "src/app/songs/[slug]/components/QuestionCard.tsx"
      provides: "Renders prompt + options through TierText with the leak-override matrix; reveal-reading button while unanswered"
      contains: "TierText"
    - path: "src/app/songs/[slug]/components/FeedbackPanel.tsx"
      provides: "Always-Tier-1 vocab block + tap-to-inspect mastery popover"
      contains: "MasteryDetailPopover"
  key_links:
    - from: "src/app/songs/[slug]/components/ExerciseTab.tsx"
      to: "/api/exercises/vocab-tiers"
      via: "fetch in handleStart before startSession()"
      pattern: "vocab-tiers"
    - from: "src/app/songs/[slug]/components/QuestionCard.tsx"
      to: "src/app/actions/exercises.ts::recordVocabAnswer"
      via: "await recordVocabAnswer({...}) inside handleSelect, then setTier(question.vocabItemId, result.newTier)"
      pattern: "recordVocabAnswer"
    - from: "src/app/songs/[slug]/components/FeedbackPanel.tsx"
      to: "/api/exercises/vocab-mastery/[vocabItemId]"
      via: "MasteryDetailPopover fetch on open"
      pattern: "vocab-mastery"
    - from: "src/app/songs/[slug]/components/TierText.tsx"
      to: "src/lib/fsrs/tier.ts"
      via: "Tier type import"
      pattern: "fsrs/tier"
---

<objective>
Wire the exercise UI to the FSRS core (Plan 01) and persistence layer (Plan 02).
This is where the user-visible behavior lands: prompts/options render at the right
tier per word, answers commit inline, the reveal-reading hatch works, and the
feedback panel always shows full info plus a tap-to-inspect mastery details popover.

Purpose: Ship the actual progressive-disclosure UX. After this plan, exercise
options stop starting at bare kanji for new learners and stop showing crutches
for words the user has reviewed.

Output: One new render component (TierText), one new popover (MasteryDetailPopover),
plus integration edits to ExerciseTab, ExerciseSession, QuestionCard, FeedbackPanel,
and the zustand store. Player lyrics and the song-page vocab section are untouched
by design (CONTEXT lock: "exercise-flow only").
</objective>

<execution_context>
@C:/Users/Thiago/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Thiago/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.2-fsrs-progressive-disclosure/08.2-CONTEXT.md
@.planning/phases/08.2-fsrs-progressive-disclosure/08.2-01-SUMMARY.md
@.planning/phases/08.2-fsrs-progressive-disclosure/08.2-02-SUMMARY.md

@src/lib/exercises/generator.ts
@src/app/songs/[slug]/components/ExerciseTab.tsx
@src/app/songs/[slug]/components/ExerciseSession.tsx
@src/app/songs/[slug]/components/QuestionCard.tsx
@src/app/songs/[slug]/components/FeedbackPanel.tsx
@src/stores/exerciseSession.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TierText render component, MasteryDetailPopover, and session-store tier slice</name>
  <files>
    src/app/songs/[slug]/components/TierText.tsx
    src/app/songs/[slug]/components/MasteryDetailPopover.tsx
    src/stores/exerciseSession.ts
  </files>
  <action>
    --- TierText.tsx ---
    Create `src/app/songs/[slug]/components/TierText.tsx`. A single component
    every exercise surface uses to render a vocab word at the right disclosure level.

    Props:
      ```ts
      interface TierTextProps {
        vocab: VocabInfo;          // from generator
        tier: Tier;                 // 1|2|3 from @/lib/fsrs/tier
        mode: "prompt" | "option" | "feedback";
        /** Force Tier 1 regardless of stored tier (used by feedback panel) */
        forceTier1?: boolean;
        /** Force "kanji only" regardless of tier (used by reading-match prompt + meaning→vocab option) */
        forceKanjiOnly?: boolean;
      }
      ```

    Render rules:
      - effectiveTier =
          forceTier1 ? 1
          : forceKanjiOnly ? 3
          : tier
      - Tier 1: surface  +  furigana below or beside (small)  +  romaji (smaller, gray)
      - Tier 2: surface  +  furigana
      - Tier 3: surface only
      - Pure-kana edge case: when vocab.surface === vocab.reading, omit the
        furigana line at every tier (nothing to render). Document in JSDoc
        per CONTEXT discretion: "kana-only words render identically at all tiers".

    Visual: keep matching the Tailwind palette already in FeedbackPanel
    (`text-gray-400` for furigana, `text-gray-500` for romaji). No animation.

    --- MasteryDetailPopover.tsx ---
    Create `src/app/songs/[slug]/components/MasteryDetailPopover.tsx`.

    Props: `{ vocabItemId: string; userId: string; trigger: ReactNode }`.

    Behavior:
      - Renders the trigger as a clickable element (button or span). On click,
        toggle a small absolutely-positioned panel.
      - On open, fetch `/api/exercises/vocab-mastery/{vocabItemId}?userId={userId}`.
      - Show: "State: New | Learning | Review | Relearning",
              "Reps: N",
              "Correct: X%",
              "Next due: <relative time, e.g. 'in 3 days', or 'not scheduled'>".
      - Loading skeleton while fetching. Quiet error state ("Mastery details unavailable").
      - Close on outside click or Escape.

    Tailwind: `absolute z-20 w-64 rounded-lg border border-gray-700 bg-gray-900 p-3 text-xs text-gray-200`
    style — match the existing dark exercise palette.

    --- src/stores/exerciseSession.ts ---
    Extend the zustand store:
      1. Add to ExerciseSessionState:
         ```ts
         tiers: Record<string, Tier>; // keyed by vocabItemId
         revealedQuestionIds: Record<string, true>; // set when user taps reveal-reading on a question.id
         ```
      2. Add to ExerciseSessionActions:
         ```ts
         setTiers: (tiers: Record<string, Tier>) => void;
         setTier: (vocabItemId: string, tier: Tier) => void;
         markRevealed: (questionId: string) => void;
         ```
      3. Initial state: `tiers: {}`, `revealedQuestionIds: {}`.
      4. `clearSession` resets both.
      5. `startSession` resets both (so a new session does not inherit prior tiers).
      6. `partialize` continues to omit `_hasHydrated` only — tiers and
         revealedQuestionIds DO persist (so a paused session resumes with the
         right tiers without a re-fetch).

    Import `Tier` from `@/lib/fsrs/tier`.
  </action>
  <verify>
    1. `npx tsc --noEmit` passes.
    2. Component-level smoke: render TierText in isolation in the dev server's
       sandbox or write a quick scratch story — confirm Tier 1 shows all three
       lines, Tier 2 omits romaji, Tier 3 shows only the surface.
    3. Store: open Redux/Zustand devtools (or `console.log(useExerciseSession.getState())`)
       — confirm `tiers` and `revealedQuestionIds` slices exist and survive a refresh.
  </verify>
  <done>TierText renders 3-tier output with leak overrides; MasteryDetailPopover
  fetches and renders the documented fields; session store carries tiers + reveal flags
  through persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ExerciseTab tier prefetch + QuestionCard inline commit, reveal hatch, and tier rendering</name>
  <files>
    src/app/songs/[slug]/components/ExerciseTab.tsx
    src/app/songs/[slug]/components/ExerciseSession.tsx
    src/app/songs/[slug]/components/QuestionCard.tsx
  </files>
  <action>
    --- ExerciseTab.tsx ---
    In `handleStart`, after `buildQuestions(...)` and before `startSession(...)`:
      1. Collect unique `vocabItemId`s from the built questions.
      2. `fetch('/api/exercises/vocab-tiers?ids=...&userId=' + userId)`.
      3. Call `store.startSession(songVersionId, questions, mode)` FIRST, then
         `store.setTiers(tiers)` so the prefetched tiers survive the startSession
         reset.
      4. On failure, log and proceed with empty tiers — every word renders Tier 1
         (matches cold-start behavior; degrades gracefully).
      5. setError on truly fatal cases only; the tier fetch failing is non-fatal.

    --- QuestionCard.tsx ---
    1. Import `useExerciseSession` and pull `tiers`, `revealedQuestionIds`,
       `markRevealed`, `setTier`. Import TierText. Import recordVocabAnswer
       from `@/app/actions/exercises`.
       NOTE: QuestionCard is a client component ("use client" already present).
       Importing the server action `recordVocabAnswer` over the RSC boundary is
       intentional — Next compiles the action call as an RPC. Do NOT add
       `"use server"` to QuestionCard; that would break the component.
    2. Add prop `userId: string` and `songVersionId: string` (thread through
       from ExerciseSession — see below).
    3. Compute `targetTier = tiers[question.vocabItemId] ?? 1` (cold-start default).
    4. Compute `revealed = !!revealedQuestionIds[question.id]`.
    5. Add a "Reveal reading" small button (`text-xs underline text-gray-400`)
       visible only when `chosen === null && targetTier !== 1 && !revealed`.
       Clicking calls `markRevealed(question.id)` so the prompt re-renders at Tier 1.
    6. Replace the bare `{question.prompt}` text with TierText for question types
       where the prompt IS a vocab surface — that means vocab_meaning, reading_match,
       and fill_lyric (for fill_lyric, prompt is the verse with a blank — keep raw
       text for the verse, but the underline-blanked target word doesn't appear in
       the prompt anyway). For meaning_vocab, the prompt is a meaning string —
       render raw (no TierText).
       Concretely:
         - vocab_meaning: render `<TierText vocab={question.vocabInfo} tier={effectiveTier} mode="prompt" />`
           where `effectiveTier = revealed ? 1 : targetTier`.
         - reading_match: For reading_match prompts, always pass
           `forceKanjiOnly={true}` — the user's tier is irrelevant because furigana
           would leak the romaji answer. Render
           `<TierText vocab={question.vocabInfo} tier={effectiveTier} mode="prompt" forceKanjiOnly={true} />`
           (`effectiveTier` is computed but ignored by TierText when forceKanjiOnly
           is set; pass it anyway for prop consistency).
         - fill_lyric and meaning_vocab prompts: render the raw string (verse / meaning).
    7. Render answer OPTIONS:
         - vocab_meaning options: meanings (strings) — render raw.
         - reading_match options: romaji — render raw.
         - meaning_vocab options: vocab surfaces. For each option, look up the
           VocabInfo from `question.vocabInfo` (correct) or `question.distractorVocab[option]`
           (distractor). Render `<TierText vocab={info} tier={...} mode="option"
           forceKanjiOnly={true} />` per CONTEXT: "meaning→vocab answer options
           must never show furigana/romaji on the answer-revealing surface".
         - fill_lyric options: vocab surfaces. Same treatment as meaning_vocab options
           (forceKanjiOnly so they all appear at the same crutch level — the verse
           context already constrains).
       For options whose VocabInfo is unavailable (defensive fallback for legacy
       questions without distractorVocab), render the option string raw.
    8. In `handleSelect`, AFTER calling `onAnswered(...)`, also fire-and-await:
       ```ts
       try {
         const result = await recordVocabAnswer({
           userId,
           vocabItemId: question.vocabItemId,
           songVersionId,
           exerciseType: question.type,
           correct,
           revealedReading: revealed,
           responseTimeMs: timeMs,
         });
         setTier(question.vocabItemId, result.newTier); // optimistic mid-session update
       } catch (err) {
         console.error('recordVocabAnswer failed:', err);
         // Non-fatal: UI still proceeds; just no mastery update for this attempt.
       }
       ```
       Do NOT block UI feedback rendering on this network call — show feedback
       immediately, persist in parallel. (FeedbackPanel renders inside QuestionCard
       once `chosen !== null`; the await happens after that state is set, fine.)

    --- ExerciseSession.tsx ---
    Pass `userId` and `songVersionId` props down into `<QuestionCard … />`.
    No other changes required; the store already exposes the new slices.
  </action>
  <verify>
    1. `npx tsc --noEmit` passes.
    2. End-to-end happy path (manual): start a fresh session for any seeded song
       as a brand-new test user.
       - All vocab options for meaning_vocab + fill_lyric show kanji only (no furigana/romaji).
       - All vocab_meaning prompts show kanji + furigana + romaji (Tier 1 default).
       - Answer one vocab_meaning question correctly. After the feedback panel
         appears, run `psql -c "SELECT count(*) FROM user_exercise_log WHERE user_id='test-user';"`
         — count should be 1. Run `SELECT state FROM user_vocab_mastery WHERE user_id='test-user';`
         — exactly one row, state >= 1.
       - Continue. If that same word appears again (unlikely in a 10-question
         session but possible in full mode), it should now render at Tier 2.
    3. Reveal-reading flow (manual):
       - Answer a few questions correctly until at least one word reaches Tier 2 or 3
         (rare in a single short session — easier to test by manually inserting a
         user_vocab_mastery row with state=2 then opening the song).
       - On a Tier 2/3 question, verify the "Reveal reading" button appears.
       - Tap it. Prompt re-renders at Tier 1.
       - Pick the correct answer. Verify the user_exercise_log row's `rating` column
         is 1 (Again), confirming the hatch downgrades the rating.
    4. Distractor leak check: pick a wrong answer in a meaning_vocab question.
       Verify only ONE user_exercise_log row was inserted (the target's vocab_item_id),
       not three — the distractor IDs MUST NOT show up in user_exercise_log for that attempt.
    5. Player surface untouched check: open the song page, scroll the lyrics, scroll
       the vocab section — confirm the global furigana/romaji toggle still controls
       both, and there is no per-word tier rendering happening on either surface.
  </verify>
  <done>Session start prefetches tiers in one call, every option/prompt renders
  through TierText, the reveal hatch is visible only when meaningful, every answer
  inline-commits to the DB with the correct vocabItemId, distractor IDs are never
  written, and the player lyrics + song vocab section are visually unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Add tap-to-inspect popovers in FeedbackPanel and SessionSummary; force feedback vocab block to Tier 1</name>
  <files>
    src/app/songs/[slug]/components/FeedbackPanel.tsx
    src/app/songs/[slug]/components/SessionSummary.tsx
    src/lib/exercises/generator.ts
  </files>
  <action>
    Edit `FeedbackPanel.tsx`:

    1. The vocab block (the `<div className="mb-3 rounded-md bg-gray-900/60 p-3">`)
       currently shows surface, reading, romaji, meaning. CONTEXT: "Feedback panel
       is always Tier 1, ignoring per-word tier." Replace the inline span layout
       with `<TierText vocab={vocab} tier={3} forceTier1 mode="feedback" />`
       followed by the meaning paragraph below it. Pass `forceTier1` so any future
       refactor that introduces a tier prop here keeps the always-Tier-1 invariant.

    2. Wrap the surface element in a MasteryDetailPopover trigger:
       ```tsx
       <MasteryDetailPopover
         vocabItemId={question.vocabItemId}
         userId={userId /* threaded down from QuestionCard via props */}
         trigger={<TierText vocab={vocab} tier={3} forceTier1 mode="feedback" />}
       />
       ```
       Add `userId: string` to FeedbackPanelProps and thread it through QuestionCard
       (which already receives it from ExerciseSession in Task 2).

    3. The wrong-pick callout already shows the chosen distractor's surface in
       red. Wrap THAT in a MasteryDetailPopover too — same treatment, with
       `vocabItemId={wrongVocab.vocab_item_id}`. NOTE: the existing
       distractorVocab map keys by surface string, not VocabInfo with vocab_item_id.
       To support this without bloating Plan 03's scope:
         - Extend VocabInfo in `@/lib/exercises/generator.ts` to include
           `vocab_item_id?: string`. Update `toVocabInfo()` to copy it through.
         - In FeedbackPanel, if wrongVocab.vocab_item_id is missing (legacy data),
           skip the popover wrapper and render the surface plain.

    4. SessionSummary tap-to-inspect (wire it now — CONTEXT requires
       "tap-to-inspect on feedback panel AND session summary"):
       Edit `src/app/songs/[slug]/components/SessionSummary.tsx`. It already
       iterates `questions` in its `answerArray.map`. Add a compact collapsible
       "Words reviewed (N)" disclosure list after the stats row. Each row shows
       the question's vocab surface (read `question.vocabInfo.surface`) wrapped
       in a `MasteryDetailPopover` trigger keyed by `question.vocabItemId`.
       Thread `userId` into SessionSummary props (passed from ExerciseSession).
       Skip rows whose `vocabItemId` is empty (defensive for legacy data).
  </action>
  <verify>
    1. `npx tsc --noEmit` passes.
    2. Manual: answer a question correctly. In the feedback panel, tap the kanji
       surface — popover opens with reps, correct%, state, due (for the new word
       it should show "State: Learning, Reps: 1, Correct: 100%, Next due: in N
       minutes"). Tap outside or Escape — popover closes.
    3. Manual (wrong answer with distractorVocab populated): pick a wrong option,
       see the wrong-pick callout. Tap the distractor surface — popover opens for
       the distractor's mastery (reps=0, state=New, Next due=not scheduled — the
       distractor was NOT logged, so its mastery is genuinely empty).
    4. Manual (SessionSummary): finish a session. Expand "Words reviewed". Tap any
       word — popover opens with the up-to-date mastery row reflecting the session's
       writes.
  </verify>
  <done>Feedback vocab block renders forced Tier 1 with a tap-to-inspect popover.
  Distractor surfaces also offer the popover when vocab_item_id is available.
  SessionSummary lists reviewed words with the same affordance. CONTEXT decision
  "feedback panel always Tier 1" is enforced at the prop level.</done>
</task>

</tasks>

<verification>
- E2E: A fresh user starts a session, answers 10 questions, refreshes the browser
  before the summary, and resumes — answered questions stay answered, tiers persist,
  recordVocabAnswer is NOT re-fired on resume (already-answered questions skip).
- DB inspection after a 10-question session for a fresh test user:
  - user_exercise_log row count == 10 (one per question, never per option).
  - user_vocab_mastery row count == count of distinct vocab_item_ids targeted in
    those 10 questions.
  - Every state column is in {0, 1, 2, 3}; due is a future timestamp.
- Player lyrics + song-page VocabularySection: visually identical to pre-08.2.
  Toggling global furigana/romaji still works; no tier rendering anywhere on those
  surfaces (CONTEXT lock).
- TypeScript build passes (`npm run build`).
</verification>

<success_criteria>
- Tier renders in exercise flow only.
- Per-answer DB writes land before the user can advance.
- Reveal-reading hatch is rated as Again (verified by inspecting user_exercise_log.rating).
- Tap-to-inspect popover surfaces real reps/correctPct/state/due — no placeholder.
- Cold-start: first-ever session for a user shows every word at Tier 1; no UI errors.
- Distractor IDs never appear in user_exercise_log.
</success_criteria>

<output>
After completion, create `.planning/phases/08.2-fsrs-progressive-disclosure/08.2-03-SUMMARY.md`
documenting: TierText props, the leak-override matrix per exercise type, and the
manual QA checklist used to validate the per-answer commit + reveal hatch.
</output>
