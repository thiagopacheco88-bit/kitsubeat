---
phase: 01-content-pipeline
plan: 08
type: execute
wave: 2
depends_on: [07]
files_modified: []
autonomous: false
requirements: [CONT-05, CONT-06, CONT-07, CONT-08, CONT-10, CONT-11]
gap_closure: true

must_haves:
  truths:
    - "data/lessons-cache/ contains lesson JSON files for all ~200 songs"
    - "Neon Postgres songs table has ~200 rows with populated lesson JSONB and timing_data"
    - "The QA agent (06-qa-agent.ts) exits code 0 with zero gaps on the fully populated database"
    - "Dev seed (07-dev-seed.ts) selects 10-20 representative songs across JLPT levels"
    - "Every song has translations in en, pt-BR, and es"
    - "Every song has grammar tags, vocabulary, verse explanations, and JLPT/difficulty assignments"
  artifacts:
    - path: "data/lessons-cache/"
      provides: "Per-song lesson JSON from Claude Batch API"
    - path: "Neon Postgres songs table"
      provides: "~200 rows with complete lesson and timing data"
  key_links:
    - from: "scripts/seed/03-generate-content.ts"
      to: "data/lessons-cache/"
      via: "Claude Batch API writes lesson JSON"
      pattern: "lessons-cache"
    - from: "scripts/seed/05-insert-db.ts"
      to: "Neon Postgres songs table"
      via: "Drizzle upsert from lessons-cache + timing-cache"
      pattern: "onConflictDoUpdate"
    - from: "scripts/seed/06-qa-agent.ts"
      to: "Neon Postgres songs table"
      via: "db.select validates all fields"
      pattern: "db\\.select.*from.*songs"
---

<objective>
Execute content generation, database population, and full validation of the 200-song catalog.

Purpose: With the manifest, lyrics, and timing data populated (Plan 07), this plan completes the pipeline by generating lesson content via Claude Batch API, inserting everything into Neon Postgres, and running the QA agent to verify zero gaps. This closes the final Phase 1 gaps and achieves the phase goal.

Output: Fully populated Neon Postgres database with ~200 songs, QA agent passing with zero gaps, dev seed selecting 10-20 songs for Phase 2.
</objective>

<execution_context>
@C:/Users/Thiago/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Thiago/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-content-pipeline/01-VERIFICATION.md
@.planning/phases/01-content-pipeline/01-04-SUMMARY.md
@.planning/phases/01-content-pipeline/01-06-SUMMARY.md
@.planning/phases/01-content-pipeline/01-07-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Generate lesson content via Claude Batch API and insert into database</name>
  <files>data/lessons-cache/</files>
  <action>
Human must run the Claude Batch API content generation and database insertion steps. These require an ANTHROPIC_API_KEY (costs ~$5-15 at batch pricing) and DATABASE_URL, plus 30min-2hr wait for batch processing.

Step A: Push Drizzle schema to Neon -- run `npx drizzle-kit push` to create/update the songs table.
Step B: Generate content -- run `npx tsx scripts/seed/run-pipeline.ts --step 5` to submit all songs to Claude Batch API. This polls until complete (30min-2hr).
Step C: Insert into DB -- run `npx tsx scripts/seed/run-pipeline.ts --step 7` to upsert all songs with lesson JSONB and timing data into Neon.
  </action>
  <verify>Confirm data/lessons-cache/ has JSON files for ~200 songs and the insert script reports ~200 rows upserted.</verify>
  <done>Lesson content generated for all ~200 songs via Claude Batch API and inserted into Neon Postgres with lesson JSONB and timing_data columns populated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run QA agent and dev seed to validate Phase 1 completion</name>
  <files>scripts/seed/06-qa-agent.ts</files>
  <action>
Run the QA agent and dev seed to verify the Phase 1 goal is fully achieved. All data should be in the database from Task 1.

Step A: Run `npx tsx scripts/seed/06-qa-agent.ts --verbose` -- expected: ~200 songs, 0 gaps, exit code 0.
Step B: Run `npx tsx scripts/seed/07-dev-seed.ts` -- expected: table of 10-20 songs across JLPT levels.
Step C: If QA reports gaps, fix by re-running content generation for specific songs (`03-generate-content.ts --limit N`) or timing extraction for specific songs.

Phase 1 goal: "All 200 anime songs have complete, QA-verified lesson content in the database ready for the player to consume."
  </action>
  <verify>QA agent exits code 0 with zero gaps. Dev seed prints a table of 10-20 songs spanning N5 through N1 and all difficulty tiers.</verify>
  <done>QA agent validates all ~200 songs with zero gaps (exit code 0). Dev seed selects 10-20 representative songs. Phase 1 goal is fully achieved.</done>
</task>

</tasks>

<verification>
1. `data/lessons-cache/` populated with ~200 lesson JSON files
2. Neon Postgres songs table has ~200 rows
3. `npx tsx scripts/seed/06-qa-agent.ts` exits code 0 with zero gaps
4. `npx tsx scripts/seed/07-dev-seed.ts` selects 10-20 songs across JLPT levels
5. All songs have translations in en, pt-BR, es
6. All songs have grammar tags, vocabulary, verse explanations
7. All songs have timing_data populated
</verification>

<success_criteria>
- Claude Batch API generates lesson content for all ~200 songs
- All data inserted into Neon Postgres with lesson JSONB and timing_data
- QA agent reports zero gaps across all songs (exit code 0)
- Dev seed selects 10-20 representative songs for Phase 2
- Phase 1 goal fully achieved: all 200 songs have complete, QA-verified lesson content
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-pipeline/01-08-SUMMARY.md`
</output>
