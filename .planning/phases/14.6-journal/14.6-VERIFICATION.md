---
phase: 14.6-journal
verified: 2026-05-10T20:30:00Z
status: human_needed
score: 13/13 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 11/13
  gaps_closed:
    - "NEXT_PUBLIC_SITE_URL env var present in .env.local — key found at line 46"
    - "coverImage null guard prevents runtime crash on missing frontmatter field — commit 783245d adds ternary guard at generateMetadata (lines 45-53) and JSON-LD (lines 91-95)"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Visit /journal in browser — card grid renders with Magikarp article"
    expected: "Article card displays with cover image area, lore badge (red), '6 min read', title and summary text over vignette overlay"
    why_human: "Visual rendering requires browser; can't verify cover-art-as-background, vignette gradient, hover scale animation, or design token appearance programmatically"
  - test: "Visit /journal/magikarp-gyarados-legend in browser — article renders"
    expected: "ArticleHero component displays with dark overlay and title; article body renders MDX content with vocabulary table, all headings visible; 'Practice This in KitsuBeat' CTA block at bottom with link to /songs"
    why_human: "MDX rendering via compileMDX produces a React tree at runtime — table and heading rendering requires browser"
  - test: "Visit /journal/nonexistent-slug — 404 page displays"
    expected: "Next.js 404 page shown (not a blank screen or unhandled error)"
    why_human: "notFound() routing behavior requires browser verification"
  - test: "View source of /journal/magikarp-gyarados-legend — check head tags"
    expected: "<title>The Legend Behind Magikarp and Gyarados — Japan's Ancient Koi Dragon Myth | KitsuBeat Journal</title>; <meta property='og:type' content='article'/>; <meta property='og:image' .../>; <link rel='canonical' href='https://kitsubeat.vercel.app/journal/magikarp-gyarados-legend'/>; <script type='application/ld+json'> present in body"
    why_human: "generateMetadata output in Next.js App Router is injected into <head> at runtime — requires browser view-source or curl on running dev server"
  - test: "Visit /sitemap.xml in browser (requires running dev server)"
    expected: "XML response containing <loc>https://kitsubeat.vercel.app</loc>, <loc>https://kitsubeat.vercel.app/journal</loc>, and <loc>https://kitsubeat.vercel.app/journal/magikarp-gyarados-legend</loc>"
    why_human: "Next.js MetadataRoute.Sitemap is served dynamically at /sitemap.xml — requires running server to inspect"
  - test: "Verify nav/footer shell appears on both /journal and /journal/[slug]"
    expected: "Same navigation and footer as /songs and other app pages — no context switch for user"
    why_human: "Layout inheritance from root layout.tsx is a visual check"
---

# Phase 14.6: Journal — Organic Traffic & Content Hub Verification Report

**Phase Goal:** Ship a file-based MDX content section at /journal that drives organic traffic via SEO-optimised articles covering anime culture, song translations, and Japanese language explainers. Articles are auto-discovered from src/content/journal/ where Subutai drops .mdx files. The section must feel native to the session-14 design system and carry a full SEO layer.
**Verified:** 2026-05-10T20:30:00Z
**Status:** human_needed
**Re-verification:** Yes — after gap closure (previous status: gaps_found, previous score: 11/13)

---

## Gap Closure Confirmation

Both gaps from the initial verification are now closed:

**Gap 1 — NEXT_PUBLIC_SITE_URL (CLOSED)**
`grep` on `.env.local` finds `NEXT_PUBLIC_SITE_URL=https://kitsubeat.vercel.app` at line 46. Key is present.

**Gap 2 — coverImage null guard (CLOSED)**
Commit `783245d` ("fix(14.6): add coverImage null guards in generateMetadata and JSON-LD (CR-01)") modified only `src/app/journal/[slug]/page.tsx`. The file now reads:

- `generateMetadata` lines 45-53: `images: frontmatter.coverImage ? [{ url: frontmatter.coverImage.startsWith('/') ? ... }] : []` — full ternary guard.
- JSON-LD lines 91-95: `image: frontmatter.coverImage ? frontmatter.coverImage.startsWith('/') ? ... : ... : undefined` — full ternary guard.

No unguarded `.startsWith('/')` call remains on `frontmatter.coverImage`. CR-01 is resolved.

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | getAllArticles() returns all articles sorted by date descending | VERIFIED | articles.ts line 40: `.sort((a,b) => new Date(b.date).getTime() - new Date(a.date).getTime())`; test "returns both articles sorted newest-first" passes |
| 2 | getAllArticles() computes readingTimeComputed when not in frontmatter | VERIFIED | articles.ts line 37: `readingTimeComputed: (data as ArticleFrontmatter).readingTime ?? rt.text`; test "computes readingTimeComputed from content when not in frontmatter" passes |
| 3 | getAllArticles() returns empty array gracefully when directory has no MDX files | VERIFIED | articles.ts line 28: `if (!fs.existsSync(CONTENT_DIR)) return []`; test "returns [] when content directory does not exist" passes |
| 4 | getArticleSource() returns null for an unknown slug without throwing | VERIFIED | articles.ts line 48: `if (!fs.existsSync(filepath)) return null`; test "returns null for an unknown slug" passes |
| 5 | getArticleSource() validates slug against [a-z0-9-]+ before filesystem access | VERIFIED | articles.ts line 44-47: `SAFE_SLUG_RE = /^[a-z0-9-]+$/`; path traversal and slash slug tests both pass |
| 6 | The library can parse a real sample article from src/content/journal/ | VERIFIED | magikarp-gyarados-legend.mdx exists with all 9 required frontmatter fields; content length 5502 chars |
| 7 | /journal index renders a card grid with cover-art backgrounds | HUMAN NEEDED | page.tsx exists, getAllArticles() wired, JournalCard renders cover-art img — visual verification required |
| 8 | /journal/[slug] renders article with hero image + MDX body | HUMAN NEEDED | [slug]/page.tsx exists, compileMDX wired, ArticleHero component wired — browser rendering required |
| 9 | Visiting /journal/nonexistent-slug returns a 404 | HUMAN NEEDED | notFound() called at line 69 when getArticleSource returns null — runtime behavior requires browser |
| 10 | Article page has og:title, og:description, og:image, og:type=article, og:publishedTime OG tags | HUMAN NEEDED | generateMetadata at lines 37-57 emits all OG fields — actual output requires browser/curl |
| 11 | Article page body contains schema.org/Article JSON-LD with XSS-safe < escape | VERIFIED | [slug]/page.tsx lines 111-116: `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd).replace(/</g, '\\u003c') }}`; jsonLd object contains all required Article fields |
| 12 | /sitemap.xml includes /journal and all article slug entries | VERIFIED | sitemap.ts lines 19-33: returns static homepage + /journal + getAllArticles() mapped entries; 4 unit tests all pass |
| 13 | NEXT_PUBLIC_SITE_URL env var present in .env.local | VERIFIED | .env.local line 46: `NEXT_PUBLIC_SITE_URL=https://kitsubeat.vercel.app` — confirmed by grep |

**Score:** 13/13 truths addressed (9 hard-verified programmatically, 4 pending human browser check, 0 failed)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/journal/articles.ts` | getAllArticles(), getArticleSource(), exported types, server-only guard | VERIFIED | `import 'server-only'` is first line; all 4 exports present; SAFE_SLUG_RE guard present |
| `src/content/journal/magikarp-gyarados-legend.mdx` | First live article with all frontmatter fields | VERIFIED | All 9 required fields; 5502 chars of substantive content; CTA block at bottom linking to /songs |
| `src/lib/journal/__tests__/articles.test.ts` | 8 unit tests covering JOUR-01 to JOUR-04 | VERIFIED | 8 tests, all pass |
| `src/app/journal/page.tsx` | Journal index page with card grid and empty state | VERIFIED | exports default + metadata; getAllArticles() wired; empty state present; no 'use client' |
| `src/app/journal/components/JournalCard.tsx` | Card with cover-art, vignette, badge, text | VERIFIED | CardLink from @/components/ui/Card; CATEGORY_COLORS/LABELS maps; cover img + vignette + badge + bottom stack |
| `src/app/journal/components/ArticleHero.tsx` | Hero with coverImage background, overlay, title | VERIFIED | Full-bleed img; dark gradient overlay; category badge; metadata line |
| `src/app/journal/[slug]/page.tsx` | Article page with generateStaticParams, generateMetadata, compileMDX, notFound | VERIFIED | All three exports present; async params await pattern; notFound() on null source; coverImage null guards at lines 45-53 and 91-95 |
| `src/app/sitemap.ts` | MetadataRoute.Sitemap at app root | VERIFIED | At src/app/ root; getAllArticles() imported; homepage + /journal + article entries |
| `src/app/sitemap.test.ts` | 4 unit tests for sitemap | VERIFIED | 4 tests, all pass |
| `public/images/journal/.gitkeep` | Cover image directory tracked | VERIFIED | File exists |
| `.env.local` (NEXT_PUBLIC_SITE_URL) | NEXT_PUBLIC_SITE_URL=https://kitsubeat.vercel.app | VERIFIED | Line 46 confirmed by grep (file is gitignored — checked on disk) |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/lib/journal/articles.ts` | `src/content/journal/` | `fs.readdirSync(CONTENT_DIR)` | WIRED | Reads live MDX files from filesystem |
| `src/lib/journal/articles.ts` | `gray-matter` | `matter(raw)` | WIRED | Frontmatter parsing |
| `src/lib/journal/articles.ts` | `reading-time` | `readingTime(content)` | WIRED | Reading time computation |
| `src/app/journal/page.tsx` | `src/lib/journal/articles.ts` | `getAllArticles()` | WIRED | Called to populate card grid |
| `src/app/journal/[slug]/page.tsx` | `next-mdx-remote/rsc` | `compileMDX(...)` | WIRED | Called in both generateMetadata and ArticlePage |
| `src/app/journal/[slug]/page.tsx` | `src/lib/journal/articles.ts` | `getArticleSource(slug)` | WIRED | Called with null guard in both functions |
| `src/app/journal/components/JournalCard.tsx` | `src/components/ui/Card.tsx` | `<CardLink>` | WIRED | Imported and rendered |
| `src/app/sitemap.ts` | `src/lib/journal/articles.ts` | `getAllArticles()` | WIRED | Mapped to sitemap URL entries |
| `src/app/journal/[slug]/page.tsx` | `NEXT_PUBLIC_SITE_URL` | `process.env.NEXT_PUBLIC_SITE_URL ?? fallback` | WIRED | Env var now present in .env.local; fallback to hardcoded string retained for production safety |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `src/app/journal/page.tsx` | `articles` | `getAllArticles()` reads `src/content/journal/*.mdx` via `fs.readdirSync` | Yes — magikarp-gyarados-legend.mdx is a real 5502-char article | FLOWING |
| `src/app/journal/[slug]/page.tsx` | `content`, `frontmatter` | `getArticleSource(slug)` reads file from disk; `compileMDX` compiles it | Yes — real MDX source → React content tree | FLOWING |
| `src/app/sitemap.ts` | `articles` | `getAllArticles()` | Yes — same data flow as index page | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| articles.test.ts — 8 tests | `npx vitest run src/lib/journal/__tests__/articles.test.ts` | 8 passed, 0 failed | PASS |
| sitemap.test.ts — 4 tests | `npx vitest run src/app/sitemap.test.ts` | 4 passed, 0 failed | PASS |
| TypeScript — journal files | `npx tsc --noEmit 2>&1 \| grep journal` | No output (no errors) | PASS |
| NEXT_PUBLIC_SITE_URL in .env.local | `grep NEXT_PUBLIC_SITE_URL .env.local` | Line 46: key present | PASS |
| coverImage null guard — generateMetadata | Read lines 45-53 of [slug]/page.tsx | `frontmatter.coverImage ? [...] : []` — ternary guard present | PASS |
| coverImage null guard — JSON-LD | Read lines 91-95 of [slug]/page.tsx | `frontmatter.coverImage ? ... : undefined` — ternary guard present | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| JOUR-01 | 14.6-01 | getAllArticles() returns sorted array from fixture MDX files | SATISFIED | articles.test.ts "returns both articles sorted newest-first" passes |
| JOUR-02 | 14.6-01 | getAllArticles() handles empty directory gracefully | SATISFIED | articles.test.ts "returns [] when content directory does not exist" passes |
| JOUR-03 | 14.6-01 | getAllArticles() computes readingTime when not in frontmatter | SATISFIED | articles.test.ts "computes readingTimeComputed from content when not in frontmatter" passes |
| JOUR-04 | 14.6-01 | getArticleSource() returns null for unknown/malicious slug | SATISFIED | articles.test.ts path traversal tests pass; SAFE_SLUG_RE guard in place |
| JOUR-05 | 14.6-02 | /journal index renders card grid (smoke) | NEEDS HUMAN | page.tsx and JournalCard wired; visual render requires browser |
| JOUR-06 | 14.6-02 | /journal/[slug] article page returns 404 for unknown slug | NEEDS HUMAN | notFound() called on null source; runtime behavior requires browser |
| JOUR-07 | 14.6-03 | Article page emits correct <title> from frontmatter | NEEDS HUMAN | generateMetadata wired with title template; requires browser/curl to observe <head> output |
| JOUR-08 | 14.6-03 | Sitemap includes /journal and /journal/[slug] entries | SATISFIED | sitemap.test.ts 4 tests pass; sitemap.ts logic verified |

**Note on JOUR-* in REQUIREMENTS.md:** JOUR-01 through JOUR-08 are phase-local requirement IDs defined in the RESEARCH.md and PLAN files for phase 14.6. They do not appear in the central `.planning/REQUIREMENTS.md` (which covers v2.0 exercise engine requirements for phases 7-12). Phase 14.6 is a v3.x milestone phase that intentionally defines its own requirement namespace. No orphaned requirements found.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/app/journal/[slug]/page.tsx` | 81 | `frontmatter.readingTime ?? '5 min read'` hardcoded fallback | Warning | Articles without readingTime frontmatter always show "5 min read" regardless of length; getAllArticles() correctly computes from content but the article page does not (WR-01) |
| `src/app/journal/components/ArticleHero.tsx` | ~64 | `new Date(article.date).toLocaleDateString(...)` on bare date string | Warning | UTC date-only strings parse as UTC midnight; users in UTC-1 to UTC-12 see previous calendar day (WR-02) |
| `src/app/journal/components/JournalCard.tsx` + `ArticleHero.tsx` | 4-15 / 4-15 | CATEGORY_LABELS and CATEGORY_COLORS copy-pasted in both components | Warning | Adding new ArticleCategory silently breaks color/label in one component only (WR-04) |

**CR-01 resolved:** `frontmatter.coverImage.startsWith('/')` called without null guard — FIXED in commit 783245d. No longer a blocker.

**Stub check:** No empty implementations, placeholder returns, or TODO stubs found in any journal file. All components render real data from live MDX files.

---

### Human Verification Required

#### 1. Journal index card grid renders correctly

**Test:** Navigate to /journal in a browser (requires `npm run dev` on port 7000)
**Expected:** Card grid showing the Magikarp/Gyarados article with cover image area (even if the actual JPG is missing — the card should render with the img src set to /images/journal/magikarp-hero.jpg), red "Lore" badge, "6 min read" reading time, full article title, and summary text. Three-column grid on desktop, single column on mobile.
**Why human:** Visual design token rendering (var(--color-*), var(--radius-*)), vignette gradient, and group-hover scale animation cannot be verified programmatically.

#### 2. Article page renders MDX body

**Test:** Navigate to /journal/magikarp-gyarados-legend
**Expected:** ArticleHero with dark overlay and title visible; article body renders all six sections (headings, the vocabulary table with kanji/romaji/meaning columns, paragraphs); "Practice This in KitsuBeat" CTA at the bottom with a working link to /songs.
**Why human:** compileMDX produces a React element tree — table rendering, heading hierarchy, and link rendering require browser.

#### 3. 404 on unknown slug

**Test:** Navigate to /journal/nonexistent-slug
**Expected:** Next.js 404 page — not a blank screen, crash page, or raw error.
**Why human:** notFound() routing is tested programmatically (it's called when source is null), but the resulting page render requires browser.

#### 4. SEO metadata in page source

**Test:** View source of /journal/magikarp-gyarados-legend (or use `curl http://localhost:7000/journal/magikarp-gyarados-legend`)
**Expected:**
- `<title>The Legend Behind Magikarp and Gyarados — Japan's Ancient Koi Dragon Myth | KitsuBeat Journal</title>` in `<head>`
- `<meta property="og:type" content="article">`
- `<meta property="og:image" content="https://kitsubeat.vercel.app/images/journal/magikarp-hero.jpg">` (absolute URL)
- `<link rel="canonical" href="https://kitsubeat.vercel.app/journal/magikarp-gyarados-legend">`
- `<script type="application/ld+json">` in page body
**Why human:** Next.js App Router generateMetadata output lives in a server-rendered `<head>` that requires an HTTP response to inspect.

#### 5. Sitemap serves correct XML

**Test:** Visit http://localhost:7000/sitemap.xml or `curl http://localhost:7000/sitemap.xml`
**Expected:** XML document with `<url><loc>https://kitsubeat.vercel.app</loc>`, `<url><loc>https://kitsubeat.vercel.app/journal</loc>`, and `<url><loc>https://kitsubeat.vercel.app/journal/magikarp-gyarados-legend</loc>`.
**Why human:** MetadataRoute.Sitemap is rendered by the Next.js runtime at request time — requires running server.

#### 6. Nav/footer shell on journal pages

**Test:** Confirm both /journal and /journal/magikarp-gyarados-legend show the same navigation and footer as /songs
**Expected:** No context switch — identical shell to the rest of the app.
**Why human:** Layout inheritance is visual.

---

## Summary

Both programmatic gaps from the initial verification are closed. All 13 must-haves now have satisfactory code evidence. The 4 human-needed truths (card grid visual, article MDX render, 404 routing, SEO head tags) remain pending browser verification — they were always in this state and are not regressions. The phase is ready for human spot-check before declaring fully passed.

---

_Verified: 2026-05-10T20:30:00Z_
_Verifier: Claude (gsd-verifier)_
