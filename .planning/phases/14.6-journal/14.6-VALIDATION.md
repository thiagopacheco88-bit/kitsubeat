---
phase: "14.6"
phase_slug: "journal"
date: "2026-05-10"
---

# Phase 14.6: Journal — Validation Strategy

## Test Framework

| Property | Value |
|----------|-------|
| Framework | Vitest 4.1.4 |
| Config file | `vitest.config.ts` (project root) |
| Quick run command | `npm run test:unit` |
| Full suite command | `npm test` |

## Phase Requirements → Test Map

| Req ID | Behavior | Test Type | Automated Command | File Exists? |
|--------|----------|-----------|-------------------|-------------|
| JOUR-01 | `getAllArticles()` returns sorted array from fixture MDX files | unit | `vitest run src/lib/journal/__tests__/articles.test.ts` | No — created in Wave 1 |
| JOUR-02 | `getAllArticles()` handles empty directory gracefully | unit | same file | No — created in Wave 1 |
| JOUR-03 | `getAllArticles()` computes readingTime when not in frontmatter | unit | same file | No — created in Wave 1 |
| JOUR-04 | `getArticleSource()` returns null for unknown slug | unit | same file | No — created in Wave 1 |
| JOUR-05 | `/journal` index renders card grid (smoke) | build smoke | `npm run build 2>&1 \| grep -E "journal"` | Verified at build time (Wave 3) |
| JOUR-06 | `/journal/[slug]` article page returns 404 for unknown slug | phase gate | `/gsd-verify-work` manual UAT | Deferred to phase gate |
| JOUR-07 | Article page emits correct `<title>` from frontmatter | build smoke | `npm run build` passes with article routes | Verified at build time (Wave 3) |
| JOUR-08 | Sitemap includes `/journal` and `/journal/[slug]` entries | unit | `vitest run src/app/sitemap.test.ts` | No — created in Wave 3 |

## Sampling Rate

- **Per task commit:** `npm run test:unit`
- **Per wave merge:** `npm test`
- **Phase gate:** Full suite green before `/gsd-verify-work`

## Wave 0 Gaps (created by plans)

- [ ] `src/lib/journal/__tests__/articles.test.ts` — covers JOUR-01 through JOUR-04; needs fixture `.mdx` files in `src/lib/journal/__tests__/fixtures/`
- [ ] `src/app/sitemap.test.ts` — covers JOUR-08; mocks `getAllArticles` to avoid filesystem
- [ ] `src/content/journal/` with at least one `.mdx` file (needed for `generateStaticParams` and smoke tests)

## Phase Gate Checklist

Before calling `/gsd-verify-work`:
- [ ] `npm run test:unit` exits 0
- [ ] `npm test` exits 0
- [ ] `npm run build` succeeds with journal routes included
- [ ] `/journal` renders card grid in browser
- [ ] `/journal/magikarp-gyarados-legend` renders article page
- [ ] `/journal/nonexistent` returns 404
- [ ] View source: `<title>` contains article title
- [ ] View source: `og:image` meta tag present
- [ ] `/sitemap.xml` contains `/journal` and article slug entries
- [ ] View source: `application/ld+json` script present on article page
