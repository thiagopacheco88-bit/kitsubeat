---
phase: 14.6-journal
plan: "02"
subsystem: ui
tags: [journal, next-mdx-remote, mdx, server-component, card, hero, static-generation]

# Dependency graph
requires:
  - 14.6-01 (getAllArticles, getArticleSource, ArticleMeta, ArticleFrontmatter types)
provides:
  - /journal index page (JournalCard grid with empty state)
  - /journal/[slug] article page (compileMDX render, notFound on missing slug)
  - JournalCard component (cover-art bg, vignette, category badge, bottom text stack)
  - ArticleHero component (full-bleed hero, dark overlay, category badge, meta line)
affects:
  - 14.6-03 (SEO layer — generateMetadata will be added to [slug]/page.tsx)
  - 14.6-04 (sitemap.ts already can import getAllArticles; journal routes now exist)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "JournalCard: CardLink(variant=flat, size=sm) + absolute <img> + linear-gradient vignette + inline style overrides for white text — mirrors AnimeCard/HeroFeatured pattern"
    - "ArticlePage: async params (await params) — Next.js 15 mandatory pattern copied from songs/[slug]/page.tsx"
    - "compileMDX from next-mdx-remote/rsc with parseFrontmatter:true — single parse returns content + frontmatter"
    - "Manual prose wrapper (space-y-4 lineHeight 1.75) — @tailwindcss/typography not installed; manual fallback per plan"
    - "generateStaticParams returns getAllArticles().map(a => ({ slug: a.slug })) — static build for all MDX slugs"

key-files:
  created:
    - src/app/journal/components/JournalCard.tsx
    - src/app/journal/components/ArticleHero.tsx
    - src/app/journal/page.tsx
    - src/app/journal/[slug]/page.tsx
  modified: []

key-decisions:
  - "Used manual prose wrapper instead of @tailwindcss/typography — plugin not installed; plan provided explicit fallback"
  - "No generateMetadata in [slug]/page.tsx — SEO intentionally deferred to Plan 03 per plan instructions"
  - "--radius-pill confirmed present in globals.css (9999px) — used directly in badge classes"

patterns-established:
  - "Pattern: journal card = CardLink + absolute position <img> + aria-hidden vignette div + category badge + bottom text stack"
  - "Pattern: article hero = bare div with relative positioning, full-bleed img, dark gradient overlay, metadata line"

requirements-completed: [JOUR-05, JOUR-06]

# Metrics
duration: 8min
completed: 2026-05-10
---

# Phase 14.6 Plan 02: Journal Pages & Components Summary

**Cover-art card grid at /journal and MDX article reader at /journal/[slug] — both Server Components with session-14 design tokens, static generation, and notFound() guard**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-05-10T19:00:00Z
- **Completed:** 2026-05-10T19:08:00Z
- **Tasks:** 2 (Task 1: JournalCard + index page; Task 2: ArticleHero + slug page)
- **Files created:** 4

## Accomplishments

- Created `JournalCard` component with cover-art background, linear-gradient vignette, three-color category badge (lore=red, language=blue, translation=green), and bottom text stack
- Created `JournalIndexPage` with page header matching songs/page.tsx, responsive 3-col card grid, and graceful empty state
- Created `ArticleHero` component with full-bleed image hero, dark overlay, category badge, title, and metadata line (reading time · date · author)
- Created `ArticlePage` with async params (Next.js 15), `compileMDX` from `next-mdx-remote/rsc`, `notFound()` for missing slugs, `generateStaticParams` for static build
- `npm run build` passes: `/journal` and `/journal/magikarp-gyarados-legend` both prerendered as static HTML (SSG)

## Task Commits

1. **Task 1: JournalCard + index page** — `f43075d` (feat) — JournalCard.tsx + page.tsx
2. **Task 2: ArticleHero + slug page** — `8778ee4` (feat) — ArticleHero.tsx + [slug]/page.tsx

## Files Created/Modified

- `src/app/journal/components/JournalCard.tsx` — CardLink-based card with cover-art background, vignette overlay, CATEGORY_COLORS badge, bottom text stack
- `src/app/journal/page.tsx` — Server Component index page; metadata export; getAllArticles() + empty state
- `src/app/journal/components/ArticleHero.tsx` — full-bleed hero component; eager-loaded image; dark gradient overlay; meta line
- `src/app/journal/[slug]/page.tsx` — async article page; compileMDX from next-mdx-remote/rsc; notFound(); generateStaticParams

## Decisions Made

- Used manual prose wrapper (`space-y-4` + `lineHeight: '1.75'`) instead of `prose prose-invert` Tailwind classes — `@tailwindcss/typography` is not installed. Plan explicitly provided this fallback — no deviation.
- No `generateMetadata` in `[slug]/page.tsx` — SEO layer intentionally deferred to Plan 03 per plan instructions (Pitfall 1 avoidance: no double compileMDX calls until generateMetadata is added in Plan 03).
- `--radius-pill` confirmed present in `src/app/globals.css` (`9999px`) — used directly in `rounded-[var(--radius-pill)]` badge classes.

## Deviations from Plan

None — plan executed exactly as written. Manual prose wrapper fallback was specified in the plan's Task 2 action block and followed exactly.

## Known Stubs

None — article content is rendered from real MDX files. `getAllArticles()` reads live filesystem data. `readingTimeComputed` always populated via `frontmatter.readingTime ?? '5 min read'` (conservative fallback for articles without readingTime frontmatter).

## Threat Flags

None — no new network endpoints or auth paths introduced. The T-14.6-04 threat (slug → getArticleSource → notFound()) is fully mitigated: SAFE_SLUG_RE guard from Plan 01 runs before any `fs` call; `notFound()` is called immediately when `getArticleSource` returns null.

## Self-Check: PASSED

| Item | Status |
|------|--------|
| src/app/journal/components/JournalCard.tsx | FOUND |
| src/app/journal/page.tsx | FOUND |
| src/app/journal/components/ArticleHero.tsx | FOUND |
| src/app/journal/[slug]/page.tsx | FOUND |
| Commit f43075d (Task 1) | FOUND |
| Commit 8778ee4 (Task 2) | FOUND |
