---
phase: 14.2-redesign-home
plan: "01"
subsystem: prerequisites
tags: [baseline, verification, screenshot, bundle]
dependency_graph:
  requires: []
  provides:
    - _temp/baseline-bundle-pre-14.2.txt (bundle baseline for Phase 14.2-10 delta comparison)
    - _temp/path-redesign/demo-home-CA-hybrid-light.png (light-theme reference for 14.2-09 visual specs)
    - _temp/path-redesign/screenshot-home-light.mjs (reproducible capture script)
    - .planning/phases/14.2-redesign-home/14.2-01-VERIFICATIONS.md (V1/V2/V3 verified facts)
  affects: []
tech_stack:
  added: []
  patterns:
    - Playwright chromium.launch() screenshot capture at 390x844 mobile viewport
    - IosFrame screen div selector (second occurrence = light phone)
key_files:
  created:
    - _temp/baseline-bundle-pre-14.2.txt
    - _temp/path-redesign/demo-home-CA-hybrid-light.png
    - _temp/path-redesign/screenshot-home-light.mjs
    - _temp/path-redesign/demo-home-CA-hybrid.html
    - .planning/phases/14.2-redesign-home/14.2-01-VERIFICATIONS.md
  modified: []
decisions:
  - V1: /kana uses ?script= query param (not /kana/{script} path segment) — SPEC §Req 5 was wrong
  - V2: lesson JSONB top-level key is verses[] — jsonb_array_length(sv.lesson->'verses') confirmed
  - V3: kb_theme cookie drives SSR theme via layout.tsx zero-flash script — Wave 5 e2e specs use context.addCookies pattern
  - Build workaround: npm run build fails in worktree due to lockfile collision; ran from main kitsubeat/ dir (same HEAD)
metrics:
  duration: 9 min
  completed: 2026-05-03
  tasks_completed: 3
  tasks_total: 3
  files_created: 5
---

# Phase 14.2 Plan 01: Pre-Implementation Prerequisites Summary

Wave 1 prerequisites captured: bundle baseline (/, /path, /songs/[slug]), light-theme reference screenshot (390x844 PNG), and three verification facts (kana route shape, lesson JSONB path, theme cookie pattern).

## Bundle Baseline (from _temp/baseline-bundle-pre-14.2.txt)

Pre-Phase-14.2 baseline captured on commit `666436f` (2026-05-03T15:11:44Z):

```
/                 310 B      111 kB First Load JS
/path             5.83 kB    124 kB First Load JS
/songs/[slug]     9 kB       129 kB First Load JS
```

D-10 soft cap on `/` delta: +10 kB (First Load JS ≤ 121 kB post-14.2).
Hard gate on `/songs/[slug]`: ≤50 kB route-specific (currently 9 kB — well within).

## Light-Theme Reference Screenshot

- Path: `_temp/path-redesign/demo-home-CA-hybrid-light.png`
- Dimensions: 390 x 844 px (iPhone 14 Pro mobile-portrait)
- File size: 132 KB
- Capture script: `_temp/path-redesign/screenshot-home-light.mjs` (Playwright, reproducible)
- Approach: Second IosFrame screen div in `demo-home-CA-hybrid.html` (dark first, light second)

## Wave 1 Verification Confirmations

**V1 — /kana route shape (D-08):** `?script=hiragana|katakana` query param confirmed.
- `src/app/kana/page.tsx` uses `searchParams.get("script")`.
- `KanaCheckpointNode.tsx` line 64: `const href = \`/kana?script=${script}\``.
- No `[script]` directory under `src/app/kana/`.
- SPEC §Req 5 reference to `/kana/{script}` was incorrect; Plan 14.2-05 uses query params.

**V2 — lesson JSONB shape (D-05):** `lesson->'verses'` confirmed.
- `src/lib/types/lesson.ts` line 171: `verses: Verse[]` at top level of `Lesson` interface.
- `jsonb_array_length(sv.lesson->'verses')` is correct for `getHeroSong` verseCount.

**V3 — kb_theme cookie pattern (Wave 5 e2e specs):** `context.addCookies([{ name: "kb_theme", value: "dark", ... }])` confirmed.
- `layout.tsx` zero-flash script reads cookie before first paint deterministically.
- `tests/e2e/path-visual-dark.spec.ts` already uses this pattern in production.

## Commit SHAs

| Task | Commit | Message |
|------|--------|---------|
| Task 1: Bundle baseline | 9293b0b | chore(14.2): capture pre-baseline bundle for /, /path, /songs/[slug] (D-10) |
| Task 2: Light-theme PNG | 590e979 | chore(14.2): capture light-theme reference screenshot for home redesign (D-11) |
| Task 3: Verifications | 349e9ba | docs(14.2): record Wave 1 verifications for /kana shape, lesson JSONB, theme cookie |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] npm run build fails in worktree due to multiple lockfile collision**
- **Found during:** Task 1
- **Issue:** The git worktree has its own `package-lock.json` (at `kitsubeat/.claude/worktrees/agent-a8b0ba02c8917772a/package-lock.json`), which conflicts with the main `kitsubeat/package-lock.json`. Next.js 15 detected both lockfiles and webpack resolved `@clerk/nextjs/server` (server-only package) into the client bundle via `ThemeToggle.tsx → user-prefs.ts → @clerk/nextjs/server`, causing a build failure.
- **Fix:** Ran `npm run build` from the main `kitsubeat/` directory (same HEAD commit `666436f`) which builds successfully. Documented the workaround at the top of `_temp/baseline-bundle-pre-14.2.txt` per plan instructions.
- **Files modified:** `_temp/baseline-bundle-pre-14.2.txt` (workaround note in header)
- **Commit:** 9293b0b

**2. [Rule 2 - Missing] demo-home-CA-hybrid.html not tracked in git at the worktree HEAD**
- **Found during:** Task 2
- **Issue:** The worktree's `_temp/path-redesign/` only has `demo-CA-hybrid-light.png` and `screenshot-light.mjs` (the 14.1 tracked files). The home demo HTML is untracked in the main repo (not yet committed). The screenshot script needs the HTML to run.
- **Fix:** Copied `demo-home-CA-hybrid.html` from `kitsubeat/_temp/path-redesign/` to the worktree and committed it alongside the script and PNG.
- **Files modified:** `_temp/path-redesign/demo-home-CA-hybrid.html` (added to worktree)
- **Commit:** 590e979

## Known Stubs

None — this plan produces only prerequisite capture artifacts (text files, PNG, JS script, markdown), not UI components.

## Threat Flags

None — all produced files are static artifacts (bundle size text, PNG image, JS capture script, markdown). No new network endpoints, auth paths, or schema changes introduced.

## Self-Check: PASSED

- FOUND: _temp/baseline-bundle-pre-14.2.txt
- FOUND: _temp/path-redesign/demo-home-CA-hybrid-light.png
- FOUND: _temp/path-redesign/screenshot-home-light.mjs
- FOUND: .planning/phases/14.2-redesign-home/14.2-01-VERIFICATIONS.md
- FOUND: .planning/phases/14.2-redesign-home/14.2-01-SUMMARY.md
- FOUND commit: 9293b0b (bundle baseline)
- FOUND commit: 590e979 (light-theme PNG)
- FOUND commit: 349e9ba (verifications doc)
