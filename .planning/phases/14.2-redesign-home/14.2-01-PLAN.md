---
phase: 14.2-redesign-home
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _temp/path-redesign/demo-home-CA-hybrid-light.png
  - _temp/path-redesign/screenshot-home-light.mjs
  - _temp/baseline-bundle-pre-14.2.txt
autonomous: true
requirements:
  - SPEC-Req-3 (verseCount JSONB shape verification — D-05)
  - SPEC-Req-5 (Foundations link target — D-08 + 14.1 D-05)
  - SPEC-AC-3 (light-theme reference screenshot — D-11)
  - CONTEXT-D-10 (pre-baseline bundle capture — cross-surface watch)
  - CONTEXT-D-11 (light-theme reference)
  - CONTEXT-D-08 (Foundations link target verification)
  - CONTEXT-D-05 (lesson JSONB shape verification)

must_haves:
  truths:
    - "Pre-baseline First Load JS for /, /path, /songs/[slug] is captured in a committed file before any 14.2 implementation starts"
    - "Light-theme reference PNG demo-home-CA-hybrid-light.png exists at the same dimensions as the dark reference"
    - "/kana route confirmed to accept ?script=hiragana|katakana query param (not /kana/{script} path segment)"
    - "song_versions.lesson JSONB confirmed to have shape { verses: [...] } via src/lib/types/lesson.ts Verse type"
  artifacts:
    - path: "_temp/baseline-bundle-pre-14.2.txt"
      provides: "Pre-implementation First Load JS sizes for /, /path, /songs/[slug]"
      contains: "/ ·"
    - path: "_temp/path-redesign/demo-home-CA-hybrid-light.png"
      provides: "Light-theme reference screenshot for visual diff spec"
      contains: "PNG image data"
    - path: "_temp/path-redesign/screenshot-home-light.mjs"
      provides: "Reproducible Puppeteer/Playwright capture script for light-theme reference"
  key_links:
    - from: "_temp/baseline-bundle-pre-14.2.txt"
      to: "Phase 14.2 verification (Plan 14.2-10)"
      via: "delta comparison"
      pattern: "First Load JS"
    - from: "_temp/path-redesign/demo-home-CA-hybrid-light.png"
      to: "tests/e2e/home-visual-light.spec.ts (Plan 14.2-09)"
      via: "snapshot reference"
      pattern: "home-light.png"
---

<objective>
Capture all pre-implementation prerequisites for Phase 14.2:

1. **Bundle baselines (D-10):** Run `npm run build` on master HEAD and record First Load JS for `/`, `/path`, `/songs/[slug]` into a committed text file. Used by Plan 14.2-10 to compute the cross-surface bundle delta.
2. **Light-theme reference (D-11):** Capture a 390×844 PNG of `_temp/path-redesign/demo-home-CA-hybrid.html` rendered with `[data-theme='light']`. Same Puppeteer/Playwright recipe 14.1 used for `demo-CA-hybrid-light.png`.
3. **Route shape verification (D-08):** Confirm `/kana` accepts `?script=hiragana|katakana` query param (NOT `/kana/{script}` path segment) so Foundations cards in Plan 14.2-05 use the correct href.
4. **JSONB shape verification (D-05):** Confirm `song_versions.lesson` JSONB key path is `lesson->'verses'` against `src/lib/types/lesson.ts::Verse` so `getHeroSong` Plan 14.2-03 selector is correct.

Purpose: Wave 1 gates eliminate guesswork before Wave 2+ implementation. Skipping these creates dependency on assumptions that may be wrong (D-08 calls this out explicitly: SPEC says `/kana/{script}`, 14.1 D-05 says `?script=`; 14.2 needs the verified answer).

Output: Bundle baseline file, light-theme reference PNG, capture script, and inline verification notes (no separate doc — verifications are recorded in this plan's commit message + the SUMMARY).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14.2-redesign-home/14.2-CONTEXT.md
@.planning/phases/14.2-redesign-home/14.2-SPEC.md
@.planning/phases/14.2-redesign-home/14.2-PATTERNS.md
@_temp/path-redesign/demo-home-CA-hybrid.html
@src/lib/types/lesson.ts
@src/app/kana/page.tsx

<interfaces>
<!-- From src/lib/types/lesson.ts (already read; lesson.verses[] is the canonical JSONB path). -->
export interface Verse {
  verse_number: number;
  start_time_ms: number;
  end_time_ms: number;
  tokens: Token[];
  translations: Record<string, string>;
  literal_meaning: Localizable;
  cultural_context?: Localizable;
  filler?: boolean;
}
// Lesson type at src/lib/types/lesson.ts contains `verses: Verse[]` —
// confirmed at PATTERNS.md line 514 + STATE.md (lesson.verses[] referenced
// in Plan 11.6-08, 10-04, 10-05 SUMMARY entries).

<!-- From src/app/kana/page.tsx lines 30–40 (already read; query-param shape confirmed). -->
function isValidScriptParam(v: string | null): v is "hiragana" | "katakana" {
  return v === "hiragana" || v === "katakana";
}
const scriptParam = searchParams.get("script");
// Route is /kana?script=hiragana — NOT /kana/hiragana.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-implementation bundle baseline for /, /path, /songs/[slug]</name>
  <read_first>
    - .planning/phases/14.2-redesign-home/14.2-CONTEXT.md (D-10 cross-surface watch rationale; lines 81–82, 240–248)
    - .planning/phases/13-performance-infrastructure/13-CONTEXT.md (D-09 size-limit hard gate on /songs/[slug] = 50 KB; this baseline does NOT replace that gate)
    - package.json (verify `build` script exists and outputs route-by-route sizes)
  </read_first>
  <files>_temp/baseline-bundle-pre-14.2.txt</files>
  <action>
    Run `npm run build` from the repo root with no source changes (verify `git status` shows only the planning directory dirty before running). Capture the build's "Route (app)" table to a text file at `_temp/baseline-bundle-pre-14.2.txt`. The file MUST contain at minimum these three lines (extracted from Next.js build output via `grep -E "^[│├└]?\s*(/|/path|/songs/\[slug\])\s" build.log` or equivalent):

    ```
    Pre-Phase-14.2 baseline — captured {ISO date} on commit {git rev-parse HEAD}
    Route                              Size       First Load JS
    ─                                  ─          ─
    /                                  {N kB}     {N kB}
    /path                              {N kB}     {N kB}
    /songs/[slug]                      {N kB}     {N kB}
    ```

    If `npm run build` is currently broken (per STATE.md "D-PRE-04 Clerk WIP files block runtime collection in build"), record the workaround actually used at the top of the file (e.g., "Build run with NEXT_DISABLE_RUNTIME_COLLECTION=1" or "Stash WIP Clerk files before build per Plan 14-00 D-PRE-04 note"). Do NOT skip the baseline — Phase 14.2 verification (Plan 14.2-10) compares against this exact file.

    Commit message: `chore(14.2): capture pre-baseline bundle for /, /path, /songs/[slug] (D-10)`
  </action>
  <acceptance_criteria>
    - File `_temp/baseline-bundle-pre-14.2.txt` exists in the repo root
    - File contains the literal string `Pre-Phase-14.2 baseline`
    - File contains a line matching regex `^/\s+\d+(\.\d+)?\s*kB\s+\d+(\.\d+)?\s*kB` (root route with size + First Load JS)
    - File contains a line matching regex `^/path\s+\d+(\.\d+)?\s*kB\s+\d+(\.\d+)?\s*kB`
    - File contains a line matching regex `^/songs/\[slug\]\s+\d+(\.\d+)?\s*kB\s+\d+(\.\d+)?\s*kB`
    - File contains current commit SHA from `git rev-parse HEAD` at time of capture
    - `git ls-files _temp/baseline-bundle-pre-14.2.txt` returns the file path (committed, not gitignored — verify `.gitignore` does NOT exclude `_temp/baseline-*`; if it does, force-add via `git add -f`)
  </acceptance_criteria>
  <verify>
    <automated>test -f _temp/baseline-bundle-pre-14.2.txt &amp;&amp; grep -q "^/" _temp/baseline-bundle-pre-14.2.txt &amp;&amp; grep -q "^/path" _temp/baseline-bundle-pre-14.2.txt &amp;&amp; grep -q "^/songs/\[slug\]" _temp/baseline-bundle-pre-14.2.txt &amp;&amp; git ls-files _temp/baseline-bundle-pre-14.2.txt | grep -q baseline</automated>
  </verify>
  <done>Baseline file exists, contains all three route lines with First Load JS sizes, contains commit SHA + capture date, and is committed to git.</done>
</task>

<task type="auto">
  <name>Task 2: Capture light-theme reference screenshot of demo-home-CA-hybrid.html</name>
  <read_first>
    - _temp/path-redesign/demo-home-CA-hybrid.html (the source HTML — verify it has a `[data-theme='light']` opt-in mechanism or accepts a CSS toggle)
    - _temp/path-redesign/demo-CA-hybrid-light.png (14.1's light-theme reference; SAME format/dimensions expected for 14.2)
    - .planning/phases/14.1-redesign-path/14.1-CONTEXT.md §D-11 (the recipe used for the 14.1 light-theme capture — same Puppeteer/Playwright pattern)
  </read_first>
  <files>
    _temp/path-redesign/demo-home-CA-hybrid-light.png,
    _temp/path-redesign/screenshot-home-light.mjs
  </files>
  <action>
    Create a Node script `_temp/path-redesign/screenshot-home-light.mjs` that uses Playwright (already installed) to:

    1. Launch chromium with viewport `{ width: 390, height: 844 }` and `deviceScaleFactor: 2`
    2. Navigate to `file://${path.resolve(__dirname, 'demo-home-CA-hybrid.html')}` (or via `npx http-server` on port 8080 if file:// breaks JSX/Babel CDN execution — match 14.1's working approach exactly)
    3. After page load, evaluate: `document.documentElement.setAttribute('data-theme', 'light')` (or set a `kb_theme=light` cookie if the demo HTML reads cookies — check the demo file)
    4. Wait 500ms for any theme-flip CSS to settle (`await page.waitForTimeout(500)`)
    5. Screenshot full page to `_temp/path-redesign/demo-home-CA-hybrid-light.png` with `fullPage: true`
    6. Close browser

    Then run the script via `node _temp/path-redesign/screenshot-home-light.mjs` and commit BOTH the script and the resulting PNG.

    Reference: `_temp/path-redesign/screenshot-*.mjs` files from 14.1 land the same shape. Read those (any of `screenshot-CA-hybrid.mjs`, `screenshot-CA-hybrid-light.mjs`) and adapt the path to `demo-home-CA-hybrid.html`. Do NOT invent a new pattern — copy 14.1's verbatim and swap the input HTML path.

    If the demo HTML's React-on-Babel renders without theming hooks, fall back to: open the file in a headed browser, use DevTools to set `documentElement.dataset.theme = 'light'`, capture via OS screenshot at 390×844 device pixel ratio 2 (782×1688 PNG output). Document the manual fallback in the script's header comment.

    Commit message: `chore(14.2): capture light-theme reference screenshot for home redesign (D-11)`
  </action>
  <acceptance_criteria>
    - File `_temp/path-redesign/demo-home-CA-hybrid-light.png` exists
    - File `_temp/path-redesign/screenshot-home-light.mjs` exists and contains the literal string `data-theme` (i.e., the script demonstrably sets light theme)
    - Output PNG is non-empty (>10 KB) and is a valid PNG (file command reports `PNG image data`)
    - Output PNG dimensions are 780×1688 OR 390×844 OR similar mobile-portrait shape (NOT 1920×1080 desktop — verify with `file _temp/path-redesign/demo-home-CA-hybrid-light.png`)
    - Both files committed via `git ls-files _temp/path-redesign/demo-home-CA-hybrid-light.png _temp/path-redesign/screenshot-home-light.mjs`
  </acceptance_criteria>
  <verify>
    <automated>test -f _temp/path-redesign/demo-home-CA-hybrid-light.png &amp;&amp; test -f _temp/path-redesign/screenshot-home-light.mjs &amp;&amp; file _temp/path-redesign/demo-home-CA-hybrid-light.png | grep -q "PNG image" &amp;&amp; grep -q "data-theme" _temp/path-redesign/screenshot-home-light.mjs</automated>
  </verify>
  <done>Light-theme reference PNG and reproducible capture script both committed; PNG is valid mobile-portrait dimensions.</done>
</task>

<task type="auto">
  <name>Task 3: Verify /kana route shape and lesson JSONB shape, record findings inline</name>
  <read_first>
    - src/app/kana/page.tsx (lines 30–40 — the `isValidScriptParam` + `searchParams.get("script")` block; CONFIRMED in plan-phase as `?script=hiragana|katakana` query param)
    - src/lib/types/lesson.ts (lines 64–79 — Verse interface with `verse_number`; the lesson JSONB shape is `{ verses: Verse[] }` because Plan 11.6-08 SUMMARY references `lesson.verses[]` as canonical)
    - src/app/path/components/KanaCheckpointNode.tsx (line 64 — `const href = ` `/kana?script=${script}` `;` — this is the 14.1 D-05 precedent that 14.2 D-08 must match)
  </read_first>
  <files>.planning/phases/14.2-redesign-home/14.2-01-VERIFICATIONS.md</files>
  <action>
    Create `.planning/phases/14.2-redesign-home/14.2-01-VERIFICATIONS.md` with EXACT content (copy verbatim, do not paraphrase):

    ```markdown
    # Phase 14.2 Wave 1 Verifications

    Captured: {ISO date} on commit {git rev-parse HEAD}

    ## V1 — /kana route shape (D-08)

    **Question:** Does /kana accept `?script=hiragana|katakana` query param (14.1 D-05) or `/kana/{script}` path segment (SPEC §Req 5)?

    **Verified:** `?script=hiragana|katakana` query param.

    **Evidence:**
    - `src/app/kana/page.tsx` lines 30–40: `isValidScriptParam(searchParams.get("script"))` reads `script` from query string.
    - `src/app/path/components/KanaCheckpointNode.tsx` line 64: `const href = `/kana?script=${script}`;` — 14.1's already-shipped link pattern.
    - No `[script]` directory exists under `src/app/kana/` (verified via `ls src/app/kana/`).

    **Resolution:** SPEC §Req 5 reference to `/kana/{script}` is a copy-from-demo without route verification. Phase 14.2 Foundations cards (Plan 14.2-05) use `/kana?script=hiragana` and `/kana?script=katakana` matching 14.1 D-05.

    ## V2 — song_versions.lesson JSONB shape (D-05)

    **Question:** Is the lesson JSONB shape `{ verses: [...] }` (so `getHeroSong` can use `jsonb_array_length(sv.lesson->'verses')`)?

    **Verified:** Yes. Lesson JSONB has top-level `verses: Verse[]` array.

    **Evidence:**
    - `src/lib/types/lesson.ts` lines 64–79: `Verse` interface with `verse_number`, `start_time_ms`, `end_time_ms`, `tokens`, `translations`. The full `Lesson` type wraps `verses: Verse[]` at the top level.
    - STATE.md Plan 11.6-08 SUMMARY: `jsonb_array_length(sv.lesson -> 'verses')` already used as the SongCard `verses_dominated_pct` denominator. Phase 14.2 reuses this exact selector.

    **Resolution:** `getHeroSong` Plan 14.2-03 uses the SQL fragment `jsonb_array_length(sv.lesson->'verses')` for verseCount, with `COALESCE(..., 0)` wrapper for null-lesson rows.

    ## V3 — kb_theme cookie writes for screenshot test fixtures

    **Question (Claude's Discretion):** Will Playwright e2e specs for Wave 5 use the `kb_theme=light` cookie pattern from `tests/e2e/path-visual-dark.spec.ts` lines 24–28?

    **Verified:** Yes — the cookie-driven theme pattern works server-side (the zero-flash script at `src/app/layout.tsx` lines 78–82 reads the cookie BEFORE first paint). Wave 5 specs use:
    ```typescript
    await context.addCookies([
      { name: "kb_theme", value: "dark", url: "http://localhost:7000", sameSite: "Lax" },
    ]);
    ```

    **Evidence:** Existing `tests/e2e/path-visual-dark.spec.ts` already uses this pattern in production; layout.tsx zero-flash script (committed Phase 14 D-09) honors the cookie deterministically.
    ```

    Replace `{ISO date}` with `date -u +%Y-%m-%dT%H:%M:%SZ` output and `{git rev-parse HEAD}` with the actual short SHA.

    Commit message: `docs(14.2): record Wave 1 verifications for /kana shape, lesson JSONB, theme cookie`
  </action>
  <acceptance_criteria>
    - File `.planning/phases/14.2-redesign-home/14.2-01-VERIFICATIONS.md` exists
    - File contains the literal string `?script=hiragana|katakana query param`
    - File contains the literal string `jsonb_array_length(sv.lesson->'verses')`
    - File contains the literal string `kb_theme=light cookie pattern` OR `kb_theme cookie writes for screenshot test fixtures`
    - File contains a 7+ character hex SHA (matches `[0-9a-f]{7,}`)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/14.2-redesign-home/14.2-01-VERIFICATIONS.md &amp;&amp; grep -q "?script=hiragana|katakana query param" .planning/phases/14.2-redesign-home/14.2-01-VERIFICATIONS.md &amp;&amp; grep -q "jsonb_array_length(sv.lesson->'verses')" .planning/phases/14.2-redesign-home/14.2-01-VERIFICATIONS.md &amp;&amp; grep -qE "[0-9a-f]{7,}" .planning/phases/14.2-redesign-home/14.2-01-VERIFICATIONS.md</automated>
  </verify>
  <done>Verification doc committed with three confirmed answers + evidence references; downstream plans 14.2-03 (getHeroSong), 14.2-05 (Foundations), 14.2-09 (visual specs) can reference V1/V2/V3 directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→repo | Captured PNG and baseline.txt land in `_temp/` and become committed artifacts. Trust boundary is the developer's local machine; outputs are deterministic given the demo HTML and `npm run build` reproducibility. |
| YouTube CDN→demo HTML | The demo HTML loads cover-art images from `img.youtube.com`. URLs in the demo are hardcoded (no user input); this is a trusted CDN. |
| cookie→layout.tsx zero-flash script | The kb_theme cookie value is read by the inline script. Already mitigated by Phase 14 T-14-03-01 (the regex `(kb_theme=(system|light|dark))` constrains the value). NOT a Phase 14.2 concern — Wave 1 only consumes the existing mitigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.2-01-01 | Information Disclosure | _temp/baseline-bundle-pre-14.2.txt | accept | File contains only route names + bundle sizes; no secrets, PII, or runtime config. Committing is intentional for cross-phase traceability. |
| T-14.2-01-02 | Tampering | _temp/path-redesign/demo-home-CA-hybrid-light.png | accept | PNG is a static visual artifact compared bit-by-bit by Playwright snapshot diff. Tampering would cause spec failure (the desired outcome — visual regression detected). No execution surface. |
| T-14.2-01-03 | Spoofing | _temp/path-redesign/screenshot-home-light.mjs | mitigate | Script reads only from local `_temp/path-redesign/demo-home-CA-hybrid.html` (no network input that could spoof reference content). Verify via `read_first` that the demo HTML hash matches the committed version (Wave 1 captures against the locked demo, not a mutated copy). |
</threat_model>

<verification>
- All three task acceptance criteria pass via the per-task `<verify>` automated commands.
- `git log --oneline -5` shows three new commits matching the prescribed messages (chore(14.2): baseline, chore(14.2): light-theme, docs(14.2): verifications).
- No source code under `src/`, `tests/`, or `prisma/` (none in this repo) modified.
</verification>

<success_criteria>
- Bundle baseline file committed with route sizes for `/`, `/path`, `/songs/[slug]` and current commit SHA
- Light-theme reference PNG committed at mobile-portrait dimensions, regenerable via `_temp/path-redesign/screenshot-home-light.mjs`
- Verification doc committed confirming `/kana?script=` query-param shape, `lesson->'verses'` JSONB path, and `kb_theme=light` cookie pattern for Wave 5 e2e specs
- All Wave 2+ plans can reference this plan's deliverables without re-running discovery
</success_criteria>

<output>
After completion, create `.planning/phases/14.2-redesign-home/14.2-01-SUMMARY.md` with:
- Captured baseline values (paste the three route lines from `_temp/baseline-bundle-pre-14.2.txt`)
- Path to light-theme PNG and its dimensions
- One-line confirmation of each Wave 1 verification (V1 query-param, V2 JSONB shape, V3 theme cookie)
- Commit SHAs of the three commits this plan produced
</output>
