---
phase: 14.2-redesign-home
plan: 15
status: complete
completed_at: 2026-05-05T11:34:00+01:00
---

# Phase 14.2 Plan 15 - Closeout Summary

## What Shipped

- Added `tests/e2e/home-axe.spec.ts`, covering `/` in 4 axe-core contexts:
  - auth + dark
  - auth + light
  - anon + dark
  - anon + light
- Added `.planning/phases/14.2-redesign-home/14.2-15-BUNDLE-DELTA.md`.
- Split the hero image fallback handler into `src/app/components/home/HeroCoverImage.tsx` so `HeroFeatured` stays a server component without passing event handlers across the RSC boundary.
- Added `--color-accent-readable` and moved header foreground accent text to it, preserving `--color-accent` for buttons/fills while clearing dark-theme AA contrast in the global header.
- Replaced the unauth header sign-in control with a plain `/sign-in` link. This avoids Clerk's `SignInButton` child-shape runtime error during anonymous/light axe runs.
- Fixed `getContinueLearning.test.ts` so its grammar-status fixture matches the production song-level `has_grammar` query and seeds a deterministic 3-star row.

## Bundle Delta

Measured by `npm run build` on commit `0084953cb322211a4a2f6213760c52c1b196790c`.

| Route | Pre | Post | Delta | Status |
|---|---:|---:|---:|---|
| `/` | 111 kB | 121 kB | +10 kB | NOTE |
| `/path` | 124 kB | 124 kB | 0 kB | OK |
| `/songs/[slug]` | 129 kB | 129 kB | 0 kB | OK |

`/` is exactly at the D-10 +10 KB soft cap, not over it.

## Verification

Passed:

- `npx playwright test tests/e2e/home-axe.spec.ts --workers=1 --reporter=line`
  - 4 passed
- `npx playwright test tests/e2e/mobile-parity.spec.ts -g "no horizontal scroll" --workers=1 --reporter=line`
  - 11 passed
- `npm run build`
  - passed
- `npx vitest run src/app/components/home src/app/components/MobileNavSheet.test.tsx src/app/path/components/KanaCheckpointNode.test.tsx tests/integration/getHeroSong.test.ts tests/integration/getContinueLearning.test.ts`
  - 12 files passed
  - 74 tests passed
- `npx eslint ...14.2 closeout files...`
  - 0 errors
  - warnings only: existing `<img>` guidance, font-loading warning, existing unused imports in `queries.ts`, ignored e2e/integration files

Not fully green:

- `npm run lint -- <files>` is not usable as a targeted command because the package script expands to `eslint .`, pulling in `.claude/worktrees` and unrelated generated handoff files.
- `npx playwright test --workers=1` timed out before producing useful failure output.
- `npm test` still fails in broader cross-phase integration areas unrelated to 14.2 closeout, including DB/schema drift (`column vi.surface does not exist`), premium-gate review queue expectations, `unstable_cache` outside Next's incremental cache context, and older progress/idempotency tests.

## Human Checkpoint

Bundle delta status is NOTE, not OVER. No bundle trim is required by Plan 14.2-15. The mobile sideways-scroll concern was explicitly rechecked: all 11 mobile no-horizontal-scroll route checks passed at 390x844.

Phase 14.2 is ready to close from the 14.2-specific verification set.
