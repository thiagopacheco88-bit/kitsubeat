---
phase: 14.2-redesign-home
plan: 12
type: execute
wave: 6
depends_on: [14.2-11]
files_modified:
  - src/app/path/page.tsx
  - src/app/path/components/PathHeader.tsx
  - src/app/path/components/PathHeader.test.tsx
autonomous: true
requirements:
  - SPEC-Req-1 (cross-surface header coherence — exactly one wordmark on /path)
  - SPEC-AC-12 (cross-surface header coherence)
  - CONTEXT-D-07 (delete PathHeader.tsx + PathHeader.test.tsx entirely; not thinned)

must_haves:
  truths:
    - "src/app/path/components/PathHeader.tsx is DELETED (file no longer exists)"
    - "src/app/path/components/PathHeader.test.tsx is DELETED (file no longer exists)"
    - "src/app/path/page.tsx no longer imports or renders <PathHeader>; renders <HeroProgress> directly as first content child"
    - "src/app/path/components/LanternStreak.tsx is NOT moved (CONTEXT D-07 leaves file in place)"
    - "Cross-surface coherence: /path renders exactly ONE wordmark + ONE LanternStreak chip post-merge (both from layout.tsx)"
    - "Existing path e2e specs continue to pass (path-visual-dark, path-kana-checkpoint-nav, path-reduced-motion, path-m1-invariant)"
  artifacts:
    - path: "src/app/path/page.tsx"
      provides: "Modified /path page without PathHeader import or render"
      contains: "<HeroProgress"
  key_links:
    - from: "src/app/path/page.tsx"
      to: "src/app/layout.tsx (Plan 14.2-11) — wordmark + LanternStreak now global"
      via: "removed local <PathHeader> render — global header is the single source"
      pattern: "(no PathHeader import)"
---

<objective>
Delete `src/app/path/components/PathHeader.tsx` AND `src/app/path/components/PathHeader.test.tsx` entirely (CONTEXT D-07: "delete-not-thin pattern, mirrors 14.1 D-06 PathHud deletion"). Edit `src/app/path/page.tsx` to drop the `<PathHeader>` import and render `<HeroProgress>` directly as the first content child.

Purpose: Once Plan 14.2-11 ships the global header with absorbed wordmark + LanternStreak, the page-local `<PathHeader>` on `/path` becomes redundant. Leaving it would render TWO wordmarks + TWO streak chips on `/path`, breaking SPEC AC #12 (cross-surface coherence: "/path renders exactly ONE wordmark"). PathHeader.tsx's whole job moved to layout.tsx — the file has no remaining responsibility.

LanternStreak.tsx STAYS at `src/app/path/components/LanternStreak.tsx` per CONTEXT D-07 — layout.tsx imports it across the route boundary. Defer relocation to Phase 14.4 when a second global-chrome consumer lands.

Output: Two files deleted; src/app/path/page.tsx edited to remove PathHeader import + render. Test assertions about wordmark/streak rendering get re-anchored to Plan 14.2-13's `tests/e2e/global-header-cross-surface.spec.ts` (covered separately).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14.2-redesign-home/14.2-CONTEXT.md
@.planning/phases/14.2-redesign-home/14.2-SPEC.md
@.planning/phases/14.2-redesign-home/14.2-PATTERNS.md
@src/app/path/page.tsx
@src/app/path/components/PathHeader.tsx
@src/app/path/components/PathHeader.test.tsx
@src/app/path/components/LanternStreak.tsx

<interfaces>
<!-- From src/app/path/page.tsx (CURRENT — has line 12 import + line 63 render of <PathHeader>). -->
import { PathHeader } from "./components/PathHeader";
// ...
<PathHeader streakCurrent={state.streak_current} />
// PATTERNS.md line 932 — exact lines to remove.

<!-- From src/app/path/components/PathHeader.tsx (DELETED in this plan). -->
// File contains the fox+wordmark + LanternStreak composition that's now
// absorbed into layout.tsx by Plan 14.2-11.

<!-- From src/app/path/components/LanternStreak.tsx (PRESERVED per D-07). -->
// Imported by layout.tsx (Plan 14.2-11) across the route boundary.
// File NOT touched in this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Edit src/app/path/page.tsx — drop PathHeader import + render</name>
  <read_first>
    - src/app/path/page.tsx (entire current file — find the PathHeader import line + render line)
    - src/app/path/components/HeroProgress.tsx (verify it exists and is the first content child after PathHeader removal)
    - .planning/phases/14.2-redesign-home/14.2-PATTERNS.md §path/page.tsx (line 932)
  </read_first>
  <action>
    Use the Edit tool to make TWO targeted changes:

    **Edit 1 — Remove the PathHeader import line.** Find the line:
    ```typescript
    import { PathHeader } from "./components/PathHeader";
    ```
    DELETE this entire line.

    **Edit 2 — Remove the PathHeader render.** Find the line:
    ```typescript
    <PathHeader streakCurrent={state.streak_current} />
    ```
    DELETE this entire line. The next sibling JSX element (likely `<HeroProgress>`) becomes the first content child after the `<h1 className="sr-only">` heading.

    **Verification post-edit:**
    - `grep -c "PathHeader" src/app/path/page.tsx` MUST return 0 (no remaining references)
    - `grep -c "<HeroProgress" src/app/path/page.tsx` MUST return ≥ 1 (HeroProgress preserved)

    If `state.streak_current` was ONLY consumed by PathHeader (no other usage in path/page.tsx), this is fine — the global header now shows the streak (Plan 14.2-11). The `state` variable continues to be fetched for `current_path_node_slug` and other PathMap consumption. Do NOT remove the `state` fetch.

    Run `npx tsc --noEmit` and `npm run lint -- src/app/path/page.tsx`.

    Commit message: `refactor(14.2): drop page-local PathHeader from /path - global header replaces (D-07)`
  </action>
  <acceptance_criteria>
    - `src/app/path/page.tsx` does NOT contain the literal string `PathHeader` (`grep -c "PathHeader" src/app/path/page.tsx` returns 0)
    - `src/app/path/page.tsx` still contains the literal string `<HeroProgress` (HeroProgress render preserved)
    - `src/app/path/page.tsx` still contains `getUserGamificationState` (state fetch preserved for PathMap consumption)
    - Running `npx tsc --noEmit` exits 0 (no orphan PathHeader import or render reference anywhere else)
    - Running `npm run lint -- src/app/path/page.tsx` reports 0 violations
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "PathHeader" src/app/path/page.tsx &amp;&amp; grep -q "<HeroProgress" src/app/path/page.tsx &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint -- src/app/path/page.tsx</automated>
  </verify>
  <done>path/page.tsx no longer imports or renders PathHeader; HeroProgress preserved as first content child; tsc + lint clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete src/app/path/components/PathHeader.tsx + PathHeader.test.tsx</name>
  <read_first>
    - src/app/path/components/PathHeader.tsx (verify file exists and is the one being deleted)
    - src/app/path/components/PathHeader.test.tsx (verify file exists and is the one being deleted)
    - .planning/phases/14.2-redesign-home/14.2-CONTEXT.md §D-07 ("delete entirely... mirrors 14.1 D-06 PathHud playbook")
    - .planning/phases/14.2-redesign-home/14.2-PATTERNS.md §Deleted (line 972)
  </read_first>
  <action>
    **Pre-deletion assertion enumeration (per revision INFO — preserve test-coverage audit trail):**

    BEFORE deleting `PathHeader.test.tsx`, read its full contents and produce an inline SUMMARY table. The table goes in `.planning/phases/14.2-redesign-home/14.2-12-SUMMARY.md` (see Output section) with three columns:

    | Assertion (PathHeader.test.tsx) | Carried by (post-delete) | Gap risk |
    |--------------------------------|--------------------------|----------|
    | e.g. "renders fox-mark SVG"   | Plan 13 global-header-cross-surface.spec.ts asserts brand-wordmark on /path | LOW |
    | e.g. "streak count = 0 styling" | NOT carried by global spec — gap | HIGH — file follow-up issue |
    | ...                           | ...                      | ...      |

    For each PathHeader.test.tsx assertion: identify whether `tests/e2e/global-header-cross-surface.spec.ts` (Plan 13) covers the equivalent contract end-to-end. Any assertion not carried by global-header-cross-surface.spec.ts MUST be flagged in the SUMMARY as a follow-up — either (a) extend global-header-cross-surface.spec.ts in a quick edit during this plan, OR (b) file a plan-14.4 issue with the gap details.

    Delete both files via git AFTER the enumeration is recorded:

    ```bash
    git rm src/app/path/components/PathHeader.tsx
    git rm src/app/path/components/PathHeader.test.tsx
    ```

    Do NOT thin, rename, or wrap PathHeader.tsx. CONTEXT D-07 explicitly rejects:
    - Thin shell wrappers (extra indirection for no behavioral benefit)
    - In-place rename (no name fits the new responsibility, which is "nothing")

    Mirrors 14.1 D-06 precedent (PathHud.tsx was deleted entirely).

    **Verification post-delete:**
    - `git ls-files src/app/path/components/PathHeader.tsx` returns empty (file no longer tracked)
    - `git ls-files src/app/path/components/PathHeader.test.tsx` returns empty
    - `test -e src/app/path/components/PathHeader.tsx` returns non-zero exit (file no longer on disk)
    - `grep -r "from.*PathHeader" src/` returns no results (no orphan imports anywhere in src/)
    - `grep -r "from.*PathHeader" tests/` returns no results

    Run `npx tsc --noEmit` (catches any orphan imports across the project).
    Run `npm test` to confirm test suite still green (PathHeader test no longer in suite).
    Run `npm run build` to confirm production build still works.

    Commit message: `chore(14.2): delete PathHeader.tsx + PathHeader.test.tsx (responsibility moved to global layout / D-07)`
  </action>
  <acceptance_criteria>
    - `test -e src/app/path/components/PathHeader.tsx` returns non-zero (file deleted)
    - `test -e src/app/path/components/PathHeader.test.tsx` returns non-zero (file deleted)
    - `git ls-files src/app/path/components/PathHeader.tsx src/app/path/components/PathHeader.test.tsx` returns empty
    - `grep -rn "PathHeader" src/ tests/ 2>/dev/null` returns no results (no orphan references)
    - `src/app/path/components/LanternStreak.tsx` STILL exists (D-07 keeps file in place)
    - Running `npx tsc --noEmit` exits 0 (no orphan imports)
    - Running `npm test` exits 0 (no test references PathHeader.test.tsx)
    - Running `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>! test -e src/app/path/components/PathHeader.tsx &amp;&amp; ! test -e src/app/path/components/PathHeader.test.tsx &amp;&amp; test -e src/app/path/components/LanternStreak.tsx &amp;&amp; ! grep -rn "PathHeader" src/ tests/ 2>/dev/null &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Both PathHeader files deleted from disk + git index; LanternStreak.tsx preserved; no orphan references; tsc + tests + build all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Removed PathHeader render -> /path DOM | The local fox+wordmark + LanternStreak no longer appear; global layout.tsx provides them. No new boundary introduced. |
| LanternStreak file location | Stays at src/app/path/components/LanternStreak.tsx; cross-route import from layout.tsx works fine in Next.js app dir (no route-locality enforcement on imports). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.2-12-01 | Tampering | Orphan PathHeader import in some other file | mitigate | grep -rn "PathHeader" src/ tests/ asserts zero remaining references. tsc would also catch any TypeScript-level orphan. Acceptance criteria enforces both. |
| T-14.2-12-02 | Information Disclosure | Wordmark/streak duplication on /path post-merge | mitigate | Plan 14.2-13's tests/e2e/global-header-cross-surface.spec.ts asserts /path renders exactly ONE wordmark + ONE LanternStreak. This plan's deletion is verified by that e2e spec post-merge. |
| T-14.2-12-03 | Tampering | LanternStreak.tsx accidentally deleted | mitigate | Acceptance criteria explicitly asserts `test -e src/app/path/components/LanternStreak.tsx` succeeds. D-07 makes the keep-in-place decision binding. |
| T-14.2-12-04 | Loss of test coverage | Wordmark + streak rendering no longer unit-tested | accept | Coverage moves to e2e (global-header-cross-surface.spec.ts ships in Plan 14.2-13). Cross-surface coherence is inherently an e2e contract. Per CONTEXT D-07: "test assertions get re-anchored to a new tests/e2e/global-header.spec.ts". |
</threat_model>

<verification>
- Both PathHeader files removed from disk + git
- src/app/path/page.tsx no longer references PathHeader
- LanternStreak.tsx STILL exists at original path
- npx tsc --noEmit + npm test + npm run build all pass
- Existing /path e2e specs (path-visual-dark, path-kana-checkpoint-nav, path-reduced-motion, path-m1-invariant) continue to pass — verify by re-running locally before commit
- Two commits (page.tsx edit; file deletions)
</verification>

<success_criteria>
- /path renders without runtime error post-merge
- /path shows exactly ONE wordmark (from global layout.tsx) and ONE LanternStreak chip (from global layout.tsx, auth-conditional)
- No orphan PathHeader references anywhere in src/ or tests/
- LanternStreak.tsx file location unchanged (D-07 binding)
- Plan 14.2-13's cross-surface header e2e spec passes
</success_criteria>

<output>
After completion, create `.planning/phases/14.2-redesign-home/14.2-12-SUMMARY.md` documenting:
- Confirmation of file deletions (PathHeader.tsx + PathHeader.test.tsx)
- Confirmation that LanternStreak.tsx is preserved at original path
- Edit summary on src/app/path/page.tsx (lines removed)
- **Per revision INFO — Assertion-loss enumeration table** (assertion / carried-by / gap-risk columns; one row per PathHeader.test.tsx `it()` block; HIGH-risk gaps flagged with follow-up plan)
- Confirmation that /path e2e specs still pass
- Commit SHA(s)
</output>
