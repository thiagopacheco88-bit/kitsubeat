---
phase: 14.2-redesign-home
plan: 12
subsystem: path-page-cleanup
tags: [delete, refactor, pathheader, cross-surface-coherence, d-07]
dependency_graph:
  requires: [14.2-11]
  provides: [pathheader-deleted, path-page-no-local-header]
  affects: [src/app/path/page.tsx]
tech_stack:
  added: []
  patterns: [delete-not-thin, mirrors-14.1-d-06]
key_files:
  created: []
  modified:
    - src/app/path/page.tsx
  deleted:
    - src/app/path/components/PathHeader.tsx
    - src/app/path/components/PathHeader.test.tsx
decisions:
  - PathHeader.tsx deleted entirely per CONTEXT D-07 (delete-not-thin pattern, mirrors 14.1 D-06 PathHud deletion)
  - LanternStreak.tsx preserved at src/app/path/components/ per D-07 binding decision
  - PathHeader.tsx comment in sr-only h1 updated to reference global layout.tsx instead
metrics:
  duration: ~5min
  completed: 2026-05-03T23:40:00Z
  tasks_completed: 2
  files_changed: 1 modified + 2 deleted
---

# Phase 14.2 Plan 12: Delete PathHeader — Cross-Surface Header Coherence Summary

**One-liner:** Deleted page-local PathHeader.tsx + test from /path and dropped its render from page.tsx; global layout.tsx header (Plan 14.2-11) is now sole wordmark + LanternStreak source.

## What Was Built

### Task 1: src/app/path/page.tsx — Drop PathHeader import + render

- Removed `import { PathHeader } from "./components/PathHeader";` line
- Removed `<PathHeader streakCurrent={state.streak_current} />` render; `<HeroProgress>` is now the first content child after the `sr-only` h1
- Updated sr-only h1 comment to reference `global layout.tsx` instead of `PathHeader`
- `state` fetch (`getUserGamificationState`) preserved — still consumed by `<HeroProgress>`, `<PathMap>`, `<ContinueAnchor>`, `<StarterPick>`
- `grep -c "PathHeader" src/app/path/page.tsx` = 0 (zero remaining references)
- `grep -c "<HeroProgress" src/app/path/page.tsx` = 1 (preserved as first content child)

**Commit:** c0f633f — `refactor(14.2): drop page-local PathHeader from /path - global header replaces (D-07)`

### Task 2: Delete PathHeader.tsx + PathHeader.test.tsx

Both files removed via `git rm`. LanternStreak.tsx preserved at original path.

**Pre-deletion file inventory:**
- `src/app/path/components/PathHeader.tsx` — 107 lines; fox-mark SVG + KitsuBeat wordmark + LanternStreak composition. Entire job moved to `src/app/layout.tsx` by Plan 14.2-11.
- `src/app/path/components/PathHeader.test.tsx` — 44 lines; 5 Vitest unit tests.

**Post-deletion verifications:**
- `test -e src/app/path/components/PathHeader.tsx` → non-zero (file absent)
- `test -e src/app/path/components/PathHeader.test.tsx` → non-zero (file absent)
- `git ls-files src/app/path/components/PathHeader.tsx` → empty (not tracked)
- `test -e src/app/path/components/LanternStreak.tsx` → zero exit (file preserved)
- `grep -rn "from.*PathHeader" src/ tests/` → no results (no orphan imports)
- `npx tsc --noEmit` → only pre-existing KanaCheckpointNode.test.tsx mock type error (present since before Plan 14.2-12; unrelated)
- `npm run build` → succeeded (exit 0); /path route: 6.03 kB route-specific, 124 kB First Load JS

**Commit:** 4da1c20 — `chore(14.2): delete PathHeader.tsx + PathHeader.test.tsx (responsibility moved to global layout / D-07)`

## Assertion-Loss Enumeration Table (PathHeader.test.tsx)

Required per plan output spec: one row per `it()` block; identifies whether Plan 14.2-13 `global-header-cross-surface.spec.ts` carries the equivalent contract.

| Assertion (PathHeader.test.tsx) | Carried by (post-delete) | Gap risk |
|--------------------------------|--------------------------|----------|
| `renders with data-testid='path-header'` | NOT carried — `path-header` testid no longer exists; global header uses `data-testid="brand-wordmark"`. Plan 14.2-13 cross-surface spec does not assert this old testid (correctly). | LOW — testid was PathHeader-specific; global header asserts brand-wordmark presence instead. |
| `contains 'KitsuBeat' wordmark text` | Plan 14.2-13 `global-header-cross-surface.spec.ts` asserts exactly ONE KitsuBeat wordmark occurrence on `/path` via `[data-testid="brand-wordmark"]` | LOW |
| `composes LanternStreak (data-testid present)` | Plan 14.2-13 asserts `[data-testid="global-lantern-streak"]` present on `/path` (auth-conditional) | LOW |
| `threads streakCurrent into LanternStreak aria-label` | NOT explicitly carried — streak count threading not re-asserted at e2e level in Plan 14.2-13's current scope | MEDIUM — follow-up: Plan 14.2-13 should add an auth-state fixture with streak > 0 and assert `aria-label` on `[data-testid="global-lantern-streak"]` resolves to `{N}-day streak`. |
| `emphasis span 'Beat' carries the accent token reference` | NOT explicitly carried as a className inspection — e2e specs do not inspect className internals | LOW — accent token is applied by the same wordmark composition now in layout.tsx; visual screenshot diff (Plan 14.2 Wave 6 visual specs) implicitly covers the rendered appearance. |

**Gap follow-up:** The `threads streakCurrent into LanternStreak aria-label` assertion (MEDIUM gap) should be added to `tests/e2e/global-header-cross-surface.spec.ts` in Plan 14.2-13. Specifically: when authenticated with `streak_current > 0`, assert that the element at `[data-testid="global-lantern-streak"]` has `aria-label` matching `{N}-day streak`. This covers the same threading contract at the correct layer (e2e, post-migration).

## File Confirmations

- `src/app/path/components/PathHeader.tsx` — DELETED
- `src/app/path/components/PathHeader.test.tsx` — DELETED
- `src/app/path/components/LanternStreak.tsx` — PRESERVED at original path (D-07 binding)
- `src/app/path/page.tsx` — MODIFIED; zero PathHeader references; HeroProgress is first content child

## Deviations from Plan

**1. [Rule 1 - Bug] Comment in sr-only h1 still referenced PathHeader**
- **Found during:** Task 1 post-edit grep verification
- **Issue:** The plan's acceptance criteria required `grep -c "PathHeader" src/app/path/page.tsx` to return 0. After removing the import and render lines, a JSDoc comment `"PathHeader provides the visible top chrome"` still remained.
- **Fix:** Updated comment text to reference `"Global layout.tsx provides the visible top chrome"` — accurate post-Plan-14.2-11.
- **Files modified:** `src/app/path/page.tsx`
- **Commit:** c0f633f

## Test Results

- `npx tsc --noEmit`: pre-existing KanaCheckpointNode.test.tsx mock type error only (present before Plan 14.2-12)
- `npx vitest run`: 718 passed / 57 failed — all failures are pre-existing DB-integration and unrelated test failures; zero PathHeader test failures (suite no longer includes PathHeader.test.tsx)
- `npm run build`: exit 0; /path builds cleanly at 6.03 kB route-specific

## Commits

| Hash | Message | Files |
|------|---------|-------|
| c0f633f | refactor(14.2): drop page-local PathHeader from /path - global header replaces (D-07) | src/app/path/page.tsx |
| 4da1c20 | chore(14.2): delete PathHeader.tsx + PathHeader.test.tsx (responsibility moved to global layout / D-07) | PathHeader.tsx, PathHeader.test.tsx (deleted) |

## Known Stubs

None — this plan is a pure deletion + refactor. No new components or data flows introduced.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. All threat model items resolved:

- T-14.2-12-01 (orphan PathHeader import): `grep -rn "from.*PathHeader" src/ tests/` returns empty. tsc confirms no TypeScript-level orphan. MITIGATED.
- T-14.2-12-02 (wordmark/streak duplication on /path): PathHeader deleted; global layout.tsx is sole source. Plan 14.2-13 e2e spec will assert ONE wordmark + ONE LanternStreak on /path post-merge. MITIGATED.
- T-14.2-12-03 (LanternStreak.tsx accidentally deleted): `test -e src/app/path/components/LanternStreak.tsx` passes. File preserved. MITIGATED.
- T-14.2-12-04 (loss of test coverage): Coverage moving to e2e in Plan 14.2-13 per CONTEXT D-07 decision. ACCEPTED — assertion-loss table above tracks the one MEDIUM gap.

## Self-Check: PASSED

| Check | Result |
|-------|--------|
| `src/app/path/components/PathHeader.tsx` deleted | PASS |
| `src/app/path/components/PathHeader.test.tsx` deleted | PASS |
| `src/app/path/components/LanternStreak.tsx` preserved | PASS |
| `src/app/path/page.tsx` has zero PathHeader references | PASS |
| `src/app/path/page.tsx` contains `<HeroProgress` | PASS |
| Task 1 commit c0f633f exists | PASS |
| Task 2 commit 4da1c20 exists | PASS |
| SUMMARY.md exists | PASS |
