# Phase 14.2 - Bundle Delta Report (CONTEXT D-10)

**Captured:** 2026-05-05T07:55:15.1203641+01:00 on commit `0084953cb322211a4a2f6213760c52c1b196790c`
**Baseline:** `_temp/baseline-bundle-pre-14.2.txt` (Plan 14.2-01)
**Soft cap on /:** +10 KB First Load JS (CONTEXT D-10 - REPORTING ONLY, not a hard CI gate)

## First Load JS Comparison

| Route         | Pre (Plan 01) | Post (Phase 14.2 close) | Delta | Status |
|---------------|--------------:|------------------------:|------:|--------|
| `/`           | 111 kB        | 121 kB                  | +10 kB | NOTE |
| `/path`       | 124 kB        | 124 kB                  | 0 kB | OK |
| `/songs/[slug]` | 129 kB      | 129 kB                  | 0 kB | OK - unchanged; Phase 13 size-limit gate remains independent |

Status legend:
- **OK** - delta within +/- 5 KB
- **NOTE** - delta within +5 to +10 KB, still within the D-10 soft cap
- **OVER** - delta > +10 KB, requiring user discussion per D-10

## Cross-Surface Watch

`<LanternStreak>` now lives in the global layout instead of only the path chrome. The measured result is effectively neutral on the watched non-home routes:

- `/path`: 0 kB First Load JS delta, matching the expectation that removing page-local PathHeader chrome offsets the global streak chip.
- `/songs/[slug]`: 0 kB First Load JS delta in the route table. The global header change did not move the route-level number.
- `/`: +10 kB First Load JS. This is exactly at the D-10 report-only soft cap, so it is a NOTE, not OVER.

## Notes / Open Items

- `npm run build` completed successfully in the main worktree with no workaround.
- Next emitted the existing metadataBase warning for Open Graph images; this is unrelated to 14.2 bundle sizing.
- The `/` delta is exactly +10 kB, so no trim is required by the plan. If later home work adds more client islands, revisit lazy-loading carousel-only behavior first.

## Phase 13 Hard Gate Verification

`/songs/[slug]` remains unchanged at 129 kB First Load JS in the Next route table. The Phase 13 gzipped route budget remains enforced separately by `size-limit`; this report is informational only.

---

*Report generated by Plan 14.2-15. Reviewed as part of phase close.*
