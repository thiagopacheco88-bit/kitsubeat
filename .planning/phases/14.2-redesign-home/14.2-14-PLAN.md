---
phase: 14.2-redesign-home
plan: 14
type: execute
wave: 7
depends_on: [14.2-01b, 14.2-10, 14.2-11, 14.2-12]
files_modified:
  - tests/e2e/home-above-fold-auth.spec.ts
  - tests/e2e/home-above-fold-unauth.spec.ts
  - tests/e2e/home-hero-fallback.spec.ts
  - tests/e2e/home-continue-learning-empty.spec.ts
  - tests/e2e/home-continue-learning-data.spec.ts
  - tests/e2e/home-foundations.spec.ts
  - tests/e2e/home-section-narrative.spec.ts
  - tests/e2e/home-anonymous-catalog-clean.spec.ts
autonomous: true
requirements:
  - SPEC-AC-1 (renders without runtime error in 4 states)
  - SPEC-AC-6 (above-fold AUTH — hero + Continue Learning visible at 390×844 + ≥1 mastery decoration via stars=3 seed)
  - Plan 14.2-01b (auth harness — authenticatedTest fixture for the AUTH-state assertions)
  - SPEC-AC-7 (above-fold UNAUTH — hero cover, JP title, Try Free Lesson, KitsuBeat wordmark all visible)
  - SPEC-AC-8 (hero-fallback — drifted slug returns 200 + 'Start Learning' CTA + console.warn)
  - SPEC-AC-9 (Continue Learning empty-state — section omitted from DOM)
  - SPEC-AC-10 (Continue Learning data-correctness — 2 in-progress rows render in updated_at DESC; progress bar widths match seed ±2%)
  - SPEC-AC-11 (Foundations — 2 cards Hiragana + Katakana, /kana navigation)
  - SPEC-AC-13 (5 sections in DOM order with stable testids)
  - SPEC-AC-14 (anonymous-catalog clean: no Continue Learning, no LanternStreak, no mastery decorations)
  - CONTEXT-D-13 (M1 invariant — verified across cards)
  - CONTEXT-D-14 (anonymous-catalog clean — comprehensive verification)

must_haves:
  truths:
    - "8 behavioral e2e specs cover SPEC AC #1, #6, #7, #8, #9, #10, #11, #13, #14"
    - "All specs use viewport: { width: 390, height: 844 } (CONTEXT D-15)"
    - "home-above-fold-auth.spec.ts seeds 1×1-star + 1×3-star songs; asserts hero + Continue Learning bottom <= 844 + ≥1 ContinueCard with star-aura or song-mastered-banner"
    - "home-above-fold-unauth.spec.ts asserts 4 selectors visible (hero cover img, hero-title-jp, Try Free Lesson button, brand-wordmark)"
    - "home-hero-fallback.spec.ts seeds drifted current_path_node_slug; asserts 200 + 'Start Learning' CTA + server log contains warn matching current_path_node_slug pattern"
    - "home-continue-learning-empty.spec.ts (auth, no in-progress songs): [data-testid='continue-learning'] count = 0"
    - "home-continue-learning-data.spec.ts (auth, 2 in-progress at known percents): exactly 2 ContinueCards in updated_at DESC; each progress fill width matches seeded percent ±2%"
    - "home-foundations.spec.ts: 2 cards (Hiragana + Katakana) visible; each click navigates to /kana?script=..."
    - "home-section-narrative.spec.ts: rendered DOM contains exactly 5 sections in order (hero-featured, continue-learning, foundations, browse-by-anime, featured-songs); zero references to 'Beginner-Friendly' / 'Recently Added' / 'Top Artists'"
    - "home-anonymous-catalog-clean.spec.ts: unauth /; [data-testid='continue-learning'] count = 0, [data-testid='global-lantern-streak'] count = 0, no descendant [data-testid='star-aura' | 'song-mastered-banner' | 'cover-card-aura'] under featured-songs"
  artifacts:
    - path: "tests/e2e/home-above-fold-auth.spec.ts"
      provides: "AC #6 viewport contract"
      contains: "hero-featured"
    - path: "tests/e2e/home-above-fold-unauth.spec.ts"
      provides: "AC #7 viewport contract"
      contains: "Try Free Lesson"
    - path: "tests/e2e/home-hero-fallback.spec.ts"
      provides: "AC #8 hero failure mode"
      contains: "Start Learning"
    - path: "tests/e2e/home-continue-learning-empty.spec.ts"
      provides: "AC #9 empty-DOM"
      contains: "toHaveCount(0)"
    - path: "tests/e2e/home-continue-learning-data.spec.ts"
      provides: "AC #10 data-correctness"
      contains: "completion_pct"
    - path: "tests/e2e/home-foundations.spec.ts"
      provides: "AC #11 Foundations"
      contains: "kana-checkpoint"
    - path: "tests/e2e/home-section-narrative.spec.ts"
      provides: "AC #13 5-section DOM order"
      contains: "hero-featured"
    - path: "tests/e2e/home-anonymous-catalog-clean.spec.ts"
      provides: "AC #14 anonymous-catalog clean"
      contains: "toHaveCount(0)"
  key_links:
    - from: "All 8 specs"
      to: "src/app/page.tsx (Plan 14.2-10) + src/app/layout.tsx (Plan 14.2-11)"
      via: "DOM assertions on rendered home + global header"
      pattern: "data-testid="
    - from: "tests/e2e/home-hero-fallback.spec.ts"
      to: "console.warn signal from getHeroSong (Plan 14.2-03)"
      via: "page.on('console') listener"
      pattern: "console\\.warn|page.on\\('console'\\)"
---

<objective>
Ship the 8 behavioral e2e specs that cover the remaining SPEC acceptance criteria not handled by Plan 14.2-13 (visual + cross-surface). These specs validate page-state contracts: above-fold viewport assertions (auth + unauth), hero-fallback graceful degrade, Continue Learning empty + data correctness, Foundations rendering + navigation, 5-section DOM order, and the comprehensive anonymous-catalog clean check.

Purpose: These are the SPEC-Req-N → AC-N traceability gates. Once green, every numbered acceptance criterion in 14.2-SPEC.md has at least one e2e or unit test pinning it.

Output: 8 e2e spec files. Snapshot baselines NOT required (these are functional, not visual).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14.2-redesign-home/14.2-CONTEXT.md
@.planning/phases/14.2-redesign-home/14.2-SPEC.md
@.planning/phases/14.2-redesign-home/14.2-PATTERNS.md
@tests/e2e/path-kana-checkpoint-nav.spec.ts
@tests/e2e/path-m1-invariant.spec.ts
@tests/support/test-db.ts
@tests/support/fixtures.ts

<interfaces>
<!-- Test scaffolding patterns from existing e2e specs (PATTERNS.md lines 732–778). -->
// path-kana-checkpoint-nav.spec.ts — viewport + DOM probe + nav assertion
// path-m1-invariant.spec.ts — DOM negation iteration pattern
// path-starter-bug-repair.spec.ts — console.warn assertion shape (mirror in home-hero-fallback)

<!-- Test DB seeding helpers (already established Phase 14.1 + 11.6). -->
// tests/support/test-db.ts: TEST_USER_ID, getTestDb, resetTestProgress, primeProgress (or equivalent)
// May need new helper: seedGamificationState({ userId, current_path_node_slug, streak_current })
//   - if not present, create inline in the hero-fallback spec.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create above-fold (auth + unauth) + hero-fallback e2e specs (AC #6, #7, #8)</name>
  <read_first>
    - tests/e2e/path-kana-checkpoint-nav.spec.ts (viewport contract pattern; PATTERNS.md lines 732-753)
    - tests/support/test-db.ts (verify TEST_USER_ID + resetTestProgress + saveSessionResults exports — needed for AC #6 seeding)
    - .planning/phases/14.2-redesign-home/14.2-SPEC.md AC #6, #7, #8
    - .planning/phases/14.2-redesign-home/14.2-CONTEXT.md §Specifics line 278 (hero failure-mode test sketch)
    - src/lib/db/queries.ts (verify HeroSongResult — for hero-fallback we need to seed gamification_state directly)
  </read_first>
  <action>
    Write `tests/e2e/home-above-fold-auth.spec.ts`:

    ```typescript
    // Per revision: use authenticatedTest fixture so the auth assertion is REAL,
    // not silently treating an unauth run as auth (Plan 14.2-01b's bypass).
    import { authenticatedTest as test, expect } from "../support/auth-fixtures";
    import { getTestDb } from "../support/test-db";

    /**
     * SPEC AC #6 — above-fold AUTH at 390×844:
     *   - hero-featured bottom <= 844
     *   - continue-learning top <= 844
     *   - ≥1 ContinueCard at stars=3 with [data-testid="continue-card-stars"] above the fold
     *
     * Seeds (per revision): authenticated user with at least one Continue Learning
     * row at stars=3 (ex1_2_3=ex4=ex6=0.85, completion_pct=0.66) so:
     *   - ContinueLearning section renders (not omitted)
     *   - ContinueCard mastery decoration ([data-testid="continue-card-stars"]) renders
     *   - Above-fold visibility check is meaningful (non-vacuous)
     *
     * Plan 02 (revised) JOINs stars; Plan 07 (revised) renders the decoration; Plan 09
     * threads stars through. This spec is the e2e gate that all three changes wire end-to-end.
     */
    test.describe("/ above-fold AUTH (AC #6)", () => {
      test.beforeEach(async ({ authedUserId }) => {
        const db = getTestDb();
        // Seed: 1 row at stars=3 against a known vocab-only song_version.
        // Pseudocode — adapt to actual drizzle schema (seeding helper or direct INSERT):
        // const songVersionId = await pickVocabOnlySongVersion(db);
        // await db.insert(userSongProgress).values({
        //   user_id: authedUserId,
        //   song_version_id: songVersionId,
        //   completion_pct: 0.66,
        //   ex1_2_3_best_accuracy: 0.85,
        //   ex4_best_accuracy: 0.85,
        //   ex6_best_accuracy: 0.85,
        //   updated_at: new Date(),
        // }).onConflictDoUpdate({...});
      });

      test("hero + Continue Learning visible above the fold + 3-star mastery decoration present", async ({
        page,
        context,
      }) => {
        await page.setViewportSize({ width: 390, height: 844 });
        await context.addCookies([
          { name: "kb_theme", value: "dark", url: "http://localhost:7000", sameSite: "Lax" },
        ]);

        await page.goto("/");
        await page.waitForLoadState("networkidle");

        // (a) hero-featured bottom <= 844
        const hero = page.locator('[data-testid="hero-featured"]');
        const heroBox = await hero.boundingBox();
        expect(heroBox).not.toBeNull();
        const heroBottom = (heroBox?.y ?? 0) + (heroBox?.height ?? 0);
        expect(heroBottom).toBeLessThanOrEqual(844);

        // (b) continue-learning top <= 844 (section MUST render — seed guarantees ≥1 row)
        const continueSection = page.locator('[data-testid="continue-learning"]');
        await expect(continueSection).toBeVisible();
        const continueBox = await continueSection.boundingBox();
        expect(continueBox?.y ?? 0).toBeLessThanOrEqual(844);

        // (c) Per revision: 3-star ribbon is in DOM AND visible above the fold (the
        // seeded stars=3 row hits Plan 07's `isMastered` branch -> renders [data-testid="continue-card-stars"]).
        const stars = continueSection.locator('[data-testid="continue-card-stars"]');
        await expect(stars.first()).toBeVisible();
        const starsBox = await stars.first().boundingBox();
        expect(starsBox).not.toBeNull();
        expect((starsBox!.y ?? 0) + (starsBox!.height ?? 0)).toBeLessThanOrEqual(844);

        // (d) Aura overlay also present (defensive — verifies stars-pass-through wiring)
        const aura = continueSection.locator('[data-testid="continue-card-aura"]');
        expect(await aura.count()).toBeGreaterThanOrEqual(1);
      });
    });
    ```

    Write `tests/e2e/home-above-fold-unauth.spec.ts`:

    ```typescript
    import { test, expect } from "@playwright/test";

    /**
     * SPEC AC #7 — above-fold UNAUTH at 390×844:
     * 4 selectors all visible (top >= 0 && bottom <= 844):
     *   - hero cover image
     *   - Japanese hero title
     *   - Try Free Lesson CTA button
     *   - KitsuBeat wordmark in global header
     */
    test.describe("/ above-fold UNAUTH (AC #7)", () => {
      test("4 signals visible above the fold for anonymous visitor", async ({ page, context }) => {
        await page.setViewportSize({ width: 390, height: 844 });
        await context.addCookies([
          { name: "kb_theme", value: "dark", url: "http://localhost:7000", sameSite: "Lax" },
        ]);
        // Do NOT set Clerk auth cookie — anonymous

        await page.goto("/");
        await page.waitForLoadState("networkidle");

        // (a) hero cover image visible
        const heroCover = page.locator('[data-testid="hero-featured"] img[src*="youtube.com"]');
        await expect(heroCover).toBeVisible();

        // (b) Japanese hero title visible with var(--font-jp)
        const heroTitleJp = page.locator('[data-testid="hero-title-jp"]');
        await expect(heroTitleJp).toBeVisible();
        const fontFamily = await heroTitleJp.evaluate((el) =>
          window.getComputedStyle(el).fontFamily,
        );
        // The computed value resolves the var; Noto Sans JP should appear in the resolved family
        expect(fontFamily.toLowerCase()).toMatch(/noto sans jp|var.*font-jp/);

        // (c) Try Free Lesson CTA visible (unauth source)
        const cta = page.locator('[data-testid="hero-cta"]');
        await expect(cta).toBeVisible();
        await expect(cta).toContainText("Try Free Lesson");

        // (d) KitsuBeat wordmark visible in global header
        const wordmark = page.locator('[data-testid="brand-wordmark"]');
        await expect(wordmark).toBeVisible();
        await expect(wordmark).toContainText("KitsuBeat");

        // All 4 within 844px viewport
        for (const loc of [heroCover, heroTitleJp, cta, wordmark]) {
          const box = await loc.boundingBox();
          expect(box).not.toBeNull();
          expect(box!.y).toBeGreaterThanOrEqual(0);
          expect(box!.y + box!.height).toBeLessThanOrEqual(844);
        }
      });
    });
    ```

    Write `tests/e2e/home-hero-fallback.spec.ts`:

    ```typescript
    // Per revision: authenticated spec uses authenticatedTest (Plan 14.2-01b bypass).
    import { authenticatedTest as test, expect } from "../support/auth-fixtures";
    import { getTestDb } from "../support/test-db";

    /**
     * SPEC AC #8 — hero failure mode:
     *   When auth user has current_path_node_slug pointing to a non-existent or
     *   archived song, hero falls back to top featured + CTA reads "Start Learning"
     *   + server emits console.warn matching the D-06 pattern.
     */
    test.describe("/ hero failure mode (AC #8)", () => {
      test.beforeEach(async ({ authedUserId }) => {
        // Seed gamification_state row for authedUserId (Plan 14.2-01b bypass user)
        // with current_path_node_slug = 'definitely-not-a-real-slug' (drifted slug).
        const db = getTestDb();
        // Pseudocode — adapt to actual drizzle schema:
        // await db.insert(gamificationState).values({
        //   user_id: authedUserId,
        //   current_path_node_slug: "definitely-not-a-real-slug",
        //   streak_current: 0,
        // }).onConflictDoUpdate({ target: gamificationState.user_id, set: { current_path_node_slug: "definitely-not-a-real-slug" } });
      });

      test("drifted slug -> 200 + 'Start Learning' CTA + server warn emitted", async ({
        page,
        context,
      }) => {
        await page.setViewportSize({ width: 390, height: 844 });
        await context.addCookies([
          { name: "kb_theme", value: "dark", url: "http://localhost:7000", sameSite: "Lax" },
        ]);

        // Capture console messages from the server-rendered page
        const consoleMessages: string[] = [];
        page.on("console", (msg) => {
          if (msg.type() === "warning") consoleMessages.push(msg.text());
        });

        const response = await page.goto("/");
        // (a) page returns 200
        expect(response?.status()).toBe(200);

        await page.waitForLoadState("networkidle");

        // (b) hero CTA reads 'Start Learning' (fallback_featured branch)
        const cta = page.locator('[data-testid="hero-cta"]');
        await expect(cta).toContainText("Start Learning");

        // (c) console.warn was emitted (server-side log; visible if logger forwards to browser
        //     console — if NOT forwarded, this assertion may need server-log inspection instead.
        //     Document inline if the warn is not visible to Playwright; consider adding a
        //     telemetry spy or checking the dev-server stdout via test harness.)
        const heroWarn = consoleMessages.find((m) =>
          m.includes("[hero-song]") && m.includes("definitely-not-a-real-slug") && m.includes("missing or lessonless"),
        );
        // If the warn is not browser-visible, this test should at minimum verify the page
        // renders without error (the Start Learning CTA text already proves the fallback fired).
        if (heroWarn) {
          expect(heroWarn).toMatch(/\[hero-song\] definitely-not-a-real-slug \(current_path_node_slug\) missing or lessonless/);
        }
        // The integration test (Plan 14.2-03 Test 4) is the primary warn-shape gate; this e2e
        // verifies end-to-end behavior reaches 200 + correct CTA.
      });
    });
    ```

    Run all 3 specs: `npx playwright test tests/e2e/home-above-fold-auth.spec.ts tests/e2e/home-above-fold-unauth.spec.ts tests/e2e/home-hero-fallback.spec.ts`. All must exit 0.

    Commit message: `test(14.2): add above-fold + hero-fallback e2e specs (AC 6/7/8)`
  </action>
  <acceptance_criteria>
    - All 3 spec files exist
    - tests/e2e/home-above-fold-auth.spec.ts contains literal strings `[data-testid="hero-featured"]`, `[data-testid="continue-learning"]`, `bounding`, `844`, `[data-testid="continue-card-stars"]`, `authenticatedTest`
    - tests/e2e/home-above-fold-auth.spec.ts does NOT contain the phrase `Adapt at execution time` (per revision: removed in favor of the now-implemented stars decoration check)
    - tests/e2e/home-above-fold-unauth.spec.ts contains literal strings `Try Free Lesson`, `brand-wordmark`, `noto sans jp`
    - tests/e2e/home-hero-fallback.spec.ts contains literal strings `definitely-not-a-real-slug`, `Start Learning`, `[hero-song]` (in regex form acceptable)
    - All 3 specs exit 0 when run via `npx playwright test`
  </acceptance_criteria>
  <verify>
    <automated>test -f tests/e2e/home-above-fold-auth.spec.ts &amp;&amp; test -f tests/e2e/home-above-fold-unauth.spec.ts &amp;&amp; test -f tests/e2e/home-hero-fallback.spec.ts &amp;&amp; grep -q "Try Free Lesson" tests/e2e/home-above-fold-unauth.spec.ts &amp;&amp; grep -q "Start Learning" tests/e2e/home-hero-fallback.spec.ts &amp;&amp; grep -q "definitely-not-a-real-slug" tests/e2e/home-hero-fallback.spec.ts</automated>
  </verify>
  <done>3 specs committed; AC #6/7/8 covered; specs run successfully against Wave 6 implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Continue Learning empty/data + Foundations + Section Narrative + Anonymous Clean specs (AC #9, #10, #11, #13, #14)</name>
  <read_first>
    - tests/e2e/path-kana-checkpoint-nav.spec.ts (kana-checkpoint testid pattern, /kana?script= verification — PATTERNS.md lines 736-752)
    - tests/e2e/path-m1-invariant.spec.ts (DOM negation iteration pattern — for anonymous-clean)
    - tests/support/test-db.ts (seeding helpers for Continue Learning data correctness; primeProgress shape)
    - .planning/phases/14.2-redesign-home/14.2-SPEC.md AC #9, #10, #11, #13, #14
    - .planning/phases/14.2-redesign-home/14.2-CONTEXT.md §Specifics line 277 (anonymous-catalog test sketch)
  </read_first>
  <action>
    Write `tests/e2e/home-continue-learning-empty.spec.ts`:

    ```typescript
    // Per revision: authenticated spec uses authenticatedTest (Plan 14.2-01b bypass).
    import { authenticatedTest as test, expect } from "../support/auth-fixtures";
    import { resetTestProgress } from "../support/test-db";

    /**
     * SPEC AC #9 — Continue Learning empty-state:
     *   Authenticated user with zero in-progress songs renders / WITHOUT
     *   [data-testid="continue-learning"] in the DOM (section omitted, NOT
     *   rendered as empty placeholder).
     */
    test.describe("/ Continue Learning empty-state (AC #9)", () => {
      test.beforeEach(async ({ authedUserId }) => {
        await resetTestProgress(authedUserId);
      });

      test("zero in-progress songs -> [data-testid='continue-learning'] absent from DOM", async ({
        page,
        context,
      }) => {
        await page.setViewportSize({ width: 390, height: 844 });
        await context.addCookies([
          { name: "kb_theme", value: "dark", url: "http://localhost:7000", sameSite: "Lax" },
        ]);

        await page.goto("/");
        await page.waitForLoadState("networkidle");

        const continueSection = page.locator('[data-testid="continue-learning"]');
        await expect(continueSection).toHaveCount(0);
      });
    });
    ```

    Write `tests/e2e/home-continue-learning-data.spec.ts`:

    ```typescript
    // Per revision: authenticated spec uses authenticatedTest (Plan 14.2-01b bypass).
    import { authenticatedTest as test, expect } from "../support/auth-fixtures";
    import { getTestDb, resetTestProgress } from "../support/test-db";

    /**
     * SPEC AC #10 — Continue Learning data-correctness:
     *   Seeded user with 2 in-progress songs at known completion_pct percentages
     *   renders 2 ContinueCards in updated_at DESC order; each progress bar's
     *   computed width matches the seeded percent ±2%.
     */
    test.describe("/ Continue Learning data-correctness (AC #10)", () => {
      test.beforeEach(async ({ authedUserId }) => {
        await resetTestProgress(authedUserId);
        // Seed 2 in-progress rows for authedUserId: completion_pct = 0.66 (newer) and 0.33 (older).
        // Use direct INSERT into user_song_progress via getTestDb() for deterministic timing.
      });

      test("2 in-progress songs -> 2 ContinueCards in updated_at DESC; progress widths match seed ±2%", async ({
        page,
        context,
      }) => {
        await page.setViewportSize({ width: 390, height: 844 });
        await context.addCookies([
          { name: "kb_theme", value: "dark", url: "http://localhost:7000", sameSite: "Lax" },
        ]);

        await page.goto("/");
        await page.waitForLoadState("networkidle");

        const cards = page.locator('[data-testid^="continue-card-"]');
        await expect(cards).toHaveCount(2);

        // First card has the higher completion_pct (newer = updated_at DESC = first card)
        const firstFill = cards.first().locator('[data-testid="continue-card-progress-fill"]');
        const firstWidth = await firstFill.evaluate((el) => (el as HTMLElement).style.width);
        const firstPct = parseFloat(firstWidth);
        expect(firstPct).toBeGreaterThanOrEqual(64); // 66 - 2
        expect(firstPct).toBeLessThanOrEqual(68); // 66 + 2

        const secondFill = cards.nth(1).locator('[data-testid="continue-card-progress-fill"]');
        const secondWidth = await secondFill.evaluate((el) => (el as HTMLElement).style.width);
        const secondPct = parseFloat(secondWidth);
        expect(secondPct).toBeGreaterThanOrEqual(31); // 33 - 2
        expect(secondPct).toBeLessThanOrEqual(35); // 33 + 2
      });
    });
    ```

    Write `tests/e2e/home-foundations.spec.ts`:

    ```typescript
    import { test, expect } from "@playwright/test";

    /**
     * SPEC AC #11 — Foundations carousel:
     *   / (auth or anon) renders exactly 2 cards in [data-testid='foundations']
     *   (Hiragana + Katakana). Each click navigates to /kana?script=...
     *   Reduced-motion suppresses the 3-star halo (covered by Plan 14.2-13 reduced-motion spec).
     */
    test.describe("/ Foundations (AC #11)", () => {
      test("renders 2 KanaCheckpointNode cards (hiragana + katakana); clicks navigate to /kana?script=...", async ({
        page,
        context,
      }) => {
        await page.setViewportSize({ width: 390, height: 844 });
        await context.addCookies([
          { name: "kb_theme", value: "dark", url: "http://localhost:7000", sameSite: "Lax" },
        ]);

        await page.goto("/");
        await page.waitForLoadState("networkidle");

        const foundations = page.locator('[data-testid="foundations"]');
        await expect(foundations).toBeVisible();

        const hira = foundations.locator('[data-testid="kana-checkpoint-hiragana"]');
        const kata = foundations.locator('[data-testid="kana-checkpoint-katakana"]');
        await expect(hira).toBeVisible();
        await expect(kata).toBeVisible();

        // Both cards use size='home' variant
        expect(await hira.getAttribute("data-size")).toBe("home");
        expect(await kata.getAttribute("data-size")).toBe("home");

        // /kana?script=hiragana navigation (V1 verified)
        expect(await hira.getAttribute("href")).toBe("/kana?script=hiragana");
        expect(await kata.getAttribute("href")).toBe("/kana?script=katakana");
      });
    });
    ```

    Write `tests/e2e/home-section-narrative.spec.ts`:

    ```typescript
    // Per revision: authenticated spec uses authenticatedTest (Plan 14.2-01b bypass).
    import { authenticatedTest as test, expect } from "../support/auth-fixtures";

    /**
     * SPEC AC #13 — 5-section narrative:
     *   Rendered / contains exactly 5 sections in DOM order, identified by
     *   data-testid: hero-featured, continue-learning, foundations, browse-by-anime,
     *   featured-songs. Page contains zero references to 'Beginner-Friendly',
     *   'Recently Added', or 'Top Artists'.
     */
    test.describe("/ section narrative (AC #13)", () => {
      test("5 sections in correct DOM order; legacy carousel labels absent (auth)", async ({
        page,
        context,
      }) => {
        await page.setViewportSize({ width: 390, height: 844 });
        await context.addCookies([
          { name: "kb_theme", value: "dark", url: "http://localhost:7000", sameSite: "Lax" },
        ]);

        await page.goto("/");
        await page.waitForLoadState("networkidle");

        // Assert all 5 testids present in correct ORDER via querySelectorAll iteration
        const order = await page.evaluate(() => {
          const ids = ["hero-featured", "continue-learning", "foundations", "browse-by-anime", "featured-songs"];
          const found: { id: string; index: number }[] = [];
          for (const id of ids) {
            const el = document.querySelector(`[data-testid="${id}"]`);
            if (el) {
              // Compute its position in document order
              found.push({ id, index: Array.from(document.querySelectorAll("*")).indexOf(el) });
            }
          }
          return found;
        });

        // All 5 sections present
        const idsFound = order.map((o) => o.id);
        expect(idsFound).toContain("hero-featured");
        expect(idsFound).toContain("foundations");
        expect(idsFound).toContain("browse-by-anime");
        expect(idsFound).toContain("featured-songs");
        // continue-learning may or may not be present depending on test user's progress;
        // when present, it must be between hero-featured and foundations.

        // DOM-order assertion: indices must be ascending in the spec order
        const indices = order.map((o) => o.index);
        for (let i = 1; i < indices.length; i++) {
          expect(indices[i]).toBeGreaterThan(indices[i - 1]!);
        }

        // Legacy carousel labels MUST NOT appear (SPEC §Req 6)
        const html = await page.content();
        expect(html).not.toMatch(/Beginner-Friendly/);
        expect(html).not.toMatch(/Recently Added/);
        expect(html).not.toMatch(/Top Artists/);
      });
    });
    ```

    Write `tests/e2e/home-anonymous-catalog-clean.spec.ts`:

    ```typescript
    import { test, expect } from "@playwright/test";

    /**
     * SPEC AC #14 — anonymous-catalog clean (the comprehensive D-14 contract):
     *   Unauthenticated / does NOT render:
     *     - [data-testid='continue-learning']
     *     - [data-testid='global-lantern-streak']
     *   AND <CoverCard> instances under featured-songs have ZERO descendant
     *     - [data-testid='star-aura']
     *     - [data-testid='song-mastered-banner']
     *     - [data-testid='cover-card-aura']
     */
    test.describe("/ anonymous-catalog clean (AC #14, D-14)", () => {
      test("unauth / -> no Continue Learning, no LanternStreak, no mastery decorations on Featured Songs", async ({
        page,
        context,
      }) => {
        await page.setViewportSize({ width: 390, height: 844 });
        await context.addCookies([
          { name: "kb_theme", value: "dark", url: "http://localhost:7000", sameSite: "Lax" },
        ]);
        // Do NOT set Clerk auth — anonymous

        await page.goto("/");
        await page.waitForLoadState("networkidle");

        // Continue Learning section ABSENT
        await expect(page.locator('[data-testid="continue-learning"]')).toHaveCount(0);

        // Global LanternStreak ABSENT
        await expect(page.locator('[data-testid="global-lantern-streak"]')).toHaveCount(0);

        // No mastery decorations under featured-songs
        const featured = page.locator('[data-testid="featured-songs"]');
        await expect(featured).toBeVisible();
        const decorations = featured.locator(
          '[data-testid="star-aura"], [data-testid="song-mastered-banner"], [data-testid="cover-card-aura"]',
        );
        await expect(decorations).toHaveCount(0);

        // Sign in button visible (the AC #14 fallback)
        await expect(page.locator('[data-testid="nav-sign-in"]')).toBeVisible();
      });
    });
    ```

    Run all 5 specs: `npx playwright test tests/e2e/home-continue-learning-empty.spec.ts tests/e2e/home-continue-learning-data.spec.ts tests/e2e/home-foundations.spec.ts tests/e2e/home-section-narrative.spec.ts tests/e2e/home-anonymous-catalog-clean.spec.ts`. All must exit 0.

    Commit message: `test(14.2): add Continue Learning + Foundations + section narrative + anonymous-catalog clean specs (AC 9/10/11/13/14)`
  </action>
  <acceptance_criteria>
    - All 5 spec files exist
    - tests/e2e/home-continue-learning-empty.spec.ts contains `toHaveCount(0)` for `continue-learning`
    - tests/e2e/home-continue-learning-data.spec.ts contains `continue-card-progress-fill` AND ±2% percent assertions (e.g., `toBeGreaterThanOrEqual(64)`)
    - tests/e2e/home-foundations.spec.ts contains `kana-checkpoint-hiragana`, `kana-checkpoint-katakana`, `/kana?script=hiragana`
    - tests/e2e/home-section-narrative.spec.ts contains all 5 testids: `hero-featured`, `continue-learning`, `foundations`, `browse-by-anime`, `featured-songs`; AND assertion that `Beginner-Friendly`, `Recently Added`, `Top Artists` strings are absent
    - tests/e2e/home-anonymous-catalog-clean.spec.ts contains `toHaveCount(0)` assertions for `continue-learning`, `global-lantern-streak`, AND mastery-decoration selectors
    - All 5 specs exit 0 when run via `npx playwright test`
  </acceptance_criteria>
  <verify>
    <automated>test -f tests/e2e/home-continue-learning-empty.spec.ts &amp;&amp; test -f tests/e2e/home-continue-learning-data.spec.ts &amp;&amp; test -f tests/e2e/home-foundations.spec.ts &amp;&amp; test -f tests/e2e/home-section-narrative.spec.ts &amp;&amp; test -f tests/e2e/home-anonymous-catalog-clean.spec.ts &amp;&amp; grep -q "kana-checkpoint-hiragana" tests/e2e/home-foundations.spec.ts &amp;&amp; grep -q "Beginner-Friendly" tests/e2e/home-section-narrative.spec.ts &amp;&amp; grep -q "global-lantern-streak" tests/e2e/home-anonymous-catalog-clean.spec.ts</automated>
  </verify>
  <done>5 behavioral specs committed; AC #9, #10, #11, #13, #14 all covered; all specs run successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture cookie auth -> Clerk session | Test-only simulation; same pattern Phase 14.1 uses. No production attack surface. |
| Direct DB INSERT into gamification_state / user_song_progress | Test-DB only (TEST_USER_ID isolated); no prod data touched. |
| Playwright page.on('console') | Test-only signal capture; no prod side effect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.2-14-01 | Tampering | Seed-state pollution across tests | mitigate | resetTestProgress(TEST_USER_ID) called in beforeEach for affected specs. Plan 14.2-13 already established this pattern works against the test DB. |
| T-14.2-14-02 | Information Disclosure | Anonymous spec leaks user data | mitigate | The anonymous-clean spec EXPLICITLY asserts the absence of Continue Learning + LanternStreak + mastery decorations. If any leak, the spec fails. |
| T-14.2-14-03 | Information Disclosure | Hero-fallback spec exposes server log to test stdout | accept | console.warn output is intentional; no secrets. Standard Phase 14.1 starter-songs warn pattern. |
| T-14.2-14-04 | M1 invariant | Foundations spec doesn't catch M1 violations | accept | M1 invariant covered by ContinueCard / CoverCard / AnimeCard / KanaCheckpointNode unit tests (Plans 04, 06, 07). E2E specs verify behavior end-to-end; fine-grained M1 lives at component level. |
</threat_model>

<verification>
- All 8 e2e spec files committed
- npx playwright test (full suite including these 8) passes
- Per-spec acceptance criteria all satisfied
- Two commits (3 specs in Task 1 + 5 specs in Task 2)
</verification>

<success_criteria>
- 8 behavioral e2e specs cover SPEC AC #1, #6, #7, #8, #9, #10, #11, #13, #14
- Combined with Plan 14.2-13's 4 visual + cross-surface + reduced-motion specs, ALL 15 SPEC acceptance criteria have ≥1 e2e or unit test
- Hero-fallback spec verifies Plan 14.2-03's warn signal end-to-end
- Continue Learning data-correctness spec validates Plan 14.2-07's progress bar clamp formula in real browser
- Anonymous-clean spec is the comprehensive D-14 enforcement gate
</success_criteria>

<output>
After completion, create `.planning/phases/14.2-redesign-home/14.2-14-SUMMARY.md` documenting:
- 8 spec files committed
- Per-spec test count (1 each in this case)
- AC traceability table (AC # -> spec file)
- Any seeding helper additions to tests/support/test-db.ts
- Commit SHA(s)
</output>
