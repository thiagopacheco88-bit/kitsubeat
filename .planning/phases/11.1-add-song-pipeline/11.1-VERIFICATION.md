---
phase: 11.1-add-song-pipeline
verified: 2026-04-26T18:00:00Z
status: passed
score: 11/11 must-haves verified
overrides_applied: 0
---

# Phase 11.1: Add-Song Pipeline Verification Report

**Phase Goal:** Close three concrete gaps in the existing add-song pipeline so adding a new song to KitsuBeat is reliable enough that no song ships a streak-killer (broken karaoke, half-written DB row, duplicate YouTube ID), and operator overhead is reduced via a single-page README.

**Verified:** 2026-04-26
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Operator can detect cross-song youtube_id duplicates by running a single command | VERIFIED | `scripts/seed/audit-yt-ids.ts` exists (80 lines). SUMMARY-01 confirms first-run found 7 real legacy dupes and exited 1 with CSV written. |
| 2 | Both batch shells halt loudly when a duplicate youtube_id is introduced | VERIFIED | `run-pending-batch.sh:100` and `overnight-orchestrator.sh:95` both contain `npx tsx --tsconfig tsconfig.scripts.json scripts/seed/audit-yt-ids.ts \|\| exit 1` as their terminal step. |
| 3 | Same youtube_id on full + tv of the SAME slug is not flagged (single-video OP/ED allowed) | VERIFIED | Cross-slug filter is `g.slugs.size > 1` (line 44 of audit-yt-ids.ts), which correctly ignores same-slug multi-version entries. |
| 4 | Per-song DB writes are wrapped in a transaction — partial failure rolls back that song only | VERIFIED | `insertSongTransactional()` at line 226 of 05-insert-db.ts wraps both `tx.insert(songs)` and `tx.insert(songVersions)` in `db.transaction(async (tx) => {...})` via WebSocket-pooled drizzle. |
| 5 | Forced failure mid-transaction leaves zero rows for failing slug in BOTH tables | VERIFIED | Integration Test 1 (`seed-05-insert-db-rollback.test.ts`) passes: `lesson: null as any` triggers NOT NULL violation; both `unwrap(songRowsRaw).length` and `unwrap(versionRowsRaw).length` assert 0. Confirmed PASSED (1388ms) per SUMMARY-02. |
| 6 | Mixed batch preserves passing song rows and leaves failing slug rows at zero | VERIFIED | Integration Test 2 passes: `unwrap(passingSong).length` asserts 1, `unwrap(passingVersion).length` asserts 1, `unwrap(failingSong).length` asserts 0. Confirmed PASSED (3516ms) per SUMMARY-02. |
| 7 | Operator can identify the right command for any of 5 named scenarios in under 30 seconds | VERIFIED | `docs/README-adding-songs.md` exists (41 lines, well under 100-line budget), contains a 5-row table with all 5 D-08 scenarios verbatim and runnable commands. |
| 8 | All SOP links in the README resolve to real gate headers | VERIFIED | All 5 gate anchors traced against actual SOP `###` headers — all resolve (Gate 1, 2, 5, 6, 8 confirmed). |
| 9 | Audit is read-only — no INSERT/UPDATE/DELETE | VERIFIED | `audit-yt-ids.ts` contains no `db.insert`, `db.update`, or `db.delete` calls. Only drizzle `select().from().innerJoin().where()`. |
| 10 | Failures log to stderr without leaking DATABASE_URL or connection strings | VERIFIED | `console.error(\`[insert-fail] slug=${song.slug} error=${(err as Error).message}\`)` — only slug and error message emitted. No `process.env.DATABASE_URL` in any console call. |
| 11 | Driver migration is contained to the seed script — src/lib/db/index.ts unchanged | VERIFIED | `git diff HEAD src/lib/db/index.ts` produces 0 lines of output. Pool and `drizzle-orm/neon-serverless` imports exist only in `05-insert-db.ts`. |

**Score:** 11/11 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/seed/audit-yt-ids.ts` | Cross-song youtube_id audit script | VERIFIED | 80 lines, read-only SELECT, writes `data/yt-id-audit.csv`, exits 0/1 per spec. All literal-string acceptance criteria pass. |
| `scripts/seed/run-pending-batch.sh` | Batch shell with terminal audit step | VERIFIED | Line 100: `npx tsx --tsconfig tsconfig.scripts.json scripts/seed/audit-yt-ids.ts \|\| exit 1` after Step 5 status heredoc. |
| `scripts/seed/overnight-orchestrator.sh` | Orchestrator with terminal audit step | VERIFIED | Line 95: `npx tsx --tsconfig tsconfig.scripts.json scripts/seed/audit-yt-ids.ts \|\| exit 1` after final `run_step` at line 92. |
| `scripts/seed/05-insert-db.ts` | Per-song transactional DB insert via WebSocket pool | VERIFIED | Imports `Pool` from `@neondatabase/serverless` and `drizzle` from `drizzle-orm/neon-serverless`. Exports `insertSongTransactional`. Uses `db.transaction()` with all inserts via `tx.*`. `pool.end()` in `finally`. |
| `tests/integration/seed-05-insert-db-rollback.test.ts` | Vitest integration test — rollback + mixed-batch | VERIFIED | At `tests/integration/` (correct path for vitest glob). Contains `describeIfTestDb` skip-guard, imports `insertSongTransactional`, 2 `it()` cases asserting rollback and mixed-batch isolation. |
| `docs/README-adding-songs.md` | Operator quick-start ≤ 100 lines | VERIFIED | 41 lines. Contains all 5 D-08 scenarios verbatim, `.env.local`, `.venv/Scripts`, `Node.js 22+`, 6 links to SOP. |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `audit-yt-ids.ts` | `src/lib/db/schema.ts` (songVersions, songs) | drizzle innerJoin SELECT | VERIFIED | `.from(songVersions).innerJoin(songs, eq(songs.id, songVersions.song_id))` at lines 25-32. |
| `run-pending-batch.sh` (final step) | `audit-yt-ids.ts` | `npx tsx ... \|\| exit 1` | VERIFIED | Line 100 is the last non-empty line in the file. |
| `overnight-orchestrator.sh` (final step) | `audit-yt-ids.ts` | `npx tsx ... \|\| exit 1` | VERIFIED | Line 95, placed after `run_step "05-insert-db"` at line 92 and before the summary heredoc. |
| `05-insert-db.ts` | `@neondatabase/serverless Pool` | `import { Pool }` | VERIFIED | Line 44 of 05-insert-db.ts. |
| `05-insert-db.ts` | `drizzle-orm/neon-serverless` | `import { drizzle as drizzlePool }` | VERIFIED | Line 45 of 05-insert-db.ts. |
| `seed-05-insert-db-rollback.test.ts` | `scripts/seed/05-insert-db.ts` (exported helper) | `import { insertSongTransactional }` | VERIFIED | Line 25: `import { insertSongTransactional } from "../../scripts/seed/05-insert-db.js"` |
| `README-adding-songs.md` | `docs/SONG_INGESTION_SOP.md` gate sections | markdown anchors | VERIFIED | 6 links; all 5 anchors (`#gate-1--curation-...`, `#gate-2--youtube-...`, `#gate-5--whisperx-...`, `#gate-6--tv-lesson-...`, `#gate-8--pre-insert-...`) resolve to real `### Gate N` headers in SOP. |

---

## Data-Flow Trace (Level 4)

Not applicable. No components rendering dynamic data were added. All artifacts are seed scripts, shell orchestrators, a Vitest integration test, and a documentation file. Level 4 data-flow tracing is scoped to UI components — skipped.

---

## Behavioral Spot-Checks

| Behavior | Evidence | Status |
|----------|----------|--------|
| Audit exits 1 on cross-slug dupe | SUMMARY-01: first run found 7 real dupe groups, exited 1, wrote CSV with locked header | PASS |
| Audit writes CSV with correct header | SUMMARY-01: `data/yt-id-audit.csv` header confirmed as `youtube_id,count,slugs,version_types` | PASS |
| Test 1 rollback assertion | SUMMARY-02: "forced failure mid-transaction rolls back BOTH songs and song_versions for failing slug" — PASSED (1388ms) | PASS |
| Test 2 mixed-batch assertion | SUMMARY-02: "mixed batch: passing slug writes both rows; failing slug writes none" — PASSED (3516ms) | PASS |
| Suite skips cleanly without TEST_DATABASE_URL | SUMMARY-02: confirmed — "2 tests skipped, exit 0" | PASS |

---

## Requirements Coverage

The SPEC defines 11 acceptance criteria (SPEC-AC-1 through SPEC-AC-11). REQUIREMENTS.md maps no REQ-IDs to this phase — the SPEC is the single source of truth.

| SPEC AC | Description | Status | Evidence |
|---------|-------------|--------|----------|
| AC-1 | `audit-yt-ids.ts` exists and runs via `npx tsx` | SATISFIED | File exists, 80 lines, complete implementation |
| AC-2 | Known-dupe fixture exits non-zero + writes CSV | SATISFIED | `g.slugs.size > 1` filter confirmed; SUMMARY-01 reports real first-run dupe detection |
| AC-3 | Clean fixture exits 0 + `OK: N rows checked, 0 duplicates` | SATISFIED | Literal string present at line 74 of audit-yt-ids.ts |
| AC-4 | `run-pending-batch.sh` propagates non-zero on dupe | SATISFIED | `\|\| exit 1` at line 100, confirmed last non-empty line |
| AC-5 | `overnight-orchestrator.sh` propagates non-zero on dupe | SATISFIED | `\|\| exit 1` at line 95, after final `run_step` |
| AC-6 | `05-insert-db.ts` per-song flow uses `db.transaction()` | SATISFIED | `await db.transaction(async (tx) => {...})` at line 226 |
| AC-7 | Forced failure rolls back BOTH tables for failing slug | SATISFIED | Integration Test 1 PASSED — confirmed by SUMMARY-02 |
| AC-8 | Successful insert leaves matching rows in both tables | SATISFIED | Integration Test 2 passing-slug assertions confirm both rows written |
| AC-9 | Mixed batch: passing writes rows, failing writes none; exit 1 only if all fail | SATISFIED | Test 2 + `failedSlugs.length === manifest.length` condition at line 451 |
| AC-10 | README exists, ≤ 100 lines, covers all 5 scenarios | SATISFIED | 41 lines, all 5 D-08 scenarios verbatim, runnable commands present |
| AC-11 | All README → SOP links resolve | SATISFIED | All 5 gate anchors verified against actual SOP `### Gate N` headers |

---

## Anti-Patterns Found

The code review (11.1-REVIEW.md) surfaced 0 critical, 5 warnings, 6 info issues. All are advisory per the context note. Confirmed blockers: none. Summary of warnings for reference:

| File | Issue | Severity | Impact on Phase Goal |
|------|-------|----------|----------------------|
| `overnight-orchestrator.sh:94-95` | Audit step bypasses `run_step` wrapper (WR-02): on a dupe, the summary JSON is never written and the step's rc is absent from `step_status` | Warning | Does not affect SPEC AC-5 (exit 1 propagation is correct); operator loses overnight summary on dupe night. Advisory fix. |
| `overnight-orchestrator.sh:24` | Missing `set -u` (WR-01) | Warning | Future-risk only; no current impact. Advisory. |
| `run-pending-batch.sh:66,70,75` | `set -e` + `\|\| log` mismatch (WR-03) | Warning | Pre-existing pattern not introduced by this phase. Advisory. |
| `audit-yt-ids.ts:53-63` | CSV writer doesn't escape commas/newlines (WR-04) | Warning | Safe today (`youtube_id ∈ [A-Za-z0-9_-]{11}`, slugs are kebab-case). Advisory hardening. |
| `seed-05-insert-db-rollback.test.ts:90-99` | Pool rebuilt per `beforeEach` — FD leak (WR-05) | Warning | Minimal practical impact; vitest process-reuse tolerates it. Advisory refactor. |

None of these prevent the phase goal. They are tracked in REVIEW.md for a future cleanup pass.

**Stub scan:** No placeholders, `return null`, `return {}`, or `TODO` markers found in phase deliverables. All implementations are substantive.

---

## Human Verification Required

None. All acceptance criteria are programmable and the critical ones (rollback semantics, CSV output, exit code propagation, SOP anchor resolution) are verified by the integration test, SUMMARY reports, and file-level grep. The README is a documentation file with no dynamic behavior requiring human UX testing.

---

## Gaps Summary

No gaps. All 11 SPEC acceptance criteria are satisfied by the delivered code. The phase goal — closing three concrete gaps (duplicate youtube_id guard, transactional DB write, operator README) — is fully achieved.

---

_Verified: 2026-04-26T18:00:00Z_
_Verifier: Claude (gsd-verifier)_
