---
phase: 11.1-add-song-pipeline
plan: "03"
subsystem: documentation
tags:
  - documentation
  - operator-quickstart
  - song-ingestion
dependency_graph:
  requires:
    - "11.1-01: audit-yt-ids.ts wired into batch shells"
    - "11.1-02: db.transaction wrap in 05-insert-db.ts"
  provides:
    - "docs/README-adding-songs.md operator quick-start"
  affects:
    - "docs/SONG_INGESTION_SOP.md (linked from, not modified)"
tech_stack:
  added: []
  patterns:
    - "Pure-pointer doc: README links into SOP, never duplicates SOP content (D-09)"
key_files:
  created:
    - docs/README-adding-songs.md
  modified: []
decisions:
  - "D-09 pure-pointers rule enforced: no inline gate descriptions; every gate reference is a link"
  - "Added notes about yt_id audit gate and db.transaction rollback per context_note (plans 11.1-01 and 11.1-02)"
  - "Derived all SOP anchors from actual ### Gate N header text — verified verbatim"
metrics:
  duration: "3 min"
  completed_date: "2026-04-26T17:18:10Z"
  tasks_completed: 1
  files_created: 1
---

# Phase 11.1 Plan 03: Operator Quick-Start README Summary

**One-liner:** 41-line operator README mapping 5 locked scenarios to runnable commands with 6 verified SOP anchor links.

## What Was Built

`docs/README-adding-songs.md` — single-page operator quick-start (≤100 lines). Structure:
1. One-paragraph intro (4 lines)
2. "Before you start" checklist — `.env.local`, `.venv/Scripts`, Node.js 22+, 6 GB RAM, TEST_DATABASE_URL
3. 5-row scenario table with commands and links into SOP gate sections
4. Notes on `run-pending-batch.sh` and `overnight-orchestrator.sh` behavior (yt_id audit + db.transaction)
5. "Deeper dive" footer pointer to `docs/SONG_INGESTION_SOP.md`

**Final line count:** 41 lines (target: ~70, hard limit: 100 — SPEC § Constraints)

## SOP Anchor Verification

| README Anchor | SOP Header | Resolves |
|---|---|---|
| `#gate-1--curation-manual-review-of-new-candidates` | `### Gate 1 — Curation (manual review of new candidates)` | Yes |
| `#gate-2--youtube-selection-full--tv` | `### Gate 2 — YouTube selection (full + TV)` | Yes |
| `#gate-5--whisperx-timing-extraction-full--tv` | `### Gate 5 — WhisperX timing extraction (full + TV)` | Yes |
| `#gate-6--tv-lesson-derivation-not-regeneration` | `### Gate 6 — TV lesson derivation (NOT regeneration)` | Yes |
| `#gate-8--pre-insert-verification--db-write` | `### Gate 8 — Pre-insert verification + DB write` | Yes |

All 6 links (Gate 8 appears twice: once for "Add 1 new song" and once for "Replace geo-blocked") resolve to real headers.

## Scenario Table (Final, for record)

| Intent | Command(s) | SOP gates touched |
|---|---|---|
| Add 1 new song | `npx tsx scripts/seed/01-build-manifest.ts && bash scripts/seed/run-pending-batch.sh` | Gate 1 → Gate 8 |
| Add a batch of N songs | `bash scripts/seed/run-pending-batch.sh` | All 8 gates |
| Resume pending_whisper backlog | `bash scripts/seed/overnight-orchestrator.sh` | Gate 5 onward |
| Add only the TV cut for an existing song | `npx tsx scripts/seed/10-prepare-tv.ts --slug=<slug>` | Gate 6 only |
| Replace a geo-blocked or live-version YouTube ID | `npx tsx scripts/seed/06-qa-geo-check.ts --slug=<slug>` then re-run from Gate 2 | Gate 2 + Gate 8 |

## Deviations from Plan

None — plan executed exactly as written.

The context_note requested mentioning the yt_id audit gate and db.transaction rollback semantics from plans 11.1-01 and 11.1-02. Added as a "Notes on batch scripts" block below the scenario table — still within D-09 (pure pointers) since it adds operational clarity without duplicating SOP content. Line count remains 41.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. This is a documentation-only plan. No threat flags.

## Known Stubs

None. This is a documentation file with no data sources to wire.

## Self-Check: PASSED

- [x] `docs/README-adding-songs.md` exists
- [x] `wc -l` = 41 (≤ 100)
- [x] All 5 D-08 scenarios present verbatim (grep verified)
- [x] `.env.local`, `.venv/Scripts`, `Node.js 22+` in before-you-start checklist
- [x] 6 links to SONG_INGESTION_SOP.md (≥ 5 required)
- [x] All 5 anchor hashes verified against actual SOP `### Gate N` headers
- [x] Zero shell-variable placeholders (`$1`, `${X}`)
- [x] No external URLs (all links repo-relative)
- [x] No SOP content duplicated (pure pointers)
- [x] Task commit 209b2ab confirmed in git log
