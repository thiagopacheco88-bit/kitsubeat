---
phase: 11.1-add-song-pipeline
plan: "01"
subsystem: seed-scripts
tags:
  - seed-script
  - audit
  - bash-orchestrator
  - neon-http
  - drizzle
dependency_graph:
  requires: []
  provides:
    - scripts/seed/audit-yt-ids.ts (cross-slug youtube_id duplicate detector)
    - audit step wired into run-pending-batch.sh and overnight-orchestrator.sh
  affects:
    - scripts/seed/run-pending-batch.sh
    - scripts/seed/overnight-orchestrator.sh
tech_stack:
  added:
    - drizzle-orm innerJoin (cross-table audit query pattern)
  patterns:
    - read-only DB audit script → CSV output
    - || exit 1 propagation in bash shells
key_files:
  created:
    - scripts/seed/audit-yt-ids.ts (80 lines)
  modified:
    - scripts/seed/run-pending-batch.sh (98 → 100 lines, +2)
    - scripts/seed/overnight-orchestrator.sh (130 → 132 lines, +2)
decisions:
  - Cross-slug filter uses set.size > 1 (same youtube_id on full+tv of SAME slug is allowed)
  - CSV locked header: youtube_id,count,slugs,version_types with pipe-separated multi-value cells
  - Both shells use || exit 1 (not run_step wrapper, which swallows exit codes)
  - Audit placed AFTER status-summary heredoc in run-pending-batch.sh (Step 6)
  - Audit placed AFTER final run_step in overnight-orchestrator.sh (Step 8), before summary block
metrics:
  duration_minutes: 2
  completed_date: "2026-04-26"
  tasks_completed: 2
  files_created: 1
  files_modified: 2
---

# Phase 11.1 Plan 01: Cross-Song YouTube ID Audit Summary

**One-liner:** Read-only drizzle audit detects cross-slug youtube_id dupes, wired as `|| exit 1` final step in both batch shells.

## Objective Achieved

Built `scripts/seed/audit-yt-ids.ts` and wired it as the terminal step in both `run-pending-batch.sh` and `overnight-orchestrator.sh` so any batch that introduces a cross-slug youtube_id duplicate halts with exit 1 before the catalog ships broken.

## Files Created / Modified

| File | Change | Lines |
|------|--------|-------|
| `scripts/seed/audit-yt-ids.ts` | Created | 80 |
| `scripts/seed/run-pending-batch.sh` | Modified (+2 lines, Step 6) | 100 |
| `scripts/seed/overnight-orchestrator.sh` | Modified (+2 lines, Step 8) | 132 |

## Dev DB First Run — Results

The first audit run against the dev DB found **7 cross-slug duplicate youtube_id groups**:

| youtube_id | Slugs | Count |
|------------|-------|-------|
| `73fGDIJ1HVc` | hajimete-kimi-to-shabetta-gagagasp, ima-made-nando-mo-the-mass-missile, parade-chaba | 3 slugs |
| `aCLLQ5xWCPI` | shinkokyuu-super-beaver, u-can-do-it-domino | 2 slugs |
| `Cg36IJgn-so` | nakushita-kotoba-no-regret-life, remember-flow | 2 slugs |
| `f9_7gGpL_RA` | pinocchio-ore-ska-band, yellow-moon-akeboshi | 2 slugs |
| `cnCi7peJSBk` | call-your-name-gv-gemie, call-your-name-mpi-casg | 2 slugs |
| `2B6nj38AdD0` | guren-no-yumiya-linked-horizon, guren-no-zahyou-linked-horizon | 2 slugs |
| `hMKf5mE3sdo` | mezase-pokemon-master-rica-matsumoto, pokemon-getto-da-ze-rica-matsumoto | 2 slugs |

**Exit code: 1** — Audit working as designed. These are pre-existing legacy dupes now surfaced for operator follow-up (not Phase 11.1 scope to fix).

**CSV output:** `data/yt-id-audit.csv` written — header line `youtube_id,count,slugs,version_types` + 7 dupe rows.

## Locked Invocation Lines in Each Shell

**run-pending-batch.sh (Step 6):**
```bash
log "Step 6: cross-song youtube_id audit"
npx tsx --tsconfig tsconfig.scripts.json scripts/seed/audit-yt-ids.ts || exit 1
```

**overnight-orchestrator.sh (Step 8):**
```bash
log "Step 8: cross-song youtube_id audit"
npx tsx --tsconfig tsconfig.scripts.json scripts/seed/audit-yt-ids.ts || exit 1
```

## Operator Follow-Up Required

The 7 cross-slug dupe groups in the dev DB are pre-existing — they were there before this plan and are now visible for the first time. These must be resolved by the operator (assigning correct unique youtube_ids per slug) before either batch shell can exit 0. This is intentional: the audit working correctly is exactly what the plan was designed to produce.

Specifically `73fGDIJ1HVc` is shared by 3 different slugs — similar to the historical `gX9WX_NP5Ug` incident (5 slugs sharing one ID) that motivated this guard.

## Deviations from Plan

None — plan executed exactly as written.

## Threat Surface Scan

No new network endpoints, auth paths, or schema changes introduced. The script is read-only (SELECT only) against Neon HTTP. CSV output lands in gitignored `data/` directory. T-11.1.01-01 mitigation verified: file contains no `process.env.DATABASE_URL` in any console call.

## Self-Check: PASSED

- `scripts/seed/audit-yt-ids.ts` exists and runs via `npx tsx`
- Commits 6df80f5 (Task 1) and e7986af (Task 2) exist in git log
- `data/yt-id-audit.csv` written with locked header as line 1
- Both shells contain `npx tsx --tsconfig tsconfig.scripts.json scripts/seed/audit-yt-ids.ts || exit 1`
- Both shells parse cleanly (`bash -n` exits 0)
- All 13 acceptance criteria grep checks pass
