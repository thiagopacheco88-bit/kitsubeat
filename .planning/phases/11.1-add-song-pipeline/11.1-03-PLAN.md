---
phase: 11.1-add-song-pipeline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/README-adding-songs.md
autonomous: true
requirements: []
requirements_addressed:
  - SPEC-AC-10: docs/README-adding-songs.md exists, is ≤ 100 lines, covers all 5 named scenarios with runnable commands
  - SPEC-AC-11: All links from README to docs/SONG_INGESTION_SOP.md gate sections resolve
tags:
  - documentation
  - operator-quickstart
  - song-ingestion
must_haves:
  truths:
    - "Operator can identify which command(s) to run for any of the 5 named scenarios in under 30 seconds"
    - "README is short enough (≤ 100 lines) to load cheaply at session start"
    - "README is a pure pointer — links into the SOP for each gate, no SOP content duplication (D-09)"
    - "Every link to docs/SONG_INGESTION_SOP.md resolves to a real anchor in that file"
    - "Before-you-start checklist names the exact env paths a fresh session must verify"
  artifacts:
    - path: docs/README-adding-songs.md
      provides: "Operator quick-start for the add-song pipeline"
      max_lines: 100
      contains: "Add 1 new song"
      contains_link_to: "docs/SONG_INGESTION_SOP.md"
  key_links:
    - from: docs/README-adding-songs.md (every scenario row)
      to: docs/SONG_INGESTION_SOP.md (gate section anchors)
      via: "markdown link with section anchor"
      pattern: "SONG_INGESTION_SOP\\.md#gate-"
    - from: docs/README-adding-songs.md (before-you-start)
      to: ".env.local + .venv/Scripts + Node.js 22+"
      via: "checklist references existing project conventions"
      pattern: "\\.env\\.local"
---

<objective>
Create `docs/README-adding-songs.md` — a single-page (≤ 100 lines) operator quick-start that maps the 5 locked operational scenarios to runnable commands and links into the existing 230-line `docs/SONG_INGESTION_SOP.md` for gate-level detail.

Purpose: New Claude Code sessions (and the operator) currently re-derive the gate ordering from the 230-line SOP every session, costing tokens and time. This README is the cheap entrypoint: "I want to add 3 songs — what command do I run?" answered in seconds, with deeper detail one click away. Catalog velocity is bottlenecked by Claude-session time (per memory note); this doc shaves session-start friction.

Output: A markdown doc structured per CONTEXT D-07 + D-08 + D-09: one-paragraph intro → "Before you start" checklist → 5-row scenario table → links into SOP gate anchors → footer pointer to the full SOP. Pure pointers, zero SOP content duplication.

**Critical scope guardrails:**
- ≤ 100 lines hard limit (target ~70 per D-07).
- D-08 locks the 5 scenarios verbatim — do not add, drop, or reword them.
- D-09 locks the "no SOP content duplication" rule — every gate-level explanation is a link, never inline text.
- Every link to `docs/SONG_INGESTION_SOP.md` MUST resolve to an actual anchor in that file (verify by reading the SOP's headers before writing the link).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/11.1-add-song-pipeline/11.1-CONTEXT.md
@.planning/phases/11.1-add-song-pipeline/11.1-SPEC.md
@.planning/phases/11.1-add-song-pipeline/11.1-PATTERNS.md
@.planning/phases/11.1-add-song-pipeline/BATCH_STATE.md

@docs/SONG_INGESTION_SOP.md

@CLAUDE.md
@kitsubeat/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write docs/README-adding-songs.md (single page, ≤ 100 lines, 5 locked scenarios, pure pointers into SOP)</name>
  <files>docs/README-adding-songs.md</files>
  <read_first>
    - docs/SONG_INGESTION_SOP.md (FULL FILE — read every header line so you can write working anchor links. Specifically note Gate 1 through Gate 8 header text verbatim; the GitHub markdown anchor for `## Gate 5 — WhisperX Timing Extraction (Full + TV)` is `#gate-5--whisperx-timing-extraction-full--tv`. Anchor rules: lowercase, spaces→dashes, em-dashes preserved as `-`, parentheses dropped, multiple consecutive hyphens collapse only when they were originally consecutive non-letter chars.)
    - .planning/phases/11.1-add-song-pipeline/11.1-CONTEXT.md (D-07, D-08, D-09 — structure + locked 5 scenarios + pure-pointers rule)
    - .planning/phases/11.1-add-song-pipeline/11.1-SPEC.md (Acceptance Criteria 10-11; Constraint "≤ 100 lines")
    - .planning/phases/11.1-add-song-pipeline/11.1-PATTERNS.md (lines 201-220 — locked structure + per-row scenario table draft)
    - .planning/phases/11.1-add-song-pipeline/BATCH_STATE.md (operator-intent context: the 5 scenarios reflect actual Phase 11/11.1 operator workflow)
    - C:\Users\thiag\.claude\projects\c--Users-thiag-velora-projects-kitsubeat\memory\MEMORY.md (relevant entries: ".venv/Scripts on PATH" — informs the before-you-start checklist; "Node at C:\\Program Files\\nodejs" — informs Node check)
  </read_first>
  <behavior>
    - Operator reading the file at session start can pick a scenario row, copy the command, and run it.
    - Every "what does Gate N do?" question is answered by clicking the SOP link, not by reading more of this README.
    - File loads in well under 1k tokens at session start (≤ 100 lines, ≤ ~3000 chars target).
    - Markdown renders cleanly on GitHub (table syntax + standard anchor links).
    - No commands include shell-injectable interpolation (operator-input variables are spelled as `<slug>` placeholders, never as `$1` or `${INPUT}` — see threat model).
  </behavior>
  <action>
    Create `docs/README-adding-songs.md` with the structure locked by CONTEXT D-07 and the 5 scenarios locked by CONTEXT D-08. Total length budget: ≤ 100 lines (SPEC § Constraints). Target: ~70 lines.

    **Step 1 — Verify SOP anchors first.** Before writing any link, open `docs/SONG_INGESTION_SOP.md` and list its `##` and `###` headers. The GitHub anchor for a header is generated by lowercasing, replacing spaces with `-`, dropping punctuation (`()`, `:`, `,`), and preserving em-dashes as `-`. For example: `## Gate 5 — WhisperX Timing Extraction (Full + TV)` → `#gate-5--whisperx-timing-extraction-full--tv` (note the double-dash after "5" because the em-dash sits between two spaces). Build a mapping of `gate-name → anchor-string` BEFORE writing the table.

    **Step 2 — File structure (5 sections, in this order):**

    Section 1 — Title + one-paragraph intro (~3 sentences, max 5 lines):
    ```markdown
    # Adding Songs to KitsuBeat — Operator Quick-Start

    The add-song pipeline turns a YouTube ID + romanised title into a fully-timed,
    lesson-attached song row in the catalog. The 8-gate procedure is documented in
    full in [docs/SONG_INGESTION_SOP.md](SONG_INGESTION_SOP.md) — this file is the
    fast lookup for "I want to do X — what command do I run?"
    ```

    Section 2 — "Before you start" checklist (D-07; 4-6 bullet points, max 12 lines):
    ```markdown
    ## Before you start

    - **`.env.local`** present at repo root with `DATABASE_URL` set to the Neon Postgres URL.
    - **Python venv** activated: `PATH=".venv/Scripts:$PATH"` (Windows) so WhisperX resolves
      ffmpeg correctly — without this, `whisperx` fails with `WinError 2` at `load_audio`.
    - **Node.js 22+** on PATH (default install location: `C:\Program Files\nodejs`).
    - **≥ 6 GB free RAM** if you'll run WhisperX with the `large-v3` model (medium model
      is fine on 4 GB; `the-hero-jam-project`-style 60% low-conf cases need large-v3).
    - **Optional but recommended:** `TEST_DATABASE_URL` pointed at a separate Neon DB so
      `npx vitest run tests/integration/` doesn't touch the dev catalog.
    ```

    Section 3 — Scenario table (D-08 locks all 5 rows verbatim; PATTERNS.md lines 209-216 has the draft commands; max ~25 lines including the table header):
    ```markdown
    ## Common scenarios

    | Intent | Command(s) | SOP gates touched |
    |---|---|---|
    | Add 1 new song | `npx tsx scripts/seed/01-build-manifest.ts && bash scripts/seed/run-pending-batch.sh` | [Gate 1 → Gate 8](SONG_INGESTION_SOP.md#gate-1--curation-manual-review-of-new-candidates) |
    | Add a batch of N songs | `bash scripts/seed/run-pending-batch.sh` | All 8 gates |
    | Resume pending_whisper backlog | `bash scripts/seed/overnight-orchestrator.sh` | [Gate 5](SONG_INGESTION_SOP.md#gate-5--whisperx-timing-extraction-full--tv) onward |
    | Add only the TV cut for an existing song | `npx tsx scripts/seed/10-prepare-tv.ts --slug=<slug>` | [Gate 6](SONG_INGESTION_SOP.md#gate-6--tv-derivation) only |
    | Replace a geo-blocked or live-version YouTube ID | `npx tsx scripts/seed/06-qa-geo-check.ts --slug=<slug>` then re-run from Gate 2 | [Gate 2](SONG_INGESTION_SOP.md#gate-2--youtube-selection-full--tv) + [Gate 8](SONG_INGESTION_SOP.md#gate-8--db-insert) |
    ```

    **CRITICAL:** the anchor strings (`#gate-5--whisperx-timing-extraction-full--tv` etc.) above are best-effort drafts. **Re-derive each anchor from the actual SOP header text you read in Step 1.** If the SOP's Gate 6 header is `## Gate 6 — TV Derivation` (no parentheses), the anchor is `#gate-6--tv-derivation`. If the SOP's Gate 6 header is `## Gate 6 — TV Derivation (10-prepare-tv.ts)`, the anchor is `#gate-6--tv-derivation-10-prepare-tvts` (parens dropped, `.` becomes nothing). Verify EACH anchor against the actual SOP file before committing.

    Section 4 — Footer pointer (D-09; max 5 lines):
    ```markdown
    ## Deeper dive

    For gate-level detail, failure modes, and recovery procedures, see
    [docs/SONG_INGESTION_SOP.md](SONG_INGESTION_SOP.md). For the audit script that
    catches cross-song YouTube ID duplicates, see
    [`scripts/seed/audit-yt-ids.ts`](../scripts/seed/audit-yt-ids.ts).
    ```

    Section 5 — (Optional, fits within budget) Empty trailing newline.

    **Strict prohibitions (per D-09 "pure pointers"):**
    - DO NOT explain what each gate does inline. Link only.
    - DO NOT duplicate the 8-gate ordering, the WhisperX flag table, the geo-restriction
      tier table, or any other content from the SOP. Link only.
    - DO NOT add a "troubleshooting" or "FAQ" section in this file. If a recurring
      issue surfaces, document it in the SOP and link from here.

    **Length verification:** after writing, confirm `wc -l < docs/README-adding-songs.md` returns ≤ 100. Target ~70.

    **Anchor verification:** open the rendered file (or visually trace each link against the SOP headers you cataloged in Step 1). Every link target MUST exist as an actual `##` or `###` header in the SOP.

    **No shell-injection in commands:** all command lines use static text or angle-bracket placeholders like `<slug>` (which a human substitutes — never an automated process). Do NOT write `${SLUG}`, `$1`, or any shell-variable form.
  </action>
  <verify>
    <automated>test "$(wc -l < docs/README-adding-songs.md)" -le 100 && grep -q "Add 1 new song" docs/README-adding-songs.md && grep -q "Add a batch of N songs" docs/README-adding-songs.md && grep -q "Resume pending_whisper backlog" docs/README-adding-songs.md && grep -q "Add only the TV cut for an existing song" docs/README-adding-songs.md && grep -q "Replace a geo-blocked" docs/README-adding-songs.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/README-adding-songs.md` exists
    - File line count: `wc -l < docs/README-adding-songs.md` returns ≤ 100 (SPEC § Constraints hard limit)
    - File contains all 5 D-08 scenario names verbatim:
      - "Add 1 new song" (grep)
      - "Add a batch of N songs" (grep)
      - "Resume pending_whisper backlog" (grep)
      - "Add only the TV cut for an existing song" (grep)
      - "Replace a geo-blocked" (grep — partial match for the 5th scenario name)
    - File contains the locked before-you-start references:
      - `.env.local` (grep)
      - `.venv/Scripts` OR `.venv\Scripts` (grep — informs Python venv setup; either separator acceptable in markdown prose)
      - `Node.js 22+` OR `Node 22` (grep — Node version pinning)
    - File contains at least 5 links to `SONG_INGESTION_SOP.md` (count: `grep -c 'SONG_INGESTION_SOP\.md' docs/README-adding-songs.md` ≥ 5)
    - Every `SONG_INGESTION_SOP.md#gate-` anchor in the README corresponds to a real `##` or `###` header in `docs/SONG_INGESTION_SOP.md` — verify by:
      1. Extracting each `#gate-...` anchor from the README (grep -oE)
      2. For each anchor, confirming a header in the SOP that lowercases-and-dashes to the same string exists
      3. Manual visual trace if grep automation is impractical
    - File contains NO shell-variable placeholders like `$1`, `${SLUG}`, `${INPUT}` (grep — confirms no shell-injection risk via the docs)
    - File does NOT duplicate SOP content (manual review): no inline gate descriptions, no WhisperX flag tables, no geo-tier tables. Each gate reference is a link.
    - Markdown table renders correctly on GitHub (manual visual check OR run a markdown linter if available)
  </acceptance_criteria>
  <done>
    File exists at the locked path, all 5 scenarios are present verbatim, line count is ≤ 100, every anchor link resolves to a real SOP header, no SOP content is duplicated, no shell-variable placeholders appear, and markdown renders cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator reading README → operator's shell | Operator copies a command from the README into their shell. The README is the trust source for what to run. |
| README → docs/SONG_INGESTION_SOP.md | Internal cross-reference; both files are repo-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.1.03-01 | Tampering | Shell-injection via README command examples | mitigate | All scenario commands use static paths and angle-bracket placeholders (`<slug>`) for human substitution. AC: file contains no `$1`, `${SLUG}`, or `${INPUT}` patterns. Operator who substitutes a hostile slug into `--slug=<slug>` is the same operator running the script — no privilege boundary crossed. |
| T-11.1.03-02 | Information Disclosure | Documentation accidentally pastes secrets (DATABASE_URL, API keys) into the README | mitigate | The "before you start" checklist references `.env.local` by name only — never includes the actual URL. AC: README does NOT contain the literal substring `postgres://` or `postgresql://` (grep verification can be added). |
| T-11.1.03-03 | Repudiation | Anchor link rot — SOP headers change, README links break, operator runs the wrong command | mitigate | AC requires every anchor in the README to correspond to a real SOP header. Future SOP edits should re-verify; document this in the future maintenance pattern (out of phase). For Phase 11.1, the verification at write-time is sufficient. |
| T-11.1.03-04 | Spoofing | Fake "deeper dive" link points outside the repo | accept | All links are repo-relative (`SONG_INGESTION_SOP.md`, `../scripts/seed/audit-yt-ids.ts`). No external URLs in the README. AC: no `http://` or `https://` substrings (verify if needed). |

`security_block_on: high` — none of these are HIGH; plan passes.
</threat_model>

<verification>
**End-to-end check after the task completes:**
1. `wc -l < docs/README-adding-songs.md` returns a number ≤ 100.
2. All 5 D-08 scenario names appear verbatim (grep).
3. `grep -c 'SONG_INGESTION_SOP\.md' docs/README-adding-songs.md` returns ≥ 5.
4. Manual visual trace: open the rendered README on GitHub (or in a markdown previewer) and click every link to `SONG_INGESTION_SOP.md#...`. Each link MUST scroll to a real heading in the SOP.
5. Re-read the README in under 60 seconds: can you, as a fresh-session operator, identify the right command for "add the TV cut of an existing song" without scrolling outside this one file? If yes, AC #10 is met.
</verification>

<success_criteria>
- [ ] `docs/README-adding-songs.md` exists at the locked path
- [ ] Line count ≤ 100 (SPEC § Constraints hard limit; target ~70 per D-07)
- [ ] All 5 D-08 scenarios present verbatim
- [ ] Before-you-start checklist names `.env.local`, `.venv/Scripts`, Node 22+, ≥6 GB RAM for large-v3
- [ ] 5+ links to `docs/SONG_INGESTION_SOP.md` with valid anchors (every anchor resolves to a real header in the SOP — SPEC AC #11)
- [ ] No SOP content duplication (D-09 — pure pointers)
- [ ] No shell-variable placeholders (`$1`, `${X}`) in command examples
- [ ] No external URLs (all links are repo-relative)
- [ ] Markdown renders cleanly on GitHub (table syntax valid, headers correct level)
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-add-song-pipeline/11.1-03-SUMMARY.md` documenting:
- Final line count of `docs/README-adding-songs.md`
- The list of SOP anchors used (extracted from the README) and a confirmation that each one matches a real SOP header
- Any deviations from the D-08 scenario wording and why (should be NONE — D-08 is locked)
- A copy of the final scenario table for record-keeping
</output>
