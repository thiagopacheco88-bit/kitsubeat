---
phase: 10-advanced-exercises-full-mastery
plan: 07
type: execute
wave: 3
depends_on: ["10-01", "10-06"]
files_modified:
  - src/app/songs/[slug]/components/StarDisplay.tsx
  - src/app/songs/[slug]/components/SessionSummary.tsx
  - src/app/songs/components/SongCard.tsx
  - src/app/songs/components/SongMasteredBanner.tsx
  - src/app/songs/components/BonusBadgeIcon.tsx
  - src/lib/db/queries.ts
  - src/app/songs/[slug]/page.tsx
  - src/app/songs/page.tsx
autonomous: true
requirements: [STAR-01, STAR-04, STAR-05, STAR-06]

must_haves:
  truths:
    - "StarDisplay renders 3 stars at the correct filled count based on deriveStars (0|1|2|3)"
    - "Star 3 celebration reuses the existing Star 1/2 confetti + star-glow code path (no new animation)"
    - "When stars transitions from <3 to 3 after saveSessionResults, confetti fires once (previousStars < 3 && stars === 3)"
    - "SongMasteredBanner is rendered on SongCard ONLY when the song has stars === 3 for the current user"
    - "BonusBadgeIcon is rendered on SongCard ONLY when deriveBonusBadge(progress) === true for the current user"
    - "Catalog query (SongListItem) fetches ex5_best_accuracy and ex7_best_accuracy so deriveBonusBadge can run at render time"
    - "Catalog query also fetches ex6_best_accuracy so deriveStars can return 0|1|2|3"
    - "Bonus badge is visually subtle — stars remain the primary signal (CONTEXT locked)"
  artifacts:
    - path: "src/app/songs/components/SongMasteredBanner.tsx"
      provides: "Ribbon/banner overlay on song-card thumbnail for 3-star songs"
      min_lines: 30
    - path: "src/app/songs/components/BonusBadgeIcon.tsx"
      provides: "Small icon adjacent to StarDisplay for ex5 + ex7 >= 80%"
      min_lines: 20
    - path: "src/lib/db/queries.ts"
      provides: "SongListItem query extended to fetch ex5/ex6/ex7 best_accuracy"
      contains: "ex6_best_accuracy"
    - path: "src/app/songs/[slug]/components/StarDisplay.tsx"
      provides: "3-star rendering + Star 3 SVG fill state"
      contains: "stars === 3"
  key_links:
    - from: "src/app/songs/components/SongCard.tsx"
      to: "src/app/songs/components/SongMasteredBanner.tsx"
      via: "conditional render when progress.stars === 3"
      pattern: "SongMasteredBanner"
    - from: "src/app/songs/components/SongCard.tsx"
      to: "src/app/songs/components/BonusBadgeIcon.tsx"
      via: "conditional render when deriveBonusBadge(progress) === true"
      pattern: "BonusBadgeIcon"
    - from: "src/app/songs/[slug]/components/SessionSummary.tsx"
      to: "canvas-confetti"
      via: "existing confetti burst extended to fire on stars === 3 transition"
      pattern: "confetti"
---

<objective>
Surface the 3-star mastery achievement and the bonus mastery badge on the song catalog. Widen `StarDisplay` to its locked 0|1|2|3 range (the type already permits it), reuse the existing Star 1/2 confetti + star-glow code path for Star 3 (no new animation per CONTEXT), add `SongMasteredBanner` as a persistent ribbon on the catalog card for 3-star songs, add `BonusBadgeIcon` (subtle) alongside stars for songs where both Ex 5 and Ex 7 best accuracy >= 80%, and extend the catalog list query to fetch the three new accuracy columns so both derivations run at render time.

Purpose: CONTEXT locked — Star 3 celebration = same confetti, plus persistent "Song Mastered" banner on the catalog card. Bonus badge = subtle icon on the catalog card alongside stars, not just on song detail page. Stars remain primary; bonus badge is secondary.
Output: Users see their mastery status across the catalog — 3 stars filled, mastered-banner ribbon, and a subtle bonus badge. Confetti fires when Star 3 is newly earned.
</objective>

<execution_context>
@C:/Users/Thiago/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Thiago/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-advanced-exercises-full-mastery/10-CONTEXT.md
@.planning/phases/10-advanced-exercises-full-mastery/10-RESEARCH.md
@.planning/phases/10-advanced-exercises-full-mastery/10-01-SUMMARY.md
@.planning/phases/10-advanced-exercises-full-mastery/10-06-SUMMARY.md

# Prior decisions this plan extends
@.planning/phases/08-exercise-engine/08-04-SUMMARY.md

# Files to extend
@src/app/songs/[slug]/components/StarDisplay.tsx
@src/app/songs/[slug]/components/SessionSummary.tsx
@src/app/songs/components/SongCard.tsx
@src/lib/db/queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: StarDisplay 0-3 + SessionSummary confetti at Star 3 + new catalog components</name>
  <files>
    src/app/songs/[slug]/components/StarDisplay.tsx
    src/app/songs/[slug]/components/SessionSummary.tsx
    src/app/songs/components/SongMasteredBanner.tsx
    src/app/songs/components/BonusBadgeIcon.tsx
  </files>
  <action>
    1. **StarDisplay** `src/app/songs/[slug]/components/StarDisplay.tsx`:
       - Per RESEARCH.md §9: the `stars` prop type already permits `0 | 1 | 2 | 3`. Verify the internal rendering loops over `[1, 2, 3]` and fills stars <= stars-prop.
       - If the 3rd star is rendered only as an outline today, swap to the filled SVG when `stars === 3`. Match the existing fill style of stars 1 and 2 (same color, same size, same animate prop behavior).
       - Preserve the `animate` prop behavior — CONTEXT: identical confetti + star-glow as Stars 1 and 2. No new animation.

    2. **SessionSummary** `src/app/songs/[slug]/components/SessionSummary.tsx`:
       - Confetti trigger condition extends from `stars > previousStars && stars > 0` to also cover `stars === 3 && previousStars < 3`.
       - Per Pitfall 7, use the BEFORE-upsert `previousStars` snapshot (Plan 06 writes ex6 into the snapshot already). No structural change needed — just include ex6 in the derivation.
       - If the summary screen shows a "You earned X stars!" message, extend it to say "You earned 3 stars — song mastered!" on the 3-star transition.
       - **Bonus badge transition** — if `deriveBonusBadge(progress)` flips from false to true after saveSessionResults, surface a small one-line callout in SessionSummary: "Bonus mastery unlocked!" — no confetti, subtle, per CONTEXT.

    3. **SongMasteredBanner** `src/app/songs/components/SongMasteredBanner.tsx`:
       - Props: `{ className?: string }`. Rendered as a ribbon/overlay strip on the song-card thumbnail.
       - Visual: a diagonal ribbon at the top-right of the thumbnail, or a pill at the top-left — CONTEXT gives Claude discretion on exact visual. Use existing Tailwind color primitives (e.g., gold/amber for mastery, matching Star styling) and keep it visible but not overwhelming.
       - Copy: "MASTERED" (all caps) or "SONG MASTERED" if space permits. Keep short — it's a glance signal.
       - Component length: 30-60 lines.

    4. **BonusBadgeIcon** `src/app/songs/components/BonusBadgeIcon.tsx`:
       - Props: `{ className?: string }`. Small icon (~16px). CONTEXT: "subtle — stars remain the primary signal."
       - Visual: a star-burst, medal, or "B" pill glyph — use a Lucide icon if the project imports lucide-react (check package.json), else inline SVG.
       - Include a `title` attribute for hover tooltip: "Bonus mastery: Grammar Conjugation + Sentence Order".
       - Component length: 20-40 lines.

    **Avoid:** Creating a new confetti primitive (CONTEXT: reuse existing). Making the bonus badge larger than the stars. Adding the banner to the song detail page — it's catalog-card only per CONTEXT; the detail page already has the stars.
  </action>
  <verify>
    - `npm run build` green.
    - Visual smoke: `npm run dev`, open a song in test DB with ex6_best_accuracy = 1.0 and complete a session → confirm 3-star rendering + confetti + summary callout.
    - `npm run test:unit` for any snapshot tests on StarDisplay — update snapshots.
  </verify>
  <done>
    StarDisplay renders 3-star filled at stars=3. SessionSummary fires confetti on Star 3 transition. SongMasteredBanner + BonusBadgeIcon components exist and render as subtle catalog decorations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Catalog query extension + SongCard integration + page wiring</name>
  <files>
    src/lib/db/queries.ts
    src/app/songs/components/SongCard.tsx
    src/app/songs/[slug]/page.tsx
    src/app/songs/page.tsx
  </files>
  <action>
    1. **Queries** `src/lib/db/queries.ts`:
       - Find the `SongListItem` query (per RESEARCH.md §9 — or the equivalent function returning song catalog data). If the catalog list already fetches `user_song_progress` for authenticated users (Phase 08.4 + 11 may have added this), extend the selected columns to include `ex5_best_accuracy`, `ex6_best_accuracy`, `ex7_best_accuracy`.
       - If the catalog list currently runs unauthenticated (`progress: null` when no user), add a minimal authenticated path: when userId is available, JOIN `user_song_progress` and return the 3 new accuracy fields alongside existing `ex1_2_3_best_accuracy` / `ex4_best_accuracy`.
       - Keep the query efficient — the catalog hits 200 rows; no N+1.
       - Extend the `SongListItem` type to include the 3 new fields (nullable).

    2. **SongCard** `src/app/songs/components/SongCard.tsx`:
       - At render time, compute:
         - `const stars = deriveStars({ ex1_2_3_best_accuracy, ex4_best_accuracy, ex6_best_accuracy })` (3-arg version from Plan 01).
         - `const bonus = deriveBonusBadge({ ex5_best_accuracy, ex7_best_accuracy })`.
       - Condition to render SongMasteredBanner: `stars === 3` only. Position: absolute overlay on the thumbnail container.
       - Condition to render BonusBadgeIcon: `bonus === true`. Position: adjacent to the StarDisplay row (e.g., inline with stars, trailing or leading).
       - Preserve the existing `showProgress` gate (render StarDisplay only when `completionPct > 0 || stars > 0`).
       - Do NOT show banner/badge to unauthenticated users (progress = null → treat as 0 stars, no badge).

    3. **Song detail page** `src/app/songs/[slug]/page.tsx` (minor):
       - If the SSR currently fetches `user_song_progress`, extend the fetched columns to include ex5/ex6/ex7 so the page can pass accurate 3-star data into StarDisplay on the song page header.

    4. **Songs catalog page** `src/app/songs/page.tsx`:
       - Ensure the page's server-side catalog fetch passes through to the updated `SongListItem` shape. If it already just renders `<SongCard>` per row, no change beyond updated types.

    **Avoid:** Rendering banner/badge for unauthenticated users. Fetching per-song user_song_progress in a loop — single join. Hardcoding style primitives that don't match the existing SongCard tokens.
  </action>
  <verify>
    - `npm run build` green; types align.
    - `npm run test:integration` SongListItem query test (if exists) passes with the new fields.
    - Visual smoke on `/songs`: a song with stars=3 renders the banner; a song with bonus=true (ex5 + ex7 >= 0.80) renders the subtle badge; other songs unchanged.
    - `npm run test:e2e` existing catalog E2E still green.
  </verify>
  <done>
    Catalog query fetches ex5/ex6/ex7. SongCard conditionally renders SongMasteredBanner + BonusBadgeIcon. No render for unauthenticated users. Song detail page StarDisplay shows 3 stars when earned.
  </done>
</task>

</tasks>

<verification>
- 3-star StarDisplay renders filled third star.
- Star 3 transition in a session fires confetti once (not on every subsequent session).
- SongMasteredBanner appears on catalog card only when stars === 3.
- BonusBadgeIcon appears on catalog card only when both ex5 and ex7 best_accuracy >= 0.80.
- Unauthenticated catalog views do not show mastery decorations.
- Song detail page also reflects 3-star state (StarDisplay consistent).
</verification>

<success_criteria>
1. User earning Star 3 (Listening Drill >= 80%) sees confetti + 3-star filled on Session Summary.
2. That song's card in /songs displays a persistent "Mastered" banner.
3. User earning bonus mastery (both Grammar Conjugation and Sentence Order >= 80%) sees a subtle bonus badge icon on the catalog card alongside the 3 stars.
4. No visual regressions for Star 1 or Star 2 state (existing Phase 8 users unaffected).
</success_criteria>

<output>
After completion, create `.planning/phases/10-advanced-exercises-full-mastery/10-07-SUMMARY.md` documenting:
- StarDisplay widening (before/after).
- SongCard render conditions for banner + badge.
- Catalog query field additions and shape.
- Confirmation that confetti reuses the existing primitive (no new animation library, no new CSS keyframes).
</output>
