---
phase: 10-advanced-exercises-full-mastery
plan: 04
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - src/lib/exercises/generator.ts
  - src/app/songs/[slug]/components/ListeningDrillCard.tsx
  - src/app/songs/[slug]/components/ExerciseSession.tsx
  - src/stores/exerciseSession.ts
  - src/lib/exercises/__tests__/listening-drill.test.ts
autonomous: true
requirements: [EXER-06, STAR-04]

must_haves:
  truths:
    - "Listening Drill question generator produces a question per verse (reusing the fill_lyric verse-blank pattern, plus a replayable audio trigger)"
    - "ListeningDrillCard renders verse text with the target token surface blanked and romaji token blanked (no leaks via toggle)"
    - "ListeningDrillCard calls usePlayer().seekTo(verseStartMs) and usePlayer().play() on mount and on each Replay click"
    - "Replay is unlimited and has no FSRS penalty"
    - "When PlayerContext.embedState === 'error' before the drill mounts, the drill card renders the CONTEXT-locked fallback message and does NOT silently substitute fill_lyric"
    - "FeedbackPanel for listening_drill still surfaces the existing explanation slot"
    - "ExerciseSession dispatches question.type === 'listening_drill' to ListeningDrillCard"
    - "Star 3 (STAR-04) unlocks via ex6_best_accuracy >= 0.80 (schema + deriveStars from Plan 01 already support this)"
  artifacts:
    - path: "src/lib/exercises/generator.ts"
      provides: "listening_drill branch in makeQuestion + verse-start-ms carry through to Question"
      contains: "listening_drill"
    - path: "src/app/songs/[slug]/components/ListeningDrillCard.tsx"
      provides: "Replay button + blanked-surface verse display + usePlayer wiring + watchdog fallback"
      min_lines: 80
    - path: "src/stores/exerciseSession.ts"
      provides: "listeningReplays: Record<questionId, number> slice (for telemetry, no FSRS penalty)"
      contains: "listeningReplays"
  key_links:
    - from: "src/app/songs/[slug]/components/ListeningDrillCard.tsx"
      to: "src/app/songs/[slug]/components/PlayerContext.tsx"
      via: "usePlayer().seekTo + play + isReady + embedState"
      pattern: "usePlayer\\(\\)\\.(seekTo|play|embedState)"
    - from: "src/app/songs/[slug]/components/ExerciseSession.tsx"
      to: "src/app/songs/[slug]/components/ListeningDrillCard.tsx"
      via: "question.type === 'listening_drill' dispatch"
      pattern: "ListeningDrillCard"
---

<objective>
Implement the Fill-the-Lyric Listening Drill exercise (EXER-06, STAR-04) end to end: extend `makeQuestion` with a `listening_drill` branch that reuses `fill_lyric`'s verse-blank selection but carries the verse start time through, build `ListeningDrillCard` that wires to the PlayerContext imperative API (Plan 02) for seek+play+replay, blank the target token's surface AND romaji line, and show the CONTEXT-locked fallback when the YouTube iframe is unavailable. Wire ExerciseSession dispatch.

Purpose: CONTEXT locked — full verse at normal speed via YouTube iframe, lyrics visible with target blanked, unlimited replays, YT watchdog fallback on failure (no fill_lyric substitution), Star 3 gate on >=80% Ex 6 accuracy.
Output: Working Listening Drill exercise integrated into the Practice tab, with a production-safe imperative player wiring, watchdog-aware fallback UI, and unit tests for the generator branch.
</objective>

<execution_context>
@C:/Users/Thiago/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Thiago/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-advanced-exercises-full-mastery/10-CONTEXT.md
@.planning/phases/10-advanced-exercises-full-mastery/10-RESEARCH.md
@.planning/phases/10-advanced-exercises-full-mastery/10-01-SUMMARY.md
@.planning/phases/10-advanced-exercises-full-mastery/10-02-SUMMARY.md

# Prior decisions
@.planning/phases/08.1-end-to-end-qa-suite/08.1-07-SUMMARY.md
@.planning/phases/08-exercise-engine/08-02-SUMMARY.md

# Files
@src/lib/exercises/generator.ts
@src/app/songs/[slug]/components/PlayerContext.tsx
@src/app/songs/[slug]/components/YouTubeEmbed.tsx
@src/app/songs/[slug]/components/VerseBlock.tsx
@src/app/songs/[slug]/components/ExerciseSession.tsx
@src/stores/exerciseSession.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generator branch + session-store slice + unit tests</name>
  <files>
    src/lib/exercises/generator.ts
    src/stores/exerciseSession.ts
    src/lib/exercises/__tests__/listening-drill.test.ts
  </files>
  <action>
    1. **Generator** `src/lib/exercises/generator.ts`:
       - Add case `listening_drill` in `makeQuestion` that mirrors fill_lyric's verse-blank logic BUT requires `verse.start_time_ms > 0` (per RESEARCH.md §7 inheritance — same constraint as fill_lyric).
       - Carry `verse.start_time_ms` through on the Question as a new optional field `verseStartMs?: number`.
       - Options = same as fill_lyric (4 vocab surfaces: correct + 3 distractors). Use existing `pickDistractorsWithVocab` with `ExerciseType === "listening_drill"`.
       - `buildQuestions` branch: only emit listening_drill questions when verses have valid `start_time_ms`; skip the song cleanly if no valid verses.
       - Ensure Question interface already has `verseStartMs?: number` (add if absent — Plan 01 added `conjugationBase?: string`, same pattern).

    2. **Session store** `src/stores/exerciseSession.ts`:
       - Add slice `listeningReplays: Record<string, number>` (keyed by question.id).
       - Add action `incrementListeningReplay(questionId)` — for telemetry only, NOT fed to FSRS (CONTEXT: unlimited replays, no penalty).
       - Reset slice on `startSession` (same lifecycle as existing `moreAccordionOpen` per Phase 08.3 pattern).
       - No persist changes — zustand `persist` middleware already covers the whole slice map.

    3. **Unit tests** `src/lib/exercises/__tests__/listening-drill.test.ts`:
       - A lesson with 3 verses all having `start_time_ms > 0` → 3 listening_drill questions emitted.
       - A lesson with 1 verse having `start_time_ms = 0` → that verse excluded, other verses emit questions.
       - A lesson with ALL verses having `start_time_ms = 0` → zero listening_drill questions (clean skip per RESEARCH.md Open Q #3).
       - Distractors never include correct answer.
       - Question carries `verseStartMs` equal to that verse's start_time_ms.

    **Avoid:** Emitting questions when timing data is missing. Calling `pickDistractorsWithVocab` without adding `listening_drill` to its switch case if it uses a type-keyed branch (verify + extend).
  </action>
  <verify>
    - `npm run test:unit` listening-drill tests green.
    - `npm run build` green; ExerciseType switch exhaustive for 5 types so far (listening_drill added, grammar_conjugation from Plan 03).
    - Session store tests (if any exist) still green.
  </verify>
  <done>
    Generator emits valid listening_drill questions with verseStartMs. Session store tracks replay count without FSRS impact. Tests cover timing-missing edge cases.
  </done>
</task>

<task type="auto">
  <name>Task 2: ListeningDrillCard + watchdog fallback + ExerciseSession dispatch</name>
  <files>
    src/app/songs/[slug]/components/ListeningDrillCard.tsx
    src/app/songs/[slug]/components/ExerciseSession.tsx
  </files>
  <action>
    1. **ListeningDrillCard** `src/app/songs/[slug]/components/ListeningDrillCard.tsx`:
       - Props: `{ question: Question; verseTokens: Token[]; onAnswer: (answer: string) => void; disabled?: boolean }`.
       - Consume PlayerContext via `usePlayer()` (from Plan 02) — need `seekTo`, `play`, `isReady`, `embedState`.
       - On mount (and when question.id changes), if `embedState === "ready"` and `isReady`, call `seekTo(question.verseStartMs); play();`. If not ready yet, wait for `isReady` via useEffect dep.
       - Render verse text: iterate `verseTokens`. For each token, if `token.surface === question.correctAnswer`, render `_____` (blanked). Also blank the romaji line for the same token (RESEARCH.md Pitfall 8). Keep furigana toggle off for the blanked token.
       - Render "Replay" button — onClick: `seekTo(question.verseStartMs); play(); store.incrementListeningReplay(question.id)`. Debounced by PlayerContext's 400ms internal coalescer (Plan 02).
       - Render 4 option buttons identical to QuestionCard's option styling.
       - **Watchdog fallback** — when `embedState === "error"` (either from initial iframe failure, 15s watchdog, or explicit onError), render the LOCKED fallback copy verbatim (keep the exact string committed here and in Phase 08.1-07's spec — do not reword):
         > "Listening Drill unavailable for this song (video not playable). Star 3 is unreachable until the video works."
       - Do NOT render option buttons or Replay in the fallback state. Emit `onAnswer("__unavailable__")` after a short delay? **No** — preferred: render a "Skip exercise" button that calls `onAnswer("__skipped__")` OR mark the question non-scoring. Simpler: just display the fallback and let the user close the tab. Document the exact chosen behavior in the SUMMARY. Recommendation: render a "Next" button that calls a new `onSkip()` prop which the caller wires to advance past this question without scoring. If "skip" semantics are too complex, fall back to rendering ONLY the fallback message and requiring user to exit the tab — CONTEXT says "Star 3 is unreachable on that song until the video works."
       - **No silent substitution of fill_lyric** — this is the CONTEXT-locked invariant.
       - Component length: 80-160 lines.

    2. **ExerciseSession dispatch** `src/app/songs/[slug]/components/ExerciseSession.tsx`:
       - Extend the switch from Plan 03 with `listening_drill`:
         ```tsx
         if (q.type === "listening_drill") return <ListeningDrillCard verseTokens={verseTokens} {...commonProps} />;
         ```
       - `verseTokens` comes from the lesson's verses — already available in the session context per phase 08 wiring. Resolve via `q.verseRef.verseNumber` → `verses[verseNumber].tokens`.

    3. **Regression:** ensure existing fill_lyric behavior unchanged. `QuestionCard` already handles fill_lyric; listening_drill does NOT go through QuestionCard.

    **Avoid:** Leaking raw YT player reference (use only Plan 02's PlayerContext API). Showing the unblanked romaji for the target token. Calling `play()` before isReady (race-prone). Using `window.__kbPlayer` (test-only hook).
  </action>
  <verify>
    - `npm run build` green.
    - Manually verify fallback message text character-for-character matches the locked string (grep it).
    - `npm run test:e2e` existing player-sync suite still green.
  </verify>
  <done>
    ListeningDrillCard renders blanked verse + 4 options + Replay button. On watchdog/error state, renders the locked fallback message. ExerciseSession dispatches to ListeningDrillCard on listening_drill type. No fill_lyric substitution path exists.
  </done>
</task>

</tasks>

<verification>
- Listening drill questions only emit for verses with start_time_ms > 0.
- ListeningDrillCard blanks both the target surface AND its romaji (no leak).
- When embedState = "error", fallback string is rendered verbatim.
- Replay button triggers seekTo + play via PlayerContext, not via window.__kbPlayer.
- session store tracks replay count without affecting FSRS.
</verification>

<success_criteria>
1. Listening Drill exercise flow runs end-to-end in the Practice tab (after Plan 06 wiring lands).
2. User can click Replay arbitrarily many times with no penalty.
3. YouTube unavailable → fallback UI shown, no silent fill_lyric substitution.
4. Correct answer writes to `ex6_best_accuracy`; Star 3 unlocks at >= 0.80 (via Plan 01's `deriveStars`).
</success_criteria>

<output>
After completion, create `.planning/phases/10-advanced-exercises-full-mastery/10-04-SUMMARY.md` documenting:
- Generator branch details + timing constraint.
- Fallback copy verbatim (kept in sync with Phase 08.1-07 regression).
- How usePlayer().seekTo debouncing interacts with rapid Replay clicks.
</output>
