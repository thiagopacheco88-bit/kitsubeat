---
phase: 10-advanced-exercises-full-mastery
plan: 05
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - scripts/audit/verse-token-distribution.ts
  - src/lib/exercises/generator.ts
  - src/app/songs/[slug]/components/SentenceOrderCard.tsx
  - src/app/songs/[slug]/components/ExerciseSession.tsx
  - src/stores/exerciseSession.ts
  - src/lib/exercises/__tests__/sentence-order.test.ts
autonomous: true
requirements: [EXER-07]

must_haves:
  truths:
    - "Sentence Order generator produces one question per verse whose token count is <= 12 (CONTEXT-locked cap)"
    - "Verses with > 12 tokens are excluded from Sentence Order for that song (not the whole song)"
    - "An audit script reports per-song verse-length distribution against the 12-token cap"
    - "Correct answer is the verse's original token.surface concatenation"
    - "SentenceOrderCard renders a shuffled pool + empty answer row; tapping a pool token moves it to the answer row (in order); tapping an answer token returns it to the pool"
    - "Each token has a UUID generated at shuffle time, never an index that would leak correct position"
    - "Translation hint is hidden by default; 'Show hint' reveal triggers revealedReading=true → FSRS rating=1 (reveal-hatch pattern)"
    - "Scoring is all-or-nothing: concatenated answer surfaces must equal correctAnswer exactly"
    - "FeedbackPanel indicates which positions were wrong (learning signal)"
    - "No data-position or data-correct-index attribute appears in the DOM (Pitfall 1)"
  artifacts:
    - path: "scripts/audit/verse-token-distribution.ts"
      provides: "Per-song histogram: verses <=12 tokens vs total verses"
      min_lines: 30
    - path: "src/lib/exercises/generator.ts"
      provides: "sentence_order branch + per-verse 12-token filter"
      contains: "sentence_order"
    - path: "src/app/songs/[slug]/components/SentenceOrderCard.tsx"
      provides: "Tap-to-build UI with pool + answer row + hint toggle + wrong-position highlighting"
      min_lines: 120
    - path: "src/stores/exerciseSession.ts"
      provides: "sentenceOrderPool, sentenceOrderAnswer, sentenceOrderHintShown slices"
      contains: "sentenceOrderPool"
  key_links:
    - from: "src/app/songs/[slug]/components/SentenceOrderCard.tsx"
      to: "src/stores/exerciseSession.ts"
      via: "pool/answer slices keyed by question.id persisted via zustand persist middleware"
      pattern: "sentenceOrderPool|sentenceOrderAnswer"
    - from: "src/app/songs/[slug]/components/SentenceOrderCard.tsx"
      to: "src/lib/fsrs/rating.ts::ratingFor"
      via: "reveal-hatch boolean propagates through onAnswer payload to recordVocabAnswer"
      pattern: "revealedReading|sentenceOrderHintShown"
    - from: "src/app/songs/[slug]/components/ExerciseSession.tsx"
      to: "src/app/songs/[slug]/components/SentenceOrderCard.tsx"
      via: "question.type === 'sentence_order' dispatch"
      pattern: "SentenceOrderCard"
---

<objective>
Implement the Sentence Order exercise (EXER-07) end to end: run a one-shot verse-token-distribution audit to validate the 12-token cap against the live catalog, extend `makeQuestion` with a `sentence_order` branch (12-token filter, per-verse basis), build `SentenceOrderCard` with tap-to-build pool/answer UI (mobile-first, no dnd library), extend the session store with pool/answer/hint slices, wire the reveal-hatch `"Show hint"` to FSRS rating=1, and wire ExerciseSession dispatch.

Purpose: CONTEXT locked — tap-to-build not drag-and-drop, 12-token cap, hint drops FSRS to 1 (reveal-hatch per Phase 08.2-01), all-or-nothing scoring, FeedbackPanel highlights wrong positions. Sentence Order contributes to the bonus badge (STAR-06) alongside Grammar Conjugation but does NOT gate stars.
Output: Working Sentence Order exercise, with audit-validated cap, session-persisted pool/answer state, and wrong-position feedback. No DOM attribute leaks correct order.
</objective>

<execution_context>
@C:/Users/Thiago/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Thiago/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-advanced-exercises-full-mastery/10-CONTEXT.md
@.planning/phases/10-advanced-exercises-full-mastery/10-RESEARCH.md
@.planning/phases/10-advanced-exercises-full-mastery/10-01-SUMMARY.md

# Prior decisions
@.planning/phases/08.2-fsrs-progressive-disclosure/08.2-01-SUMMARY.md
@.planning/phases/08-exercise-engine/08-03-SUMMARY.md

# Files
@src/lib/exercises/generator.ts
@src/stores/exerciseSession.ts
@src/app/songs/[slug]/components/ExerciseSession.tsx
@src/app/songs/[slug]/components/VerseBlock.tsx
@src/lib/fsrs/rating.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verse audit + generator branch + session-store slices</name>
  <files>
    scripts/audit/verse-token-distribution.ts
    src/lib/exercises/generator.ts
    src/stores/exerciseSession.ts
    src/lib/exercises/__tests__/sentence-order.test.ts
  </files>
  <action>
    1. **Audit script** `scripts/audit/verse-token-distribution.ts`:
       - Iterate all seeded `song_versions` with non-null `lesson`.
       - For each song, count verses with `tokens.length <= 12` and total verses.
       - Output: `song_id | slug | total_verses | eligible_verses | eligible_pct` sorted by eligible_pct asc.
       - Emit a summary: `N songs have >=3 eligible verses, M have 1-2, K have 0`.
       - Write to `.planning/phases/10-advanced-exercises-full-mastery/verse-token-distribution.md` and commit.
       - **Run the script.** If <80% of songs have >=3 eligible verses, document the finding in the artifact and note a follow-up for the deferred "clause-boundary tokenization" idea. The 12-cap is still locked per CONTEXT for v1 — audit is informational, not blocking.

    2. **Generator** `src/lib/exercises/generator.ts`:
       - **Replace the throw-stub body** in the existing `case "sentence_order":` branch (pre-stubbed by Plan 10-01). Do NOT add a new case — the slot already exists so wave-2 plans 03/04/05 don't collide. The `verseTokens` and `translation` Question fields are also pre-added by Plan 10-01; do NOT re-add them here, just populate them.
       - Body:
         - Skip verses where `verse.tokens.length > 12`.
         - `correctAnswer = verse.tokens.map(t => t.surface).join("")`.
         - Populate `verseTokens` with the full token array.
         - Populate `translation` if present on the verse (for the "Show hint" reveal).
         - Options field is not used for this type (4 options don't apply to tap-to-build). Keep options as empty array.
       - `buildQuestions`: for sentence_order mode, iterate all verses with ≤12 tokens. If a song has zero eligible verses, emit zero sentence_order questions (skip cleanly).

    3. **Session store slices** `src/stores/exerciseSession.ts`:
       - Add slices keyed by question.id (survive across questions in a session; reset on `startSession`):
         - `sentenceOrderPool: Record<string, Array<{uuid: string; surface: string}>>`
         - `sentenceOrderAnswer: Record<string, Array<{uuid: string; surface: string}>>`
         - `sentenceOrderHintShown: Record<string, true>` (presence = shown; maps to revealedReading=true for FSRS per Phase 08.2-01)
       - Actions: `initSentenceOrder(questionId, shuffledTokens)`, `moveToAnswer(questionId, uuid)`, `moveToPool(questionId, uuid)`, `showHint(questionId)`.
       - `initSentenceOrder` uses `crypto.randomUUID()` for each token's uuid and applies Fisher-Yates shuffle (existing helper in `generator.ts` per 08-02 decision).
       - Zustand `persist` middleware already covers these fields.

    4. **Unit tests** `src/lib/exercises/__tests__/sentence-order.test.ts`:
       - A lesson with mixed verse lengths (5, 10, 14, 20 tokens) → 2 sentence_order questions emitted (for 5 and 10 token verses; 14 and 20 excluded).
       - A lesson with all verses > 12 tokens → zero questions.
       - Each question's `correctAnswer` equals the verse's original concatenation.
       - Session store: `initSentenceOrder` + `moveToAnswer` twice + `moveToPool` → answer length 1, pool has both original tokens minus 1 (the still-in-answer), UUIDs preserved.

    **Avoid:** Storing the correct index on any token (Pitfall 1 — leak). Emitting questions for verses > 12 tokens. Using `Math.random()` raw — use the existing Fisher-Yates helper so Phase 08.1-02's determinism assertions still hold.
  </action>
  <verify>
    - `npm run audit:verse-tokens` (or `tsx scripts/audit/verse-token-distribution.ts`) runs, emits histogram, writes the markdown artifact.
    - `npm run test:unit` sentence-order tests green.
    - `npm run build` green — ExerciseType exhaustive switch satisfied with 7 branches.
  </verify>
  <done>
    Audit artifact committed. Generator emits ≤12-token sentence_order questions. Session store holds pool/answer/hint slices. Tests cover cap boundary and hint reveal.
  </done>
</task>

<task type="auto">
  <name>Task 2: SentenceOrderCard + reveal-hatch + wrong-position feedback + dispatch</name>
  <files>
    src/app/songs/[slug]/components/SentenceOrderCard.tsx
    src/app/songs/[slug]/components/ExerciseSession.tsx
  </files>
  <action>
    1. **SentenceOrderCard** `src/app/songs/[slug]/components/SentenceOrderCard.tsx`:
       - Props: `{ question: Question; onAnswer: (answer: string, meta?: { revealedReading?: boolean }) => void; disabled?: boolean }`.
       - On mount, if the session store has no entry for `question.id`, call `initSentenceOrder(question.id, question.verseTokens)` (shuffles pool).
       - Subscribe to `sentenceOrderPool[question.id]` and `sentenceOrderAnswer[question.id]` via zustand selectors.
       - Render two rows:
         - **Answer row** (top): tokens in order the user has tapped; tap to return to pool.
         - **Pool row** (below): tokens remaining; tap to append to answer row.
       - Each token rendered as `<button data-token-uuid={t.uuid}>{t.surface}</button>` — NO data-position, NO data-correct-index. Styling follows existing QuestionCard option button patterns for consistency.
       - "Show hint" toggle: button that, on click, calls `showHint(question.id)` → renders `question.translation` below. Once shown, UI does not hide it back.
       - "Submit" button: enabled only when pool is empty (all tokens placed). On click:
         - `const answerStr = answer.map(t => t.surface).join("")`
         - `const revealedReading = sentenceOrderHintShown[question.id] === true`
         - `onAnswer(answerStr, { revealedReading })`
       - **Wrong-position feedback** after answer: FeedbackPanel receives the question + user answer. For sentence_order specifically, SentenceOrderCard exposes (via a ref or callback prop) the per-position correctness so FeedbackPanel can highlight wrong tokens. Simplest: the card itself renders a small "Incorrect positions" strip below the feedback header when `question.correctAnswer !== answerStr`, highlighting each mismatching answer-row token in red (compute from the zustand answer[] vs verseTokens order). OR: pass `correctOrder: Token[]` + `userAnswer: Token[]` to FeedbackPanel via props — add a new prop and render inside FeedbackPanel. **Choose whichever minimizes FeedbackPanel coupling** — recommend in-card feedback strip that renders after answer is submitted, keeping FeedbackPanel generic.
       - Component length: 120-200 lines. Extract the shared button styling to a local helper if it bloats.

    2. **FSRS reveal-hatch wiring** — the `onAnswer(answer, { revealedReading })` meta propagates up through ExerciseSession to `recordVocabAnswer`. Verify the existing `ratingFor()` in Phase 08.2-01 handles `revealedReading === true → rating = 1` for the `sentence_order` type. If `ratingFor` switch is not yet exhaustive on the new type (Plan 01 added the weight but not the switch), add a case: when `revealedReading`, return 1; else return `RATING_WEIGHTS.sentence_order` if correct else 1 (same semantics as fill_lyric).

    3. **ExerciseSession dispatch** `src/app/songs/[slug]/components/ExerciseSession.tsx`:
       - **Replace the throw-stub body** in the existing `if (q.type === "sentence_order")` branch (pre-stubbed by Plan 10-01) with the real dispatch:
         ```tsx
         if (q.type === "sentence_order") return <SentenceOrderCard {...commonProps} />;
         ```
       - Do NOT add a new conditional — the slot already exists so wave-2 plans 03/04/05 don't collide on this file. Plans 03 and 04 independently replace their own stub bodies.
       - Ensure `onAnswer` handler at the session level accepts the optional `meta` arg and threads it to `recordVocabAnswer`.
       - For the other 6 types, `meta` remains undefined — backwards compatible.

    4. **DOM audit** — add a small guard test (or manual grep) to confirm no `data-correct` or `data-position` attribute in SentenceOrderCard's rendered DOM. The existing Phase 08.1-06 regression test infrastructure is the right place, but a simple grep check in this plan's tests suffices:
       ```ts
       // unit test: render SentenceOrderCard, query for [data-correct], [data-position] → expect 0
       ```

    **Avoid:** Using indexes from `question.verseTokens` as React keys for answer row (Pitfall 1 — leak). Storing the unshuffled order in `sentenceOrderPool`. Making the "Show hint" toggle bidirectional (CONTEXT says it's a one-way reveal-hatch).
  </action>
  <verify>
    - `npm run build` green; ExerciseType 7-branch switch exhaustive.
    - `npm run test:unit` new SentenceOrderCard rendering test green (includes the no-leak DOM check).
    - `npm run test:e2e` existing exercise flows still green.
  </verify>
  <done>
    SentenceOrderCard renders tap-to-build UI. Show hint sets revealedReading=true. Submit only enabled when pool empty. No correct-order leak in DOM. ExerciseSession dispatches sentence_order to SentenceOrderCard. FSRS rating collapses to 1 on reveal.
  </done>
</task>

</tasks>

<verification>
- Verse-token audit artifact committed.
- Sentence Order questions never produced for verses > 12 tokens.
- No `data-position` or `data-correct-index` attribute in SentenceOrderCard DOM.
- Show hint → revealedReading=true → FSRS rating=1 (reuses Phase 08.2-01 reveal-hatch).
- Wrong positions are visually highlighted in feedback.
</verification>

<success_criteria>
1. User opens Practice tab on a song with eligible verses and completes a Sentence Order question by tapping tokens into order.
2. User can tap an answer-row token to return it to the pool.
3. Show hint reveals translation and caps FSRS rating at 1 for that question.
4. All-or-nothing scoring: any token wrong → whole question scored incorrect; but feedback shows which positions were wrong.
5. Songs with no ≤12-token verses produce zero Sentence Order questions (skip cleanly).
</success_criteria>

<output>
After completion, create `.planning/phases/10-advanced-exercises-full-mastery/10-05-SUMMARY.md` documenting:
- Verse-token audit summary stats (% songs with >=3 eligible verses).
- UUID-keyed token shuffle strategy (no position leak).
- Reveal-hatch flow from SentenceOrderCard → ExerciseSession → recordVocabAnswer → ratingFor.
</output>
