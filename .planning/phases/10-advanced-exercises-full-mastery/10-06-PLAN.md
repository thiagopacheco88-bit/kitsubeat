---
phase: 10-advanced-exercises-full-mastery
plan: 06
type: execute
wave: 3
depends_on: ["10-01", "10-03", "10-04", "10-05"]
files_modified:
  - src/app/songs/[slug]/components/ExerciseTab.tsx
  - src/app/songs/[slug]/components/AdvancedDrillsUpsellModal.tsx
  - src/app/actions/exercises.ts
  - tests/e2e/regression-premium-gate.spec.ts
  - tests/e2e/advanced-drill-quota.spec.ts
autonomous: true
requirements: [FREE-05, STAR-04, STAR-06]

must_haves:
  truths:
    - "ExerciseTab renders a third 'Advanced Drills' mode card alongside the existing Short/Full modes"
    - "Tab-open (mode-card click) pre-checks checkExerciseAccess for each target exercise type with { songVersionId }"
    - "11th distinct song's Listening Drill tab-open triggers the upsell modal before the session starts"
    - "4th distinct song's Grammar Conjugation or Sentence Order tab-open triggers the upsell modal before the session starts"
    - "Counter increments ONLY on first answer of first question (saveSessionResults / recordVocabAnswer path), not on tab-open"
    - "recordSongAttempt uses ON CONFLICT DO NOTHING — idempotent under parallel devices"
    - "saveSessionResults persists ex5/ex6/ex7 best_accuracy via GREATEST so stars never regress"
    - "Server-side gate is load-bearing: saveSessionResults and recordVocabAnswer call checkExerciseAccess and reject quota-exhausted attempts (with a documented post-answer fallback modal)"
    - "The Phase 08.1-07 test.fixme on regression-premium-gate.spec.ts is unfixed and the test passes"
    - "UI never imports EXERCISE_FEATURE_FLAGS (Phase 08.1-07 contract preserved)"
  artifacts:
    - path: "src/app/songs/[slug]/components/ExerciseTab.tsx"
      provides: "Advanced Drills mode card + pre-session access check + upsell trigger"
      contains: "Advanced Drills"
    - path: "src/app/songs/[slug]/components/AdvancedDrillsUpsellModal.tsx"
      provides: "Full-screen / modal upsell UI at tab-open with quota copy"
      min_lines: 60
    - path: "src/app/actions/exercises.ts"
      provides: "saveSessionResults extended for ex5/6/7 + counter increment + server-side quota re-check"
      contains: "ex6_best_accuracy"
    - path: "tests/e2e/regression-premium-gate.spec.ts"
      provides: "Unfixed Phase 08.1-07 test.fixme + new passing assertions"
    - path: "tests/e2e/advanced-drill-quota.spec.ts"
      provides: "E2E: 10 listening songs then 11th locked; 3 advanced-drill songs then 4th locked"
  key_links:
    - from: "src/app/songs/[slug]/components/ExerciseTab.tsx"
      to: "src/lib/exercises/access.ts::checkExerciseAccess"
      via: "server action called with { songVersionId } at mode-card click"
      pattern: "checkExerciseAccess.*songVersionId"
    - from: "src/app/actions/exercises.ts::saveSessionResults"
      to: "src/lib/exercises/counters.ts::recordSongAttempt"
      via: "increment on first answer of first question only"
      pattern: "recordSongAttempt"
    - from: "src/app/actions/exercises.ts::saveSessionResults"
      to: "user_song_progress ex5/ex6/ex7 columns"
      via: "GREATEST upsert on the three new accuracy columns"
      pattern: "ex6_best_accuracy"
---

<objective>
Wire Plans 03/04/05's exercise cards into the Practice tab as a third "Advanced Drills" mode, land the tab-open premium-gate UX (full-screen modal upsell), implement counter increment on first-answer (not on tab-open), extend `saveSessionResults` to persist ex5/ex6/ex7 best accuracy, and unfix the Phase 08.1-07 `regression-premium-gate.spec.ts` test.fixme so it passes live.

Purpose: This is the integration plan. Plans 03/04/05 deliver the cards and generators; this plan makes the Practice tab actually invoke them, enforces the 10-listening / 3-advanced-drill quotas at the data layer, and persists mastery so Plan 07 can surface Star 3 and the bonus badge.
Output: End-to-end Advanced Drills mode with quota gating, counter persistence, and passing regression E2E.
</objective>

<execution_context>
@C:/Users/Thiago/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Thiago/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-advanced-exercises-full-mastery/10-CONTEXT.md
@.planning/phases/10-advanced-exercises-full-mastery/10-RESEARCH.md
@.planning/phases/10-advanced-exercises-full-mastery/10-01-SUMMARY.md
@.planning/phases/10-advanced-exercises-full-mastery/10-03-SUMMARY.md
@.planning/phases/10-advanced-exercises-full-mastery/10-04-SUMMARY.md
@.planning/phases/10-advanced-exercises-full-mastery/10-05-SUMMARY.md

# Prior decisions this plan finalizes
@.planning/phases/08.1-end-to-end-qa-suite/08.1-07-SUMMARY.md
@.planning/phases/08-exercise-engine/08-04-SUMMARY.md

# Files
@src/app/songs/[slug]/components/ExerciseTab.tsx
@src/app/actions/exercises.ts
@src/lib/exercises/access.ts
@src/lib/exercises/counters.ts
@tests/e2e/regression-premium-gate.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExerciseTab Advanced Drills mode + upsell modal + tab-open gate</name>
  <files>
    src/app/songs/[slug]/components/ExerciseTab.tsx
    src/app/songs/[slug]/components/AdvancedDrillsUpsellModal.tsx
  </files>
  <action>
    1. **AdvancedDrillsUpsellModal** `src/app/songs/[slug]/components/AdvancedDrillsUpsellModal.tsx`:
       - Full-screen or large modal (follow existing modal styling in the codebase — check for a shared `Modal` component first; if none, render as an inline full-screen overlay using Tailwind classes matching existing confetti/session-summary styles).
       - Props: `{ family: "listening" | "advanced_drill"; quotaUsed: number; quotaLimit: number; onClose: () => void; onUpgrade: () => void }`.
       - Copy (exact, commit-as-locked):
         - Heading: "You've used your free [Listening Drill | Advanced Drill] songs"
         - Body: "Free users get {quotaLimit} songs of [Listening Drill | Grammar Conjugation + Sentence Order] practice. Upgrade to Premium for unlimited access."
         - CTA: "Upgrade to Premium" (onUpgrade) + "Not now" (onClose).
       - `family === "listening"` → quotaLimit=10, wording "Listening Drill"; family === "advanced_drill" → quotaLimit=3, wording "Grammar Conjugation + Sentence Order".
       - Component length: 60-100 lines.

    2. **ExerciseTab** `src/app/songs/[slug]/components/ExerciseTab.tsx`:
       - Add a third mode card: "Advanced Drills". Click handler is an async function that:
         a. Calls `checkExerciseAccess(userId, "listening_drill", { songVersionId })` AND `checkExerciseAccess(userId, "grammar_conjugation", { songVersionId })` in parallel (via Promise.all).
         b. If EITHER returns `allowed: false, reason: "quota_exhausted"` → set state to show the upsell modal for the corresponding family. Do not start the session.
         c. If both allowed → proceed to the normal session-start path, but build questions with the Advanced Drills question type list: `["grammar_conjugation", "listening_drill", "sentence_order"]` passed into `buildQuestions`.
       - Pass the `songVersionId` through to checkExerciseAccess — it's already available via the SongContent context / props chain; thread it explicitly.
       - Keep Short / Full mode cards unchanged — they do NOT gate (existing Phase 8 types stay free per EXERCISE_FEATURE_FLAGS).
       - Handle the "YouTube unavailable" pre-check: if `PlayerContext.embedState === "error"` at mode-card click, show a small warning toast "Listening Drill will be unavailable for this song" but still proceed (so the user can still do Grammar Conjugation + Sentence Order). ListeningDrillCard's own fallback (Plan 04) handles the in-session case.
       - Ensure the existing cap-filter + JLPT-pool fetch in ExerciseTab (from Phase 08.4-04) still runs; just parallelize the new access checks.

    3. **UI invariant** — `ExerciseTab` MUST call `checkExerciseAccess` via a server action (either an existing route or a new one). **Do NOT import `EXERCISE_FEATURE_FLAGS` or `checkExerciseAccess` directly in the UI** — that violates Phase 08.1-07's regression contract. Add a thin server action `getAdvancedDrillAccess(songVersionId): Promise<{ listeningAllowed, advancedAllowed, listeningQuotaRemaining, advancedQuotaRemaining }>` in `src/app/actions/exercises.ts` (Task 2) that wraps the gate calls. ExerciseTab calls this server action.

    **Avoid:** Importing feature-flags.ts or access.ts from the UI file directly — route through server actions. Hiding the Advanced Drills card for free users — CONTEXT locked: always render the card; the gate decides whether the session starts.
  </action>
  <verify>
    - `npm run build` green.
    - `grep "EXERCISE_FEATURE_FLAGS\|import.*access.ts" src/app/songs/\[slug\]/components/ExerciseTab.tsx` returns zero (no direct UI import).
    - `npm run test:e2e` — existing ExerciseTab E2E tests (Short/Full modes) still green.
  </verify>
  <done>
    ExerciseTab has 3 mode cards. Advanced Drills click triggers server-action gate check. Upsell modal renders at quota exhaustion before session starts. UI never imports feature-flags.
  </done>
</task>

<task type="auto">
  <name>Task 2: Server-side quota enforcement + saveSessionResults extension + counter increment</name>
  <files>
    src/app/actions/exercises.ts
  </files>
  <action>
    1. **New server action** `getAdvancedDrillAccess(songVersionId): Promise<AccessSummary>`:
       - Reads `userId` from the existing auth/placeholder pattern (Phase 08.4 uses PLACEHOLDER_USER_ID — reuse the same single source).
       - Calls `checkExerciseAccess(userId, "listening_drill", { songVersionId })` + `checkExerciseAccess(userId, "grammar_conjugation", { songVersionId })` (grammar_conjugation + sentence_order share the family, so checking one is sufficient for the advanced_drill family).
       - Returns `{ listeningAllowed, advancedAllowed, listeningQuotaRemaining, advancedQuotaRemaining }`.
       - Server action exported with `"use server"` directive.

    2. **saveSessionResults extension**:
       - Fetch the existing progress row per Phase 08-04's pattern (SELECT before UPDATE to capture previousStars).
       - When session results include new exercise types, derive each's accuracy:
         - ex5_accuracy = `countCorrect(grammar_conjugation) / total(grammar_conjugation)` — only compute if type present.
         - ex6_accuracy = same for listening_drill.
         - ex7_accuracy = same for sentence_order.
       - Upsert using GREATEST so mastery never regresses:
         ```ts
         ex5_best_accuracy: sql`GREATEST(COALESCE(${userSongProgress.ex5_best_accuracy}, 0), ${ex5_accuracy})`,
         ex6_best_accuracy: sql`GREATEST(...)`,
         ex7_best_accuracy: sql`GREATEST(...)`,
         ```
       - Compute `previousStars` using the BEFORE-upsert snapshot (including ex6) so Plan 07's Star 3 confetti fires correctly on first breach of the 3-star threshold.

    3. **Counter increment on first answer** — either in `saveSessionResults` (post-session) or in `recordVocabAnswer` (per-answer). Recommendation: inside `recordVocabAnswer`, after successfully recording the first answer of the session for one of the 3 new types, call `recordSongAttempt(userId, QUOTA_FAMILY[exerciseType], songVersionId)`. ON CONFLICT DO NOTHING makes it idempotent even if called multiple times. This matches CONTEXT: "Counter increments on first attempt per song."
       - Alternatively do it in `saveSessionResults` at end-of-session — simpler but loses the "tab-open did not yet count" invariant cleanly if the session is abandoned mid-flight. RESEARCH.md Pitfall 5 explicitly calls out "first answer" not "tab-open" — so per-answer is safer. Implement there.

    4. **Server-side re-check at write time** — after incrementing, re-count rows for `(userId, family)`. If count > limit AND user is not premium → refund the increment via `DELETE FROM user_exercise_song_counters WHERE user_id=? AND family=? AND song_version_id=?` and throw a `QuotaExhaustedError`. The action returns `{ ok: false, reason: "quota_exhausted" }` to the caller. UI behavior: show the upsell modal post-answer (one-time slippage allowed — RESEARCH.md Pitfall 6 documents this trade-off).
       - Wrap the counter insert + re-count in a db.transaction() so the refund is atomic with the check.

    **Avoid:** Incrementing the counter on tab-open (Pitfall 5). Doing a non-atomic check-then-insert (Pitfall 6). Storing `stars` directly — derivation is read-time only.
  </action>
  <verify>
    - `npm run test:integration` exercises: 10 recordSongAttempt calls for same (user, listening) → COUNT = 10; 11th call with new songVersionId + non-premium → gate refuses.
    - `npm run test:unit` existing `saveSessionResults` tests still green (extended with ex5/6/7 coverage).
    - `grep "recordSongAttempt" src/app/actions/exercises.ts` confirms wiring.
  </verify>
  <done>
    saveSessionResults persists ex5/6/7 accuracy. recordVocabAnswer increments counter on first attempt. Quota exhaustion at write time refunds the insert and returns a structured error. Server action getAdvancedDrillAccess wraps the gate for UI consumption.
  </done>
</task>

<task type="auto">
  <name>Task 3: Unfix Phase 08.1-07 premium-gate regression + new advanced-drill quota E2E</name>
  <files>
    tests/e2e/regression-premium-gate.spec.ts
    tests/e2e/advanced-drill-quota.spec.ts
  </files>
  <action>
    1. **Unfix** `tests/e2e/regression-premium-gate.spec.ts`:
       - Remove the `test.fixme(...)` guard (see Phase 08.1-07 SUMMARY: "Premium-gate test.fixme is intentional Phase 10 follow-up").
       - Ensure the test asserts: when EXERCISE_FEATURE_FLAGS has a "premium" or "song_quota" exercise + non-premium user + exhausted quota → saveSessionResults rejects the write. It can additionally assert that checkExerciseAccess is the gate being called (not a UI-side flag check).
       - If the test was written with stale assumptions that reference the old free-for-all, update assertions to match the new song_quota semantics.
       - Keep the UI-imports-feature-flags regression assertion intact (grep the project for EXERCISE_FEATURE_FLAGS imports under `src/app` and `src/stores` — expect zero).

    2. **New E2E** `tests/e2e/advanced-drill-quota.spec.ts`:
       - Seeded test user + 11 seeded song_versions (the existing 3 seeds + 8 additional — or reuse existing seeds with the counter rotated via direct DB insert; fastest path: seed `user_exercise_song_counters` with 10 entries for family=listening, then open the 11th song's Advanced Drills).
       - Assertion 1 (listening quota): open the 11th song's Practice tab → click Advanced Drills mode → upsell modal appears with "10 songs" copy; session does not start.
       - Assertion 2 (advanced drill quota): seed 3 advanced_drill counter entries → open 4th song's Advanced Drills → upsell modal with "3 songs" copy.
       - Assertion 3 (independent counters): exhausted listening (10) does NOT block advanced_drill access on a new song; quota remaining = 3 - X.
       - Assertion 4 (premium bypass): set the user as premium → both gates allow unconditionally.
       - Use the existing Playwright config and TEST_DATABASE_URL seam from Phase 08.1.
       - Skip gracefully if TEST_DATABASE_URL unset (existing describe.skip pattern).

    **Avoid:** Hard-coding UUIDs — use seeded slug lookups. Flakiness via retries — Phase 08.1-01 locked `retries: 0`. Testing UI-flag imports in this spec — that already lives in regression-single-gate.test.ts per Phase 08.1-07.
  </action>
  <verify>
    - `npm run test:e2e` — regression-premium-gate passes (no test.fixme).
    - `npm run test:e2e -- advanced-drill-quota.spec.ts` — all 4 assertions pass.
    - `npm run test:measure` — 15-min budget still met (add a `@slow` tag if the new spec pushes the budget).
  </verify>
  <done>
    test.fixme removed from regression-premium-gate. New quota E2E covers 11th-song-listening, 4th-song-advanced-drill, independent counters, premium bypass.
  </done>
</task>

</tasks>

<verification>
- Advanced Drills mode card visible in ExerciseTab.
- 11th distinct song's Listening Drill tab-open → upsell modal (no session start, no counter increment).
- 4th distinct song's Advanced Drills tab-open → upsell modal.
- Counter increments on first answer, idempotent on repeat.
- saveSessionResults writes ex5/6/7 best accuracy via GREATEST.
- Phase 08.1-07 test.fixme removed; regression test passes.
- UI code does not import EXERCISE_FEATURE_FLAGS (grep check).
</verification>

<success_criteria>
1. Free user completes 10 Listening Drill songs; 11th song's Advanced Drills tab-open shows upsell; quota for advanced_drill still allows 3 songs.
2. Free user completes 3 songs of Grammar Conjugation OR Sentence Order; 4th song's tab-open shows upsell.
3. Premium user can access unlimited.
4. Mastery persists: ex5/6/7 best accuracy columns reflect session results and never regress.
5. Phase 08.1-07 premium-gate regression is live-passing.
</success_criteria>

<output>
After completion, create `.planning/phases/10-advanced-exercises-full-mastery/10-06-SUMMARY.md` documenting:
- Exact tab-open flow (server-action name + response shape).
- Counter increment timing (per-answer, not per-tab-open) + idempotency guarantee.
- Test.fixme unfix confirmation and linked line numbers.
- Known trade-off: one answer of slippage possible under cross-device race (Pitfall 6) — documented in upsell copy.
</output>
