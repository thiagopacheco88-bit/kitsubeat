---
phase: 10-advanced-exercises-full-mastery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/songs/[slug]/components/PlayerContext.tsx
  - src/app/songs/[slug]/components/YouTubeEmbed.tsx
  - src/app/songs/[slug]/components/__tests__/PlayerContext.test.tsx
autonomous: true
requirements: [EXER-06]

must_haves:
  truths:
    - "PlayerContext exposes seekTo(ms: number), play(), pause(), and isReady: boolean as first-class API"
    - "YouTubeEmbed internalizes the raw YT.Player reference and never leaks it in production bundles"
    - "seekTo(ms) converts ms to seconds internally and calls the YT player's seekTo(sec, true)"
    - "Rapid replay clicks (debounce ~400ms) do not race the YT onStateChange listener"
    - "window.__kbPlayer test-only hook remains gated on NEXT_PUBLIC_APP_ENV === 'test' (unchanged)"
  artifacts:
    - path: "src/app/songs/[slug]/components/PlayerContext.tsx"
      provides: "seekTo/play/pause/isReady imperative API via React context"
      contains: "seekTo"
    - path: "src/app/songs/[slug]/components/YouTubeEmbed.tsx"
      provides: "Registers seek/play/pause callbacks on PlayerContext inside onReady"
      contains: "setSeekTo"
    - path: "src/app/songs/[slug]/components/__tests__/PlayerContext.test.tsx"
      provides: "Unit tests with mocked YT player verifying callback wiring"
  key_links:
    - from: "src/app/songs/[slug]/components/YouTubeEmbed.tsx"
      to: "src/app/songs/[slug]/components/PlayerContext.tsx"
      via: "onReady registers seek/play/pause; onDestroy clears"
      pattern: "setSeekTo|setPlay|setPause|setIsReady"
    - from: "src/app/songs/[slug]/components/ListeningDrillCard.tsx (Plan 04)"
      to: "src/app/songs/[slug]/components/PlayerContext.tsx"
      via: "usePlayer().seekTo + play"
      pattern: "usePlayer\\(\\)\\.(seekTo|play)"
---

<objective>
Promote the test-only `window.__kbPlayer` hook to a first-class, React-idiomatic imperative API on `PlayerContext`. Extend `PlayerContext` with `seekTo(ms)`, `play()`, `pause()`, and `isReady: boolean`. Have `YouTubeEmbed.onReady` register implementations that wrap the raw YT player reference (kept private inside the embed). This unblocks the Listening Drill card (Plan 04) needing imperative audio control, while keeping the YT player object out of production-facing app code.

Purpose: CONTEXT locked — "Full verse plays at normal speed via the existing YouTube iframe." Phase 08.1-05 locked — "Test-only instrumentation gated EXCLUSIVELY on NEXT_PUBLIC_APP_ENV === 'test'." Listening Drill needs production-grade imperative controls that don't leak the test hook.
Output: Extended PlayerContext + rewired YouTubeEmbed + unit tests verifying the context surface.
</objective>

<execution_context>
@C:/Users/Thiago/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Thiago/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-advanced-exercises-full-mastery/10-CONTEXT.md
@.planning/phases/10-advanced-exercises-full-mastery/10-RESEARCH.md

# Prior decisions this plan respects
@.planning/phases/08.1-end-to-end-qa-suite/08.1-05-SUMMARY.md
@.planning/phases/08.1-end-to-end-qa-suite/08.1-07-SUMMARY.md

# Files being modified
@src/app/songs/[slug]/components/PlayerContext.tsx
@src/app/songs/[slug]/components/YouTubeEmbed.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend PlayerContext with imperative API</name>
  <files>
    src/app/songs/[slug]/components/PlayerContext.tsx
  </files>
  <action>
    1. Widen the `PlayerContext` value type:
       ```ts
       interface PlayerContextValue {
         // existing
         currentTimeMs: number;
         embedState: "loading" | "ready" | "error";  // promote from YouTubeEmbed state
         // NEW
         seekTo: (ms: number) => void;
         play: () => void;
         pause: () => void;
         isReady: boolean;
       }
       ```
    2. Use `useRef<{seekTo: (ms:number)=>void; play: ()=>void; pause: ()=>void} | null>(null)` inside `PlayerProvider` for the imperative callback bundle. Expose stable `seekTo/play/pause` wrappers (useCallback) that dispatch through the ref (so consumers don't re-render when the callbacks rebind).
    3. Provide `setImperativeApi(api: { seekTo, play, pause } | null)` as a second context (or additional fields on the same context) that YouTubeEmbed calls inside `onReady` and `onDestroy`. The simplest shape: add `_register(api)` internal on context value; YouTubeEmbed calls `_register({...})` on onReady and `_register(null)` on onDestroy.
    4. `isReady` derives from `embedState === "ready"` AND api ref is non-null.
    5. The fallback `seekTo`/`play`/`pause` implementations when no-op (api ref null) log a dev-time warning and return silently — do not throw (the listening drill card must handle null gracefully).
    6. Debounce-guard the exposed `play()` and `seekTo()` by checking `isReady` first — consumers can rely on calls being safe to spam.

    **Avoid:** Exposing the raw YT.Player reference. Breaking the existing `currentTimeMs` + verse-sync consumers — those fields stay exactly as-is. Using `window.__kbPlayer` inside PlayerContext (that hook stays test-only and separate).
  </action>
  <verify>
    - `npm run build` succeeds.
    - `npm run test:unit` passes — existing player-sync tests (if any) still green.
    - `grep "YT.Player" src/app/songs/\[slug\]/components/PlayerContext.tsx` returns nothing (raw type does not leak into context).
  </verify>
  <done>
    PlayerContext exposes seekTo/play/pause/isReady. Old currentTimeMs + embedState surface unchanged. Calls before onReady are no-ops (safe).
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire YouTubeEmbed into the context + replay-safety debounce + test</name>
  <files>
    src/app/songs/[slug]/components/YouTubeEmbed.tsx
    src/app/songs/[slug]/components/__tests__/PlayerContext.test.tsx
  </files>
  <action>
    1. In `YouTubeEmbed.onReady` (line ~120 per RESEARCH.md reference), after the existing test-only `window.__kbPlayer` assignment, call `playerContext._register({ seekTo: (ms) => playerRef.current?.seekTo(ms/1000, true), play: () => playerRef.current?.playVideo(), pause: () => playerRef.current?.pauseVideo() })`.
    2. On component unmount (existing cleanup effect), call `playerContext._register(null)` to prevent stale refs.
    3. Add a small internal debouncer inside the exported imperative `play()` / `seekTo()` wrappers in PlayerContext (not inside YouTubeEmbed) — coalesce calls within a 400ms window using a ref-based timestamp guard, per RESEARCH.md Pitfall 2. A `seekTo(ms)` call within 400ms of a prior call cancels the pending one, applies the new ms, then calls `pauseVideo() → seekTo → 50ms delay → playVideo`. Simplest: expose a `seekAndPlay(ms)` convenience so ListeningDrillCard replay is one call; internals handle the pause/seek/delay/play dance.
    4. **Test** `src/app/songs/[slug]/components/__tests__/PlayerContext.test.tsx`:
       - Mount a `PlayerProvider` around a test consumer that calls `seekTo(5000)` and `play()`.
       - Simulate YouTubeEmbed registering a mock api (spies on seekTo/playVideo/pauseVideo).
       - Assert: seekTo(5000) → mock.seekTo called with (5, true). play() → mock.playVideo called.
       - Before registration: calls are no-ops (do not throw).
       - Rapid 10 seekTo calls within 400ms coalesce — mock.seekTo called <= 2 times (or exactly 1 if trailing edge) — assert upper bound only to avoid over-specifying debounce impl.

    **Avoid:** Exposing `window.__kbPlayer` to production code paths. Removing or weakening the existing `NEXT_PUBLIC_APP_ENV === 'test'` gate on `__kbPlayer` — phase 08.1 regression tests depend on that gate.
  </action>
  <verify>
    - `npm run test:unit` PlayerContext test passes.
    - `npm run test:e2e` Phase 08.1-05 player-sync specs still pass (regression — existing verse-sync path untouched).
    - `grep "__kbPlayer" src/app/songs/\[slug\]/components/YouTubeEmbed.tsx` shows the test-only gate still present (`if (process.env.NEXT_PUBLIC_APP_ENV === 'test')`).
  </verify>
  <done>
    YouTubeEmbed registers seek/play/pause callbacks on PlayerContext at onReady and clears them on unmount. Tests verify the callback wiring and debounce bound. Phase 08.1 e2e player-sync still green.
  </done>
</task>

</tasks>

<verification>
- Verse-sync (phase 08.1-05) regression unaffected — existing verse-sync E2E tests pass.
- ListeningDrillCard (Plan 04) will call `usePlayer().seekTo(verseStartMs); usePlayer().play()` without importing YouTubeEmbed internals.
- Test-only `__kbPlayer` hook remains; its env gate unchanged.
</verification>

<success_criteria>
1. `usePlayer().seekTo(ms)` works from any consumer inside the song page.
2. `usePlayer().play()` and `pause()` work imperatively.
3. Rapid replay spam (< 400ms apart) coalesces — no YT API race crashes.
4. `isReady` boolean reflects when the iframe is loaded AND registered.
5. Phase 08.1-05 E2E tests remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/10-advanced-exercises-full-mastery/10-02-SUMMARY.md` documenting:
- New PlayerContext surface (seekTo signature, debounce behavior).
- How YouTubeEmbed registers and cleans up.
- Confirmation that `__kbPlayer` test-only gate still present.
</output>
