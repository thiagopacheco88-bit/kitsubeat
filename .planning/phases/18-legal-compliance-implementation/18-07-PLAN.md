---
phase: 18-legal-compliance-implementation
plan: "07"
type: execute
wave: 5
depends_on: ["18-01", "18-02", "18-03", "18-04", "18-05", "18-06"]
files_modified:
  - tests/e2e/a11y.spec.ts
  - src/app/songs/[slug]/components/QuestionCard.tsx
  - src/app/songs/[slug]/components/SentenceOrderCard.tsx
  - docs/phase18-operator-checklist.md
autonomous: false
requirements:
  - REQ-A11Y-07
  - REQ-A11Y-08
  - REQ-A11Y-20
  - REQ-A11Y-26
  - REQ-A11Y-28
  - REQ-A11Y-32
  - REQ-A11Y-33
  - REQ-A11Y-34
  - REQ-A11Y-35
  - REQ-A11Y-48
  - REQ-MINORS-04
  - REQ-MINORS-05
  - REQ-MINORS-06

user_setup:
  - service: ICO Registration
    why: "REQ-PRIV-UK-POLICY-02 — ICO registration required before processing beta users' data"
    env_vars: []
    dashboard_config:
      - task: "Register at ico.org.uk/for-organisations/register/ — ~10 minutes, £40/year for micro-organisations"
        location: "https://ico.org.uk/for-organisations/register/"
      - task: "Insert ICO registration number into src/app/legal/privacy/page.tsx replacing placeholder [ZB000000]"
        location: "src/app/legal/privacy/page.tsx"
  - service: Email Setup
    why: "REQ-PRIV-UK-POLICY-03 — privacy@kitsubeat.com and support@kitsubeat.com must be reachable"
    env_vars: []
    dashboard_config:
      - task: "Create privacy@kitsubeat.com email alias in your email provider"
        location: "Your email provider admin dashboard"
      - task: "Create support@kitsubeat.com email alias"
        location: "Your email provider admin dashboard"

must_haves:
  truths:
    - "axe-core a11y scan (RUN_A11Y=1) passes at >=95 for all new legal/onboarding routes"
    - "QuestionCard exercise option buttons have role='list'/'listitem' pattern (not listbox/option)"
    - "SentenceOrderCard token buttons have single-pointer tap alternative (not drag-only)"
    - "a11y.spec.ts ROUTES includes all 6 new Phase 18 routes (added in Plan 01 Wave 0)"
    - "docs/phase18-operator-checklist.md exists with ICO/email/DPIA tasks"
    - "Human verified: legal pages render correctly at max-w-3xl in dark + light theme"
    - "Human verified: cookie banner does not flash on repeat visits"
  artifacts:
    - path: "tests/e2e/a11y.spec.ts"
      provides: "Extended a11y spec with Phase 18 routes (already done in Plan 01; verify here)"
      contains: "/legal/terms"
    - path: "docs/phase18-operator-checklist.md"
      provides: "Operator tasks: ICO registration, email setup, DPIA document checklist"
      min_lines: 30
  key_links:
    - from: "tests/e2e/a11y.spec.ts"
      to: "src/app/legal/terms/page.tsx"
      via: "ROUTES array includes /legal/terms — axe-core scans the rendered page"
      pattern: "/legal/terms"
---

<objective>
WCAG ARIA fixes on exercise components (QuestionCard + SentenceOrderCard), a11y scan verification for all Phase 18 routes, operator checklist document, and human-verify checkpoint.

Purpose: Phase 18 cannot ship without passing the a11y gate (RUN_A11Y=1 npm run test:e2e:a11y). This plan targets the two exercise components with known ARIA interaction contract issues (Pitfall 8 from RESEARCH.md) and confirms the full a11y sweep covers all new surfaces. The operator checklist documents the non-code tasks (ICO, email, DPIA) that must complete before Phase 19.
Output: 2 exercise component fixes + operator checklist + a11y scan passing + human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-legal-compliance-implementation/18-RESEARCH.md
@.planning/phases/18-legal-compliance-implementation/18-UI-SPEC.md
@.planning/phases/18-legal-compliance-implementation/18-PATTERNS.md
@.planning/phases/18-legal-compliance-implementation/18-06-SUMMARY.md

<interfaces>
<!-- WCAG ARIA option button fix (PATTERNS.md §WCAG fixes + RESEARCH Pitfall 8): -->
// Do NOT use role="listbox" + role="option" on exercise buttons — implicit keyboard contract breaks Enter/Space
// Use role="list" on container + role="listitem" on each li + button inside with aria-pressed:
```tsx
<ul role="list" aria-label="Answer choices">
  {choices.map((choice, idx) => (
    <li key={choice} role="listitem">
      <button
        type="button"
        aria-pressed={chosenAnswer === choice}
        style={{ minHeight: "44px" }}
        className="... focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--color-accent)]/40 ..."
        onClick={() => handleChoice(choice)}
      >
        {choice}
      </button>
    </li>
  ))}
</ul>
```

<!-- SentenceOrderCard single-pointer alternative (REQ-A11Y-33): -->
// The existing tap-to-move pattern (tap token → moves to answer row) IS the single-pointer alternative
// Verify no drag-only interaction exists; if drag is implemented, ensure tap works independently
// If tokens use drag events, ensure they also respond to click/tap

<!-- Focus ring (from Button.tsx): -->
"focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--color-accent)]/40"

<!-- Operator checklist document structure: -->
Create docs/phase18-operator-checklist.md with:
- ICO registration steps
- Email alias setup
- DPIA document template reference
- privacy@kitsubeat.com / support@kitsubeat.com setup
- Phase 19 prerequisite checklist
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WCAG ARIA fixes + operator checklist</name>
  <files>
    src/app/songs/[slug]/components/QuestionCard.tsx,
    src/app/songs/[slug]/components/SentenceOrderCard.tsx,
    docs/phase18-operator-checklist.md
  </files>
  <read_first>
    - src/app/songs/[slug]/components/QuestionCard.tsx (FULL FILE — find the exercise option button rendering; check if it currently uses role="listbox"/"option" or any problematic ARIA; check existing className patterns for focus rings)
    - src/app/songs/[slug]/components/SentenceOrderCard.tsx (FULL FILE — find token buttons; verify tap-to-move pattern exists; check if drag is the only interaction; check focus ring presence)
    - tests/e2e/a11y.spec.ts (confirm ROUTES already includes /legal/terms from Plan 01 additions)
  </read_first>
  <action>
    Edit src/app/songs/[slug]/components/QuestionCard.tsx:
    - Find the exercise option buttons rendering section
    - If options are currently in a div with no role: wrap in <ul role="list" aria-label="Answer choices"> with each option in <li role="listitem">
    - If options currently use role="listbox"/"option": replace with role="list"/"listitem" pattern
    - Add aria-pressed={chosenAnswer === choice} to each option button
    - Ensure each option button has focus-visible:ring-2 focus-visible:ring-[var(--color-accent)]/40 (copy from Button.tsx CVA base)
    - Ensure min-h-[44px] on option buttons (WCAG 2.5.5)
    - Do NOT change any existing click handlers, animation, or visual styling beyond ARIA attributes and focus ring

    Edit src/app/songs/[slug]/components/SentenceOrderCard.tsx:
    - Find the token buttons (the draggable/tappable sentence tokens)
    - Verify tap (click) works independently of drag — if tokens only have dragstart/dragging handlers but no onClick/onPointerUp, add click handler that calls the same move-to-answer function (REQ-A11Y-33 single-pointer alternative)
    - Add focus-visible ring to token buttons if not present
    - Add aria-pressed={tokenIsInAnswerRow} to each token button
    - Ensure min-h-[44px] on token buttons
    - Do NOT break the existing drag functionality if present

    Create docs/phase18-operator-checklist.md:
    ```markdown
    # Phase 18 — Operator Checklist

    Pre-Phase-19 launch tasks that require human action (not code).

    ## 1. ICO Registration (BLOCKING — must complete before Phase 19)

    Requirement: REQ-PRIV-UK-POLICY-02

    Steps:
    1. Go to: https://ico.org.uk/for-organisations/register/
    2. Select "Data Controller" registration
    3. Select micro-organisation fee tier (turnover < £632,000) — approximately £40/year
    4. Complete registration — you'll receive an ICO number (format: ZB123456) by email within minutes
    5. Open src/app/legal/privacy/page.tsx
    6. Replace `[ICO REGISTRATION NUMBER: ZB000000]` with your actual number
    7. Commit + deploy

    ## 2. Email Aliases (BLOCKING — must complete before Phase 19)

    Requirement: REQ-PRIV-UK-POLICY-03, REQ-MINORS-29

    - Create privacy@kitsubeat.com (for GDPR/LGPD SAR and data protection enquiries)
    - Create support@kitsubeat.com (for general support + under-13 age error contact)
    - Send a test email to each alias; confirm receipt
    - Update RESEND_API_KEY in Vercel if not already set (required for birthday-transitions cron email)

    ## 3. DPIA Document (BLOCKING — must complete before Phase 19)

    Requirement: REQ-MINORS-04, REQ-MINORS-05

    Complete a Data Protection Impact Assessment covering:
    - Processing of children's (13-17) personal data
    - LGPD Art. 14 assessment for Brazilian minor users (REQ-MINORS-BR-02)
    - Risk assessment for FSRS behavioral scheduling on minor accounts
    - Proposed mitigations (minor defaults, 18th-birthday transition, no behavioral advertising)

    Save as: .planning/legal/DPIA.md (not committed to public repo — gitignore if needed)

    ## 4. Supabase + Vercel IDTA Verification (before Phase 19)

    Requirement: REQ-PRIV-UK-XFER-01, REQ-PRIV-UK-XFER-02

    - Verify Supabase has a UK IDTA addendum: https://supabase.com/docs/guides/platform/shared-responsibility-model
    - Verify Vercel has a UK IDTA or SCCs addendum for UK data processing
    - If neither has IDTA: execute Standard Contractual Clauses manually before Phase 19 beta opens

    ## 5. Terms & Conditions Effective Date

    Before Phase 19 launch:
    - Set TERMS_EFFECTIVE_DATE in src/lib/legal/versions.ts to the Phase 19 launch date
    - Format: "YYYY-MM-DD" (e.g., "2026-06-01")

    ## 6. CRON_SECRET Environment Variable

    - Verify CRON_SECRET is set in Vercel environment variables
    - The birthday-transitions cron (and existing daily-reminder + weekly-recap) requires this secret
    - Set in Vercel Dashboard → Settings → Environment Variables

    ## Checklist Summary

    - [ ] ICO registration number obtained and inserted in privacy page
    - [ ] privacy@kitsubeat.com alias live and tested
    - [ ] support@kitsubeat.com alias live and tested
    - [ ] DPIA document drafted and saved to .planning/legal/DPIA.md
    - [ ] Supabase + Vercel IDTA/SCCs verified
    - [ ] TERMS_EFFECTIVE_DATE set to Phase 19 launch date
    - [ ] CRON_SECRET set in Vercel
    - [ ] RESEND_API_KEY set in Vercel
    ```

    Run: npx tsc --noEmit && npm run test:unit
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test:unit 2>&1 | tail -10</automated>
  </verify>
  <done>tsc exits 0; test:unit exits 0; grep "role=\"list\"" src/app/songs/[slug]/components/QuestionCard.tsx returns match; grep "aria-pressed" src/app/songs/[slug]/components/QuestionCard.tsx returns match; docs/phase18-operator-checklist.md exists with ICO section.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — a11y sweep + legal page visual check + cookie banner behavior</name>
  <what-built>
    All Phase 18 code is shipped:
    - Migration 0019 applied (Wave 0)
    - Server actions: recordConsent, completeOnboarding, DSAR endpoint, birthday cron (Wave 1)
    - 5 legal pages at /legal/* (Wave 2)
    - Age-gate onboarding page + middleware terms check (Wave 3)
    - CookieConsentBanner wired in RootLayout + AiBadge on FeedbackPanel/LearnCard + ProfileNudgeBanner + DataExportButton (Wave 4)
    - Integration tests green + profile page wiring + a11y ARIA fixes (Wave 5)
  </what-built>
  <how-to-verify>
    Start the dev server: npm run dev (http://localhost:7000)

    Step 1 — Cookie banner behavior:
    1. Open a fresh private/incognito window (no kb_consent cookie)
    2. Visit http://localhost:7000 — confirm the cookie consent banner appears at the bottom
    3. Confirm the "Reject non-essential" button receives focus automatically
    4. Click "Accept all cookies" — confirm banner disappears
    5. Reload the page — confirm banner does NOT reappear (kb_consent cookie set)
    6. Open DevTools → Application → Cookies → confirm kb_consent=granted cookie exists

    Step 2 — Legal pages visual check:
    1. Visit http://localhost:7000/legal/terms — confirm: H1 "Terms & Conditions", version line, HR separator, H2 sections, legal nav footer, no horizontal scroll
    2. Visit http://localhost:7000/legal/privacy — confirm: H1 "Privacy Policy", Portuguese LGPD section visible with "Para usuários brasileiros" heading
    3. Visit http://localhost:7000/legal/refund — confirm: prominent deferred callout visible before any section content
    4. Toggle dark/light theme — confirm all 3 pages render correctly in both themes
    5. Confirm legal nav footer links work (click each link)

    Step 3 — Age-gate page:
    1. Visit http://localhost:7000/onboarding/age-gate
    2. Confirm: H1 "Complete your profile", DOB input, terms checkbox with links, "Continue to KitsuBeat" button
    3. Enter a DOB that makes you < 13 years old — confirm "Age requirement not met" message appears
    4. Enter a DOB making you 15 — confirm minor awareness section appears with 3 bullet points + acknowledgment checkbox
    5. Enter a DOB making you 25 — confirm minor awareness section is NOT shown

    Step 4 — a11y automated scan (run this command):
    RUN_A11Y=1 npx playwright test tests/e2e/a11y.spec.ts 2>&1 | tail -30

    Expected: All routes pass at >=95 axe score. If any new route fails, note the specific violation.

    Step 5 — AiBadge on lesson panel:
    1. Visit any song page (e.g., http://localhost:7000/songs/[any-slug])
    2. Start an exercise session; complete a question; see FeedbackPanel
    3. In FeedbackPanel "More" section, confirm "AI-assisted" badge appears on the mnemonic

    Step 6 — Profile page:
    1. Sign in and visit http://localhost:7000/profile
    2. Confirm "Account" section with "Download my data" button is visible
    3. Click "Download my data" — confirm browser initiates a JSON download
  </how-to-verify>
  <resume-signal>Type "approved" to confirm all checks passed, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright a11y test runner → local dev server | Automated scan of rendered HTML; no user data crossing |
| Human verifier → browser | Manual verification of compliance UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07-01 | Tampering | QuestionCard ARIA role change breaks keyboard nav | mitigate | Use role="list"/"listitem" pattern (not listbox/option) — no implicit keyboard contract; Pitfall 8 mitigation; manual keyboard test in human-verify checkpoint |
| T-18-07-02 | Denial of Service | a11y scan adds to CI time | accept | Scan is gated behind RUN_A11Y=1 env variable (nightly only per RESEARCH.md); not in PR CI; confirmed pattern from existing a11y.spec.ts |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0
2. `npm run test:unit` exits 0
3. `RUN_A11Y=1 npx playwright test tests/e2e/a11y.spec.ts` passes (>=95 on new routes)
4. `grep -n "/legal/terms" tests/e2e/a11y.spec.ts` returns match (Route added in Plan 01)
5. `grep -n "role=\"list\"" src/app/songs/[slug]/components/QuestionCard.tsx` returns match
6. `docs/phase18-operator-checklist.md` exists with ICO registration section
7. Human checkpoint approved
</verification>

<success_criteria>
- QuestionCard uses role="list"/"listitem" pattern with aria-pressed on options
- SentenceOrderCard has click/tap alternative to drag; focus rings present
- RUN_A11Y=1 a11y scan passes for all new legal/onboarding routes
- Operator checklist document exists with 6 pre-launch tasks
- Cookie banner does not flash on repeat visits (human verified)
- Legal pages render correctly in dark + light theme (human verified)
- Age-gate page shows minor awareness step correctly (human verified)
- AiBadge visible in FeedbackPanel (human verified)
- DataExportButton triggers browser download (human verified)
- TypeScript compiles clean
- All unit + integration tests green
</success_criteria>

<output>
After completion, create `.planning/phases/18-legal-compliance-implementation/18-07-SUMMARY.md`
</output>
