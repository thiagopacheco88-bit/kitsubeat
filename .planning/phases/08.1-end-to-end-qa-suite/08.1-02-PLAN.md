---
phase: 08.1-end-to-end-qa-suite
plan: 02
type: execute
wave: 2
depends_on: ["08.1-01"]
files_modified:
  - src/lib/exercises/__tests__/generator.test.ts
  - src/lib/exercises/__tests__/access.test.ts
  - src/lib/db/__tests__/derive-stars.test.ts
  - src/lib/exercises/__tests__/distractor-picker.test.ts
autonomous: true
requirements:
  - QA-UNIT-GENERATOR       # → EXER-10 (distractor selection) + EXER-01..04 (per-type generation)
  - QA-UNIT-DERIVESTARS     # → STAR-02 (star 1 at >=80% ex1_2_3) + STAR-03 (star 2 at >=80% ex4, gated on star 1)
  - QA-UNIT-ACCESS          # → FREE-01 (premium-gate abstraction) + FREE-06 (data-access-layer enforcement)
  - QA-UNIT-DISTRACTORS     # → EXER-10 (same-song + same-JLPT pool, never random)

must_haves:
  truths:
    - "Generator tests cover all 4 exercise types, cap/skip logic, vocab_item_id skipping, and verse timing gating (extends existing file)"
    - "deriveStars returns 2 at both >=0.80, 1 at ex1_2_3>=0.80 & ex4<0.80, 0 otherwise, including null inputs"
    - "checkExerciseAccess allows 'free' exercises and denies 'premium' with reason 'premium_required'"
    - "Distractor picker yields exactly 3 unique non-correct answers even when song vocab is thin"
    - "All unit tests complete in under 15 seconds wall-clock"
  artifacts:
    - path: "src/lib/exercises/__tests__/generator.test.ts"
      provides: "Extended coverage for buildQuestions (already exists — add gaps)"
      contains: "describe(\"buildQuestions\""
    - path: "src/lib/exercises/__tests__/access.test.ts"
      provides: "Unit tests for checkExerciseAccess gate"
      min_lines: 30
    - path: "src/lib/db/__tests__/derive-stars.test.ts"
      provides: "Unit tests for deriveStars pure function"
      min_lines: 30
    - path: "src/lib/exercises/__tests__/distractor-picker.test.ts"
      provides: "Unit tests targeting distractor-selection logic (via buildQuestions with thin vocab + JLPT fallback)"
      min_lines: 40
  key_links:
    - from: "src/lib/exercises/__tests__/access.test.ts"
      to: "src/lib/exercises/access.ts::checkExerciseAccess"
      via: "named import"
      pattern: "checkExerciseAccess"
    - from: "src/lib/db/__tests__/derive-stars.test.ts"
      to: "src/lib/db/schema.ts::deriveStars"
      via: "named import"
      pattern: "deriveStars"
    - from: "src/lib/exercises/__tests__/distractor-picker.test.ts"
      to: "src/lib/exercises/generator.ts::buildQuestions"
      via: "named import"
      pattern: "buildQuestions"
---

<objective>
Build out the Vitest unit layer for Phase 8's pure-TypeScript building blocks. Tests must be fully isolated — no DB, no network, no mocks of network. The generator already has a solid test file (extend it); access + deriveStars + distractor-picker are new.

Purpose: These are the cheapest, fastest signals that business logic didn't regress. They run in every developer's watch loop and gate CI before Playwright.

Output:
- Extended generator.test.ts with two added scenarios (see Task 1)
- New access.test.ts covering the single-gate decision path
- New derive-stars.test.ts covering the 2/1/0 threshold logic
- New distractor-picker.test.ts exercising thin-vocab fallback to JLPT pool
</objective>

<execution_context>
@C:/Users/Thiago/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Thiago/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08.1-end-to-end-qa-suite/08.1-CONTEXT.md
@src/lib/exercises/generator.ts
@src/lib/exercises/access.ts
@src/lib/exercises/feature-flags.ts
@src/lib/db/schema.ts
@src/lib/exercises/__tests__/generator.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend generator tests + add distractor-picker tests</name>
  <files>src/lib/exercises/__tests__/generator.test.ts, src/lib/exercises/__tests__/distractor-picker.test.ts</files>
  <action>
    Extend src/lib/exercises/__tests__/generator.test.ts (append inside existing describe block) with two new tests:
    1. `it("short mode seed is deterministic across runs")` — call buildQuestions twice with the same inputs in short mode and assert the question ordering is identical (prevents accidental non-seeded Math.random introduction). Skip if the generator is intentionally non-deterministic — in that case assert at least that every run produces the same LENGTH and type-mix counts.
    2. `it("short mode type mix is balanced across 4 types when all unlocked")` — in full mode with 5 vocab, assert count of each ExerciseType (vocab_meaning, meaning_vocab, reading_match, fill_lyric) is within ±1 of 5. This catches regressions where one type dominates.

    Create src/lib/exercises/__tests__/distractor-picker.test.ts:
    - Import buildQuestions + ExerciseType from "../generator"
    - Test A: with 3 vocab entries (minimum) and a 20-item JLPT_POOL, every question still has exactly 3 distractors and every distractor is either a pool entry or another song vocab entry (never correct answer).
    - Test B: with 3 vocab entries and an EMPTY JLPT_POOL, buildQuestions still returns questions with 3 distractors — assert fallback uses song vocab and NO question has fewer than 3 distractors. (If current generator cannot satisfy this, the test documents the known thin-pool limitation — use `it.fails` to encode it as a known gap for Phase 8 follow-up.)
    - Test C: distractors for `meaning_vocab` prompts (prompt=meaning, answer=surface) are all kanji/kana strings — not accidentally English meanings. Use a regex `/^[\u3040-\u30ff\u4e00-\u9fff]+$/` to assert character class, OR pull from vocab.surface set.
    - Test D: distractors for `vocab_meaning` prompts (answer=english meaning) are English-ish — not surface forms. Assert distractors appear in the union of (song vocab meanings + JLPT pool meanings).

    Do NOT mock any module. These are pure-function tests.
  </action>
  <verify>
    `npm run test:unit -- src/lib/exercises/__tests__/generator.test.ts` passes.
    `npm run test:unit -- src/lib/exercises/__tests__/distractor-picker.test.ts` passes.
    `grep -c "it(" src/lib/exercises/__tests__/generator.test.ts` shows +2 tests vs baseline.
  </verify>
  <done>
    Generator coverage has +2 tests; distractor-picker has 4 tests asserting pool fallback, character-class purity, and the 3-distractor invariant under thin-vocab conditions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add checkExerciseAccess unit tests</name>
  <files>src/lib/exercises/__tests__/access.test.ts</files>
  <action>
    Create src/lib/exercises/__tests__/access.test.ts:
    - Import `checkExerciseAccess` from "../access" and `EXERCISE_FEATURE_FLAGS` from "../feature-flags"
    - `describe("checkExerciseAccess")` with these tests:
      1. Every exercise type flagged "free" in EXERCISE_FEATURE_FLAGS resolves `{ allowed: true }` for any user id
      2. Every exercise type flagged "premium" resolves `{ allowed: false, reason: "premium_required" }` for a non-subscribed user
      3. An exercise type NOT present in EXERCISE_FEATURE_FLAGS defaults to premium (allowed: false) — this encodes the policy that unknowns are locked
      4. Passing an empty string as exerciseType resolves to premium (defensive default)
    - Derive the free/premium lists programmatically from EXERCISE_FEATURE_FLAGS using Object.entries so the test stays correct when flags change.
    - Do not assume a specific Clerk integration — the current implementation has a TODO for subscription lookup; the test must remain valid until that lands.
  </action>
  <verify>
    `npm run test:unit -- src/lib/exercises/__tests__/access.test.ts` passes.
    All tests finish in under 1 second.
  </verify>
  <done>
    Access gate has 4 tests covering free/premium/unknown/empty. Tests are resilient to EXERCISE_FEATURE_FLAGS edits.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add deriveStars unit tests</name>
  <files>src/lib/db/__tests__/derive-stars.test.ts</files>
  <action>
    Create src/lib/db/__tests__/derive-stars.test.ts:
    - Import `deriveStars` from "@/lib/db/schema"
    - `describe("deriveStars")` with a table-driven test enumerating:
      - { ex1_2_3: 0.80, ex4: 0.80 } → 2 (both at exactly threshold)
      - { ex1_2_3: 1.0, ex4: 1.0 } → 2
      - { ex1_2_3: 0.80, ex4: 0.79 } → 1 (ex4 under threshold)
      - { ex1_2_3: 0.80, ex4: null } → 1 (ex4 not attempted)
      - { ex1_2_3: 0.79, ex4: 0.80 } → 0 (ex1_2_3 under STAR-02's 0.80 threshold; star 1 not earned, so star 2 cannot be earned either — derives from STAR-02 → STAR-03 ordering)
      - { ex1_2_3: null, ex4: null } → 0
      - { ex1_2_3: null, ex4: 0.95 } → 0 (star 1 not earned per STAR-02; STAR-03's star 2 is gated on star 1 — STAR-02 → STAR-03 ordering invariant)
      - { ex1_2_3: 0, ex4: 0 } → 0
    - Use `it.each(cases)("…", ({ ex1_2_3, ex4, expected }) => …)`.
    - Confirm return type is `0 | 1 | 2` (TypeScript type assertion via `satisfies 0 | 1 | 2` in test).
  </action>
  <verify>
    `npm run test:unit -- src/lib/db/__tests__/derive-stars.test.ts` passes with 8 cases.
  </verify>
  <done>
    deriveStars has exhaustive threshold + null-input coverage. STAR-02/STAR-03 ordering invariant is explicitly tested.
  </done>
</task>

</tasks>

<verification>
- `npm run test:unit` passes end-to-end in <15s
- No test file imports from src/lib/db/index.ts (no DB connection in unit layer)
- No `vi.mock`/`jest.mock` of network modules — tests are pure
</verification>

<success_criteria>
- Generator coverage extended with determinism + type-mix
- access.test.ts encodes single-gate policy
- derive-stars.test.ts enumerates threshold matrix
- distractor-picker.test.ts encodes thin-vocab fallback and character-class purity
- All tests run in isolated Node environment
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-end-to-end-qa-suite/08.1-02-SUMMARY.md` documenting test counts per file and any `it.fails` entries marking known Phase 8 follow-up gaps.
</output>
