---
phase: 08.1-end-to-end-qa-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - vitest.config.ts
  - tests/support/test-db.ts
  - tests/support/fixtures.ts
  - tests/support/reporter-terminal.ts
  - tests/support/seed-test-db.ts
  - package.json
  - .env.test.example
autonomous: true
requirements:
  - QA-INFRA-CONFIGS
  - QA-INFRA-TESTDB
  - QA-INFRA-FIXTURES
  - QA-INFRA-REPORTER
  - QA-INFRA-SCRIPTS

must_haves:
  truths:
    - "Playwright config sets retries: 0 (zero-flake policy)"
    - "Vitest config includes scripts/ and src/ test roots"
    - "npm scripts exist for each layer (test:unit, test:integration, test:e2e, test:qa, test:all)"
    - "Terminal-first reporter prints one-line summary with failing test paths; HTML report is opt-in only"
    - "Tests run against a dedicated test DB (TEST_DATABASE_URL), never pollute dev DB"
    - "Test user fixture seeds user_song_progress + user_vocab_mastery in known state, cleared between runs"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Playwright config with retries:0, list reporter default, html opt-in, baseURL, webServer"
      contains: "retries: 0"
    - path: "vitest.config.ts"
      provides: "Vitest workspace including unit + integration test globs"
      contains: "include"
    - path: "tests/support/test-db.ts"
      provides: "resetTestDb(), seedTestUser(), getTestDb() helpers using TEST_DATABASE_URL"
      min_lines: 40
    - path: "tests/support/fixtures.ts"
      provides: "Playwright test fixtures (testUser, freshDb, seededSong) extending base test"
      min_lines: 30
    - path: "tests/support/seed-test-db.ts"
      provides: "Idempotent script that seeds 3 known songs + test user into TEST_DATABASE_URL"
      min_lines: 40
    - path: "tests/support/reporter-terminal.ts"
      provides: "Custom Playwright reporter printing one-line pass/fail + failing test file paths"
      min_lines: 30
    - path: "package.json"
      provides: "Scripts: test:unit, test:integration, test:e2e, test:qa, test:all"
      contains: "test:e2e"
    - path: ".env.test.example"
      provides: "Template listing TEST_DATABASE_URL and any other required env for tests"
  key_links:
    - from: "playwright.config.ts"
      to: "tests/support/reporter-terminal.ts"
      via: "reporter array entry"
      pattern: "reporter-terminal"
    - from: "tests/support/fixtures.ts"
      to: "tests/support/test-db.ts"
      via: "imports resetTestDb / seedTestUser"
      pattern: "test-db"
    - from: "package.json scripts"
      to: "vitest / playwright / tsx QA script"
      via: "command strings"
      pattern: "vitest run|playwright test|tsx.*06-qa-agent"
---

<objective>
Stand up the shared test infrastructure for the QA suite: configure vitest + playwright for all four layers (unit, integration, E2E, seed QA), pick the test-DB strategy, write shared fixtures + reset helpers, add a terminal-first reporter, and wire the npm scripts. No tests are authored in this plan — only the scaffolding other plans will consume.

Purpose: Every subsequent plan (02-07) depends on a predictable test environment. Zero-flake policy and the 15-minute speed budget are enforced at the config level here so later plans cannot accidentally re-introduce retries.

Output:
- playwright.config.ts hardened (retries:0, terminal reporter, html opt-in, webServer reused)
- vitest.config.ts expanded to cover unit + integration directories
- tests/support/* helpers for DB reset, test user, seeded-song fixture
- package.json scripts for each layer
- .env.test.example documenting test env

### Traceability map: synthetic QA-* IDs → real REQUIREMENTS.md IDs → plan/task coverage

Each synthetic `QA-*` requirement ID in the 8 plans' frontmatter maps to one or more real requirement IDs from `.planning/REQUIREMENTS.md`. Use this map to verify coverage during planning/audit.

| Synthetic QA ID             | Real Requirement ID(s)         | Plan / Task                  |
|-----------------------------|--------------------------------|------------------------------|
| QA-UNIT-GENERATOR           | EXER-01, EXER-02, EXER-03, EXER-04, EXER-10 | 08.1-02 / Task 1             |
| QA-UNIT-DISTRACTORS         | EXER-10                        | 08.1-02 / Task 1             |
| QA-UNIT-ACCESS              | FREE-01, FREE-02, FREE-06      | 08.1-02 / Task 2             |
| QA-UNIT-DERIVESTARS         | STAR-02, STAR-03               | 08.1-02 / Task 3             |
| QA-INT-API-JLPT             | EXER-10 (distractor pool)      | 08.1-03 / Task 1             |
| QA-INT-ADMIN-API            | (infrastructure parity — no roadmap ID) | 08.1-03 / Task 1     |
| QA-INT-SAVESESSION          | STAR-02, STAR-03, EXER-08      | 08.1-03 / Task 2             |
| QA-INT-PROGRESS-QUERIES     | STAR-02, STAR-03               | 08.1-03 / Task 3             |
| QA-SEED-UUID                | DATA-02                        | 08.1-04 / Task 1             |
| QA-SEED-FURIGANA            | (data-quality gate — no roadmap ID) | 08.1-04 / Task 1         |
| QA-SEED-GEO                 | (operational gate — no roadmap ID) | 08.1-04 / Task 2          |
| QA-SEED-TVPACK-SKIP         | (operational gate — no roadmap ID) | 08.1-04 / Task 1          |
| QA-PLAYER-LOAD              | (Phase 7 player acceptance)    | 08.1-05 / Task 1             |
| QA-PLAYER-LESSON            | (Phase 7 player acceptance)    | 08.1-05 / Task 1             |
| QA-PLAYER-SYNC              | (Phase 7 player acceptance)    | 08.1-05 / Task 2             |
| QA-PLAYER-PANELS            | (Phase 7 player acceptance)    | 08.1-05 / Task 3             |
| QA-EX-FULLSESSION           | EXER-01, EXER-02, EXER-03, EXER-04, EXER-08 | 08.1-06 / Task 1   |
| QA-EX-STARS                 | STAR-02, STAR-03               | 08.1-06 / Task 1             |
| QA-EX-RESUME                | EXER-09                        | 08.1-06 / Task 2             |
| QA-EX-FSRS                  | (Phase 8 user_vocab_mastery wiring, CONTEXT-locked) | 08.1-06 / Task 3 |
| QA-REG-CROSSSONG            | EXER-09 (isolation invariant)  | 08.1-07 / Task 1             |
| QA-REG-PREMIUMGATE          | FREE-01, FREE-06               | 08.1-07 / Task 1             |
| QA-REG-GEO                  | (CONTEXT coverage-scope bullet) | 08.1-07 / Task 2            |
| QA-REG-STALESEED            | DATA-02                        | 08.1-07 / Task 3             |
| QA-INFRA-*                  | (infrastructure — no roadmap ID) | 08.1-01 (all tasks)        |
| QA-HARD-SPEED / FLAKEQ / CI / DOCS | (operational budget — CONTEXT-locked) | 08.1-08 (all tasks) |

Verification-context coverage confirmed: FREE-01, FREE-02, FREE-06, EXER-01, EXER-02, EXER-03, EXER-04, EXER-08, EXER-09, EXER-10, STAR-02, STAR-03, DATA-02 each map to at least one plan/task above.
</objective>

<execution_context>
@C:/Users/Thiago/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Thiago/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-end-to-end-qa-suite/08.1-CONTEXT.md
@playwright.config.ts
@vitest.config.ts
@tests/app.spec.ts
@package.json
@src/lib/db/index.ts
@src/lib/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden playwright.config.ts + vitest.config.ts and add terminal reporter</name>
  <files>playwright.config.ts, vitest.config.ts, tests/support/reporter-terminal.ts</files>
  <action>
    Edit playwright.config.ts:
    - Add `retries: 0` explicitly (zero-flake policy) and `fullyParallel: true`
    - Set `reporter: [['./tests/support/reporter-terminal.ts'], ['html', { open: 'never', outputFolder: 'playwright-report' }]]` — HTML is generated but never auto-opened (opt-in via `npx playwright show-report`)
    - Keep `webServer.reuseExistingServer: true` (speed) and add `webServer.timeout: 120_000`
    - Add `expect.timeout: 5000` so slow assertions fail fast
    - Keep chromium only (one project) to stay under 15 min

    Edit vitest.config.ts:
    - Extend `test.include` to `['src/**/*.{test,spec}.ts', 'tests/integration/**/*.{test,spec}.ts']`
    - Add `test.exclude: ['node_modules/**', 'tests/e2e/**', 'tests/support/**']`
    - Add `test.reporters: ['default']` and `test.pool: 'threads'`
    - Keep `globals: true`, `environment: 'node'`, and the `@` path alias

    Create tests/support/reporter-terminal.ts:
    - Implement Playwright Reporter interface (onBegin, onTestEnd, onEnd)
    - On pass: print `PASS  <file>::<title> (<duration>ms)` dimmed
    - On fail: print `FAIL  <file>::<title>` and the first line of the error
    - At end: print total/passed/failed/skipped counts + one-line `X failed:` followed by bulleted file paths
    - No colors required; keep it plain ASCII so Windows cmd renders it
    - Do NOT call process.exit — let Playwright handle exit code

    Do NOT add `@playwright/test` retries: anywhere. Do NOT set test.retry() in any test file — this is a hard rule for the suite.
  </action>
  <verify>
    Run `npx playwright test --list` — exits 0 and lists tests/app.spec.ts.
    Run `npm test` — vitest discovers src/lib/exercises/__tests__/generator.test.ts and it passes.
    Grep `retries:` in playwright.config.ts returns exactly one line containing `0`.
    Grep `.retry(` in tests/**/*.ts returns zero matches.
  </verify>
  <done>
    Playwright config has retries:0 and custom terminal reporter wired. Vitest includes cover src + tests/integration. `npm test` and `npx playwright test --list` both succeed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test DB strategy + shared fixtures + seed script</name>
  <files>tests/support/test-db.ts, tests/support/fixtures.ts, tests/support/seed-test-db.ts, .env.test.example</files>
  <action>
    Decision (Claude's Discretion per CONTEXT): use a **dedicated test DB via TEST_DATABASE_URL** with a single test user ("test-user-e2e"). The test DB is the dev DB pointed at a different database name on the same Neon/Postgres — isolation without ephemeral provisioning overhead. Tests never write to the dev DB.

    Create tests/support/test-db.ts:
    - Export `getTestDb()` — returns a drizzle instance wired to `process.env.TEST_DATABASE_URL`. Throw if unset.
    - Export `resetTestProgress(userId: string)` — `DELETE FROM user_song_progress WHERE user_id = $1; DELETE FROM user_vocab_mastery WHERE user_id = $1; DELETE FROM user_exercise_log WHERE user_id = $1;`
    - Export `seedTestUser()` — returns the constant userId `"test-user-e2e"` (no Clerk yet — the app uses TODO userId). Idempotent.
    - Export `TEST_USER_ID = "test-user-e2e"` and `SEEDED_SLUGS = ['again-yui', ...]` (populated by Task 3's seed script — keep array literal pointing at 3 well-formed songs chosen from the current DB: 2 with full timing + lesson data, 1 with geo-restricted youtube_id for the regression plan). Document how to change.

    Create tests/support/seed-test-db.ts:
    - Tsx-runnable script (`npx tsx tests/support/seed-test-db.ts`) that:
      1. Connects to TEST_DATABASE_URL
      2. Verifies the 3 SEEDED_SLUGS exist in `songs` + have at least one `song_versions` row with lesson JSONB populated
      3. Calls `resetTestProgress(TEST_USER_ID)` so every CI run starts clean
      4. Exits 1 with a clear error if any seeded slug is missing (prevents silent suite breakage when dev DB is wiped)
    - Does NOT copy from dev DB — assumes operator has already run `npm run seed:dev` against TEST_DATABASE_URL. Document in header comment.

    Create tests/support/fixtures.ts:
    - Re-export `test` and `expect` from @playwright/test extended with two fixtures:
      - `testUser`: async fixture that returns `TEST_USER_ID` and calls `resetTestProgress` after the test (`use` → await cleanup)
      - `seededSong`: async fixture that returns `{ slug, songVersionId }` for a given name, read once from TEST_DATABASE_URL and cached per worker
    - Keep the fixture file pure — no inline DB queries at module scope (Playwright parses the file at collect time).

    Create .env.test.example:
    - `TEST_DATABASE_URL=postgres://...` with comment: "Point at a separate database from DATABASE_URL. Run `npx tsx tests/support/seed-test-db.ts` before the suite."
    - `NEXT_PUBLIC_APP_ENV=test` — UI can branch on this if a test ever needs to bypass a real-world side effect. (No code change yet, just reserving.)
  </action>
  <verify>
    `npx tsx tests/support/seed-test-db.ts` — exits 0 when TEST_DATABASE_URL is set and the 3 seeded slugs exist; exits 1 with a clear error when one is missing.
    Import fixtures in a scratch test file (`tests/support/smoke.spec.ts` — delete after) and confirm `testUser` + `seededSong` resolve.
    `SELECT count(*) FROM user_song_progress WHERE user_id = 'test-user-e2e'` in TEST_DATABASE_URL returns 0 after seed script runs.
  </verify>
  <done>
    Test DB + fixtures + reset helpers exist. Seed script is idempotent and safety-checks the catalog. .env.test.example documents required env. No test code leaks into dev DB.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire npm scripts for each layer</name>
  <files>package.json</files>
  <action>
    Add scripts to package.json (preserve existing scripts — only add):
    - `"test:unit": "vitest run src/"` — fast Vitest on src/ unit tests only
    - `"test:integration": "vitest run tests/integration/"` — Node integration tests (created in plan 03)
    - `"test:e2e": "playwright test"` — Playwright E2E (uses config)
    - `"test:qa": "tsx --tsconfig tsconfig.scripts.json scripts/seed/06-qa-agent.ts"` — seed/content QA (plan 04 extends this)
    - `"test:all": "npm run test:seed && npm run test:unit && npm run test:integration && npm run test:qa && npm run test:e2e"` — sequential, layered. **Ordering note:** `test:seed` runs FIRST so the test DB catalog is guaranteed fresh before any layer queries it. If TEST_DATABASE_URL is unset, `test:seed` fails loudly and the whole suite aborts early — this is intentional (better than running 15 minutes of tests against a missing/stale DB). README-testing.md (plan 08) must document this ordering.
    - `"test:seed": "tsx tests/support/seed-test-db.ts"` — idempotent test-DB seed
    - `"test:report": "playwright show-report"` — opt-in HTML report

    Keep existing `test` and `test:watch` scripts pointing at vitest (backwards compat).

    Do NOT add retry flags, `--retries`, or Playwright `--last-failed` loops anywhere.
  </action>
  <verify>
    `npm run test:unit` — runs vitest on src/ only; the existing generator.test.ts passes.
    `npm run test:qa` — runs the existing 06-qa-agent.ts (may or may not exit 0 depending on DB state; the script itself runs).
    `npm run test:e2e -- --list` — lists tests/app.spec.ts without error.
    `npm run test:seed` — errors out clearly if TEST_DATABASE_URL missing (proves the guard); succeeds when env is set.
  </verify>
  <done>
    Every test layer has a dedicated npm script. `test:all` runs them sequentially. `test:seed` is wired. No hidden retry logic.
  </done>
</task>

</tasks>

<verification>
After all three tasks:
- `grep -n "retries" playwright.config.ts` returns exactly one match set to `0`.
- `grep -rn "\.retry(" tests/` returns zero matches.
- `npm run test:unit`, `npm run test:e2e -- --list`, and `npm run test:qa` all exit 0 in a clean checkout with TEST_DATABASE_URL set.
- `tests/support/` contains test-db.ts, fixtures.ts, seed-test-db.ts, reporter-terminal.ts.
</verification>

<success_criteria>
- Playwright + Vitest configs support all four test layers
- retries:0 is enforced at config level (zero-flake gate)
- Terminal-first reporter prints concise failing-test list
- Dedicated test DB via TEST_DATABASE_URL; dev DB is untouched
- Test user + DB reset helpers exposed as fixtures for plans 02-07
- npm scripts for unit / integration / e2e / qa / seed / all
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-end-to-end-qa-suite/08.1-01-SUMMARY.md` documenting:
- Final playwright + vitest config shape
- TEST_USER_ID and SEEDED_SLUGS values chosen
- Any deviations from CONTEXT.md decisions
</output>
