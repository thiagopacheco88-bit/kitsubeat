---
phase: 08.1-end-to-end-qa-suite
plan: 03
type: execute
wave: 2
depends_on: ["08.1-01"]
files_modified:
  - tests/integration/api-jlpt-pool.test.ts
  - tests/integration/save-session-results.test.ts
  - tests/integration/queries-progress.test.ts
  - tests/integration/admin-songs-api.test.ts
autonomous: true
requirements:
  - QA-INT-API-JLPT
  - QA-INT-SAVESESSION
  - QA-INT-PROGRESS-QUERIES
  - QA-INT-ADMIN-API

must_haves:
  truths:
    - "GET /api/exercises/jlpt-pool returns vocab rows at the requested level and rejects missing jlpt_level"
    - "saveSessionResults writes user_song_progress row, applies GREATEST for accuracy, LEAST(100) for completion, and returns {stars, previousStars}"
    - "A second saveSessionResults call with higher accuracy updates best accuracy; a lower one does NOT regress it"
    - "user_vocab_mastery row is created/updated by the session flow for each answered vocab_item_id (via whichever hook exists today — or the test documents the gap)"
    - "getUserSongProgress query returns derived stars — deriveStars is applied at read time, not stored"
    - "All integration tests reset the test user's rows before AND after each run so the suite is hermetic"
  artifacts:
    - path: "tests/integration/api-jlpt-pool.test.ts"
      provides: "Integration test hitting /api/exercises/jlpt-pool route handler directly"
      min_lines: 40
    - path: "tests/integration/save-session-results.test.ts"
      provides: "Integration test for src/app/actions/exercises.ts::saveSessionResults"
      min_lines: 60
    - path: "tests/integration/queries-progress.test.ts"
      provides: "Integration test for lib/db/queries.ts progress/star derivation"
      min_lines: 40
    - path: "tests/integration/admin-songs-api.test.ts"
      provides: "Smoke integration test for /api/admin/songs route"
      min_lines: 30
  key_links:
    - from: "tests/integration/save-session-results.test.ts"
      to: "src/app/actions/exercises.ts::saveSessionResults"
      via: "direct import + await call"
      pattern: "saveSessionResults"
    - from: "tests/integration/api-jlpt-pool.test.ts"
      to: "src/app/api/exercises/jlpt-pool/route.ts::GET"
      via: "import { GET }, call with NextRequest"
      pattern: "api/exercises/jlpt-pool"
    - from: "tests/integration/*.test.ts"
      to: "tests/support/test-db.ts"
      via: "resetTestProgress / TEST_USER_ID import"
      pattern: "resetTestProgress"
---

<objective>
Write Node-side integration tests that exercise the DB + API surface without a browser. These cover the back-end contract the E2E tests will rely on — if the integration layer passes but E2E fails, the bug is UI-only.

Purpose: Cheapest way to catch DB/API regressions without paying Playwright cost. Runs in ~30s against TEST_DATABASE_URL.

Output:
- jlpt-pool API route test (happy + error path)
- saveSessionResults test (first run, repeat run with lower accuracy, repeat with higher)
- getUserSongProgress / getSongProgressMap queries test (star derivation)
- admin songs route smoke test
</objective>

<execution_context>
@C:/Users/Thiago/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Thiago/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.1-end-to-end-qa-suite/08.1-CONTEXT.md
@src/app/actions/exercises.ts
@src/app/api/exercises/jlpt-pool/route.ts
@src/app/api/admin/songs/route.ts
@src/lib/db/queries.ts
@src/lib/db/schema.ts
@tests/support/test-db.ts
@tests/support/fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration test for jlpt-pool API + admin songs smoke</name>
  <files>tests/integration/api-jlpt-pool.test.ts, tests/integration/admin-songs-api.test.ts</files>
  <action>
    Create tests/integration/api-jlpt-pool.test.ts:
    - Import `GET` from "@/app/api/exercises/jlpt-pool/route"
    - Import `NextRequest` from "next/server"
    - Set `process.env.DATABASE_URL = process.env.TEST_DATABASE_URL` before the first import that reads the DB (use `vi.hoisted` or a top-level `beforeAll` — simpler: assign in a `tests/integration/setup.ts` that vitest config auto-loads via `test.setupFiles`). Add `setupFiles: ['./tests/integration/setup.ts']` scoped to integration via config merge OR just do the assignment at the top of this file before imports.
    - Tests:
      1. `GET` with `?jlpt_level=N5` → status 200, body is an array, every row has `dictionary_form`, `reading`, `romaji`, `meaning`, `part_of_speech`
      2. `GET` with no query params → status 400, body `{ error: /jlpt_level/i }`
      3. `GET` with `?jlpt_level=N9` (nonsense level) → status 200 with empty array (no crash)
    - Build the NextRequest via `new NextRequest(new URL('http://localhost/api/exercises/jlpt-pool?jlpt_level=N5'))`

    Create tests/integration/admin-songs-api.test.ts:
    - Similar pattern — import the admin songs route handler (GET) and assert:
      1. Returns 200 and an array of songs with id/slug/title fields
      2. Array length matches `SELECT count(*) FROM songs` (integration parity)
    - If the route requires auth/admin header, skip with a TODO documenting the Clerk dependency — do not mock auth here.
  </action>
  <verify>
    `npm run test:integration -- tests/integration/api-jlpt-pool.test.ts` passes against TEST_DATABASE_URL.
    `npm run test:integration -- tests/integration/admin-songs-api.test.ts` passes.
    Both complete in <5s.
  </verify>
  <done>
    Both API routes have integration coverage; the jlpt-pool error path is explicit; admin-songs is a smoke-level parity check.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integration test for saveSessionResults</name>
  <files>tests/integration/save-session-results.test.ts</files>
  <action>
    Create tests/integration/save-session-results.test.ts:
    - Import `saveSessionResults` from "@/app/actions/exercises"
    - Import `TEST_USER_ID`, `resetTestProgress`, `getTestDb` from "@/../tests/support/test-db" (or relative path)
    - Read one `song_versions.id` from the test DB (any seeded song with lesson data) into `songVersionId` in `beforeAll`
    - `beforeEach`: await resetTestProgress(TEST_USER_ID)

    Tests:
    1. First call with all-correct answers across vocab_meaning + fill_lyric → returns `{ stars: 2, previousStars: 0, completionPct: 15 }` (short mode increment). Row in user_song_progress: `ex1_2_3_best_accuracy = 1.0`, `ex4_best_accuracy = 1.0`, `sessions_completed = 1`.
    2. Second call (same user/song) with LOWER accuracy (50% on ex1_2_3, 50% on ex4) → row still has `ex1_2_3_best_accuracy = 1.0`, `ex4_best_accuracy = 1.0` (GREATEST wins), `sessions_completed = 2`, `completion_pct = 30` (short+short cap).
    3. Start fresh (reset), first call with ex1_2_3-only answers (no fill_lyric) → stars = 1, `ex4_best_accuracy = NULL`.
    4. Second call adds a 90% fill_lyric batch → stars becomes 2, ex4_best_accuracy = 0.9, previousStars = 1.
    5. Call with `mode: "full"` → completion_pct increments by 30 per call, capped at 100.
    6. Answers array containing entries with an unknown `type` string (e.g. `"grammar_conjugation"`, `"listening_drill"`, or `""`) → the call MUST NOT throw. Assert the locked behavior (confirmed by planner via code-read of `src/app/actions/exercises.ts`): `saveSessionResults` filters answers by `ex1_2_3Types.includes(a.type)` and `a.type === "fill_lyric"` — any unknown `type` matches NEITHER filter and is silently dropped from both accuracy computations. Concrete assertion: call saveSessionResults with a single answer of type `"grammar_conjugation"` (correct=true) PLUS one valid `vocab_meaning` answer (correct=true). Expected result: `ex1_2_3_best_accuracy = 1.0` (only the vocab_meaning counted), `ex4_best_accuracy = NULL` (no fill_lyric), `sessions_completed = 1`, `completion_pct = 15`, `stars = 1`. No error is thrown. Also assert that calling with ONLY unknown-type answers inserts a row with both accuracy columns NULL and `stars = 0`.

    Cleanup in `afterAll`: resetTestProgress(TEST_USER_ID) so the DB is clean for the next run.
  </action>
  <verify>
    `npm run test:integration -- tests/integration/save-session-results.test.ts` passes.
    Manual check after run: `SELECT * FROM user_song_progress WHERE user_id = 'test-user-e2e'` returns 0 rows (cleanup worked).
  </verify>
  <done>
    saveSessionResults has 6 tests covering upsert, GREATEST accuracy wins, LEAST(100) completion cap, NULL-group handling, and stars ↔ previousStars transitions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integration test for progress queries (deriveStars at read time)</name>
  <files>tests/integration/queries-progress.test.ts</files>
  <action>
    Create tests/integration/queries-progress.test.ts:
    - Import `getUserSongProgress` + `getSongProgressMap` (or equivalent) from "@/lib/db/queries"
    - Import `saveSessionResults` from "@/app/actions/exercises" (to prime state)
    - beforeEach: resetTestProgress(TEST_USER_ID)

    Tests:
    1. With no user_song_progress row: query returns undefined/null (or empty map), `stars` is not thrown — no row is fine.
    2. After saveSessionResults writes ex1_2_3=0.85, ex4=0.85: query row has `stars: 2` — the stars field is added by the query, NOT by the DB. Confirm by also asserting the table has NO `stars` column (`SELECT column_name FROM information_schema.columns WHERE table_name = 'user_song_progress'` and expect no 'stars').
    3. After saveSessionResults writes ex1_2_3=0.85, ex4=0.70: query row has `stars: 1`.
    4. Progress map for multiple song versions returns stars per version correctly.

    This test anchors the "stars are derived at read time" architectural decision from Phase 08-01.
  </action>
  <verify>
    `npm run test:integration -- tests/integration/queries-progress.test.ts` passes.
    The information_schema assertion is explicit — if someone ever adds a `stars` column the test fails loudly.
  </verify>
  <done>
    Progress queries proven to derive stars at read time; the "no stars column" invariant is asserted.
  </done>
</task>

</tasks>

<verification>
- All integration tests run via `npm run test:integration` in <30s total
- None of the tests touch DATABASE_URL — they resolve to TEST_DATABASE_URL via setup
- `SELECT count(*) FROM user_song_progress WHERE user_id = 'test-user-e2e'` returns 0 after suite completes
</verification>

<success_criteria>
- API routes + server actions + queries have end-to-end integration coverage
- GREATEST/LEAST upsert semantics are locked
- Star derivation at read time is asserted (no schema column)
- Tests are hermetic (before+after cleanup)
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-end-to-end-qa-suite/08.1-03-SUMMARY.md` documenting which setup-files approach was used and any routes that had to be .skip'd due to auth gating.
</output>
