---
phase: "18.3"
plan: "08"
subsystem: "navigation"
tags: ["nav", "i18n", "anime"]
dependency_graph:
  requires: ["18.3-07"]
  provides: ["anime-nav-link"]
  affects: ["src/app/layout.tsx", "src/app/components/MobileNavSheet.tsx"]
tech_stack:
  added: []
  patterns: ["next-intl translation keys", "Next.js Link component"]
key_files:
  modified:
    - src/app/layout.tsx
    - src/app/components/MobileNavSheet.tsx
    - src/messages/en/common.json
    - src/messages/es/common.json
    - src/messages/pt-BR/common.json
decisions:
  - "Desktop nav uses t('nav.anime') translation key; mobile nav uses hardcoded 'Anime' string (matches existing MobileNavSheet pattern)"
  - "Anime link placed after Songs and before Kana in both desktop and mobile nav"
metrics:
  duration: "3 minutes"
  completed: "2026-05-23"
  tasks_completed: 1
  tasks_total: 1
  files_changed: 5
---

# Phase 18.3 Plan 08: Anime Nav Link Summary

**One-liner:** Anime nav link added to desktop header (t translation key) and mobile sheet (hardcoded string), with nav.anime key in all 3 locale files.

## Tasks Completed

| # | Task | Commit | Status |
|---|------|--------|--------|
| 1 | Add Anime nav link to layout.tsx and MobileNavSheet.tsx | 40b4e1c | Done |

## What Was Built

- `src/app/layout.tsx`: Added `<Link href="/anime">{t('nav.anime')}</Link>` after Songs and before Kana in the `hidden sm:contents` desktop nav block
- `src/app/components/MobileNavSheet.tsx`: Added `{ href: "/anime", label: "Anime" }` after Songs and before Kana in the links array (hardcoded string, matching existing file convention)
- `src/messages/en/common.json`: Added `"anime": "Anime"` to nav object
- `src/messages/es/common.json`: Added `"anime": "Anime"` to nav object
- `src/messages/pt-BR/common.json`: Added `"anime": "Anime"` to nav object

## Placement in Nav

Desktop (layout.tsx): Path → Songs → **Anime** → Kana → Journal

Mobile (MobileNavSheet.tsx): Path → Songs → **Anime** → Kana → Journal → Progress → Profile

## Deviations from Plan

None — plan executed exactly as written.

The MobileNavSheet uses hardcoded strings (not `t()` calls) — confirmed by reading the file first. Used hardcoded `"Anime"` as instructed by the plan's conditional.

## Verification

- `grep "nav.anime" src/app/layout.tsx` — 1 line (desktop nav t() call)
- `grep "href.*\"/anime\"" src/app/layout.tsx` — 1 line (desktop nav Link)
- `grep "anime" src/app/components/MobileNavSheet.tsx` — 1 line
- All 3 locale files have `"anime": "Anime"` — confirmed
- `npx tsc --noEmit` — zero errors

## Self-Check: PASSED

- [x] `src/app/layout.tsx` — edited, committed at 40b4e1c
- [x] `src/app/components/MobileNavSheet.tsx` — edited, committed at 40b4e1c
- [x] `src/messages/en/common.json` — edited, committed at 40b4e1c
- [x] `src/messages/es/common.json` — edited, committed at 40b4e1c
- [x] `src/messages/pt-BR/common.json` — edited, committed at 40b4e1c
