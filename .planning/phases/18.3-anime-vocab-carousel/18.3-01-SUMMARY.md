---
phase: "18.3"
plan: "01"
subsystem: "database"
tags: ["schema", "migration", "drizzle", "neon", "anime-vocab-carousel"]
dependency_graph:
  requires: []
  provides: ["anime_vocab_catalog table in Neon", "animeVocabCatalog Drizzle export", "AnimeVocabCatalog type"]
  affects: ["src/lib/db/schema.ts", "drizzle/0024_anime_vocab_catalog.sql"]
tech_stack:
  added: []
  patterns: ["hand-written SQL migration (apply-migrations.ts pattern)", "Drizzle FK with onDelete cascade"]
key_files:
  created: ["drizzle/0024_anime_vocab_catalog.sql"]
  modified: ["src/lib/db/schema.ts"]
decisions:
  - "Used migration number 0024 (not 0025 as planned) — 0024 slot was empty; plan assumed 0024 existed"
  - "Hand-wrote SQL migration instead of drizzle-kit generate — interactive prompt from D-01 schema drift"
  - "Applied via apply-migrations.ts (project's established migration pattern)"
metrics:
  duration: "~8 min"
  completed: "2026-05-23"
  tasks: 2
  files: 2
---

# Phase 18.3 Plan 01: Anime Vocab Catalog Schema + Migration Summary

**One-liner:** anime_vocab_catalog join table with FK to vocabulary_items, UNIQUE(anime_slug, vocab_item_id), INDEX(anime_slug, display_order), applied to Neon via hand-written migration 0024.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add animeVocabCatalog to schema.ts | ebab4ef | src/lib/db/schema.ts |
| 2 | Generate migration 0024 and push to Neon | 108230f | drizzle/0024_anime_vocab_catalog.sql |

## Verification Results

- `grep "animeVocabCatalog" src/lib/db/schema.ts` — 2 lines (table def + type export); unique/index constraints inside table def at lines 987-988
- `grep "references.*vocabularyItems.id" src/lib/db/schema.ts` — line 981 within new table block with `{ onDelete: "cascade" }`
- `grep "anime_vocab_catalog_slug_vocab_unique" src/lib/db/schema.ts` — 1 line
- `grep "anime_vocab_catalog_slug_order_idx" src/lib/db/schema.ts` — 1 line
- `export type AnimeVocabCatalog` — 1 line at 991
- `export type AnimeMetadata` — pre-existing at 962 (no change needed)
- `drizzle/0024_anime_vocab_catalog.sql` — exists with `CREATE TABLE "anime_vocab_catalog"` and FK DDL
- `npx tsc --noEmit` — exits 0, zero errors
- Neon table verification — `TABLE_OK` confirmed via `information_schema.tables` query

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Migration number 0024 used instead of 0025**
- **Found during:** Task 2
- **Issue:** Plan specified migration 0024 as the latest, instructing to create 0025. Actual latest was 0023 — slot 0024 was vacant.
- **Fix:** Created `drizzle/0024_anime_vocab_catalog.sql` (next sequential number). The table DDL, constraints, and all acceptance criteria are identical.
- **Files modified:** `drizzle/0024_anime_vocab_catalog.sql`
- **Commit:** 108230f

**2. [Rule 3 - Blocking] drizzle-kit generate triggers interactive prompt (D-01 schema drift)**
- **Found during:** Task 2 — `npm run db:generate` blocked on interactive column-rename prompt for `songs.popularity_rank`
- **Issue:** Known pre-existing issue (D-01 in STATE.md). The drizzle-kit journal is out of sync with the actual DB schema, causing the generator to misidentify columns as renamed.
- **Fix:** Hand-wrote SQL migration following the project's established pattern (all migrations 0011+ are hand-written; applied via `scripts/apply-migrations.ts`). The plan mentioned this as a possibility: "if `db:generate` produces a file named differently... that is fine."
- **Files modified:** `drizzle/0024_anime_vocab_catalog.sql`
- **Commit:** 108230f

**3. [Rule 3 - Blocking] .env.local missing in worktree**
- **Found during:** Task 2 migration application
- **Issue:** Git worktrees don't share .env.local files from the parent repo. `scripts/apply-migrations.ts` requires `DATABASE_URL`.
- **Fix:** Copied `.env.local` from the main project directory to the worktree before running the migration script.
- **Files modified:** `.env.local` (worktree-only, not committed — gitignored)

## Known Stubs

None. This plan is pure schema + migration — no UI components, no data rendering stubs.

## Threat Flags

No new network endpoints, auth paths, or trust boundary surface introduced. The migration creates a table in Neon via a server-side operator script (not user-triggered). T-18.3-01 (ON DELETE CASCADE FK) is implemented as specced.

## Self-Check: PASSED

- `src/lib/db/schema.ts` — file exists and contains animeVocabCatalog export
- `drizzle/0024_anime_vocab_catalog.sql` — file exists with correct DDL
- Commit ebab4ef — verified in git log
- Commit 108230f — verified in git log
- `anime_vocab_catalog` table live in Neon — TABLE_OK confirmed
