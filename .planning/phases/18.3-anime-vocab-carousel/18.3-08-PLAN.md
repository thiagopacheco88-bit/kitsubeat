---
phase: 18.3
plan: 08
type: execute
wave: 5
depends_on:
  - "18.3-07"
files_modified:
  - src/app/layout.tsx
  - src/app/components/MobileNavSheet.tsx
  - src/messages/en/common.json
  - src/messages/es/common.json
  - src/messages/pt-BR/common.json
autonomous: true
requirements:
  - CULT-01

must_haves:
  truths:
    - "Desktop nav shows 'Anime' link (t('nav.anime')) between existing nav items"
    - "Mobile nav sheet includes Anime entry linking to /anime"
    - "All 3 locale message files have nav.anime key"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Desktop nav Anime link"
      contains: "nav.anime"
    - path: "src/app/components/MobileNavSheet.tsx"
      provides: "Mobile nav Anime entry"
      contains: "/anime"
  key_links:
    - from: "src/app/layout.tsx desktop nav"
      to: "/anime"
      via: "Link href='/anime'"
      pattern: "href.*\"/anime\""
    - from: "src/app/components/MobileNavSheet.tsx"
      to: "/anime"
      via: "links array entry"
      pattern: "anime"
---

<objective>
Add "Anime" nav link to desktop header and mobile nav sheet, plus the nav.anime translation key to all 3 locale message files.

Purpose: Makes the anime section discoverable from everywhere in the app without breaking existing navigation.

Output: Three files modified: layout.tsx, MobileNavSheet.tsx, and 3 message files.
</objective>

<execution_context>
@C:/Users/thiag/get-shit-done/workflows/execute-plan.md
@C:/Users/thiag/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18.3-anime-vocab-carousel/18.3-CONTEXT.md
@.planning/phases/18.3-anime-vocab-carousel/18.3-RESEARCH.md
@.planning/phases/18.3-anime-vocab-carousel/18.3-07-SUMMARY.md

<interfaces>
<!-- Nav patterns (verified from RESEARCH.md) -->

From src/app/layout.tsx (verified line ~190-236):
```typescript
// Desktop nav — inside className="hidden sm:contents" block
// Pattern (links use t('nav.X') translation key):
<Link
  href="/journal"
  className="whitespace-nowrap text-sm text-[var(--color-text-muted)] transition-colors hover:text-[var(--color-text)]"
>
  {t('nav.journal')}
</Link>
// ADD after this: href="/anime", key t('nav.anime')
```

From src/app/components/MobileNavSheet.tsx (verified lines ~39-48):
```typescript
const links = [
  { href: "/path", label: t("nav.path") },
  { href: "/songs", label: t("nav.songs") },
  // ADD: { href: "/anime", label: t("nav.anime") },
  { href: "/kana", label: t("nav.kana") },
  // ...
];
```

From src/messages/en/common.json (verified structure):
```json
{
  "nav": {
    "path": "Path",
    "songs": "Songs",
    "kana": "Kana",
    "journal": "Journal"
    // ADD: "anime": "Anime"
  }
}
```

NOTE: The MobileNavSheet may use hardcoded strings or translation keys — read the file first to confirm.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Anime nav link to layout.tsx and MobileNavSheet.tsx</name>
  <files>
    src/app/layout.tsx,
    src/app/components/MobileNavSheet.tsx,
    src/messages/en/common.json,
    src/messages/es/common.json,
    src/messages/pt-BR/common.json
  </files>
  <read_first>
    - src/app/layout.tsx lines 180-250 (read the full desktop nav block to find exact insertion point)
    - src/app/components/MobileNavSheet.tsx (read fully — confirm whether links array uses t() or hardcoded strings)
    - src/messages/en/common.json (read the nav section to find where to add anime key)
    - src/messages/es/common.json (read the nav section)
    - src/messages/pt-BR/common.json (read the nav section)
  </read_first>
  <action>
**Step 1: Edit `src/app/layout.tsx`**

Find the desktop nav link cluster (inside `className="hidden sm:contents"` div). Locate the journal link:
```html
<Link href="/journal" ...>{t('nav.journal')}</Link>
```

Add the Anime link AFTER the journal link (or alongside songs — place it in a logical position between Songs and Journal):

```html
<Link
  href="/anime"
  className="whitespace-nowrap text-sm text-[var(--color-text-muted)] transition-colors hover:text-[var(--color-text)]"
>
  {t('nav.anime')}
</Link>
```

IMPORTANT: Match the EXACT className of the adjacent nav links. Do not introduce new CSS classes.

**Step 2: Edit `src/app/components/MobileNavSheet.tsx`**

Read the file first. If it uses `t()` translation keys, add:
```typescript
{ href: "/anime", label: t("nav.anime") }
```

If it uses hardcoded strings, add:
```typescript
{ href: "/anime", label: "Anime" }
```

Place after Songs or before Kana (logical ordering in the mobile menu).

**Step 3: Edit all 3 message files**

In each file, find the `"nav"` object and add `"anime"` key:

`src/messages/en/common.json` — add: `"anime": "Anime"`
`src/messages/es/common.json` — add: `"anime": "Anime"` (same word in Spanish)
`src/messages/pt-BR/common.json` — add: `"anime": "Anime"` (same word in Portuguese)

Place the key near other nav entries (next to "songs" or "journal" for consistency).

After all edits:
```bash
npx tsc --noEmit 2>&1 | grep "layout\|MobileNav\|common" | head -10
```
  </action>
  <verify>
    <automated>grep -n "nav.anime\|/anime" src/app/layout.tsx src/app/components/MobileNavSheet.tsx 2>/dev/null | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "nav.anime" src/app/layout.tsx` returns 1 line (desktop nav t() call)
    - `grep "href.*\"/anime\"" src/app/layout.tsx` returns 1 line (desktop nav Link)
    - `grep "anime" src/app/components/MobileNavSheet.tsx` returns at least 1 line
    - `grep "\"anime\".*\"Anime\"" src/messages/en/common.json` returns 1 line
    - `grep "\"anime\".*\"Anime\"" src/messages/es/common.json` returns 1 line
    - `grep "\"anime\".*\"Anime\"" "src/messages/pt-BR/common.json"` returns 1 line
    - `npx tsc --noEmit` produces zero errors after these changes
    - `npm run build 2>&1 | grep -E "error|anime" | head -10` — no new build errors
  </acceptance_criteria>
  <done>Anime nav link added to desktop and mobile nav; all 3 locale files have nav.anime key; build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nav links | Static links — no user input; hardcoded href values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.3-16 | Tampering | nav link href | accept | Hardcoded "/anime" href; no dynamic user-controlled content |
</threat_model>

<verification>
After task completes:

1. `grep "nav.anime\|href.*anime" src/app/layout.tsx` — returns 2 lines
2. `grep "anime" src/app/components/MobileNavSheet.tsx` — returns at least 1 line
3. `cat src/messages/en/common.json | grep anime` — returns "anime": "Anime"
4. `cat src/messages/pt-BR/common.json | grep anime` — returns "anime": "Anime"
5. `cat src/messages/es/common.json | grep anime` — returns "anime": "Anime"
6. `npx tsc --noEmit` — zero errors
</verification>

<success_criteria>
- Desktop nav has "Anime" link using t('nav.anime')
- Mobile nav sheet has "Anime" entry to /anime
- All 3 locale message files (en, es, pt-BR) have nav.anime key with value "Anime"
- Zero TypeScript errors and build passes
</success_criteria>

<output>
After completion, create `.planning/phases/18.3-anime-vocab-carousel/18.3-08-SUMMARY.md`
</output>
