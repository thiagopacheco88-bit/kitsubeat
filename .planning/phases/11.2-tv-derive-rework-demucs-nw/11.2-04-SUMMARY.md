---
phase: "11.2"
plan: "04"
subsystem: "tv-pipeline"
tags: ["archive", "git-mv", "deferred", "lyrics-cache-blocker"]
dependency_graph:
  requires:
    - "scripts/seed/10b-derive-tv-lessons-nw.ts (Plan 11.2-02 — replacement for archived LCS scripts)"
  provides:
    - "scripts/seed/_attic/10b-derive-tv-lessons-lcs.ts (renamed verbatim from old 10b)"
    - "scripts/seed/_attic/10b2-derive-tv-monotonic.ts (renamed verbatim)"
  affects:
    - "scripts/seed/ — only 10b-derive-tv-lessons-nw.ts remains as canonical TV-derive entry point"
tech_stack:
  added: []
  patterns:
    - "_attic/ archive convention (rename, don't delete; preserves git history + future reference)"
key_files:
  created:
    - "scripts/seed/_attic/10b-derive-tv-lessons-lcs.ts (499 lines, renamed from scripts/seed/10b-derive-tv-lessons.ts)"
    - "scripts/seed/_attic/10b2-derive-tv-monotonic.ts (175 lines, renamed verbatim)"
  modified: []
  deferred:
    - "data/lessons-cache/mountain-a-go-go-too-captain-straydum.json (Task 1 deferred — see deviations)"
decisions:
  - "Task 1 (D-04 mountain-a-go-go regen) DEFERRED to Plan 11.2-05 D-05 straggler triage. Plan author assumed only the lesson-generation step needed JP-token forcing; investigation revealed the upstream lyrics-cache JSON is itself romaji-only ('Get up!!... Sumimasen / Boku tama ni...'), and the 225 tokens were tokenized from that romaji input (every token has pos: 名詞 even for English words like 'Get', 'up'). Re-running 03-generate-content.ts (or the equivalent inline-Claude-Code prompt) would feed romaji to the LLM and produce romaji output again. Real fix requires upstream lyrics refetch + retokenize — out of scope for Wave 2."
  - "Task 1 deferral approved by user. Mountain-a-go-go will fail NW alignment in Plan 05's batch run (1/60 song failure expected); Plan 05's D-05 fallback ladder triage handles stragglers explicitly. Real upstream fix likely lands in Phase 11.1 (Add-Song Pipeline) or a follow-up phase — note WIP at scripts/seed/fetch-utanet-lyrics.ts (currently stashed) appears to address lyrics-fetch quality."
  - "User constraint: feedback_local_only_tooling memory rule forbids API credit spend. Plan 11.2-04 had marked D-04 as a 'locked exception' to that rule, but exception rescinded at execution time — user opted to defer rather than spend."
  - "Both archive moves use git mv (not delete + add) so blame/history survives. Filenames suffixed -lcs to disambiguate from the new -nw variant."
metrics:
  duration: "~2 min (skipped Task 1; Task 2 only)"
  completed: "2026-04-26"
  tasks_completed: "1/3 (Task 2 only; Task 1 deferred to Plan 05 straggler triage; Task 3 wrap-up done by this SUMMARY)"
deviations:
  - "Task 1 (mountain-a-go-go regen) DEFERRED — see decisions[0]. Did NOT modify data/lessons-cache/mountain-a-go-go-too-captain-straydum.json. The 'must_have' truth 'data/lessons-cache/mountain-a-go-go-too-captain-straydum.json has Japanese kana/kanji tokens' is UNMET and intentionally so."
  - "Task 1 ANTHROPIC_API_KEY pre-flight not executed (skipped along with Task 1)."
  - "User-visual-verification checkpoint not triggered (no regen happened)."
  - "Acceptance criterion that would have validated mountain-a-go-go.json post-regen is unmet by design — the song is now expected to surface as a Wave 3 batch-run failure, where Plan 11.2-05 D-05 ladder triage handles it (or escalates to a follow-up phase for the lyrics-fetch fix)."
commits:
  - "0fe6352 feat(11.2-04): archive LCS-era TV derive scripts to _attic/ (D-02)"
followups:
  - "Plan 11.2-05 must NOT block on mountain-a-go-go failure — treat as expected straggler, document outcome, proceed with the other 59 songs."
  - "Lyrics-fetch fix (likely owned by stashed scripts/seed/fetch-utanet-lyrics.ts) is the real upstream resolution — reattach when convenient via `git stash pop`."
---

# Plan 11.2-04 — Pre-batch fixups (PARTIAL)

## Goal

Two pre-batch fixups before Wave 3's 60-song NW batch:
1. **D-04** — Regenerate `mountain-a-go-go-too-captain-straydum` full lesson with Japanese tokens.
2. **D-02** — Archive old LCS scripts to `_attic/`.

## Outcome

**Task 2 (D-02 archive) — COMPLETE.** Both old LCS-era scripts moved to `_attic/` via `git mv`:

- `scripts/seed/10b-derive-tv-lessons.ts` → `scripts/seed/_attic/10b-derive-tv-lessons-lcs.ts` (499 lines)
- `scripts/seed/10b2-derive-tv-monotonic.ts` → `scripts/seed/_attic/10b2-derive-tv-monotonic.ts` (175 lines)

Verified no live importers via grep — only `10b-derive-tv-lessons-nw.ts` (Plan 11.2-02) is referenced by tests/code. Doc/planning files mention the old script names but that's narrative not import-graph.

**Task 1 (D-04 regen) — DEFERRED.** Plan author's assumption was wrong: only the lesson-regen step was supposed to need JP-token forcing, but the upstream `data/lyrics-cache/mountain-a-go-go-too-captain-straydum.json` is itself romaji-only:

```
raw_lyrics: "Get up!!... Sumimasen\nBoku tama ni, boku tama ni\nYume ni yabureru..."
tokens: [{ surface: "Get", reading: "Get", romaji: "Get", pos: "名詞" }, ...]
```

Re-running `03-generate-content.ts` (or the inline-Claude-Code equivalent via `print-lesson-prompt.ts`) would feed romaji to the LLM and yield romaji-token output again. The real fix is upstream: refetch canonical Japanese lyrics → re-tokenize via kuromoji → THEN regen the lesson. Out of scope for Wave 2.

User opted to defer rather than spend API credits or work the upstream fix. Mountain-a-go-go is now an expected Wave 3 failure handled by Plan 11.2-05's D-05 straggler triage.

## What changed

| Path | Change |
|------|--------|
| `scripts/seed/10b-derive-tv-lessons.ts` | renamed → `scripts/seed/_attic/10b-derive-tv-lessons-lcs.ts` |
| `scripts/seed/10b2-derive-tv-monotonic.ts` | renamed → `scripts/seed/_attic/10b2-derive-tv-monotonic.ts` |
| `data/lessons-cache/mountain-a-go-go-too-captain-straydum.json` | UNCHANGED (Task 1 deferred) |

## Followups for downstream plans

- **Plan 11.2-05 (Wave 3):** Expect `mountain-a-go-go-too-captain-straydum` to fail NW alignment. Do NOT block the batch on it. D-05 ladder triage applies. Document the 1/60 failure and proceed.
- **Phase 11.1 (Add-Song Pipeline) or follow-up:** Owns the real upstream fix — fetch canonical JP lyrics via `fetch-utanet-lyrics.ts` (currently in `git stash@{0}` as WIP) → kuromoji-retokenize → regen lesson via `print-lesson-prompt.ts`.
