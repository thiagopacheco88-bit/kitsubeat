---
phase: 11.2
plan: 05
type: execute
wave: 3
depends_on: [11.2-01, 11.2-02, 11.2-04]
files_modified:
  - data/audio-tv/
  - data/vocal-stems-tv/
  - data/timing-cache-tv-stem/
  - data/lessons-cache-tv-nw/
autonomous: false
requirements: [SPEC-REQ-1, SPEC-REQ-2, SPEC-REQ-6]

must_haves:
  truths:
    - "data/audio-tv/ contains 60 mp3 files (one per TV song)"
    - "data/vocal-stems-tv/htdemucs/ contains 60 vocal-stem subdirectories"
    - "data/timing-cache-tv-stem/ contains 60 JSON timing files"
    - "data/lessons-cache-tv-nw/ contains 60 LessonSchema-valid JSON files (one per TV song) including the 4 stragglers OR each straggler has a documented disposition recorded in the phase's STRAGGLER-DISPOSITIONS.md"
    - "Rerunning any pipeline step produces zero new work (idempotency holds across the batch)"
    - "WhisperX ran with PYTHONIOENCODING=utf-8 + .venv/Scripts on PATH (no Japanese-output crashes)"
    - "Default to ONE WhisperX instance per memory feedback_ask_before_parallel_whisper; user explicitly approved any deviation"
    - "Pre-flight gate (manifest count, disk free, WhisperX concurrency choice) is approved by the user BEFORE the 5-8h batch starts"
  artifacts:
    - path: "data/lessons-cache-tv-nw/"
      provides: "60 NW-derived TV lessons in the parallel rollout directory (D-08)"
    - path: ".planning/phases/11.2-tv-derive-rework-demucs-nw/STRAGGLER-DISPOSITIONS.md"
      provides: "Per-straggler outcome record per SPEC-REQ-6 (loaded-and-passing | replaced-youtube-id | hand-curated | dropped-from-catalog | deferred-with-rationale)"
  key_links:
    - from: "data/songs-manifest-tv.json"
      to: "data/audio-tv/{slug}.mp3"
      via: "tv-demucs-isolate.py"
      pattern: "tv-demucs-isolate"
    - from: "data/timing-cache-tv-stem/{slug}.json"
      to: "data/lessons-cache-tv-nw/{slug}.json"
      via: "10b-derive-tv-lessons-nw.ts"
      pattern: "10b-derive-tv-lessons-nw"
---

<objective>
Run the full TV-derive pipeline against all 60 TV songs (SPEC-REQ-1, SPEC-REQ-2). End state: 60 LessonSchema-valid JSON files in `data/lessons-cache-tv-nw/`, including resolutions for all 4 stragglers (SPEC-REQ-6). The 60-song batch is the largest single execution unit in this phase — estimated 5-8 hours wall-clock per SPEC.md constraints (Demucs CPU at ~0.3× realtime).

Purpose: This is the productive step. Plans 01-04 set up tooling; Plan 05 produces the artifacts that Plans 06-07 validate and ship. Stragglers get the D-05 fallback ladder (vocals-absent fast-path → YouTube replacement → hand-curate → drop with rationale).

Output: Populated `data/audio-tv/`, `data/vocal-stems-tv/`, `data/timing-cache-tv-stem/`, `data/lessons-cache-tv-nw/` directories (each with 60 entries — minus any straggler dropped from the catalog with rationale), plus a `STRAGGLER-DISPOSITIONS.md` recording the per-straggler outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11.2-tv-derive-rework-demucs-nw/11.2-SPEC.md
@.planning/phases/11.2-tv-derive-rework-demucs-nw/11.2-CONTEXT.md
@.planning/todos/pending/2026-04-18-fix-4-tv-lesson-derivation-stragglers.md
@scripts/seed/tv-demucs-isolate.py
@scripts/seed/tv-transcribe-stems.py
@scripts/seed/10b-derive-tv-lessons-nw.ts
@scripts/seed/10-prepare-tv.ts
@scripts/debug/rebuild-sign-flow-tv.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-flight gate before the 5-8h batch — manifest count, disk free, WhisperX concurrency</name>
  <what-built>
Before the executor sinks 5-8 hours of wall-clock time into Demucs + WhisperX over 60 songs, the user explicitly acknowledges the run parameters. A 5-8h auto task with no pre-flight gate is high-cost-of-error: manifest miscounts, low disk, or accidental parallel WhisperX invocations are MUCH cheaper to catch here than 4 hours into the batch.

This checkpoint surfaces three concrete numbers and one explicit decision; the user types `approved` to release the long-running batch (now Task 2), or `abort` / `wait` to halt before any compute spend.

Estimated checkpoint cost: <2 minutes of operator attention.
Estimated cost of skipping this checkpoint: 5-8 hours of wasted wall-clock on a run with bad inputs.
  </what-built>
  <how-to-verify>
The executor runs the following commands and surfaces the output to the user. The user reviews and approves.

1. **Manifest count (must equal 60 per SPEC-REQ-1):**
   ```bash
   node -e "console.log('manifest entries:', require('./data/songs-manifest-tv.json').length)"
   ```
   Expected: `manifest entries: 60`. If less, run `npx tsx scripts/seed/10-prepare-tv.ts` BEFORE this checkpoint and re-run the count.

2. **Disk free at data/ root (need >2GB for safety per Task 2 budget):**
   ```bash
   df -h . | awk 'NR==1 || NR==2'
   ```
   Surface the "Avail" column to the user. If <2GB, ABORT and free space before approving.

3. **WhisperX concurrency the executor will use (per memory `feedback_ask_before_parallel_whisper`):**
   - Default: **single WhisperX instance, sequential per song**.
   - The user MAY choose to run multiple WhisperX instances in parallel if hardware permits, but MUST approve that deviation explicitly.
   - The executor surfaces: "I plan to run ONE WhisperX instance sequentially across the 60 songs. Approve, or specify a parallelism count (e.g., 'use 2 parallel WhisperX instances')."

4. **mountain-a-go-go pre-fix sanity (Plan 04 dependency):**
   ```bash
   node -e "const l = require('./data/lessons-cache/mountain-a-go-go-too-captain-straydum.json'); const s = l.verses.flatMap(v => v.tokens || []).map(t => t.surface).find(x => x && x.trim()) || ''; console.log('mountain-a-go-go first surface:', s, '— is JP?', /[぀-ゟ゠-ヿ一-鿿]/.test(s));"
   ```
   Must report `is JP? true`. If false, ABORT — Plan 04 Task 1 did not land.

5. **sign-flow smoke artifact present (Plan 02 dependency):**
   ```bash
   test -f data/lessons-cache-tv-nw/sign-flow.json && echo "sign-flow smoke OK" || echo "sign-flow smoke MISSING"
   ```
   Must print `sign-flow smoke OK`. If missing, ABORT — Plan 02 Task 2 did not land.
  </how-to-verify>
  <resume-signal>
Type "approved" to release Task 2 with single-WhisperX-instance default. Type "approved — N parallel" (e.g., `approved — 2 parallel`) to release Task 2 with N parallel WhisperX instances (must justify with hardware capacity). Type "abort" to halt; the user remediates whatever the pre-flight surfaced (manifest, disk, or upstream plan). Type "wait" to defer; the executor pauses indefinitely.
  </resume-signal>
  <action>
Pause execution and surface the <what-built> and <how-to-verify> blocks above to the user. Run the five pre-flight commands and present their actual output to the user alongside the question. Do NOT proceed past this checkpoint until the user responds with the resume-signal token. Record the user's parallelism choice in the executor's session state — Task 2 will read it.
  </action>
  <verify>
    <automated>true</automated>
  </verify>
  <done>User has explicitly approved the run parameters: manifest is 60, disk is sufficient, WhisperX concurrency is locked, and Plan 02 + Plan 04 prerequisites are confirmed. Task 2 is released to spend the 5-8h.</done>
</task>

<task type="auto">
  <name>Task 2: Run the 60-song batch pipeline (download → demucs → transcribe → NW derive)</name>
  <files>
    data/audio-tv/,
    data/vocal-stems-tv/,
    data/timing-cache-tv-stem/,
    data/lessons-cache-tv-nw/
  </files>
  <read_first>
    - data/songs-manifest-tv.json (the input manifest — confirm 60 entries; if not 60, investigate before kicking off the batch)
    - .planning/phases/11.2-tv-derive-rework-demucs-nw/11.2-01-SUMMARY.md (CLI signatures from Plan 01 scripts)
    - .planning/phases/11.2-tv-derive-rework-demucs-nw/11.2-02-SUMMARY.md (CLI signatures + sign-flow smoke-test result from Plan 02)
  </read_first>
  <action>
**Pre-conditions assumed satisfied (Task 1 pre-flight gate passed):**
- `data/songs-manifest-tv.json` has 60 entries (verified at gate).
- `data/lessons-cache/mountain-a-go-go-too-captain-straydum.json` has Japanese tokens (verified at gate).
- `data/lessons-cache-tv-nw/sign-flow.json` exists (verified at gate).
- WhisperX concurrency choice was approved at the gate (default: ONE; per memory `feedback_ask_before_parallel_whisper`).
- Disk free was confirmed >2GB at the gate.

If any of these is now FALSE (e.g., the user moved files between gate and run), ABORT and bounce back to Task 1.

**Step 1: Download + isolate (idempotent — `--all`).**
```bash
python scripts/seed/tv-demucs-isolate.py --all 2>&1 | tee .planning/phases/11.2-tv-derive-rework-demucs-nw/batch-demucs.log
```
Estimated wall-clock: ~3-5h on CPU (htdemucs ~0.3× realtime; TV cuts are 60-90s, so ~5min/song × 60 = 5h). Already-downloaded mp3s skip; already-isolated stems skip.

**Step 2: Transcribe stems (idempotent — `--all`).**
```bash
PYTHONIOENCODING=utf-8 PATH=".venv/Scripts:$PATH" python scripts/seed/tv-transcribe-stems.py --all 2>&1 | tee .planning/phases/11.2-tv-derive-rework-demucs-nw/batch-transcribe.log
```
(The script also injects PATH and PYTHONIOENCODING internally per Plan 01 — the explicit env prefix is belt+braces given the memory feedback about WinError 2.) Estimated wall-clock: ~1-2h (WhisperX large-v3 ja is ~5× realtime on CPU; per-song ~15s).

If the user approved parallel WhisperX at the Task 1 gate (e.g., 2 parallel), launch N instances against disjoint slug subsets instead of `--all`. If unsure, fall back to single-instance (the safe default).

**Step 3: NW-derive (idempotent — `--all`).**
```bash
npx tsx scripts/seed/10b-derive-tv-lessons-nw.ts --all 2>&1 | tee .planning/phases/11.2-tv-derive-rework-demucs-nw/batch-derive.log
```
Estimated wall-clock: ~2-5min total (NW DP is fast; mostly file I/O). Each invocation produces `data/lessons-cache-tv-nw/{slug}.json`.

**Step 4: Verify counts.**
```bash
echo "audio-tv: $(ls data/audio-tv/*.mp3 2>/dev/null | wc -l)"
echo "vocal-stems-tv: $(ls -d data/vocal-stems-tv/htdemucs/*/ 2>/dev/null | wc -l)"
echo "timing-cache-tv-stem: $(ls data/timing-cache-tv-stem/*.json 2>/dev/null | wc -l)"
echo "lessons-cache-tv-nw: $(ls data/lessons-cache-tv-nw/*.json 2>/dev/null | wc -l)"
```
Target: each line shows 60 (or 60 minus any straggler dropped per Task 3's D-05 ladder).

**Step 5: Idempotency rerun.** Rerun all three pipeline steps. Each script must print only `[skip]` lines:
```bash
python scripts/seed/tv-demucs-isolate.py --all 2>&1 | grep -v "^\[skip" | head
python scripts/seed/tv-transcribe-stems.py --all 2>&1 | grep -v "^\[skip" | head
npx tsx scripts/seed/10b-derive-tv-lessons-nw.ts --all 2>&1 | grep -v "^\[skip" | head
```
The grep output must contain no `[N/M]` per-song processing lines after the rerun.

**Step 6: Identify any failures or low-confidence outputs.**
- For each slug, check `data/lessons-cache-tv-nw/{slug}.json` exists.
- For each slug present, parse and check no verse has `start_time_ms === 0 && end_time_ms === 0` for ALL verses (that would indicate the NW derive emitted everything as low-coverage — basically the new flavor of `[no_detected_verses]`).
- Build a candidate-stragglers list: any slug missing the file OR with all-zero timing.
- The 4 known stragglers from the todo: `my-answer-seamo`, `mountain-a-go-go-too-captain-straydum`, `newsong-tacica`, `whats-up-people-maximum-the-hormone`. mountain-a-go-go was pre-fixed in Plan 04, so it should now produce a normal lesson. Confirm.

If the candidate-stragglers list is non-empty, Task 3 (D-05 fallback ladder) handles them.
  </action>
  <acceptance_criteria>
    - `data/audio-tv/` contains exactly 60 .mp3 files (or 60 minus catalog drops with rationale documented in Task 3's STRAGGLER-DISPOSITIONS.md)
    - `data/vocal-stems-tv/htdemucs/` contains exactly 60 subdirectories each with a `vocals.wav`
    - `data/timing-cache-tv-stem/` contains exactly 60 .json files
    - `data/lessons-cache-tv-nw/` contains 60 .json files (or 60 minus documented drops)
    - All `data/lessons-cache-tv-nw/*.json` files pass `LessonSchema.safeParse` (run a one-liner that imports the schema and validates each)
    - Idempotency rerun: each pipeline script prints only skip messages (no `[N/M]` per-song lines after rerun)
    - Sign-flow's lesson in `data/lessons-cache-tv-nw/sign-flow.json` is unchanged from Plan 02's smoke-test version (same content hash)
    - mountain-a-go-go's NW lesson exists and has reasonable verse timing (NOT all-zero)
  </acceptance_criteria>
  <verify>
    <automated>node -e "
const fs = require('fs'), path = require('path');
const files = fs.readdirSync('data/lessons-cache-tv-nw').filter(f => f.endsWith('.json'));
console.log('lessons-cache-tv-nw count:', files.length);
const tvAudio = fs.readdirSync('data/audio-tv').filter(f => f.endsWith('.mp3')).length;
const tvStem = fs.readdirSync('data/timing-cache-tv-stem').filter(f => f.endsWith('.json')).length;
console.log('audio-tv:', tvAudio, 'timing-cache-tv-stem:', tvStem);
let valid = 0, allZero = [];
for (const f of files) {
  const l = JSON.parse(fs.readFileSync(path.join('data/lessons-cache-tv-nw', f), 'utf8'));
  if (!l.verses || !Array.isArray(l.verses) || l.verses.length === 0) { console.error('BAD:', f); process.exit(1); }
  const allZeroTiming = l.verses.every(v => v.start_time_ms === 0 && v.end_time_ms === 0);
  if (allZeroTiming) allZero.push(f);
  valid++;
}
console.log('valid lessons:', valid, '/', files.length);
console.log('all-zero-timing (need straggler triage):', allZero);
if (valid < 56) process.exit(2);
"</automated>
  </verify>
  <done>
The 60-song batch is complete. The four target output directories are populated with 60 entries each (minus any documented catalog drops). Sign-flow's lesson is preserved from Plan 02's smoke test. The candidate-stragglers list (any slug with missing or all-zero output) is captured in batch-derive.log for Task 3's triage. Idempotency holds.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Triage straggler outcomes via D-05 fallback ladder</name>
  <decision>For each straggler that DID NOT produce a usable lesson in Task 2, apply the D-05 fallback ladder. The user must approve the disposition for each one.</decision>
  <context>
SPEC-REQ-6 requires every straggler to land in one of: `loaded-and-passing` | `loaded-and-flagged-by-audit-with-rationale` | `removed-from-catalog-with-rationale` | `deferred-with-rationale`. No silent unresolved state.

**The 4 known stragglers + expected outcomes:**

| Slug | Pre-fix? | Likely outcome |
|------|----------|----------------|
| mountain-a-go-go-too-captain-straydum | YES (Plan 04) | loaded-and-passing |
| my-answer-seamo | NO | NW result depends on whether the TV cut is genuinely a different song or the same song re-arranged. Likely D-05 step 1 (replace YouTube ID) or step 3 (drop). |
| newsong-tacica | NO | TV cut is "ご視聴ありがとうございました" outro voiceover — vocals absent. D-05 step 0: skip directly to step 3 (drop with rationale) OR step 1 (replace ID). |
| whats-up-people-maximum-the-hormone | NO | Mixed English+Japanese ("What's up with my mind? アーメンボール!"). NW may partially align; depends on coverage threshold. |

**D-05 ladder (per straggler):**
- **Step 0 — Vocals-absent fast-path:** Inspect `data/timing-cache-tv-stem/{slug}.json`. If WhisperX produced essentially no Japanese (silence, English-only, outro voiceover), skip to step 3.
- **Step 1 — YouTube replacement:** Run `npx tsx scripts/seed/find-youtube-replacements.ts --slug X` to find a working replacement. If found and approved, update `song_versions.youtube_id` (note: this writes to DB; isolate from rollout to keep changes traceable), rerun the pipeline on the slug. (Per CONTEXT.md SPEC out-of-scope: "Replacing TV YouTube source IDs at the catalog level is handled separately by find-youtube-replacements.ts; this phase only invokes that workflow per-straggler.")
- **Step 2 — Hand-curate via segment-anchor:** Model after `scripts/debug/rebuild-sign-flow-tv.ts`. Per Claude's discretion in CONTEXT.md: extract into `scripts/seed/lib/segment-anchor.ts` only if ≥2 stragglers need it. If only 0-1 stragglers reach this step, keep inline in a phase-specific debug script.
- **Step 3 — Drop with rationale:** `DELETE FROM song_versions WHERE version_type='tv' AND song_id=...`. Document why in STRAGGLER-DISPOSITIONS.md. Verify `SongContent.tsx` hides the TV/Full toggle when only one version exists (per CONTEXT.md deferred note); if not, file a small frontend follow-up.
  </context>
  <options>
    <option id="ladder-walkthrough">
      <name>Walk the ladder per straggler in order; user approves each disposition</name>
      <pros>Deterministic; matches D-05 exactly; per-straggler decision recorded</pros>
      <cons>Requires user attention per straggler — but that is the intent of SPEC-REQ-6</cons>
    </option>
    <option id="batch-disposition">
      <name>Walk the ladder, then present all dispositions in one batch for user approval</name>
      <pros>Single approval interrupt</pros>
      <cons>Higher rollback cost if user disagrees with a mid-ladder decision (e.g., "no, do not drop newsong-tacica yet — try replacement first")</cons>
    </option>
  </options>
  <action-after-decision>
After the user picks an approach (default: ladder-walkthrough), the executor:

1. **For each straggler with a usable NW lesson from Task 2:** record outcome `loaded-and-passing` in STRAGGLER-DISPOSITIONS.md. No ladder needed.

2. **For each straggler that failed (all-zero timing or missing file):** apply the ladder per the user's choice. After each ladder step, re-run the pipeline on that one slug and confirm the output.

3. **Write `.planning/phases/11.2-tv-derive-rework-demucs-nw/STRAGGLER-DISPOSITIONS.md`** with a row per straggler:
   ```markdown
   | Slug | Outcome | Rationale | Lesson file |
   |------|---------|-----------|-------------|
   | my-answer-seamo | replaced-youtube-id | Original TV cut was different recording; replacement passed pipeline | data/lessons-cache-tv-nw/my-answer-seamo.json |
   | newsong-tacica | removed-from-catalog | TV cut is YouTube outro voiceover — no vocals to align. song_versions row deleted on YYYY-MM-DD. | (n/a) |
   | ... | ... | ... | ... |
   ```

4. **Close the folded todo:** `git mv .planning/todos/pending/2026-04-18-fix-4-tv-lesson-derivation-stragglers.md .planning/todos/done/2026-04-18-fix-4-tv-lesson-derivation-stragglers.md` (with a one-line postscript pointing at this phase's STRAGGLER-DISPOSITIONS.md). Per CONTEXT.md "Folded Todos" note.

5. **If a straggler was dropped via DELETE on song_versions:** record the song_id + slug + delete timestamp in STRAGGLER-DISPOSITIONS.md so the snapshot in Plan 03/07 can be cross-referenced.
  </action-after-decision>
  <resume-signal>
For each straggler outcome, type `approve {slug}={outcome}` (e.g., `approve newsong-tacica=removed-from-catalog`). Or `approve all` after reviewing the proposed dispositions. Type `revisit {slug}` to step back up the ladder.
  </resume-signal>
  <action>
Pause execution and surface the <what-built> and <how-to-verify> blocks above to the user. Do NOT proceed past this checkpoint until the user responds with the resume-signal token. On approval, continue to the next task. On rejection, halt and surface the issue to the user for remediation.
  </action>
  <verify>
    <automated>true</automated>
  </verify>
  <done>User has typed the resume-signal token (or otherwise unambiguously approved); the work in <what-built> is acknowledged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External YouTube → local pipeline | yt-dlp downloads from YouTube IDs in `song_versions` (already trusted catalog data) |
| Local pipeline → DB writes | D-05 step 3 issues a DELETE on `song_versions`; step 1 may issue an UPDATE on `youtube_id` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.2-14 | Tampering | Batch produces 60 lessons; if any are quietly degenerate, Plan 06 audit catches them but only on lessons that wrote successfully | mitigate | Task 2 acceptance script counts files + validates schema; Task 2 verifier identifies all-zero-timing lessons as candidate stragglers; Plan 06 audit + spot-check is the gate before rollout |
| T-11.2-15 | Tampering | D-05 step 3 DELETEs from song_versions; permanent without rollback | mitigate | Plan 03's snapshot script captures pre-delete state if run before any DELETE; STRAGGLER-DISPOSITIONS.md records the song_id + slug for forensic recovery; user-approval checkpoint gates each delete |
| T-11.2-16 | Denial of service | 5-8h batch run is interruptable; resuming requires idempotency to hold | mitigate | All three scripts (Plan 01 + Plan 02) skip-if-exists by default; Task 2 Step 5 verifies idempotency post-run |
| T-11.2-17 | Information disclosure | Batch logs (batch-demucs.log etc.) may include slug/youtube_id pairs | accept | Catalog data is public; logs land under `.planning/phases/.../` and may be committed; no credentials in logs |
| T-11.2-17b | Denial of service | Batch launched against bad inputs (manifest miscount, low disk, accidental parallel WhisperX) wastes 5-8h of wall-clock | mitigate | Task 1 pre-flight checkpoint:human-verify gate surfaces manifest count, disk free, concurrency choice, and upstream-plan sanity BEFORE compute spend |

ASVS L1: human-approval gate (Task 1 pre-flight + Task 3 disposition checkpoint) on the largest cost-of-error operations; pre-rollout snapshot in Plan 03 captures pre-state for rollback; idempotent reruns prevent partial-batch corruption.
</threat_model>

<verification>
- Task 1 pre-flight gate approved before compute spend
- All four target directories have 60 entries (or 60 minus documented drops)
- Idempotency rerun produces zero work
- All output JSON passes LessonSchema.safeParse
- STRAGGLER-DISPOSITIONS.md exists with a row per straggler
- The folded todo has been moved to `.planning/todos/done/`
</verification>

<success_criteria>
- SPEC-REQ-1 acceptance: `data/audio-tv/`, `data/vocal-stems-tv/htdemucs/`, `data/timing-cache-tv-stem/` each contain 60 entries; rerunning produces zero work
- SPEC-REQ-2 acceptance: 60 LessonSchema-valid lessons in `data/lessons-cache-tv-nw/`; zero `[no_detected_verses]` outputs (low-coverage verses still emit, just with degraded timing)
- SPEC-REQ-6 acceptance: each straggler has a documented outcome in STRAGGLER-DISPOSITIONS.md
- Pre-flight gate (Task 1) approved by user before the 5-8h batch (Task 2) ran
</success_criteria>

<output>
After completion, create `.planning/phases/11.2-tv-derive-rework-demucs-nw/11.2-05-SUMMARY.md` documenting: pre-flight gate outcome (manifest count, disk free, WhisperX concurrency choice), actual wall-clock time per pipeline step, any deviations from idempotency (and how they were resolved), the exact straggler dispositions, and any decisions about the segment-anchor.ts extraction (D-05 step 2 — extracted only if ≥2 stragglers needed it).
</output>
</content>
</invoke>