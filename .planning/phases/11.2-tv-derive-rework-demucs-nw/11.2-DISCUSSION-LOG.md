# Phase 11.2: TV-Derive Rework with Demucs + Needleman-Wunsch — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-26
**Phase:** 11.2-tv-derive-rework-demucs-nw
**Areas discussed:** Pipeline script shape + legacy fate, Straggler triage workflow, Spot-check selection + audit floor, Catalog rollout staging
**SPEC.md status:** Loaded (6 requirements locked) — discussion focused on HOW only

---

## Context Setup

### Existing CONTEXT.md handling

| Option | Description | Selected |
|--------|-------------|----------|
| Update it — reframe as implementation decisions | Rewrite CONTEXT.md to focus on HOW; old solution-shape content folded in or dropped where redundant with SPEC | ✓ |
| View it first | Show existing CONTEXT.md, then decide | |
| Skip — leave existing CONTEXT.md as-is | Use the existing draft unchanged | |

**User's choice:** Update it — reframe as implementation decisions.

---

## Pipeline script shape + legacy fate

### Q1: TV pipeline structure relative to existing ab-* scripts

| Option | Description | Selected |
|--------|-------------|----------|
| New files mirroring ab-* | scripts/seed/tv-demucs-isolate.py + tv-transcribe-stems.py — clear separation, no flag-gymnastics | ✓ |
| --tv flag on existing ab-* scripts | Modify ab-* to accept --tv, switching paths internally — one source of truth, mixes flows | |
| Inline into 10b/10c family | Add 10aa/10ab numbered TV pipeline steps — keeps numbering contiguous, copies ab-* logic | |

**User's choice:** New files mirroring ab-*.

### Q2: NW derive script replacement strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Replace 10b in place | Rewrite scripts/seed/10b-derive-tv-lessons.ts with NW; old code in git history | |
| New file 10b-derive-tv-lessons-nw.ts; archive old | Ship NW as new script; move old to scripts/seed/_attic/10b-derive-tv-lessons-lcs.ts | ✓ |
| 10b stays, NW lives at 10b3-derive-tv-lessons-nw.ts | Keep 10b alongside 10b2 + add 10b3 NW — three coexisting derive scripts | |

**User's choice:** New file + archive old (recommended given LCS is documented dead end).

### Q3: Audit script location and name

| Option | Description | Selected |
|--------|-------------|----------|
| scripts/seed/audit-tv-lessons.ts | Sits next to existing audit-* scripts (audit-catalog, audit-corrupted-lyrics, audit-lesson-coverage) | ✓ |
| scripts/seed/10d-audit-tv-lessons.ts | Numbered as next step in TV pipeline after 10c | |
| scripts/audit/tv-lessons.ts (new dir) | New top-level audit/ directory if multiple audit scripts will land | |

**User's choice:** "go with recommendations" → scripts/seed/audit-tv-lessons.ts (option 1, matches existing convention).

---

## Straggler triage workflow

### Q1: Sequencing of NW + per-song fixes for the 4 stragglers

| Option | Description | Selected |
|--------|-------------|----------|
| Pre-fix mountain-a-go-go upstream first, then run full pipeline | Regen full-version lesson with proper Japanese tokens BEFORE NW; removes one variable | |
| Run NW on all 4 first, then triage based on outcome | Black-box test all 60 (incl. 4); per-failure decision after | |
| Hybrid: pre-fix mountain-a-go-go; let NW try the other 3 | Surgical: fix the unavoidable case, let NW test itself on the harder cases | ✓ |

**User's choice:** "go with recommendations" → Hybrid (option 3, recommended).

### Q2: Per-song fallback ranking for post-NW failures

| Option | Description | Selected |
|--------|-------------|----------|
| Replace YouTube ID first, hand-curate as last resort | Order: YouTube replacement → hand-curate via segment-anchor → drop with rationale | ✓ (with addition) |
| Hand-curate first, drop if unfixable | Order: hand-curate → drop; skips YouTube replacement | |
| Drop fast, defer fix to a follow-up todo | Mark TV row with sentinel or NULL; open fresh todo | |

**User's choice:** "go with recommendation, but if it has no vocal, it should be deleted, right?"
**Notes:** Augmented option 1 with a fast-path Step 0 — if the WhisperX transcript shows the TV cut has no usable Japanese vocals (silence/credits/karaoke), skip directly to delete. Final ladder captured in CONTEXT.md as D-05 (steps 0–3).

### Q3: Drop policy when a song has to be removed from TV catalog

| Option | Description | Selected |
|--------|-------------|----------|
| Delete the song_versions tv row entirely | DELETE FROM song_versions WHERE version_type='tv' AND slug=X — frontend toggle hides naturally | ✓ |
| Keep the row, mark lesson as dropped sentinel | Keep row + new column flag (tv_status='unavailable'); UI shows message | |
| Keep row with lesson=NULL, frontend hides toggle on null lesson | Keep row, leave lesson=NULL, update SongContent.tsx to hide toggle on null | |

**User's choice:** Delete the song_versions tv row entirely.

---

## Spot-check selection + audit floor

### Q1: How are the 8 spot-check songs selected?

| Option | Description | Selected |
|--------|-------------|----------|
| Adversarial — known-tricky cases at each level | Songs that stress alignment: chorus repetition, fast vocals, mixed JP/EN; sign-flow included | ✓ |
| Popularity-weighted — top 2 TV songs at each level | Reflects what users hit; may miss edge cases | |
| Mixed — 1 popular + 1 adversarial per level | Best of both: validate user-impact AND alignment difficulty | |
| User picks specific slugs | I'll provide the 8 slugs explicitly during plan/execute | |

**User's choice:** Adversarial.

### Q2: Audit verse-density floor

| Option | Description | Selected |
|--------|-------------|----------|
| 0.08 verses/sec (catches sign-flow comfortably) | 5/85=0.059 flagged; 8/90=0.089 passes | ✓ |
| 0.10 verses/sec (stricter) | Risk of false positives on legitimate 8-verse OPs | |
| 0.06 verses/sec (looser, sign-flow on the edge) | 5/80=0.0625 passes — too loose | |
| Add a min-verse-count of 8 alongside | Hybrid: density < 0.08 OR verse_count < 8 OR any verse > 15s | |

**User's choice:** "go with recommendations" → 0.08 verses/sec (option 1).

---

## Catalog rollout staging

### Q1: Promotion path for 60 NW-derived TV lessons

| Option | Description | Selected |
|--------|-------------|----------|
| Parallel directory, diff-and-promote | data/lessons-cache-tv-nw/ + audit + spot-check + rename swap → 10c | ✓ |
| In-place overwrite, audit-gated per song | NW overwrites data/lessons-cache-tv/{slug}.json; per-song audit gates each | |
| Blow-away + rederive fresh | rm -rf data/lessons-cache-tv/, run NW from scratch | |

**User's choice:** "go with recommendations" → Parallel directory, diff-and-promote (option 1).

### Q2 (initial): Rollback path

| Option | Description | Selected |
|--------|-------------|----------|
| Git history of data/lessons-cache-tv/ + manual 10c rerun | Standard git checkout + 10c — assumes lessons-cache-tv is committed | (re-asked) |
| Snapshot tv lesson rows pre-rollout into a backup table/file | Dump song_versions.lesson before 10c runs catalog-wide | |
| Just re-derive from scratch | Fix-forward only; pipeline is deterministic + idempotent | |

**Issue surfaced:** data/ is gitignored (line 55 of .gitignore). The "git history rollback" path is broken. Re-asked with corrected options.

### Q2 (re-asked): Rollback path (corrected)

| Option | Description | Selected |
|--------|-------------|----------|
| Snapshot song_versions.lesson pre-rollout to a backup JSON in .planning/ | Phase artifact, durable; restore via small TS script | |
| Fix-forward only — re-derive deterministically | No snapshot; rerun pipeline on regressed slug — practical, can't recover from NW algorithm regression | |
| Both — snapshot + fix-forward (Recommended) | Snapshot as escape hatch; fix-forward as daily tool | ✓ |

**User's choice:** Both — snapshot + fix-forward.

---

## Wrap-up

| Option | Description | Selected |
|--------|-------------|----------|
| I'm ready for context | Write CONTEXT.md now | ✓ |
| One more area: Phase 11.1 sequencing | Discuss whether 11.2 can start before 11.1 SPEC locks | |
| One more area: WhisperX/Demucs operational details | Lock environment specifics | |
| Explore more gray areas | Generate 2-3 additional based on what surfaced | |

**User's choice:** I'm ready for context.

**Phase 11.1 sequencing** captured in CONTEXT.md `<deferred>` section as a likely false-positive coupling for plan-phase to verify.
**WhisperX/Demucs operational details** captured in CONTEXT.md D-01 (PYTHONIOENCODING, .venv/Scripts on PATH) and `<deferred>` (parallelization policy per memory).

---

## Claude's Discretion

Areas captured in CONTEXT.md `<decisions>` → Claude's Discretion:
- NW scoring tuning (start uniform; escalate to phoneme-class only if spot-check fails)
- Audit run mode (DB canonical; optional --from-disk flag)
- Segment-anchor utility extraction threshold (≥2 stragglers needed)
- WhisperX batch size (default 16, lower if RAM-constrained)
- TV duration source for audit (lesson last verse OR stem timing — implementer's call)

## Deferred Ideas

Ideas surfaced during discussion that belong in other phases or follow-ups (full text in CONTEXT.md `<deferred>`):
- Phase 11.1 sequencing — likely false-positive coupling; plan-phase verifies
- WhisperX parallelization policy — ask before parallel runs (per user memory)
- NW phoneme-class scoring escalation
- segment-anchor.ts extraction (defer until 2+ stragglers need it)
- Add new TV pipeline to docs/SONG_INGESTION_SOP.md (post-phase docs pass)
- SongContent.tsx null-lesson toggle handling (frontend follow-up if any straggler is dropped)
