---
phase: 11.2
plan: 04
type: execute
wave: 2
depends_on: [11.2-02]
files_modified:
  - data/lessons-cache/mountain-a-go-go-too-captain-straydum.json
  - scripts/seed/_attic/10b-derive-tv-lessons-lcs.ts
  - scripts/seed/_attic/10b2-derive-tv-monotonic.ts
autonomous: false
requirements: [SPEC-REQ-6]

must_haves:
  truths:
    - "data/lessons-cache/mountain-a-go-go-too-captain-straydum.json has Japanese kana/kanji tokens (NOT romaji) — root cause for the 0% TV-overlap is removed"
    - "scripts/seed/10b-derive-tv-lessons.ts moved to scripts/seed/_attic/10b-derive-tv-lessons-lcs.ts (renamed, not deleted) per D-02"
    - "scripts/seed/10b2-derive-tv-monotonic.ts moved to scripts/seed/_attic/ verbatim per D-02"
    - "ANTHROPIC_API_KEY presence is verified before invoking 03-generate-content.ts; if absent or credits unavailable, a checkpoint:decision pauses the plan rather than failing mid-call"
    - "The user has visually verified the regenerated mountain-a-go-go full-version lesson before the 60-song batch runs"
  artifacts:
    - path: "data/lessons-cache/mountain-a-go-go-too-captain-straydum.json"
      provides: "Full-version lesson with Japanese tokens (post-regen)"
      contains: "verses"
    - path: "scripts/seed/_attic/10b-derive-tv-lessons-lcs.ts"
      provides: "Archived LCS implementation (referenceable, not invoked)"
      min_lines: 400
    - path: "scripts/seed/_attic/10b2-derive-tv-monotonic.ts"
      provides: "Archived windowed-LCS dead end"
      min_lines: 100
  key_links:
    - from: "scripts/seed/10b-derive-tv-lessons.ts"
      to: "scripts/seed/_attic/10b-derive-tv-lessons-lcs.ts"
      via: "git mv (rename)"
      pattern: "_attic"
---

<objective>
Two pre-batch fixups that must land BEFORE Plan 11.2-05 (60-song NW batch run):

1. **D-04 mountain-a-go-go pre-fix:** Regenerate the full-version lesson for `mountain-a-go-go-too-captain-straydum` with Japanese (kana/kanji) tokens instead of romaji. The TV failure for this song is upstream — the full lesson tokens were generated as romaji ("Get up Sumimasen Bokutama ni..."), giving guaranteed 0% NW overlap with the TV transcript ("僕たまに僕たまに夢にあふれる..."). NW cannot fix this; the fix is to rerun `03-generate-content.ts` for this slug forcing JP tokens. Once fixed, NW handles the song like any other in the batch.

2. **D-02 archive old LCS scripts to `_attic/`:** Move `scripts/seed/10b-derive-tv-lessons.ts` → `scripts/seed/_attic/10b-derive-tv-lessons-lcs.ts` (rename) and `scripts/seed/10b2-derive-tv-monotonic.ts` → `scripts/seed/_attic/10b2-derive-tv-monotonic.ts`. The new NW script in Plan 11.2-02 is the canonical replacement.

Purpose: SPEC-REQ-6 (stragglers). The 4 stragglers go into the batch in Plan 05; mountain-a-go-go MUST have its upstream data fixed first or it will fail NW too. The other 3 stragglers (`my-answer-seamo`, `newsong-tacica`, `whats-up-people-maximum-the-hormone`) go through the new pipeline as normal songs and get D-05 fallback ladder triage in Plan 05.

Output: A regenerated `data/lessons-cache/mountain-a-go-go-too-captain-straydum.json` with Japanese tokens, plus two scripts archived to `_attic/`. Order matters: regen first (slow — calls Claude API), archive second (fast).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11.2-tv-derive-rework-demucs-nw/11.2-SPEC.md
@.planning/phases/11.2-tv-derive-rework-demucs-nw/11.2-CONTEXT.md
@.planning/todos/pending/2026-04-18-fix-4-tv-lesson-derivation-stragglers.md
@scripts/seed/03-generate-content.ts
@data/lessons-cache/mountain-a-go-go-too-captain-straydum.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate mountain-a-go-go full-version lesson with Japanese tokens (D-04 pre-fix)</name>
  <files>data/lessons-cache/mountain-a-go-go-too-captain-straydum.json</files>
  <read_first>
    - data/lessons-cache/mountain-a-go-go-too-captain-straydum.json (current state — confirm tokens are romaji "Bokutama ni..." not kanji "僕たまに...")
    - scripts/seed/03-generate-content.ts (the existing content-generation flow; identify the entrypoint that takes a slug and regenerates the lesson)
    - .planning/todos/pending/2026-04-18-fix-4-tv-lesson-derivation-stragglers.md (item #2 — captures the root cause: romaji-instead-of-Japanese tokens in the full lesson)
  </read_first>
  <action>
**Pre-flight (before invoking 03-generate-content.ts):**

D-04 is a locked exception to the project memory `feedback_local_only_tooling` (which prohibits new API spend). The exception is approved, but a silent mid-execution failure if credits are zero is NOT acceptable. Verify credentials BEFORE the API call:

1. **Confirm ANTHROPIC_API_KEY is present:**
   ```bash
   grep -q "^ANTHROPIC_API_KEY=" .env.local || { echo "ABORT: ANTHROPIC_API_KEY missing from .env.local"; exit 1; }
   ```
   If the variable is absent OR is set but empty, escalate.

2. **If credentials are absent OR the user reports credits are unavailable, escalate via `checkpoint:decision`** with the following options (do NOT just fail):
   - **(a) top up credits** — user funds the Anthropic account, re-runs Task 1 from this pre-flight step
   - **(b) use Ollama local LLM via a compatible script** — adapt `scripts/seed/03-generate-content.ts` (or write a sibling) to call a local model (qwen2.5:14b-instruct or similar, per memory `feedback_local_only_tooling`). Output must conform to LessonSchema; the rest of Task 1's validation chain runs unchanged
   - **(c) hand-edit the JSON** — manually replace the romaji tokens in `data/lessons-cache/mountain-a-go-go-too-captain-straydum.json` with JP equivalents from the song's published lyrics; LessonSchema.safeParse must still pass

3. **Do NOT proceed to Step 1 (the regen call) until credentials are confirmed OR an alternative path (b/c) is approved.**

**Step 1 — Confirm the diagnosis.** Read `data/lessons-cache/mountain-a-go-go-too-captain-straydum.json`. Confirm `verses[0].tokens[0].surface` (or equivalent first-token surface) starts with romaji like `Bokutama` / `Get up` / `Sumimasen` rather than Japanese kana/kanji like `僕` / `たまに` / `夢`. If the lesson ALREADY has Japanese tokens, skip ahead to the verification subtask: this means the upstream regeneration already happened — log that finding and skip the regen invocation.

**Step 2 — Regenerate using `scripts/seed/03-generate-content.ts`.** Read the script header / argparse to find the slug-targeting flag (likely `--slug X` per existing convention). Run the regeneration:

```bash
npx tsx scripts/seed/03-generate-content.ts --slug mountain-a-go-go-too-captain-straydum --force
```

(`--force` per the same convention as `10b/10c` to bypass any cache that would short-circuit the regen.)

If `03-generate-content.ts` requires additional flags to force JP tokens (e.g., a language hint, a model override), pass them. If the script does NOT have an option to force JP-token output and the regen produces romaji again, escalate via a `checkpoint:decision` (see Task 2 below) — that means a deeper fix into `03-generate-content.ts` itself is needed and we should not silently ship.

**Step 3 — Validate output.**
- Confirm `data/lessons-cache/mountain-a-go-go-too-captain-straydum.json` first-token surface is now Japanese (kana or kanji code points: 0x3040-0x309F, 0x30A0-0x30FF, 0x4E00-0x9FFF). A simple node check works:
  ```bash
  node -e "
    const l = require('./data/lessons-cache/mountain-a-go-go-too-captain-straydum.json');
    const firstSurface = l.verses[0].tokens.find(t => t.surface)?.surface || '';
    const isJapanese = /[぀-ゟ゠-ヿ一-鿿]/.test(firstSurface);
    if (!isJapanese) { console.error('STILL ROMAJI:', firstSurface); process.exit(1); }
    console.log('OK: Japanese tokens confirmed:', firstSurface);
  "
  ```
- Also confirm the regenerated file passes `LessonSchema.safeParse` (run a one-liner that imports the schema).

**Step 4 — Commit guard:** the regenerated file lives in `data/lessons-cache/` which IS gitignored (`data/` exclusion). That is fine — the lesson data is regenerated from authoritative sources. Do NOT add it to git. The TV pipeline downstream consumes it from disk.
  </action>
  <acceptance_criteria>
    - ANTHROPIC_API_KEY presence verified before regen invocation; no silent failure if credits unavailable
    - If credentials were absent, the executor paused via checkpoint:decision and the user picked (a) top-up, (b) Ollama, or (c) hand-edit; the chosen path was logged in the plan summary
    - File `data/lessons-cache/mountain-a-go-go-too-captain-straydum.json` exists and is non-empty
    - First non-empty `tokens[].surface` in the lesson contains at least one character in the Japanese Unicode ranges (U+3040-U+309F hiragana, U+30A0-U+30FF katakana, or U+4E00-U+9FFF CJK)
    - The file passes `LessonSchema.safeParse` (zod validation succeeds)
    - File contains the literal string `mountain-a-go-go` somewhere (sanity — confirms it's the right slug's lesson)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "^ANTHROPIC_API_KEY=" .env.local && node -e "const l = require('./data/lessons-cache/mountain-a-go-go-too-captain-straydum.json'); const firstSurface = l.verses.flatMap(v => v.tokens || []).map(t => t.surface).find(s => s && s.trim()) || ''; if (!/[぀-ゟ゠-ヿ一-鿿]/.test(firstSurface)) { console.error('STILL ROMAJI:', firstSurface); process.exit(1); } console.log('OK:', firstSurface);"</automated>
  </verify>
  <done>
mountain-a-go-go's full-version lesson has Japanese tokens. Credential pre-flight ran before any API spend. The TV pipeline in Plan 11.2-05 will be able to NW-align this song's TV transcript against meaningful Japanese chars (no longer 0% overlap by construction).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human spot-check the regenerated mountain-a-go-go lesson</name>
  <what-built>
mountain-a-go-go-too-captain-straydum's full-version lesson was regenerated by Task 1 to use Japanese tokens. Before kicking off the 60-song NW batch (Plan 05) — which is a 5-8 hour wall-clock investment — the user should eyeball the regen for quality:
- Verses are sensible (lyrics match what the user knows of the song).
- Translations are present and readable.
- Vocabulary entries are non-empty.
- No obvious LLM hallucination (e.g., wrong song's lyrics).

If the regen looks bad, this is the cheap point to retry `03-generate-content.ts` with different parameters before sinking 5-8 hours into a doomed batch.
  </what-built>
  <how-to-verify>
1. Open `data/lessons-cache/mountain-a-go-go-too-captain-straydum.json` in an editor (or pretty-print: `node -e "console.log(JSON.stringify(require('./data/lessons-cache/mountain-a-go-go-too-captain-straydum.json'), null, 2))" | head -200`).
2. Eyeball `verses[0..4]`: surfaces should be Japanese kana/kanji, translations should be English, tokens should look like real lyric breakdowns.
3. (Optional) Cross-reference the song's actual lyrics on the artist's published source — sanity check the LLM did not hallucinate a different song.
4. Look up the song on YouTube briefly; the verses should match what is sung in the recording.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Task 3 (archive). Type any other response to flag a problem; the user should then re-run `03-generate-content.ts` with different params and re-verify.</resume-signal>
  <action>
Pause execution and surface the <what-built> and <how-to-verify> blocks above to the user. Do NOT proceed past this checkpoint until the user responds with the resume-signal token. On approval, continue to the next task. On rejection, halt and surface the issue to the user for remediation.
  </action>
  <verify>
    <automated>true</automated>
  </verify>
  <done>User has typed the resume-signal token (or otherwise unambiguously approved); the work in <what-built> is acknowledged.</done>
</task>

<task type="auto">
  <name>Task 3: Archive old LCS scripts to _attic/ (D-02 cleanup, AFTER mountain-a-go-go pre-fix is approved)</name>
  <files>
    scripts/seed/_attic/10b-derive-tv-lessons-lcs.ts,
    scripts/seed/_attic/10b2-derive-tv-monotonic.ts
  </files>
  <read_first>
    - scripts/seed/10b-derive-tv-lessons.ts (the file being moved — verify it is the LCS version, 499 lines)
    - scripts/seed/10b2-derive-tv-monotonic.ts (the windowed-LCS dead end being archived, 175 lines)
  </read_first>
  <action>
**Per D-02 (CONTEXT.md):** archive the LCS implementations to `scripts/seed/_attic/`. The new NW script (`10b-derive-tv-lessons-nw.ts` from Plan 02) is the canonical replacement.

**Pre-flight:** confirm `scripts/seed/10b-derive-tv-lessons-nw.ts` already exists from Plan 02 (depends_on=[11.2-02] in this plan's frontmatter enforces ordering). If it does not exist, abort with a clear error — moving 10b before its replacement is in place would break anyone running scripts from this branch.

**Move sequence (use `git mv` so the rename is tracked):**

```bash
mkdir -p scripts/seed/_attic

# 1. Rename the LCS script so the archived name documents what it was.
git mv scripts/seed/10b-derive-tv-lessons.ts scripts/seed/_attic/10b-derive-tv-lessons-lcs.ts

# 2. Move the windowed-LCS dead end verbatim (already named -monotonic; D-02 keeps the name).
git mv scripts/seed/10b2-derive-tv-monotonic.ts scripts/seed/_attic/10b2-derive-tv-monotonic.ts
```

**Add a one-line README guard inside `scripts/seed/_attic/` if missing**, so future readers know what this directory is for:

```bash
test -f scripts/seed/_attic/README.md || cat > scripts/seed/_attic/README.md <<'EOF'
# _attic/

Archived seed scripts that are no longer invoked by the active pipeline but are
preserved for historical reference (e.g., to compare algorithm choices against
the version currently shipped). Do NOT add new working scripts here. Do NOT
import from these files in active scripts.

See git history for the move that placed each file here.
EOF
```

**Search-and-replace audit:** after the move, grep the codebase for any active import or reference to the old paths. There should be NONE. Allowed exceptions: comments, .planning/ docs, this PLAN.md, the SUMMARY files.

```bash
# Should return no hits in scripts/, src/, or active runtime:
git grep -n "scripts/seed/10b-derive-tv-lessons\.ts" -- 'scripts/' 'src/'
git grep -n "scripts/seed/10b2-derive-tv-monotonic\.ts" -- 'scripts/' 'src/'

# References within .planning/ documenting the move are FINE.
```

If any active script still imports the old file path, escalate — that means there is a hidden coupling we did not anticipate. Do NOT just patch the import to point at `_attic/`; that defeats the archive.

**Do NOT delete the files** — git mv preserves them. The point of `_attic/` is preservation.
  </action>
  <acceptance_criteria>
    - File `scripts/seed/_attic/10b-derive-tv-lessons-lcs.ts` exists
    - File `scripts/seed/_attic/10b2-derive-tv-monotonic.ts` exists
    - File `scripts/seed/10b-derive-tv-lessons.ts` does NOT exist (it was moved/renamed)
    - File `scripts/seed/10b2-derive-tv-monotonic.ts` does NOT exist (it was moved)
    - File `scripts/seed/10b-derive-tv-lessons-nw.ts` DOES exist (sanity — Plan 02's output is in place)
    - No active import of the old paths under `scripts/` or `src/` (grep returns zero)
    - File `scripts/seed/_attic/README.md` exists (informational guard)
  </acceptance_criteria>
  <verify>
    <automated>test -f scripts/seed/_attic/10b-derive-tv-lessons-lcs.ts && test -f scripts/seed/_attic/10b2-derive-tv-monotonic.ts && test ! -f scripts/seed/10b-derive-tv-lessons.ts && test ! -f scripts/seed/10b2-derive-tv-monotonic.ts && test -f scripts/seed/10b-derive-tv-lessons-nw.ts && test -f scripts/seed/_attic/README.md && ! git grep -q "scripts/seed/10b-derive-tv-lessons\.ts" -- 'scripts/' 'src/' && ! git grep -q "scripts/seed/10b2-derive-tv-monotonic\.ts" -- 'scripts/' 'src/'</automated>
  </verify>
  <done>
The two LCS scripts are archived to `_attic/`; only the new NW script is callable from the active pipeline. No active code imports the old paths.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude API → local lesson cache | Task 1 invokes 03-generate-content.ts which calls the Claude API; output written to gitignored `data/lessons-cache/` |
| Local file rename → git history | Task 3's git mv operations modify the working tree; require commits to land |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.2-11 | Tampering | Regenerated lesson could differ from original in non-token-language ways | mitigate | Human-verify checkpoint (Task 2) gates the batch behind operator visual check; LessonSchema.safeParse pre-validates structure |
| T-11.2-12 | Information disclosure | API key for Claude content generation in env | accept | Same env pattern used for the original 200 lessons; no new exposure |
| T-11.2-13 | Tampering | Accidental delete of LCS script during archive | mitigate | Use `git mv` (not `rm`+`cp`); preserves history; README in _attic warns against active imports |
| T-11.2-13b | Denial of service | Mid-call API failure due to missing/exhausted credentials wastes wall-clock and leaves lesson in indeterminate state | mitigate | Pre-flight ANTHROPIC_API_KEY check (Task 1 step 0); checkpoint:decision escalation with three remediation paths (top-up, Ollama, hand-edit) before any API call |

Low surface — local pipeline + archive operation; Task 2 checkpoint is the principal control on regen quality.
</threat_model>

<verification>
- mountain-a-go-go full-version lesson has Japanese tokens, passes LessonSchema, human-approved
- Old LCS scripts archived; new NW script in place; no broken imports
- Wave 3 (Plan 11.2-05) can begin the 60-song batch run
- Credential pre-flight ran; no silent API-credits failure
</verification>

<success_criteria>
- D-04 mountain-a-go-go pre-fix complete and human-verified
- D-02 archive complete with no orphaned imports
- Plan 11.2-05 has all preconditions satisfied
- Credential pre-flight executed (`ANTHROPIC_API_KEY` confirmed OR an alternative path approved by checkpoint:decision)
</success_criteria>

<output>
After completion, create `.planning/phases/11.2-tv-derive-rework-demucs-nw/11.2-04-SUMMARY.md` documenting: (a) credential pre-flight outcome — key present, or escalation path chosen (Ollama / hand-edit / topped up), (b) whether `03-generate-content.ts` had a JP-forcing flag or required deeper intervention, (c) the user's verdict on the regenerated lesson at the human-verify checkpoint, (d) any active-import surprises caught during the archive grep audit.
</output>
</content>
</invoke>