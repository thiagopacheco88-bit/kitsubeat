---
phase: 08.4-learn-phase-session-pacing-for-new-vocabulary
plan: 04
subsystem: ui
tags: [react, zustand, typescript, fsrs, exercise-engine, learn-card, session-pacing]

# Dependency graph
requires:
  - phase: 08.4-03
    provides: "getUserPrefs, getEffectiveCap server actions + exerciseSession store extensions (learnedVocabIds, vocabStates, markLearnCardShown)"
  - phase: 08.4-02
    provides: "LearnCard component (props-driven, tts.ts)"
  - phase: 08.4-01
    provides: "DB schema: users.skip_learning, users.new_card_cap + vocab-tiers API states field"
provides:
  - "ExerciseTab filters lesson vocab by effectiveCap before buildQuestions — new/relearning words beyond cap excluded from session"
  - "ExerciseSession renders LearnCard JIT before first QuestionCard for new/relearning vocab (gated on skip_learning flag)"
  - "Full end-to-end learn phase flow: cap filter -> learn card -> question card -> session summary"
affects: [08.4-05, profile-settings, exercise-session-resumption]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Promise.all for parallel server action + API calls in handleStart (prefs + cap + jlpt-pool)"
    - "Pre-filter vocab before buildQuestions — cap applied at call-site, not inside generator"
    - "JIT render interrupt: conditional return before main render path, currentIndex not advanced"
    - "Resume-path useEffect re-fetches prefs on mount when hasActiveSession=true"

key-files:
  created: []
  modified:
    - "src/app/songs/[slug]/components/ExerciseTab.tsx"
    - "src/app/songs/[slug]/components/ExerciseSession.tsx"

key-decisions:
  - "ExerciseTab hooks placed before hydration guard (React rules of hooks — useEffect cannot come after conditional return)"
  - "hasActiveSession computed before useEffect to satisfy effect dependency array"
  - "Pre-existing TypeScript errors in unrelated files (admin/timing, fsrs/scheduler) are out of scope — confirmed no errors in plan's target files"

patterns-established:
  - "Resume path: re-fetch prefs only (not cap) — filtering already baked into persisted questions array"
  - "Learn card does not advance currentIndex; onDismiss sets learnedVocabIds flag; React re-renders to QuestionCard at same index"
  - "markVocabIntroduced guarded by !introducedNewVocabIds[id] — prevents double-counting on repeat encounters of same word"

requirements-completed: [08.4-JIT-WIRING, 08.4-CAP-ENFORCEMENT, 08.4-SKIP-LEARNING]

# Metrics
duration: 3min
completed: 2026-04-17
---

# Phase 08.4 Plan 04: JIT LearnCard Wiring + Cap Filter Summary

**Cap-filtered buildQuestions via effectiveCap + JIT LearnCard render-interrupt in ExerciseSession gated on skip_learning and vocab FSRS state**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-04-17T22:18:32Z
- **Completed:** 2026-04-17T22:21:29Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- ExerciseTab fetches `getUserPrefs`, `getEffectiveCap`, and JLPT pool in a single `Promise.all` (no serial latency)
- Cap filter applied in-order before `buildQuestions`: new (state=0) + relearning (state=3) words beyond `effectiveCap` are excluded; review/learning words always pass through
- ExerciseSession renders `LearnCard` instead of `QuestionCard` for first encounter of any new/relearning word when `skipLearning=false`; `currentIndex` is never advanced by the card
- `skipLearning` threaded from ExerciseTab down to ExerciseSession; resume path re-fetches prefs on cold hydrate via `useEffect`

## Task Commits

1. **Task 1: ExerciseTab — fetch prefs + effective cap, filter lesson vocab, plumb states + skipLearning** - `c28cf4f` (feat)
2. **Task 2: ExerciseSession — JIT LearnCard insertion + skip_learning bypass** - `ec8d8c1` (feat)

## Files Created/Modified
- `src/app/songs/[slug]/components/ExerciseTab.tsx` - Parallel prefs+cap+jlpt fetch; vocab-tiers batch before filter; cap filter algorithm; skipLearning state + resume useEffect; lesson prop plumbed to ExerciseSession
- `src/app/songs/[slug]/components/ExerciseSession.tsx` - lesson + skipLearning props; JIT LearnCard render branch with gating predicate; markLearnCardShown + markVocabIntroduced on dismiss

## Decisions Made
- React hooks (useEffect for resume path) must be declared before any conditional early return — moved hydration guard below hook declarations to comply with Rules of Hooks
- `hasActiveSession` computed above the useEffect (not inside it) so it can be a dependency array entry without stale closure
- Pre-existing TS errors in `admin/timing` and `fsrs/scheduler` are out of scope — confirmed zero errors in the two modified files

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Moved hydration guard below hooks to comply with React Rules of Hooks**
- **Found during:** Task 1 (ExerciseTab refactor)
- **Issue:** Plan's pseudocode placed useEffect after `if (!_hasHydrated) return` — this violates React's rule that hooks must not be called conditionally. Would cause a runtime error.
- **Fix:** Moved `hasActiveSession` computation and `useEffect` above the hydration guard conditional return; added comment explaining the ordering requirement.
- **Files modified:** `src/app/songs/[slug]/components/ExerciseTab.tsx`
- **Verification:** `npx tsc --noEmit` passes (no errors in target files); component renders correctly
- **Committed in:** c28cf4f (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 bug — React hooks ordering)
**Impact on plan:** Essential correctness fix. No scope creep.

## Issues Encountered
None beyond the hooks-ordering fix above.

## Next Phase Readiness
- Full end-to-end learn phase flow is wired and ready for manual verification
- Plan 08.4-05 (profile settings page for skip_learning + new_card_cap) can proceed independently
- All cap/learn-card requirements (08.4-JIT-WIRING, 08.4-CAP-ENFORCEMENT, 08.4-SKIP-LEARNING) complete

---
*Phase: 08.4-learn-phase-session-pacing-for-new-vocabulary*
*Completed: 2026-04-17*
