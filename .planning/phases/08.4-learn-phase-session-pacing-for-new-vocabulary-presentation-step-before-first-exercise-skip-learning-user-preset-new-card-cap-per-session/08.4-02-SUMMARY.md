---
phase: 08.4-learn-phase-session-pacing-for-new-vocabulary
plan: 02
subsystem: ui
tags: [react, tts, web-speech-api, learn-card, japanese, furigana, kanji-breakdown]

# Dependency graph
requires:
  - phase: 08.3-mnemonic-and-kanji-breakdown-for-vocabulary-feedback
    provides: KanjiBreakdownSection component + mnemonic/kanji_breakdown on Question type
  - phase: 08-exercise-engine
    provides: Question type from generator.ts with vocabInfo

provides:
  - LearnCard presentational component at src/app/songs/[slug]/components/LearnCard.tsx
  - TTS utility module at src/lib/tts.ts (speakJapanese, hasJapaneseVoice, onVoicesChanged)

affects:
  - 08.4-04 (Plan 04 mounts LearnCard inside ExerciseSession)

# Tech tracking
tech-stack:
  added: [Web Speech API (browser-native, no external service)]
  patterns:
    - Props-driven presentational components with zero Zustand coupling
    - onVoicesChanged subscription pattern for async voice list population on Chromium
    - stopPropagation on interactive sub-elements inside a clickable card container

key-files:
  created:
    - src/app/songs/[slug]/components/LearnCard.tsx
    - src/lib/tts.ts
  modified: []

key-decisions:
  - "LearnCard is 100% props-driven — no Zustand imports; session coupling deferred to Plan 04"
  - "tts.ts uses Web Speech API (browser-native); no external TTS service or API key needed"
  - "voiceReady state initialized from hasJapaneseVoice() + onVoicesChanged subscription covers Chromium async voice loading"
  - "Three stopPropagation sites: speaker button, Show-more button, revealed body — all other taps advance via onClick=onDismiss on outer wrapper"

patterns-established:
  - "Clickable card pattern: outer div has onClick=handler; interactive sub-controls use e.stopPropagation()"
  - "TTS gating: render speaker icon only when hasJapaneseVoice() returns true, re-checked via onVoicesChanged"

requirements-completed: [08.4-LEARN-CARD-UI]

# Metrics
duration: 2min
completed: 2026-04-17
---

# Phase 08.4 Plan 02: LearnCard Component Summary

**Props-driven LearnCard with tap-to-dismiss, single Show-more reveal for mnemonic+kanji breakdown, and Web Speech API speaker icon gated on Japanese voice availability**

## Performance

- **Duration:** 2 min
- **Started:** 2026-04-17T22:09:38Z
- **Completed:** 2026-04-17T22:11:18Z
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments

- `LearnCard.tsx` created: renders surface+furigana+romaji+meaning+POS by default; single "Show more" button reveals mnemonic (if present) and KanjiBreakdownSection (if present) without advancing
- `tts.ts` created: `speakJapanese()`, `hasJapaneseVoice()`, and `onVoicesChanged()` using browser Web Speech API — no external service, no API key
- Tap semantics locked: 3 stopPropagation sites (speaker button, reveal button, revealed body); everything else calls `onDismiss`
- Speaker icon only rendered when a Japanese voice is available (checked both at mount and on `voiceschanged` event for Chromium async loading)
- Zero Zustand imports — component is storybook-friendly and ready for Plan 04 to mount

## Task Commits

1. **Task 1: Create LearnCard component with tap-to-dismiss + Show more reveal + speaker icon** - `b0448e3` (feat)

## Files Created/Modified

- `src/app/songs/[slug]/components/LearnCard.tsx` - Presentational learn card; 155 lines; imports KanjiBreakdownSection + tts helpers
- `src/lib/tts.ts` - Web Speech API TTS utilities: speakJapanese, hasJapaneseVoice, onVoicesChanged

## Decisions Made

- Used Web Speech API (browser-native) for TTS — no external TTS provider needed; CONTEXT granted Claude discretion on TTS implementation
- `voiceReady` is local `useState` initialized synchronously from `hasJapaneseVoice()` + `onVoicesChanged` subscription — covers Chromium's async voice list population without an extra network call
- Revealed body stops propagation on its own `onClick` so users can scroll/read the breakdown without triggering card dismissal

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Created missing src/lib/tts.ts**
- **Found during:** Task 1 (Create LearnCard component)
- **Issue:** Plan's component skeleton imports `speakJapanese`, `hasJapaneseVoice`, `onVoicesChanged` from `@/lib/tts` but the file did not exist in the codebase
- **Fix:** Created `src/lib/tts.ts` implementing all three functions using the Web Speech API (browser-native SpeechSynthesis)
- **Files modified:** src/lib/tts.ts
- **Verification:** `npx tsc --noEmit` produces no errors from the new files; LearnCard imports resolve cleanly
- **Committed in:** b0448e3 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking dependency)
**Impact on plan:** Required for plan to compile. Web Speech API is the appropriate implementation per CONTEXT discretion grant.

## Issues Encountered

Pre-existing TypeScript errors in `src/app/admin/timing/[songId]/page.tsx`, `src/app/api/admin/songs/route.ts`, and `src/lib/fsrs/scheduler.ts` — these are unrelated to this plan's changes and were present before execution. No action taken (out-of-scope per deviation rules).

## Next Phase Readiness

- `LearnCard` is ready for Plan 04 to import and mount inside `ExerciseSession`
- `tts.ts` is available for any other component that needs Japanese TTS (e.g., vocabulary review cards)
- No blockers from this plan

---
*Phase: 08.4-learn-phase-session-pacing-for-new-vocabulary*
*Completed: 2026-04-17*
