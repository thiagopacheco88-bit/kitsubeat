---
phase: 08.4-learn-phase-session-pacing-for-new-vocabulary
plan: 01
subsystem: database
tags: [drizzle, postgres, neon, tts, web-speech-api, fsrs]

# Dependency graph
requires:
  - phase: 08.2-fsrs-progressive-disclosure
    provides: user_vocab_mastery table with FSRS state column
  - phase: 08.3-mnemonic-and-kanji-breakdown-for-vocabulary-feedback
    provides: vocab-tiers API foundation

provides:
  - users table (skip_learning + new_card_cap) in Neon DB via migration 0005
  - Drizzle users pgTable + User/NewUser types in schema.ts
  - vocab-tiers API now returns states map (0|1|2|3) alongside tiers
  - src/lib/tts.ts with speakJapanese, hasJapaneseVoice, onVoicesChanged

affects: [08.4-02, 08.4-03, 08.4-04, 08.4-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "idempotent migrations: CREATE TABLE IF NOT EXISTS + ADD COLUMN IF NOT EXISTS"
    - "additive API changes: new fields alongside existing ones preserve backward compat"
    - "SSR-safe browser API wrappers: typeof window === undefined guard pattern"

key-files:
  created:
    - drizzle/0005_user_prefs.sql
    - src/lib/tts.ts
  modified:
    - src/lib/db/schema.ts
    - src/app/api/exercises/vocab-tiers/route.ts

key-decisions:
  - "skip_learning DEFAULT false = do NOT skip (cards show) — column polarity matches CONTEXT default-ON behavior"
  - "new_card_cap DEFAULT 10 locked from RESEARCH Area 7 (2–6 session progression for p25 catalog)"
  - "tts.ts already existed in codebase from 08.4-02 pre-commit — verified sufficient, no overwrite needed"
  - "states map in vocab-tiers is additive — existing callers destructuring only { tiers } unaffected"

patterns-established:
  - "Migration pattern: 0005 uses IF NOT EXISTS on both CREATE TABLE and ALTER TABLE for idempotency"
  - "users table has text PK (Clerk user_id) with no FK to other tables — matches existing codebase convention"

requirements-completed: [08.4-STORAGE, 08.4-STATE-ACCESS, 08.4-TTS]

# Metrics
duration: 8min
completed: 2026-04-17
---

# Phase 08.4 Plan 01: Data Foundation Summary

**users table (skip_learning + new_card_cap) + vocab-tiers states map + Web Speech API TTS helper — all foundation pieces for learn phase wiring**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-04-17T22:03:00Z
- **Completed:** 2026-04-17T22:11:45Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments
- Created and applied `drizzle/0005_user_prefs.sql` to Neon DB — verified idempotent on re-run
- Added `users` pgTable to schema.ts with `User`/`NewUser` types after subscriptions block
- Extended `/api/exercises/vocab-tiers` to return `states: Record<string, 0|1|2|3>` alongside existing `tiers` (additive, backward-compatible)
- Confirmed `src/lib/tts.ts` already exists with all three required exports (`speakJapanese`, `hasJapaneseVoice`, `onVoicesChanged`)

## Task Commits

Each task was committed atomically:

1. **Task 1: Create users table migration + mirror in Drizzle schema** - `941621f` (feat)
2. **Task 2: Extend vocab-tiers API response with states map** - `7adb961` (feat)
3. **Task 3: Add Web Speech API helper for Japanese TTS** - already committed in `b0448e3` (prior session)

**Plan metadata:** _(docs commit follows)_

## Files Created/Modified
- `drizzle/0005_user_prefs.sql` - CREATE TABLE IF NOT EXISTS users with skip_learning + new_card_cap
- `src/lib/db/schema.ts` - Added users pgTable + User/NewUser types after subscriptions
- `src/app/api/exercises/vocab-tiers/route.ts` - Added stateMap built from mastery rows, returned alongside tierMap
- `src/lib/tts.ts` - Already exists: speakJapanese, hasJapaneseVoice, onVoicesChanged (Web Speech API, SSR-safe)

## Decisions Made
- `skip_learning DEFAULT false` means "do NOT skip" (cards show) — matches CONTEXT "default ON" behavior
- `new_card_cap DEFAULT 10` locked from RESEARCH Area 7
- `tts.ts` was already present from an earlier 08.4-02 commit — verified it exports all three required functions; no overwrite needed
- states map uses same DB rows as tier map — no extra query needed

## Deviations from Plan

None — plan executed exactly as written. The only discovery was that `src/lib/tts.ts` already existed with correct content from a prior commit (`b0448e3`). No fix required.

## Issues Encountered
- Pre-existing TypeScript errors in unrelated files (admin timing page, backup tsx files, scheduler.ts) — out of scope per deviation rules, not caused by this plan's changes. schema.ts and vocab-tiers route.ts both compile cleanly.

## User Setup Required
None - no external service configuration required. Migration already applied to live Neon DB.

## Next Phase Readiness
- `users` table live on Neon; Plans 03 and 05 can write/read skip_learning and new_card_cap
- `states` field on vocab-tiers API ready for Plan 04 JIT wiring (distinguishes New=0 from Relearning=3)
- `tts.ts` importable from any client component for speaker icon in learn card

---
*Phase: 08.4-learn-phase-session-pacing-for-new-vocabulary*
*Completed: 2026-04-17*
