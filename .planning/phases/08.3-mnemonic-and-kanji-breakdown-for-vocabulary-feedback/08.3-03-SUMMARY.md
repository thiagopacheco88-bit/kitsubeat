---
phase: 08.3-mnemonic-and-kanji-breakdown-for-vocabulary-feedback
plan: 03
subsystem: api
tags: [zod, lesson-generation, claude-api, prompt-engineering, typescript]

# Dependency graph
requires:
  - phase: 08.3-01
    provides: DB schema migration adding mnemonic + kanji_breakdown jsonb columns
  - phase: 08.3-02
    provides: VocabEnrichmentSchema + enrich types for shape reference

provides:
  - VocabEntrySchema extended with optional mnemonic + kanji_breakdown (scripts/types/lesson.ts)
  - lesson-prompt Section 6 updated to request both fields from Claude on every future generation
  - LESSON_JSON_SCHEMA auto-updated via zodToJsonSchema to reflect new optional fields

affects: [08.3-04, 08.3-05, scripts/seed/03-generate-content.ts, scripts/seed/03b-generate-content-sequential.ts]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Inline schema mirroring: enrich-shape sub-schemas duplicated inline in lesson.ts to avoid cross-module runtime imports"
    - "Optional generation fields: new fields optional in VocabEntrySchema so existing lessons without enrichment still validate"

key-files:
  created: []
  modified:
    - scripts/types/lesson.ts
    - scripts/lib/lesson-prompt.ts

key-decisions:
  - "Inline EnrichLocalizableSchema/EnrichKanjiCharSchema/EnrichKanjiBreakdownSchema in lesson.ts — no import from enrich.ts to preserve generation-time isolation"
  - "mnemonic and kanji_breakdown both .optional() in VocabEntrySchema — existing lessons without these fields still validate; Wave 4 QA catches coverage"
  - "Escaped backticks (\\`) in template literal for code-fence examples — prevents TS parser from misinterpreting inline JSON examples as template expressions"

patterns-established:
  - "Forward-compatible schema extension: add fields as optional so new generation enriches forward without invalidating legacy JSONB blobs"

requirements-completed: [08.3-GENERATION-FORWARD]

# Metrics
duration: 2min
completed: 2026-04-17
---

# Phase 08.3 Plan 03: Lesson-Prompt Forward-Enrichment Summary

**VocabEntrySchema extended with optional mnemonic + kanji_breakdown and lesson-prompt Section 6 updated so all future song generations include enrichment inline**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-04-17T07:29:54Z
- **Completed:** 2026-04-17T07:32:06Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- `VocabEntrySchema` in `scripts/types/lesson.ts` now accepts optional `mnemonic` (localizable) and `kanji_breakdown` (nullable localizable breakdown) fields
- `buildLessonPrompt()` Section 6 instructs Claude to emit both fields for every vocabulary entry; Section 10 output-format example updated to show both fields
- `LESSON_JSON_SCHEMA` (zodToJsonSchema export) automatically reflects new optional fields — no manual update needed
- Both `03-generate-content.ts` and `03b-generate-content-sequential.ts` pick up prompt change via shared `buildLessonPrompt` import

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend VocabEntrySchema with enrichment fields** - `e2ff9d8` (feat)
2. **Task 2: Update lesson-prompt Section 6 to request mnemonic + kanji_breakdown** - `d5bda78` (feat)

## Files Created/Modified
- `scripts/types/lesson.ts` - Added EnrichLocalizableSchema, EnrichKanjiCharSchema, EnrichKanjiBreakdownSchema helpers; extended VocabEntrySchema with mnemonic + kanji_breakdown
- `scripts/lib/lesson-prompt.ts` - Extended Section 6 with mnemonic + kanji_breakdown field specs; updated Section 10 output format example

## Decisions Made
- Inlined the three enrich sub-schemas rather than importing from `scripts/types/enrich.ts` — keeps generation-time module graph clean and avoids runtime cross-module coupling
- Both new fields are `.optional()` in VocabEntrySchema: an LLM response that omits them still passes generation-time validation; shape is strictly checked when present
- Backtick fencing inside template literal required `\`` escaping — discovered during TS compilation (TS parser treats unescaped backtick as template string terminator)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Escaped backticks in template literal for code-fence example**
- **Found during:** Task 2 (lesson-prompt update)
- **Issue:** Triple backtick ` ``` ` and inline backticks inside the template literal caused TS2322/TS1005 parse errors — TS treated the code fence as a template expression boundary
- **Fix:** Replaced all ` ``` ` with `\`\`\`` and inline `` `x` `` with `` \`x\` `` inside the template literal, matching the existing escaping pattern already used elsewhere in the file
- **Files modified:** scripts/lib/lesson-prompt.ts
- **Verification:** `npx tsc --project tsconfig.scripts.json --noEmit` produces zero errors in lesson-prompt.ts
- **Committed in:** d5bda78 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug — template literal escape)
**Impact on plan:** Necessary correctness fix; no scope creep.

## Issues Encountered
- Pre-existing TS errors across other scripts files (backfill-geo-check.ts, src/lib/exercises/, etc.) — out of scope for this plan; neither introduced nor worsened by these changes

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Forward-generation prompt + schema are ready; all future runs of 03-generate-content.ts / 03b will produce enriched vocab entries
- Plan 08.3-04 (backfill script for existing songs) can now rely on VocabEntrySchema validating mnemonic + kanji_breakdown when present
- Plan 08.3-05 (FeedbackPanel UI) can import VocabEntry type and expect both optional fields

---
*Phase: 08.3-mnemonic-and-kanji-breakdown-for-vocabulary-feedback*
*Completed: 2026-04-17*
