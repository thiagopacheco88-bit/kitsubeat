---
phase: 18.2-pre-launch-qa-safety-net
plan: "05"
subsystem: testing
tags: [playwright, e2e, api, health-check, auth-guard]

# Dependency graph
requires:
  - phase: 18.2-01
    provides: research + verified API route status codes
  - phase: 18.2-02
    provides: playwright infrastructure and fixture patterns
  - phase: 18.2-03
    provides: cron-auth.spec.ts pattern (excluded routes)
provides:
  - Stateless API health smoke tests for client-errors, vocab-tiers, birthday-transitions
affects: [18.2-06, qa-suite]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Import from @playwright/test directly (not fixtures.ts) for DB-free API health tests"
    - "Flexible 4xx assertions for ASSUMED auth patterns (toBeGreaterThanOrEqual + toBeLessThan)"
    - "Exact assertions for VERIFIED auth patterns (toBe(401))"

key-files:
  created:
    - tests/e2e/api-health.spec.ts
  modified: []

key-decisions:
  - "Use toBeGreaterThanOrEqual(400) + toBeLessThan(500) for birthday-transitions — auth pattern assumed not verified"
  - "Import from @playwright/test not tests/support/fixtures.ts — no DB dependency needed for stateless routes"
  - "Exclude daily-reminder and weekly-recap — already covered in cron-auth.spec.ts"

patterns-established:
  - "API health tests: direct @playwright/test import for zero-dependency stateless route checks"
  - "Flexible 4xx range assertion pattern for assumed auth guards vs exact 401 for verified ones"

requirements-completed: [D-05, D-09]

# Metrics
duration: 5min
completed: 2026-05-16
---

# Phase 18.2 Plan 05: API Health Smoke Tests Summary

**Stateless API health checks for 3 key routes (client-errors 200, vocab-tiers 401, birthday-transitions 4xx) using Playwright request fixture with zero DB/auth dependencies**

## Performance

- **Duration:** 5 min
- **Started:** 2026-05-16T11:08:04Z
- **Completed:** 2026-05-16T11:13:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Created `tests/e2e/api-health.spec.ts` with 3 tests covering routes that would silently break the app if they went down
- All 3 tests verified passing on a running dev server (PLAYWRIGHT_BASE_URL=http://localhost:7100)
- No DB connection, no auth env vars required — imports from `@playwright/test` directly
- Excluded daily-reminder and weekly-recap routes already covered by cron-auth.spec.ts

## Task Commits

1. **Task 1: Create api-health.spec.ts** - `5be1fe1` (test)

## Files Created/Modified
- `tests/e2e/api-health.spec.ts` - 3 Playwright API health smoke tests for key routes

## Decisions Made
- Used `toBeGreaterThanOrEqual(400)` + `toBeLessThan(500)` for birthday-transitions (auth pattern was ASSUMED from research, not verified from codebase at time of plan authoring — flexible assertion is more resilient)
- Used exact `toBe(401)` for vocab-tiers (auth pattern VERIFIED from route.ts line 30-32)
- Used exact `toBe(200)` + `toMatchObject({ ok: true })` for client-errors (response shape VERIFIED from route.ts line 35)

## Deviations from Plan

None - plan executed exactly as written. The spec content was provided verbatim in the plan and implemented as specified.

**Note on test execution:** The dev server at localhost:7000 was running a stale build that returned 404 for all API routes (pre-existing issue also affecting cron-auth.spec.ts). Tests were verified against a fresh dev server started on port 7100. This is a pre-existing infrastructure issue not caused by this plan. Tests pass correctly when the dev server properly compiles and serves the routes.

## Issues Encountered
- Dev server at port 7000 returned 404 for API routes (stale build / wrong .next output). Tests were verified against a fresh dev server on port 7100 where all 3 tests passed: 3 passed, 0 failed, 0 skipped.

## Threat Surface Scan
No new network endpoints or auth paths introduced — this is a test-only file.

## Known Stubs
None — test file contains no stub patterns.

## Next Phase Readiness
- api-health.spec.ts is ready for CI integration
- The birthday-transitions flexible assertion (`>= 400, < 500`) can be tightened to exact `toBe(401)` once the auth pattern is confirmed via a green run with proper secret header
- Pre-existing dev server stale-build issue should be resolved for the playwright suite to run reliably at port 7000

---
*Phase: 18.2-pre-launch-qa-safety-net*
*Completed: 2026-05-16*
