---
phase: "18.2"
plan: "06"
type: execute
wave: 3
depends_on:
  - "18.2-01"
  - "18.2-02"
  - "18.2-03"
  - "18.2-04"
  - "18.2-05"
files_modified:
  - package.json
autonomous: true
requirements:
  - D-06

must_haves:
  truths:
    - "npm run test:e2e:qa runs the full Playwright suite with --workers=4"
    - "The existing 'test:qa' script (seed QA agent) is preserved unchanged"
    - "The existing 'test:e2e' script is preserved unchanged (still runs with default worker count)"
    - "playwright.config.ts workers field remains absent (default CPU count preserved)"
    - "npm run test:e2e:qa completes and exits 0 when all specs pass"
  artifacts:
    - path: "package.json"
      provides: "test:e2e:qa script wiring"
      contains: "test:e2e:qa"
  key_links:
    - from: "package.json scripts.test:e2e:qa"
      to: "playwright.config.ts"
      via: "playwright test --workers=4"
      pattern: "test:e2e:qa.*playwright test.*workers"
---

<objective>
Add `"test:e2e:qa"` to `package.json` scripts — the single-command Playwright runner for
pre-ship validation. Uses `--workers=4` as a CLI flag (NOT in playwright.config.ts) so the
dev workflow `test:e2e` is unaffected. The existing `test:qa` seed agent script is preserved.

Purpose: `npm run test:e2e:qa` becomes the canonical pre-ship safety command.
Output: package.json gains one new script entry. No other file changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18.2-pre-launch-qa-safety-net/18.2-CONTEXT.md
@.planning/phases/18.2-pre-launch-qa-safety-net/18.2-RESEARCH.md

<interfaces>
<!-- Key patterns extracted from codebase. Use verbatim — no exploration needed. -->

From package.json (existing scripts — must NOT be modified):
```json
"test:e2e": "playwright test",
"test:qa": "tsx --tsconfig tsconfig.scripts.json scripts/seed/06-qa-agent.ts",
"test:e2e:a11y": "cross-env RUN_A11Y=1 playwright test a11y.spec.ts",
```

From playwright.config.ts:
```typescript
// workers key is ABSENT — Playwright defaults to CPU count when fullyParallel: true
// fullyParallel: true is already set — --workers=4 only changes the cap, not the behavior
// DO NOT add workers to playwright.config.ts — that would affect ALL playwright runs
```

From 18.2-RESEARCH.md Pitfall 2:
// package.json line 36: "test:qa" → seed agent script
// DO NOT overwrite it. New script MUST be named "test:e2e:qa" (consistent with test:e2e naming)

From 18.2-RESEARCH.md Pitfall 3:
// Option (b) is correct: pass --workers=4 only in the script CLI flag
// Leave playwright.config.ts unchanged — workers key stays absent
// This preserves dev workflow behavior (test:e2e uses CPU-count default)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test:e2e:qa script to package.json</name>
  <files>package.json</files>
  <read_first>
    - package.json (lines 1-55 — read all scripts to identify exact insertion point and verify test:qa collision)
    - playwright.config.ts (lines 1-93 — verify workers key is absent before deciding NOT to add it)
  </read_first>
  <action>
Read `package.json` and add exactly one new script entry. The new entry must be inserted
immediately after `"test:e2e:a11y"` (line 35 in current file) to keep test:e2e:* scripts grouped.

New script to add:
```json
"test:e2e:qa": "playwright test --workers=4",
```

Before editing, verify:
1. `"test:qa"` script exists and points to the seed agent (tsx ...06-qa-agent.ts) — do NOT modify it
2. `"test:e2e:qa"` does NOT already exist — if it does, check if it's correct and skip the edit
3. `"test:e2e"` script exists as `"playwright test"` — do NOT modify it

The resulting scripts section (relevant entries) must be:
```json
"test:e2e": "playwright test",
"test:e2e:a11y": "cross-env RUN_A11Y=1 playwright test a11y.spec.ts",
"test:e2e:qa": "playwright test --workers=4",
"test:qa": "tsx --tsconfig tsconfig.scripts.json scripts/seed/06-qa-agent.ts",
```

DO NOT:
- Add workers to playwright.config.ts (would change all Playwright runs)
- Rename or modify the existing "test:qa" script
- Modify the "test:e2e" script
- Add any other changes to package.json beyond the one new script entry
  </action>
  <verify>
    <automated>node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log(JSON.stringify({qa: p.scripts['test:e2e:qa'], existing_qa: p.scripts['test:qa'], e2e: p.scripts['test:e2e']}, null, 2))"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` is valid JSON (parse succeeds)
    - `scripts["test:e2e:qa"]` equals exactly `"playwright test --workers=4"`
    - `scripts["test:qa"]` still equals `"tsx --tsconfig tsconfig.scripts.json scripts/seed/06-qa-agent.ts"` (unchanged)
    - `scripts["test:e2e"]` still equals `"playwright test"` (unchanged)
    - `playwright.config.ts` has NOT been modified (workers key still absent)
    - `npm run test:e2e:qa` resolves to `playwright test --workers=4` (verify via `npm run test:e2e:qa -- --list 2>&1 | head -5`)
  </acceptance_criteria>
  <done>package.json has the test:e2e:qa script. Existing scripts are unchanged. Running `npm run test:e2e:qa` launches Playwright with 4 workers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Added `"test:e2e:qa": "playwright test --workers=4"` to package.json. All existing scripts
    (test:e2e, test:qa, test:e2e:a11y) are unchanged. playwright.config.ts is unchanged.
  </what-built>
  <how-to-verify>
    Run the following command (requires dev server running on :7000 or it will start one):

    ```
    npm run test:e2e:qa -- --reporter=line 2>&1 | tail -30
    ```

    Expected output:
    - Tests start across 4 workers (look for "Running X tests using 4 workers")
    - All existing specs pass (19 locale/nav/API specs from this phase + existing 67 specs)
    - Exit code 0

    If the dev server is not running, the command will start it automatically (up to 2 min timeout).
    If tests fail, note the failing test names — they may be pre-existing failures in the existing suite.

    Also verify: `npm run test:qa` still runs the seed agent (NOT Playwright):
    ```
    npm run test:qa -- --help 2>&1 | head -5
    ```
    This should NOT show Playwright output — it should show the seed agent output.
  </how-to-verify>
  <resume-signal>Type "approved" if test:e2e:qa runs with 4 workers and test:qa still runs the seed agent. Describe any failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm scripts → Playwright CLI | Package.json is the entry point for CI/CD pipeline invocations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.2-06-01 | Tampering | test:qa script overwritten by new test:e2e:qa | mitigate | Acceptance criteria explicitly verifies both scripts exist with correct values after edit |
| T-18.2-06-02 | Denial of Service | workers=4 exhausting CI resources | accept | 4 workers is a conservative cap (Phase 08.1 ran fine with fullyParallel, CPU default). CI can override with --workers=2 flag. |
</threat_model>

<verification>
Run: `node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log(p.scripts['test:e2e:qa'])"`

Expected: `playwright test --workers=4`

Run: `node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log(p.scripts['test:qa'])"`

Expected: `tsx --tsconfig tsconfig.scripts.json scripts/seed/06-qa-agent.ts`
</verification>

<success_criteria>
- package.json has test:e2e:qa = "playwright test --workers=4"
- test:qa seed script is unchanged
- test:e2e script is unchanged
- playwright.config.ts workers field remains absent
- Human checkpoint confirms the full suite runs with 4 workers
</success_criteria>

<output>
After completion, create `.planning/phases/18.2-pre-launch-qa-safety-net/18.2-06-SUMMARY.md`
</output>
