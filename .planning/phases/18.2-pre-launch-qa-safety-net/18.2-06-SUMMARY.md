---
phase: 18.2-pre-launch-qa-safety-net
plan: "06"
subsystem: testing
tags: [playwright, e2e, package-json, scripts, pre-ship]

# Dependency graph
requires:
  - phase: 18.2-01
    provides: research + pitfall documentation (test:qa collision risk)
  - phase: 18.2-02
    provides: playwright infrastructure + playwright.config.ts baseline
  - phase: 18.2-03
    provides: cron-auth.spec.ts
  - phase: 18.2-04
    provides: onboarding terms specs
  - phase: 18.2-05
    provides: api-health.spec.ts
provides:
  - npm run test:e2e:qa — canonical pre-ship Playwright command with 4 workers
affects: [CI/CD pipeline, pre-launch safety checklist]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pass --workers=N as CLI flag in package.json script — never in playwright.config.ts — to avoid affecting all Playwright runs"

key-files:
  created: []
  modified:
    - package.json

key-decisions:
  - "workers=4 is a CLI flag in the script, not in playwright.config.ts — preserves test:e2e default CPU-count behavior"
  - "Script named test:e2e:qa (not overwriting test:qa seed agent) — consistent test:e2e:* namespace"
  - "Inserted after test:e2e:a11y to keep test:e2e:* entries grouped"

requirements-completed: [D-06]

# Metrics
duration: 3min
completed: 2026-05-16
---

# Phase 18.2 Plan 06: test:e2e:qa Script Wiring Summary

**One-liner package.json script addition: `"test:e2e:qa": "playwright test --workers=4"` as the canonical pre-ship Playwright command, isolating 4-worker cap to CI invocations without touching playwright.config.ts or existing scripts**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-05-16T11:14:00Z
- **Completed:** 2026-05-16T11:17:26Z
- **Tasks:** 1 auto + 1 checkpoint (human-verify pending)
- **Files modified:** 1

## Accomplishments
- Added `"test:e2e:qa": "playwright test --workers=4"` to package.json after `test:e2e:a11y`
- Preserved `test:e2e` (`playwright test`) unchanged — dev workflow unaffected
- Preserved `test:qa` (`tsx ...06-qa-agent.ts`) unchanged — seed agent unaffected
- Did NOT add workers to playwright.config.ts — all other Playwright runs keep CPU-count default
- package.json parses as valid JSON with correct script value verified

## Task Commits

1. **Task 1: Add test:e2e:qa script to package.json** - `2921492` (feat)

## Files Created/Modified
- `package.json` — added one script entry: `"test:e2e:qa": "playwright test --workers=4"`

## Decisions Made
- `--workers=4` passed as CLI flag in the script, not set in playwright.config.ts — ensures test:e2e (dev workflow) continues to use CPU-count default
- Script name `test:e2e:qa` follows existing `test:e2e:*` namespace (alongside `test:e2e:a11y`), distinct from `test:qa` seed agent

## Deviations from Plan

None — plan executed exactly as written. Single script entry added, no other file touched.

## Checkpoint Status

**Task 2 (checkpoint:human-verify) is pending** — awaiting human verification that:
- `npm run test:e2e:qa -- --reporter=line` runs with 4 workers and all specs pass
- `npm run test:qa` still invokes the seed agent (not Playwright)

## Threat Surface Scan
No new network endpoints or auth paths introduced — package.json scripts only.

## Known Stubs
None — this plan adds a single CI invocation alias; no UI or data stubs.

---
*Phase: 18.2-pre-launch-qa-safety-net*
*Completed: 2026-05-16*
