---
phase: 12-learning-path-and-gamification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drizzle/0008_gamification.sql
  - src/lib/db/schema.ts
  - scripts/seed/14-seed-reward-slots.ts
  - scripts/audit/verify-starter-picks.ts
autonomous: true
requirements: [SC1, SC3, SC4]

must_haves:
  truths:
    - "DB has new gamification columns on users table (XP, level, streak, path state, audio prefs)"
    - "DB has user_cosmetics table tracking per-user equipped cosmetics"
    - "DB has reward_slot_definitions table that v4.0 Phase 21 can INSERT into without code changes"
    - "Drizzle schema.ts reflects the new columns/tables 1:1 so queries.ts can reference them type-safely"
    - "Running the seed script populates the v3.0 cosmetic catalog (avatar borders + color themes + badges)"
    - "Running the starter-pick verification script outputs 3 viable basic-tier slugs with non-null lessons"
  artifacts:
    - path: "drizzle/0008_gamification.sql"
      provides: "Additive migration: new columns on users, new user_cosmetics and reward_slot_definitions tables"
      contains: "ALTER TABLE \"users\" ADD COLUMN IF NOT EXISTS \"xp_total\""
    - path: "src/lib/db/schema.ts"
      provides: "Drizzle table definitions extended with gamification columns + userCosmetics + rewardSlotDefinitions tables"
      contains: "xpTotal"
    - path: "scripts/seed/14-seed-reward-slots.ts"
      provides: "Idempotent seed populating reward_slot_definitions with v3.0 cosmetic catalog"
      contains: "INSERT INTO reward_slot_definitions"
    - path: "scripts/audit/verify-starter-picks.ts"
      provides: "Verification query: outputs 3 candidate basic-tier songs with lessons"
      contains: "difficulty_tier = 'basic'"
  key_links:
    - from: "drizzle/0008_gamification.sql"
      to: "src/lib/db/schema.ts"
      via: "column names match between SQL DDL and pgTable definitions"
      pattern: "xp_total.*xpTotal|streak_current.*streakCurrent"
    - from: "scripts/seed/14-seed-reward-slots.ts"
      to: "reward_slot_definitions"
      via: "idempotent INSERT ... ON CONFLICT DO NOTHING per slot id"
      pattern: "ON CONFLICT"
---

<objective>
Lay the data foundation for Phase 12: additive DB migration, Drizzle schema updates, v3.0 cosmetic catalog seed, and a starter-pick verification script. Every other plan depends on these columns/tables existing.

Purpose: Without the schema, no gamification write boundary (Plan 04), no path state (Plan 06), no JLPT HUD (Plan 05) can function. This plan ships zero UI and zero business logic — only the durable storage contract.

Output:
- Migration `drizzle/0008_gamification.sql` applying cleanly to an existing dev DB without data loss
- Extended `src/lib/db/schema.ts` matching the SQL 1:1
- Idempotent seed `scripts/seed/14-seed-reward-slots.ts` populating the v3.0 cosmetic catalog
- Verification script `scripts/audit/verify-starter-picks.ts` producing 3 valid starter-song candidates
</objective>

<execution_context>
@C:/Users/Thiago/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Thiago/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-learning-path-and-gamification/12-CONTEXT.md
@.planning/phases/12-learning-path-and-gamification/12-RESEARCH.md

# Schema reference — read before editing
@src/lib/db/schema.ts
@drizzle/0005_user_prefs.sql
@drizzle/0006_review_daily_counter.sql
@drizzle/0007_advanced_exercises.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 0008_gamification.sql and extend Drizzle schema.ts</name>
  <files>
    drizzle/0008_gamification.sql
    src/lib/db/schema.ts
  </files>
  <action>
    Create `drizzle/0008_gamification.sql` using the exact column set from RESEARCH.md Section B2:

    1. `ALTER TABLE "users" ADD COLUMN IF NOT EXISTS ...`:
       - `xp_total INTEGER NOT NULL DEFAULT 0`
       - `level INTEGER NOT NULL DEFAULT 1`
       - `xp_today INTEGER NOT NULL DEFAULT 0`
       - `xp_today_date DATE`
       - `streak_current INTEGER NOT NULL DEFAULT 0`
       - `streak_best INTEGER NOT NULL DEFAULT 0`
       - `last_streak_date DATE`
       - `streak_tz TEXT`
       - `grace_used_this_week BOOLEAN NOT NULL DEFAULT FALSE`
       - `streak_week_start DATE`
       - `current_path_node_slug TEXT`
       - `sound_enabled BOOLEAN NOT NULL DEFAULT TRUE`
       - `haptics_enabled BOOLEAN NOT NULL DEFAULT TRUE`

    2. `CREATE TABLE IF NOT EXISTS "user_cosmetics" (id uuid PK, user_id text FK->users.id, slot_id text NOT NULL, unlocked_at timestamptz DEFAULT now(), equipped boolean DEFAULT false, UNIQUE(user_id, slot_id))` + index on `user_id`.

    3. `CREATE TABLE IF NOT EXISTS "reward_slot_definitions" (id text PK, slot_type text NOT NULL, level_threshold integer NOT NULL, content jsonb NOT NULL, active boolean DEFAULT true, created_at timestamptz DEFAULT now())`.

    Then extend `src/lib/db/schema.ts`:
    - Add the 13 new columns to the existing `users` pgTable (follow the `// Phase 12: Gamification —` comment pattern from Phase 10 precedent — see lines touching `ex5_best_accuracy` in Phase 10 migration 0007).
    - Add two new pgTable exports: `userCosmetics` (mirrors user_cosmetics SQL) and `rewardSlotDefinitions` (mirrors reward_slot_definitions SQL, with `content: jsonb('content').$type<RewardSlotContent>().notNull()` — the type import lands in Plan 03, so for now type it as `jsonb('content').notNull()` and widen in Plan 03).
    - Follow the existing `pgTable` patterns (uuid default gen_random_uuid, timestamptz with { withTimezone: true, mode: 'string' }, etc.). Reference `userSongProgress` and `userExerciseLog` tables for idiomatic Drizzle-pg shapes.

    DO NOT run `drizzle-kit generate` — the SQL file is hand-written to match Phase 10's convention (see 0007_advanced_exercises.sql). Hand-writing gives us `IF NOT EXISTS` guards that drizzle-kit does not emit.

    Use `PLACEHOLDER_USER_ID` pattern implicitly — columns are additive on existing users rows with safe defaults per RESEARCH Section L1, so no backfill script is required.
  </action>
  <verify>
    1. `npm run db:push` applies cleanly against the dev DB with zero errors (it will detect schema drift and pick up the new columns/tables).
    2. Or, apply the raw SQL: `psql $DATABASE_URL -f drizzle/0008_gamification.sql` returns `ALTER TABLE` + `CREATE TABLE` acknowledgments with no errors.
    3. `psql $DATABASE_URL -c "\d users"` shows the 13 new columns with correct types + defaults.
    4. `psql $DATABASE_URL -c "\d user_cosmetics"` and `\d reward_slot_definitions` show the new tables.
    5. `npx tsc --noEmit` passes — schema.ts changes compile cleanly.
    6. `npm run test:unit` passes — no regressions in existing unit tests.
  </verify>
  <done>
    - Migration file exists at drizzle/0008_gamification.sql with all 13 user columns + user_cosmetics + reward_slot_definitions DDL.
    - schema.ts exports `users` pgTable with 13 new columns, `userCosmetics` pgTable, `rewardSlotDefinitions` pgTable.
    - DB apply succeeds (verified via `\d users`).
    - tsc --noEmit passes.
    - Existing `npm run test:unit` still green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Seed v3.0 cosmetic catalog + starter-pick verification script</name>
  <files>
    scripts/seed/14-seed-reward-slots.ts
    scripts/audit/verify-starter-picks.ts
  </files>
  <action>
    **Seed script** `scripts/seed/14-seed-reward-slots.ts`:
    - Follow the pattern of existing seeds (e.g., `scripts/seed/08-set-popularity.ts`): `import { db } from '../src/lib/db';` + tsx entrypoint + idempotent writes.
    - INSERT these v3.0 cosmetic rows into `reward_slot_definitions` with `ON CONFLICT (id) DO UPDATE SET content = EXCLUDED.content, level_threshold = EXCLUDED.level_threshold, active = EXCLUDED.active` (idempotent):

      | id | slot_type | level_threshold | content |
      |---|---|---|---|
      | `avatar_border_kitsune_fire` | avatar_border | 3 | { type:"avatar_border", css_class:"ring-4 ring-orange-500", preview_color:"#f97316", label:"Kitsune Fire" } |
      | `color_theme_ember` | color_theme | 7 | { type:"color_theme", css_vars:{"--color-accent":"#ff6b35"}, label:"Ember" } |
      | `avatar_border_night_fox` | avatar_border | 10 | { type:"avatar_border", css_class:"ring-4 ring-indigo-400", preview_color:"#818cf8", label:"Night Fox" } |
      | `badge_scholar_fox` | badge | 15 | { type:"badge", icon:"📚", label:"Scholar Fox", description:"Reached level 15 — diligent study" } |
      | `color_theme_sakura` | color_theme | 20 | { type:"color_theme", css_vars:{"--color-accent":"#f472b6"}, label:"Sakura" } |

    Note: Streak-milestone rewards (7/30/100-day) are XP-economy grants, NOT reward_slot_definitions rows (per RESEARCH Section 5 note). Do NOT seed them here.

    Add `npm run seed:reward-slots` script in `package.json`:
    ```
    "seed:reward-slots": "tsx --tsconfig tsconfig.scripts.json scripts/seed/14-seed-reward-slots.ts"
    ```

    **Starter-pick verification script** `scripts/audit/verify-starter-picks.ts`:
    - Run the exact SQL from RESEARCH Section C:
      ```sql
      SELECT s.slug, s.title, s.anime, s.difficulty_tier, s.jlpt_level, s.popularity_rank
      FROM songs s
      WHERE s.difficulty_tier = 'basic'
        AND EXISTS (SELECT 1 FROM song_versions sv WHERE sv.song_id = s.id AND sv.lesson IS NOT NULL)
      ORDER BY s.popularity_rank ASC NULLS LAST
      LIMIT 10;
      ```
    - Print results as a table.
    - Also print the researcher's 3 recommended candidates (`wind-akeboshi`, `utakata-hanabi-supercell`, `crossing-field-lisa`) with a clear ✓ / ✗ per row indicating whether each has `difficulty_tier = 'basic'` and a non-null lesson — so the user can confirm or override in Plan 03.
    - If any recommended candidate fails the basic-tier check, print a "SUBSTITUTION NEEDED" line listing the next-best candidate from the top-10 query that matches criteria AND introduces a different anime franchise (for variety).

    Add `npm run audit:starter-picks` script to `package.json`:
    ```
    "audit:starter-picks": "tsx --tsconfig tsconfig.scripts.json scripts/audit/verify-starter-picks.ts"
    ```

    Why avoid picking the final 3 in this plan: the decision is user-facing (anime variety, vibe balance). Plan 03 hosts the checkpoint:decision.
  </action>
  <verify>
    1. `npm run seed:reward-slots` exits 0; running twice produces no duplicate errors (idempotency).
    2. `psql $DATABASE_URL -c "SELECT id, slot_type, level_threshold FROM reward_slot_definitions ORDER BY level_threshold"` shows 5 rows matching the table above.
    3. `npm run audit:starter-picks` prints a 10-row candidate table AND a ✓/✗ check for the 3 researcher recommendations.
    4. `npx tsc --noEmit -p tsconfig.scripts.json` passes for the scripts.
  </verify>
  <done>
    - reward_slot_definitions populated with exactly 5 v3.0 cosmetic rows.
    - Verification script outputs 10 candidates + researcher-pick verdicts.
    - Both scripts idempotent (re-runnable safely).
    - package.json has `seed:reward-slots` and `audit:starter-picks` entries.
    - No new npm dependencies added.
  </done>
</task>

</tasks>

<verification>
- Migration applies cleanly; `\d users` shows 13 new columns; both new tables exist.
- `npx tsc --noEmit` passes.
- Seed script populates 5 cosmetic rows idempotently.
- Starter-pick audit surfaces viable basic-tier candidates for Plan 03's decision checkpoint.
- `npm run test:unit` still green (no regressions).
- No new runtime deps installed (no `date-fns-tz`, no second confetti lib).
</verification>

<success_criteria>
- SC1 partial: path-state column `current_path_node_slug` exists; advancement logic lives in Plan 04.
- SC3 partial: streak columns exist; logic lives in Plan 02; write path in Plan 04.
- SC4 partial: cosmetic catalog + reward_slot_definitions schema exists; rendering in Plan 06.
- SC6 indirect: `difficulty_tier` left untouched (no re-tagging); JLPT-level column on vocabulary_items untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/12-learning-path-and-gamification/12-01-SUMMARY.md` covering: columns added, tables added, seed rows inserted, starter-pick candidates confirmed or flagged for substitution.
</output>
