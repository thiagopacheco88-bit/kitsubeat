---
phase: 18.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/i18n/routing.ts
  - src/i18n/request.ts
  - src/i18n/navigation.ts
  - src/messages/en/common.json
  - src/messages/en/songs.json
  - src/messages/en/exercises.json
  - src/messages/en/journal.json
  - src/messages/en/settings.json
  - src/messages/en/errors.json
  - src/messages/pt-BR/common.json
  - src/messages/pt-BR/songs.json
  - src/messages/pt-BR/exercises.json
  - src/messages/pt-BR/journal.json
  - src/messages/pt-BR/settings.json
  - src/messages/pt-BR/errors.json
  - src/messages/es/common.json
  - src/messages/es/songs.json
  - src/messages/es/exercises.json
  - src/messages/es/journal.json
  - src/messages/es/settings.json
  - src/messages/es/errors.json
  - tests/e2e/i18n-locale-routing.spec.ts
  - tests/e2e/i18n-language-picker.spec.ts
  - src/lib/journal/__tests__/articles-locale.test.ts
  - src/app/songs/components/__tests__/SongGrid-language-filter.test.tsx
autonomous: true
requirements:
  - I18N-01
  - I18N-02
  - I18N-03
  - I18N-04
  - I18N-05
  - I18N-06
  - I18N-07
  - I18N-08

must_haves:
  truths:
    - "next-intl@4.12.0 is installed in package.json"
    - "src/i18n/routing.ts exports routing with locales en/pt-BR/es and cookie name kb_locale"
    - "src/i18n/request.ts exports getRequestConfig that loads all 6 message namespaces"
    - "src/i18n/navigation.ts exports locale-aware Link, useRouter, usePathname, getPathname"
    - "All 18 message JSON files exist (6 namespaces × 3 locales) with full EN strings and PT-BR/ES stubs"
    - "4 test stub files exist with correct test shapes (Wave 0 Nyquist rule)"
  artifacts:
    - path: "src/i18n/routing.ts"
      provides: "defineRouting config with localeCookie: { name: 'kb_locale' }"
      contains: "kb_locale"
    - path: "src/i18n/request.ts"
      provides: "getRequestConfig loading 6 namespaces per locale"
      contains: "getRequestConfig"
    - path: "src/i18n/navigation.ts"
      provides: "createNavigation exports"
      contains: "createNavigation"
    - path: "src/messages/en/common.json"
      provides: "EN nav + language strings"
      contains: "changeLanguage"
    - path: "src/messages/pt-BR/common.json"
      provides: "PT-BR nav strings"
      contains: "changeLanguage"
    - path: "src/messages/es/common.json"
      provides: "ES nav strings"
      contains: "changeLanguage"
  key_links:
    - from: "src/i18n/routing.ts"
      to: "src/i18n/request.ts"
      via: "routing import"
      pattern: "from.*./routing"
    - from: "src/i18n/request.ts"
      to: "src/messages/${locale}/*.json"
      via: "dynamic import"
      pattern: "messages.*locale"
---

<objective>
Install next-intl@4.12.0, scaffold the three i18n config files, create all 18 message JSON files with complete EN source strings and PT-BR/ES stubs, and create the 4 test stub files required by Wave 0.

Purpose: This is the blocking foundation for all other plans in Phase 18.1. Nothing can be implemented until next-intl is installed and the message architecture is in place.
Output: 26 files created — 3 i18n config files, 18 message JSON files (6 namespaces × 3 locales), 4 test stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18.1-ui-internationalization/18.1-CONTEXT.md
@.planning/phases/18.1-ui-internationalization/18.1-RESEARCH.md
@.planning/phases/18.1-ui-internationalization/18.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install next-intl and scaffold i18n config files</name>
  <files>
    package.json
    src/i18n/routing.ts
    src/i18n/request.ts
    src/i18n/navigation.ts
  </files>
  <read_first>
    - package.json (confirm next-intl not already installed, note existing dependencies)
    - src/middleware.ts (confirm Clerk middleware structure before wiring)
    - next.config.ts (confirm existing plugin chain: withSentryConfig(enableAnalyzer(...)))
  </read_first>
  <action>
Run: `npm install next-intl@4.12.0`

Create `src/i18n/routing.ts` with EXACT content:
```typescript
import { defineRouting } from 'next-intl/routing';

export const routing = defineRouting({
  locales: ['en', 'pt-BR', 'es'],
  defaultLocale: 'en',
  localePrefix: 'as-needed',
  localeCookie: {
    name: 'kb_locale',
    maxAge: 60 * 60 * 24 * 365,
  },
});
```

Create `src/i18n/request.ts` with EXACT content:
```typescript
import { getRequestConfig } from 'next-intl/server';
import { hasLocale } from 'next-intl';
import { routing } from './routing';

export default getRequestConfig(async ({ requestLocale }) => {
  const requested = await requestLocale;
  const locale = hasLocale(routing.locales, requested)
    ? requested
    : routing.defaultLocale;

  return {
    locale,
    messages: {
      ...(await import(`../../messages/${locale}/common.json`)).default,
      ...(await import(`../../messages/${locale}/songs.json`)).default,
      ...(await import(`../../messages/${locale}/exercises.json`)).default,
      ...(await import(`../../messages/${locale}/journal.json`)).default,
      ...(await import(`../../messages/${locale}/settings.json`)).default,
      ...(await import(`../../messages/${locale}/errors.json`)).default,
    },
  };
});
```

Create `src/i18n/navigation.ts` with EXACT content:
```typescript
import { createNavigation } from 'next-intl/navigation';
import { routing } from './routing';

export const { Link, redirect, usePathname, useRouter, getPathname } =
  createNavigation(routing);
```
  </action>
  <verify>
    <automated>node -e "const p = require('./package.json'); if (!p.dependencies['next-intl']) throw new Error('next-intl not installed');" && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies contains `"next-intl": "^4.12.0"` or exact `"4.12.0"`
    - src/i18n/routing.ts contains `localeCookie` and `kb_locale`
    - src/i18n/routing.ts contains `localePrefix: 'as-needed'`
    - src/i18n/routing.ts contains `locales: ['en', 'pt-BR', 'es']`
    - src/i18n/request.ts contains `getRequestConfig`
    - src/i18n/request.ts contains `messages/${locale}/common.json`
    - src/i18n/navigation.ts contains `createNavigation`
    - src/i18n/navigation.ts contains `getPathname`
  </acceptance_criteria>
  <done>next-intl installed; 3 i18n config files exist with exact content from RESEARCH.md Patterns 1, 7, and navigation export.</done>
</task>

<task type="auto">
  <name>Task 2: Create all 18 message JSON files with EN source strings and PT-BR/ES stubs</name>
  <files>
    src/messages/en/common.json
    src/messages/en/songs.json
    src/messages/en/exercises.json
    src/messages/en/journal.json
    src/messages/en/settings.json
    src/messages/en/errors.json
    src/messages/pt-BR/common.json
    src/messages/pt-BR/songs.json
    src/messages/pt-BR/exercises.json
    src/messages/pt-BR/journal.json
    src/messages/pt-BR/settings.json
    src/messages/pt-BR/errors.json
    src/messages/es/common.json
    src/messages/es/songs.json
    src/messages/es/exercises.json
    src/messages/es/journal.json
    src/messages/es/settings.json
    src/messages/es/errors.json
  </files>
  <read_first>
    - src/app/layout.tsx (current nav link labels to extract)
    - src/app/songs/page.tsx (songs page heading and subheading strings)
    - src/app/songs/components/SongGrid.tsx (filter label strings, empty state text)
    - src/app/journal/page.tsx (journal page heading strings)
    - .planning/phases/18.1-ui-internationalization/18.1-UI-SPEC.md (copywriting contract section)
    - .planning/phases/18.1-ui-internationalization/18.1-RESEARCH.md (translation key convention examples)
  </read_first>
  <action>
Create directory structure: `src/messages/en/`, `src/messages/pt-BR/`, `src/messages/es/`

**EN source strings (must be complete — PT-BR/ES will translate these in Wave 4):**

`src/messages/en/common.json`:
```json
{
  "nav": {
    "path": "Path",
    "songs": "Songs",
    "kana": "Kana",
    "journal": "Journal",
    "profile": "Profile",
    "admin": "Admin",
    "signIn": "Sign in",
    "changeLanguage": "Change language",
    "selectLanguage": "Select language"
  },
  "language": {
    "en": "English",
    "ptBR": "Português",
    "es": "Español"
  },
  "skip": {
    "toMain": "Skip to main content"
  }
}
```

`src/messages/en/songs.json`:
```json
{
  "heading": "Songs",
  "subheading": "Browse openings, endings, and full tracks by level, difficulty, artist, or anime.",
  "filter": {
    "search": "Search songs, artists, anime...",
    "language": "Language",
    "clear": "Clear",
    "langJa": "JA",
    "langEn": "EN",
    "langPt": "PT",
    "langEs": "ES"
  },
  "empty": {
    "noLanguage": {
      "heading": "No songs in this language",
      "body": "Try a different language or clear the filter to see all songs.",
      "cta": "Clear filter"
    },
    "noFilters": {
      "heading": "No songs match your filters",
      "body": "Try clearing JLPT or difficulty filters, or searching for a different artist or anime."
    }
  }
}
```

`src/messages/en/exercises.json`:
```json
{
  "session": {
    "start": "Start Session",
    "resume": "Resume Session",
    "finish": "Finish",
    "skip": "Skip",
    "next": "Next",
    "correct": "Correct!",
    "incorrect": "Incorrect",
    "rating": {
      "again": "Again",
      "hard": "Hard",
      "good": "Good",
      "easy": "Easy"
    }
  },
  "types": {
    "vocabMeaning": "Vocabulary → Meaning",
    "meaningVocab": "Meaning → Vocabulary",
    "readingMatch": "Reading Match",
    "fillLyric": "Fill the Lyric",
    "conjugation": "Grammar Conjugation",
    "listeningDrill": "Listening Drill",
    "sentenceOrder": "Sentence Order"
  },
  "instructions": {
    "fillLyric": "Listen to the verse and pick the missing word.",
    "listeningDrill": "Listen to the verse without lyrics and identify the word.",
    "sentenceOrder": "Tap the words to rebuild the sentence in the correct order.",
    "conjugation": "Pick the correct conjugated form of the highlighted verb."
  },
  "summary": {
    "heading": "Session Complete",
    "stars": "{count} star earned",
    "starsPlural": "{count} stars earned",
    "mastered": "You earned 3 stars — song mastered!",
    "bonusUnlocked": "Bonus mastery unlocked!"
  },
  "premium": {
    "upsellHeading": "Unlock Advanced Drills",
    "upsellBody": "Advanced drills include Listening Drill, Grammar Conjugation, and Sentence Order exercises.",
    "upgrade": "Upgrade to Premium"
  }
}
```

`src/messages/en/journal.json`:
```json
{
  "heading": "Journal",
  "subheading": "Japanese language and culture through the lens of anime.",
  "readMore": "Read more",
  "readingTime": "{time} read",
  "badge": {
    "en": "EN"
  },
  "article": {
    "backToJournal": "Back to Journal",
    "publishedOn": "Published on {date}",
    "tags": "Tags"
  }
}
```

`src/messages/en/settings.json`:
```json
{
  "heading": "Profile",
  "language": {
    "label": "Language",
    "description": "Choose the language for the app interface."
  },
  "theme": {
    "label": "Theme",
    "light": "Light",
    "dark": "Dark",
    "system": "System"
  },
  "account": {
    "heading": "Account",
    "email": "Email",
    "signOut": "Sign out"
  },
  "data": {
    "heading": "Your Data",
    "export": "Export my data",
    "exporting": "Exporting..."
  }
}
```

`src/messages/en/errors.json`:
```json
{
  "generic": {
    "heading": "Something went wrong",
    "body": "Please try again. If the problem persists, contact support.",
    "retry": "Try again"
  },
  "notFound": {
    "heading": "Page not found",
    "body": "The page you are looking for does not exist.",
    "backHome": "Back to home"
  },
  "locale": {
    "switchFailed": "Couldn't save your language preference. Your selection will be remembered for this session."
  },
  "song": {
    "notFound": "Song not found.",
    "noLesson": "This song doesn't have a lesson yet."
  },
  "exercise": {
    "listeningUnavailable": "Listening Drill unavailable for this song (video not playable). Star 3 is unreachable until the video works."
  }
}
```

**PT-BR stubs — full translations (not placeholders):**

`src/messages/pt-BR/common.json`:
```json
{
  "nav": {
    "path": "Trilha",
    "songs": "Músicas",
    "kana": "Kana",
    "journal": "Blog",
    "profile": "Perfil",
    "admin": "Admin",
    "signIn": "Entrar",
    "changeLanguage": "Alterar idioma",
    "selectLanguage": "Selecionar idioma"
  },
  "language": {
    "en": "English",
    "ptBR": "Português",
    "es": "Español"
  },
  "skip": {
    "toMain": "Pular para o conteúdo principal"
  }
}
```

`src/messages/pt-BR/songs.json`:
```json
{
  "heading": "Músicas",
  "subheading": "Explore aberturas, encerramentos e faixas completas por nível, dificuldade, artista ou anime.",
  "filter": {
    "search": "Pesquisar músicas, artistas, anime...",
    "language": "Idioma",
    "clear": "Limpar",
    "langJa": "JA",
    "langEn": "EN",
    "langPt": "PT",
    "langEs": "ES"
  },
  "empty": {
    "noLanguage": {
      "heading": "Nenhuma música neste idioma",
      "body": "Tente outro idioma ou limpe o filtro para ver todas as músicas.",
      "cta": "Limpar filtro"
    },
    "noFilters": {
      "heading": "Nenhuma música encontrada",
      "body": "Tente limpar os filtros de JLPT ou dificuldade, ou pesquise por outro artista ou anime."
    }
  }
}
```

`src/messages/pt-BR/exercises.json`:
```json
{
  "session": {
    "start": "Iniciar Sessão",
    "resume": "Retomar Sessão",
    "finish": "Concluir",
    "skip": "Pular",
    "next": "Próximo",
    "correct": "Correto!",
    "incorrect": "Incorreto",
    "rating": {
      "again": "Novamente",
      "hard": "Difícil",
      "good": "Bom",
      "easy": "Fácil"
    }
  },
  "types": {
    "vocabMeaning": "Vocabulário → Significado",
    "meaningVocab": "Significado → Vocabulário",
    "readingMatch": "Correspondência de Leitura",
    "fillLyric": "Complete a Letra",
    "conjugation": "Conjugação Gramatical",
    "listeningDrill": "Treino de Audição",
    "sentenceOrder": "Ordem das Palavras"
  },
  "instructions": {
    "fillLyric": "Ouça o verso e escolha a palavra que falta.",
    "listeningDrill": "Ouça o verso sem a letra e identifique a palavra.",
    "sentenceOrder": "Toque nas palavras para reconstruir a frase na ordem correta.",
    "conjugation": "Escolha a forma conjugada correta do verbo destacado."
  },
  "summary": {
    "heading": "Sessão Concluída",
    "stars": "{count} estrela conquistada",
    "starsPlural": "{count} estrelas conquistadas",
    "mastered": "Você ganhou 3 estrelas — música dominada!",
    "bonusUnlocked": "Maestria bônus desbloqueada!"
  },
  "premium": {
    "upsellHeading": "Desbloqueie os Treinos Avançados",
    "upsellBody": "Os treinos avançados incluem Treino de Audição, Conjugação Gramatical e Ordem das Palavras.",
    "upgrade": "Assinar Premium"
  }
}
```

`src/messages/pt-BR/journal.json`:
```json
{
  "heading": "Blog",
  "subheading": "Língua e cultura japonesa através das lentes do anime.",
  "readMore": "Ler mais",
  "readingTime": "Leitura de {time}",
  "badge": {
    "en": "EN"
  },
  "article": {
    "backToJournal": "Voltar ao Blog",
    "publishedOn": "Publicado em {date}",
    "tags": "Tags"
  }
}
```

`src/messages/pt-BR/settings.json`:
```json
{
  "heading": "Perfil",
  "language": {
    "label": "Idioma",
    "description": "Escolha o idioma da interface do aplicativo."
  },
  "theme": {
    "label": "Tema",
    "light": "Claro",
    "dark": "Escuro",
    "system": "Sistema"
  },
  "account": {
    "heading": "Conta",
    "email": "E-mail",
    "signOut": "Sair"
  },
  "data": {
    "heading": "Seus Dados",
    "export": "Exportar meus dados",
    "exporting": "Exportando..."
  }
}
```

`src/messages/pt-BR/errors.json`:
```json
{
  "generic": {
    "heading": "Algo deu errado",
    "body": "Tente novamente. Se o problema persistir, entre em contato com o suporte.",
    "retry": "Tentar novamente"
  },
  "notFound": {
    "heading": "Página não encontrada",
    "body": "A página que você está procurando não existe.",
    "backHome": "Voltar ao início"
  },
  "locale": {
    "switchFailed": "Não foi possível salvar sua preferência de idioma. Sua seleção será lembrada nesta sessão."
  },
  "song": {
    "notFound": "Música não encontrada.",
    "noLesson": "Esta música ainda não tem uma lição."
  },
  "exercise": {
    "listeningUnavailable": "Treino de Audição indisponível para esta música (vídeo não reproduzível). A Estrela 3 só poderá ser alcançada quando o vídeo funcionar."
  }
}
```

**ES stubs — full translations:**

`src/messages/es/common.json`:
```json
{
  "nav": {
    "path": "Ruta",
    "songs": "Canciones",
    "kana": "Kana",
    "journal": "Blog",
    "profile": "Perfil",
    "admin": "Admin",
    "signIn": "Iniciar sesión",
    "changeLanguage": "Cambiar idioma",
    "selectLanguage": "Seleccionar idioma"
  },
  "language": {
    "en": "English",
    "ptBR": "Português",
    "es": "Español"
  },
  "skip": {
    "toMain": "Ir al contenido principal"
  }
}
```

`src/messages/es/songs.json`:
```json
{
  "heading": "Canciones",
  "subheading": "Explora aperturas, cierres y pistas completas por nivel, dificultad, artista o anime.",
  "filter": {
    "search": "Buscar canciones, artistas, anime...",
    "language": "Idioma",
    "clear": "Limpiar",
    "langJa": "JA",
    "langEn": "EN",
    "langPt": "PT",
    "langEs": "ES"
  },
  "empty": {
    "noLanguage": {
      "heading": "No hay canciones en este idioma",
      "body": "Prueba con otro idioma o limpia el filtro para ver todas las canciones.",
      "cta": "Limpiar filtro"
    },
    "noFilters": {
      "heading": "No hay canciones que coincidan",
      "body": "Intenta limpiar los filtros de JLPT o dificultad, o busca otro artista o anime."
    }
  }
}
```

`src/messages/es/exercises.json`:
```json
{
  "session": {
    "start": "Iniciar Sesión",
    "resume": "Reanudar Sesión",
    "finish": "Finalizar",
    "skip": "Omitir",
    "next": "Siguiente",
    "correct": "¡Correcto!",
    "incorrect": "Incorrecto",
    "rating": {
      "again": "De nuevo",
      "hard": "Difícil",
      "good": "Bien",
      "easy": "Fácil"
    }
  },
  "types": {
    "vocabMeaning": "Vocabulario → Significado",
    "meaningVocab": "Significado → Vocabulario",
    "readingMatch": "Correspondencia de Lectura",
    "fillLyric": "Completa la Letra",
    "conjugation": "Conjugación Gramatical",
    "listeningDrill": "Práctica de Escucha",
    "sentenceOrder": "Orden de Palabras"
  },
  "instructions": {
    "fillLyric": "Escucha el verso y elige la palabra que falta.",
    "listeningDrill": "Escucha el verso sin letra e identifica la palabra.",
    "sentenceOrder": "Toca las palabras para reconstruir la oración en el orden correcto.",
    "conjugation": "Elige la forma conjugada correcta del verbo resaltado."
  },
  "summary": {
    "heading": "Sesión Completada",
    "stars": "{count} estrella ganada",
    "starsPlural": "{count} estrellas ganadas",
    "mastered": "¡Ganaste 3 estrellas — canción dominada!",
    "bonusUnlocked": "¡Dominio adicional desbloqueado!"
  },
  "premium": {
    "upsellHeading": "Desbloquea los Ejercicios Avanzados",
    "upsellBody": "Los ejercicios avanzados incluyen Práctica de Escucha, Conjugación Gramatical y Orden de Palabras.",
    "upgrade": "Actualizar a Premium"
  }
}
```

`src/messages/es/journal.json`:
```json
{
  "heading": "Blog",
  "subheading": "Lengua y cultura japonesa a través del anime.",
  "readMore": "Leer más",
  "readingTime": "Lectura de {time}",
  "badge": {
    "en": "EN"
  },
  "article": {
    "backToJournal": "Volver al Blog",
    "publishedOn": "Publicado el {date}",
    "tags": "Etiquetas"
  }
}
```

`src/messages/es/settings.json`:
```json
{
  "heading": "Perfil",
  "language": {
    "label": "Idioma",
    "description": "Elige el idioma de la interfaz de la aplicación."
  },
  "theme": {
    "label": "Tema",
    "light": "Claro",
    "dark": "Oscuro",
    "system": "Sistema"
  },
  "account": {
    "heading": "Cuenta",
    "email": "Correo electrónico",
    "signOut": "Cerrar sesión"
  },
  "data": {
    "heading": "Tus Datos",
    "export": "Exportar mis datos",
    "exporting": "Exportando..."
  }
}
```

`src/messages/es/errors.json`:
```json
{
  "generic": {
    "heading": "Algo salió mal",
    "body": "Inténtalo de nuevo. Si el problema persiste, contacta al soporte.",
    "retry": "Intentar de nuevo"
  },
  "notFound": {
    "heading": "Página no encontrada",
    "body": "La página que buscas no existe.",
    "backHome": "Volver al inicio"
  },
  "locale": {
    "switchFailed": "No se pudo guardar tu preferencia de idioma. Tu selección se recordará durante esta sesión."
  },
  "song": {
    "notFound": "Canción no encontrada.",
    "noLesson": "Esta canción aún no tiene una lección."
  },
  "exercise": {
    "listeningUnavailable": "Práctica de Escucha no disponible para esta canción (el video no se puede reproducir). La Estrella 3 no estará disponible hasta que el video funcione."
  }
}
```
  </action>
  <verify>
    <automated>node -e "['en','pt-BR','es'].forEach(l => ['common','songs','exercises','journal','settings','errors'].forEach(n => { try { require('./src/messages/'+l+'/'+n+'.json'); } catch(e) { throw new Error('Missing: '+l+'/'+n+'.json'); } })); console.log('All 18 message files present')"</automated>
  </verify>
  <acceptance_criteria>
    - All 18 files exist at src/messages/{locale}/{namespace}.json
    - src/messages/en/common.json contains `changeLanguage`
    - src/messages/pt-BR/common.json contains `Alterar idioma`
    - src/messages/es/common.json contains `Cambiar idioma`
    - src/messages/en/songs.json contains `noLanguage` under `empty`
    - src/messages/pt-BR/songs.json contains `Nenhuma música neste idioma`
    - src/messages/es/songs.json contains `No hay canciones en este idioma`
    - src/messages/en/errors.json contains `listeningUnavailable`
    - No file contains placeholder text like "TODO" or "STUB"
  </acceptance_criteria>
  <done>All 18 message JSON files created with complete EN source strings and full PT-BR/ES translations.</done>
</task>

<task type="auto">
  <name>Task 3: Create Wave 0 test stubs for i18n requirements</name>
  <files>
    tests/e2e/i18n-locale-routing.spec.ts
    tests/e2e/i18n-language-picker.spec.ts
    src/lib/journal/__tests__/articles-locale.test.ts
    src/app/songs/components/__tests__/SongGrid-language-filter.test.tsx
  </files>
  <read_first>
    - tests/e2e/i18n-locale-routing.spec.ts (confirm does not exist yet)
    - src/lib/journal/__tests__/ directory (check for existing test files)
    - src/app/songs/components/__tests__/ directory (check for existing test files)
    - playwright.config.ts (confirm baseURL and test runner setup)
    - vitest.config.ts (confirm test environment setup)
  </read_first>
  <action>
Create `tests/e2e/i18n-locale-routing.spec.ts`:
```typescript
import { test, expect } from '@playwright/test';

// I18N-01: PT-BR route renders PT-BR strings
// I18N-02: ES route renders ES strings
// I18N-07: hreflang headers present on locale pages
// These tests require the app running with next-intl wired (Wave 2+).
// Wave 0 stubs — implement after Wave 2 is complete.

test.describe('i18n locale routing', () => {
  test.skip('PT-BR route renders PT-BR nav strings', async ({ page }) => {
    await page.goto('/pt-BR/songs');
    await expect(page.getByRole('link', { name: 'Músicas' })).toBeVisible();
  });

  test.skip('ES route renders ES nav strings', async ({ page }) => {
    await page.goto('/es/songs');
    await expect(page.getByRole('link', { name: 'Canciones' })).toBeVisible();
  });

  test.skip('EN root / renders EN nav strings (canonical, no prefix)', async ({ page }) => {
    await page.goto('/songs');
    await expect(page.getByRole('link', { name: 'Songs' })).toBeVisible();
  });

  test.skip('hreflang link headers present on PT-BR pages', async ({ page }) => {
    const response = await page.goto('/pt-BR/songs');
    const linkHeader = response?.headers()['link'] ?? '';
    expect(linkHeader).toContain('hreflang');
  });

  test.skip('html lang attribute set to pt-BR on PT-BR pages', async ({ page }) => {
    await page.goto('/pt-BR/songs');
    const lang = await page.locator('html').getAttribute('lang');
    expect(lang).toBe('pt-BR');
  });

  test.skip('html lang attribute set to es on ES pages', async ({ page }) => {
    await page.goto('/es/songs');
    const lang = await page.locator('html').getAttribute('lang');
    expect(lang).toBe('es');
  });
});
```

Create `tests/e2e/i18n-language-picker.spec.ts`:
```typescript
import { test, expect } from '@playwright/test';

// I18N-03: Globe picker switches locale + cookie set
// These tests require the full app running with LanguagePicker wired (Wave 3+).
// Wave 0 stubs.

test.describe('i18n language picker', () => {
  test.skip('globe button is visible in the nav', async ({ page }) => {
    await page.goto('/');
    const globeBtn = page.getByRole('button', { name: 'Change language' });
    await expect(globeBtn).toBeVisible();
  });

  test.skip('clicking globe button opens language dropdown', async ({ page }) => {
    await page.goto('/');
    await page.getByRole('button', { name: 'Change language' }).click();
    await expect(page.getByRole('option', { name: 'Português' })).toBeVisible();
  });

  test.skip('selecting Português navigates to /pt-BR/ and sets kb_locale cookie', async ({ page, context }) => {
    await page.goto('/');
    await page.getByRole('button', { name: 'Change language' }).click();
    await page.getByRole('option', { name: 'Português' }).click();
    await page.waitForURL(/\/pt-BR\//);
    const cookies = await context.cookies();
    const localeCookie = cookies.find(c => c.name === 'kb_locale');
    expect(localeCookie?.value).toBe('pt-BR');
  });

  test.skip('Escape key closes the language dropdown', async ({ page }) => {
    await page.goto('/');
    await page.getByRole('button', { name: 'Change language' }).click();
    await page.keyboard.press('Escape');
    await expect(page.getByRole('option', { name: 'Português' })).not.toBeVisible();
  });
});
```

Create `src/lib/journal/__tests__/articles-locale.test.ts`:
```typescript
import { describe, it, expect, vi } from 'vitest';

// I18N-06: Journal locale articles appear first for matching locale
// These tests require getArticlesByLocale implemented in articles.ts (Wave 3b).
// Wave 0 stubs.

describe('getArticlesByLocale', () => {
  it.todo('returns EN articles from CONTENT_DIR for locale=en');
  it.todo('returns PT-BR articles from CONTENT_DIR/pt-BR for locale=pt-BR');
  it.todo('returns ES articles from CONTENT_DIR/es for locale=es');
  it.todo('returns empty array if locale subdir does not exist');
  it.todo('articles are sorted by date descending');
});

describe('getArticleSourceByLocale', () => {
  it.todo('returns locale MDX content when locale file exists');
  it.todo('falls back to EN content when locale file is missing');
  it.todo('returns null for slugs with path traversal attempts (e.g. ../admin)');
  it.todo('returns null for slugs with uppercase letters (SAFE_SLUG_RE guard)');
});
```

Create `src/app/songs/components/__tests__/SongGrid-language-filter.test.tsx`:
```typescript
import { describe, it, expect } from 'vitest';

// I18N-05: Language filter chip renders and filters songs
// These tests require SongGrid extended with languageFilter (Wave 3b).
// Wave 0 stubs.

describe('SongGrid language filter', () => {
  it.todo('renders JA, EN, PT, ES filter chips');
  it.todo('clicking JA chip sets languageFilter to "ja" and filters songs');
  it.todo('clicking active chip again deselects it (shows all songs)');
  it.todo('Clear button resets languageFilter alongside JLPT and difficulty');
  it.todo('filtering to JA shows only songs with language === "ja"');
  it.todo('filtering to EN shows only songs with language === "en"');
  it.todo('each chip has aria-pressed matching its active state');
});
```
  </action>
  <verify>
    <automated>npx vitest run src/lib/journal/__tests__/articles-locale.test.ts 2>&1 | grep -E "passed|skipped|todo|fail"</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/i18n-locale-routing.spec.ts exists and contains `test.skip` for 6 tests
    - tests/e2e/i18n-language-picker.spec.ts exists and contains `test.skip` for 4 tests
    - src/lib/journal/__tests__/articles-locale.test.ts exists with `it.todo` stubs
    - src/app/songs/components/__tests__/SongGrid-language-filter.test.tsx exists with `it.todo` stubs
    - `npx vitest run src/lib/journal/__tests__/articles-locale.test.ts` exits without crash (todos are valid)
  </acceptance_criteria>
  <done>All 4 Wave 0 test stub files created. Executors for Wave 3+ will implement these tests when the features are built.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → middleware | Locale segment value from URL path enters `[locale]` param |
| Cookie → middleware | `kb_locale` cookie value drives locale detection |
| Message files → client | Translation JSON loaded server-side and passed via NextIntlClientProvider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.1-01 | Tampering | `[locale]` URL segment | mitigate | `hasLocale(routing.locales, locale)` whitelist check in layout.tsx (Wave 2) — only `['en','pt-BR','es']` pass; others hit `notFound()` |
| T-18.1-02 | Tampering | `kb_locale` cookie value | accept | Cookie is preference-only, not session-critical; worst case: wrong locale rendered; next-intl validates against routing.locales before use |
| T-18.1-03 | Information Disclosure | Translation message JSON | accept | Messages are public UI strings, not secrets; no PII in translation files |
</threat_model>

<verification>
- `npm install next-intl@4.12.0` succeeds
- `node -e "require('./src/i18n/routing.ts')"` does not throw (TypeScript compile check via tsc)
- All 18 JSON files parseable (node -e "require('./src/messages/...')")
- 4 test stub files discoverable by `npx playwright test --list` and `npx vitest run`
- `npx tsc --noEmit` passes or shows only pre-existing errors (not new errors from i18n files)
</verification>

<success_criteria>
- next-intl@4.12.0 in package.json dependencies
- 3 i18n config files in src/i18n/ with exact content from RESEARCH.md
- 18 message JSON files across src/messages/en/, src/messages/pt-BR/, src/messages/es/
- All EN strings complete, all PT-BR/ES strings fully translated (not stubs)
- 4 Wave 0 test stub files exist and are discoverable
</success_criteria>

<output>
After completion, create `.planning/phases/18.1-ui-internationalization/18.1-01-SUMMARY.md`
</output>
