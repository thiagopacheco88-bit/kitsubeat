---
phase: 11.3-fix-untranslated-jp-verses
plan: 08
type: execute
wave: 6
depends_on: ["11.3-07"]
files_modified:
  - data/lessons-cache/again-yui.json
  - data/lessons-cache/whats-up-people-maximum-the-hormone.json
  - data/lessons-cache/diver-nico-touches-the-walls.json
  - data/lessons-cache/mezamero-yasei-matchy-with-question.json
  - data/lessons-cache/name-of-love-cinema-staff.json
autonomous: true
requirements: [SPEC-REQ-6, SPEC-REQ-7]
must_haves:
  truths:
    - "All 5 remaining in-scope songs (after the smoke song) have their patch files applied and pushed to Neon"
    - "The single-command audit-untranslated-verses.ts --verify --slugs=<6-csv> exits 0, proving cache + DB both have zero stubs for ALL 6 in-scope slugs"
    - "Phase 11.3 SPEC acceptance criterion #9 is satisfied verbatim — the named --verify command from SPEC.md line 111 exits 0"
  artifacts:
    - path: "data/lessons-cache/{5 remaining slugs}.json"
      provides: "Patched lessons with zero stub verses for the remaining 5 in-scope songs"
      contains: "again-yui"
  key_links:
    - from: "scripts/seed/apply-verse-patch.ts (--all OR per-slug loop)"
      to: "data/lessons-cache/<5 slugs>.json"
      via: "two-pass merge per slug"
      pattern: "replace_verse_number"
    - from: "scripts/seed/05-insert-db.ts --slug=<each>"
      to: "Neon song_versions.lesson"
      via: "per-slug push, called 5 times"
      pattern: "05-insert-db.*--slug="
    - from: "audit-untranslated-verses.ts --verify --slugs=<6-csv>"
      to: "exit code 0"
      via: "joint cache + DB stub-count gate"
      pattern: "exit 0"
---

<objective>
Apply the verse patches for the 5 remaining in-scope songs (everything EXCEPT yellow-moon-akeboshi which Plan 07 already shipped), push each to Neon via `05-insert-db.ts --slug=<each>`, then run the final phase verification gate: `audit-untranslated-verses.ts --verify --slugs=<all 6>` MUST exit 0.

Per SPEC.md acceptance criterion 9 (verbatim):
> `npx tsx scripts/seed/audit-untranslated-verses.ts --verify --slugs=again-yui,whats-up-people-maximum-the-hormone,diver-nico-touches-the-walls,mezamero-yasei-matchy-with-question,name-of-love-cinema-staff,yellow-moon-akeboshi` exits 0

That single command is the closing gate of the entire phase. When it exits 0, the 6 worst-offender songs (159 stubs) are clean across both data/lessons-cache/ and Neon song_versions.lesson — and the phase ships.

Purpose: SPEC-REQ-6 ("DB push per slug") + SPEC-REQ-7 ("Zero-stub verification gate"). Plan 07 proved the pipeline works for one song; Plan 08 fans it out to the remaining five and locks the phase exit.

Output: 5 patched cache files + 5 Neon row updates + 1 single passing verification command.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11.3-fix-untranslated-jp-verses/11.3-SPEC.md
@.planning/phases/11.3-fix-untranslated-jp-verses/11.3-CONTEXT.md
@.planning/phases/11.3-fix-untranslated-jp-verses/11.3-07-SUMMARY.md

<interfaces>
<!-- Plan 03 extended apply-verse-patch.ts: -->
<!--   npx tsx scripts/seed/apply-verse-patch.ts <slug>  (or --all) -->

<!-- Plan 02 audit-untranslated-verses.ts --verify gate: -->
<!--   --verify --slugs=<csv>  exits 0 only when both cache + DB have zero stubs across the listed slugs -->

<!-- Existing 05-insert-db.ts (commit 7f1dc95): -->
<!--   --slug=<slug>  pushes one slug's lesson JSONB to Neon song_versions.lesson -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply patches + push 5 remaining in-scope songs to Neon</name>
  <files>data/lessons-cache/again-yui.json, data/lessons-cache/whats-up-people-maximum-the-hormone.json, data/lessons-cache/diver-nico-touches-the-walls.json, data/lessons-cache/mezamero-yasei-matchy-with-question.json, data/lessons-cache/name-of-love-cinema-staff.json</files>
  <read_first>
    - .planning/phases/11.3-fix-untranslated-jp-verses/11.3-07-SUMMARY.md (confirm smoke passed; this plan only runs after Plan 07's checkpoint approval)
    - .planning/verse-patches/again-yui.json (sanity check at least the first slug's patch file has the expected 38 replace_verse_number entries before applying)
    - scripts/seed/apply-verse-patch.ts (Plan 03 extended file)
    - scripts/seed/05-insert-db.ts (the existing --slug= filter)
  </read_first>
  <action>
    Iterate the 5 remaining slugs (in this exact order — same order as SPEC.md table, minus yellow-moon-akeboshi which Plan 07 already shipped):

    1. `again-yui` (38 stubs)
    2. `whats-up-people-maximum-the-hormone` (29 stubs)
    3. `diver-nico-touches-the-walls` (28 stubs)
    4. `mezamero-yasei-matchy-with-question` (22 stubs)
    5. `name-of-love-cinema-staff` (21 stubs)

    For EACH slug, in order:

    a. **Pre-snapshot stub count:**
       ```bash
       node -e "const l=require('./data/lessons-cache/<slug>.json');const stubs=l.verses.filter(v=>v.tokens.length===1&&/[぀-ゟ゠-ヿ一-鿿]/.test(v.tokens[0].surface)&&['en','pt-BR','es'].some(loc=>(v.translations?.[loc]??'').startsWith('(')));console.log('<slug> pre-patch stubs:',stubs.length)"
       ```
       Expected counts (must match SPEC table): again-yui 38, whats-up... 29, diver... 28, mezamero... 22, name-of-love... 21. Total 138 (+ 21 already-shipped yellow-moon = 159).

    b. **Apply patch:**
       ```bash
       npx tsx scripts/seed/apply-verse-patch.ts <slug>
       ```
       Capture stdout. Expect `[ok] <slug>: <before> → <after> verses` with skipped count typically 0 (or ≥0 if any append-mode entries from prior auto-coverage runs are already in the file — those are tracked separately and idempotency catches them).

    c. **Cache-side post-patch sanity:**
       ```bash
       node -e "<same one-liner as step a, but reports post-patch>"
       ```
       Expect `<slug> post-patch stubs: 0`. If non-zero: STOP, do not push to Neon for this slug. Investigate which patch entries failed to apply (likely a verse_number mismatch — the lesson cache verse_numbers may have shifted if a prior auto-coverage append entry was applied between Plan 02's audit and Plan 08's apply). This is a real risk because Plan 02's queue records the verse_number AT AUDIT TIME, and any append-mode entries applied AFTERWARD shift downstream verse_numbers. If detected, the recovery path is: re-run Plan 02's audit + Plan 05's drafter for the affected slug only, with `--slug=<slug> --resume` to fill in the gaps.

    d. **Push to Neon:**
       ```bash
       npx tsx scripts/seed/05-insert-db.ts --slug=<slug>
       ```
       Capture stdout. The script's existing logging confirms the JSONB write.

    e. **Per-slug verify:**
       ```bash
       npx tsx scripts/seed/audit-untranslated-verses.ts --verify --slugs=<slug>
       ```
       Expect exit 0 with `[verify cache] <slug>: 0 stubs` and `[verify db] <slug>: 0 stubs`.

       If exit non-zero on the DB side: 05-insert-db.ts may have failed silently or hit a constraint. Re-read its stdout from step (d) and investigate.

    Continue to the next slug only when steps (a)–(e) are clean.

    NOTE: do NOT use `apply-verse-patch.ts --all`. The --all runner would also re-apply patches for slugs OUTSIDE the in-scope 6 (the patches dir has ~10 files from prior auto-coverage runs, including non-in-scope songs). Per-slug invocation isolates the rollout to the 5 target songs.

    NOTE on verse_number drift: the risk in step (c) is theoretical only IF prior append-mode entries were applied between when Plan 02 audited and when Plan 08 runs. Check this by examining `git status` on `data/lessons-cache/` — if no other process has touched these 5 files since Plan 02 ran, the verse_numbers in the queue are still correct. The apply-patch idempotency guard ALSO protects against double-apply but does not recover from drift.
  </action>
  <verify>
    <automated>for slug in again-yui whats-up-people-maximum-the-hormone diver-nico-touches-the-walls mezamero-yasei-matchy-with-question name-of-love-cinema-staff; do npx tsx scripts/seed/audit-untranslated-verses.ts --verify --slugs="$slug" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 5 slugs: cache-side post-patch stub count is 0.
    - For each of the 5 slugs: 05-insert-db.ts --slug=<slug> exits 0.
    - For each of the 5 slugs: --verify --slugs=<slug> exits 0 with both [verify cache]: 0 and [verify db]: 0.
    - All 5 cache files appear in `git status` as modified (proves the apply step actually wrote).
  </acceptance_criteria>
  <done>5 cache files patched + pushed; per-slug verify exits 0 for each.</done>
</task>

<task type="auto">
  <name>Task 2: Final phase verification gate — single combined --verify command across all 6 in-scope slugs</name>
  <files></files>
  <read_first>
    - scripts/seed/audit-untranslated-verses.ts (the file from Plan 02 — confirm its --verify mode reads cache + DB)
    - .planning/phases/11.3-fix-untranslated-jp-verses/11.3-SPEC.md (acceptance criterion 9 — copy the exact command to invoke)
  </read_first>
  <action>
    Run the verbatim command from SPEC.md acceptance criterion 9:

    ```bash
    npx tsx scripts/seed/audit-untranslated-verses.ts --verify --slugs=again-yui,whats-up-people-maximum-the-hormone,diver-nico-touches-the-walls,mezamero-yasei-matchy-with-question,name-of-love-cinema-staff,yellow-moon-akeboshi
    ```

    The command MUST exit 0. The stdout MUST contain six `[verify cache] <slug>: 0 stubs` lines AND six `[verify db] <slug>: 0 stubs` lines AND a final `[verify] PASS — zero stubs across cache + DB for 6 slugs` line.

    If exit non-zero:
    1. Identify which slug + source (cache/db) flagged stubs from stdout.
    2. If cache-side has stubs: re-run Plan 03's `apply-verse-patch.ts <slug>` for that slug.
    3. If DB-side has stubs but cache is clean: re-run `05-insert-db.ts --slug=<slug>` (cache wasn't pushed cleanly).
    4. Re-run this verify command. Iterate until exit 0.

    Capture stdout for the SUMMARY artifact. The phase ships when this command exits 0.

    Do NOT mark the phase complete until this single command exits 0.

    Implements SPEC-REQ-7 final acceptance verbatim.
  </action>
  <verify>
    <automated>npx tsx scripts/seed/audit-untranslated-verses.ts --verify --slugs=again-yui,whats-up-people-maximum-the-hormone,diver-nico-touches-the-walls,mezamero-yasei-matchy-with-question,name-of-love-cinema-staff,yellow-moon-akeboshi</automated>
  </verify>
  <acceptance_criteria>
    - The exact SPEC-acceptance-9 command exits 0.
    - Stdout contains 6 `[verify cache]` and 6 `[verify db]` zero-stub lines.
    - Stdout contains the final `PASS` line.
    - All 159 in-scope stubs are now resolved across cache + DB; no other slugs were affected.
  </acceptance_criteria>
  <done>Phase 11.3 verification gate passed; SPEC acceptance criterion 9 satisfied; phase ready to mark complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| apply-verse-patch.ts → 5 cache files | File rewrite; idempotency-guarded; per-slug isolation |
| 05-insert-db.ts → Neon | Existing channel; ran already on yellow-moon in Plan 07 |
| --verify gate → exit code | Process boundary; the single source of truth for phase completion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.3-24 | Tampering | Apply-patch silently misses some replace entries due to verse_number drift | mitigate | Per-slug step (c) cache-side post-patch count gate catches this before pushing to Neon. Recovery path documented (re-audit + --resume drafter). |
| T-11.3-25 | Tampering | 05-insert-db.ts pushes a partially-stale lesson | mitigate | Per-slug step (e) verify gate cross-checks DB against the same predicate. |
| T-11.3-26 | Repudiation | Phase declared "done" without the SPEC's exact verify command running | mitigate | Task 2 explicitly runs the SPEC-verbatim command and asserts exit code 0. The verify command itself is the audit trail. |
| T-11.3-27 | Information disclosure | DB credentials | accept | Same env vars as everything else; no new exposure. |
</threat_model>

<verification>
- 5 per-slug verify exit codes were 0 (Task 1).
- The combined SPEC-acceptance-9 verify command exit code is 0 (Task 2).
- Cache + DB both clean across all 6 in-scope slugs.
</verification>

<success_criteria>
- SPEC-REQ-6 met: each of the 6 in-scope slugs has been pushed via `05-insert-db.ts --slug=<each>`.
- SPEC-REQ-7 met: the SPEC-verbatim --verify command exits 0.
- Phase 11.3 acceptance criteria 7, 8, and 9 (the cache + DB + combined gates) are all satisfied.
- The 6 worst-offender songs (159 stub verses) are now clean across the entire pipeline.
- The remaining 96 catalog songs (811 stubs) remain deferred to follow-up todo `11.3.1` per SPEC.md boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/11.3-fix-untranslated-jp-verses/11.3-08-SUMMARY.md` recording: per-slug pre/post stub counts, 5 apply-patch stdouts, 5 05-insert-db stdouts, 5 per-slug verify exit codes, the final combined --verify stdout, and the literal exit code 0 confirmation. Append a phase-completion line: "Phase 11.3 — 159/159 stubs resolved across 6 slugs; remaining 96 songs (~811 stubs) deferred to 11.3.1."
</output>
