---
phase: 11.3-fix-untranslated-jp-verses
plan: 04
status: superseded
type: execute
completed: 2026-04-27
self_check: SUPERSEDED
---

# Plan 11.3-04 SUMMARY — qwen vs llama pilot (SUPERSEDED)

## Outcome

**Plan SUPERSEDED.** Pilot did not run because Plan 11.3-01 never finalized either model's manifest (see 11.3-01-SUMMARY.md for the network failure details).

## What was supposed to happen

Run a 10-verse head-to-head pilot of qwen2.5:14b-instruct vs llama3.1:8b drawn from across 5 of the 6 in-scope songs (excluding the smoke-test song), measure schema-pass rate + per-verse latency, write `MODEL-PILOT.md`, lock the winner into `scripts/lib/ollama-config.ts` as `LOCKED_OLLAMA_MODEL`.

## What actually happened

Operator pivoted to inline Claude drafting for the entire phase. Selecting an Ollama model is moot — the LLM choice is **Claude Opus** (the agent doing the drafting), not a local model.

`scripts/lib/ollama-config.ts` was NOT created. `MODEL-PILOT.md` was NOT created. Plan 05 (drafter) was also superseded.

## Forward path

If 11.3.1 retries the Ollama path for the remaining 842 stubs, the pilot needs to run before scaling. The pilot script template at `scripts/seed/pilot-ollama-models.ts` was never written; PATTERNS.md sections "Per-stub Ollama call" + "Schema validation gate" still apply.

## Files

None.
