---
phase: 11.3-fix-untranslated-jp-verses
plan: 07
type: execute
wave: 5
depends_on: ["11.3-03", "11.3-05", "11.3-06"]
files_modified:
  - data/lessons-cache/yellow-moon-akeboshi.json
autonomous: false
requirements: [SPEC-REQ-5, SPEC-REQ-6]
must_haves:
  truths:
    - "After applying yellow-moon-akeboshi's patch file, every previously-stub verse in data/lessons-cache/yellow-moon-akeboshi.json has a 7-field token array, all 3 locales populated, and translations not starting with `(`"
    - "After 05-insert-db.ts --slug=yellow-moon-akeboshi, Neon song_versions.lesson for that slug contains the patched lesson"
    - "Operator confirms via manual Vercel play-through that every verse on /songs/yellow-moon-akeboshi renders without `(untranslated)` text"
  artifacts:
    - path: "data/lessons-cache/yellow-moon-akeboshi.json"
      provides: "Patched lesson with zero stub verses"
      contains: "yellow-moon-akeboshi"
  key_links:
    - from: "scripts/seed/apply-verse-patch.ts (with replace-mode from Plan 03)"
      to: "data/lessons-cache/yellow-moon-akeboshi.json"
      via: "two-pass merge (replace first, then append per D-11)"
      pattern: "replace_verse_number"
    - from: "scripts/seed/05-insert-db.ts --slug=yellow-moon-akeboshi"
      to: "Neon song_versions.lesson JSONB"
      via: "per-slug Drizzle update"
      pattern: "yellow-moon-akeboshi"
    - from: "Vercel-deployed song page"
      to: "Neon-stored lesson"
      via: "page render path"
      pattern: "(untranslated)"
---

<objective>
Run the full Phase 11.3 pipeline against the smallest in-scope song — `yellow-moon-akeboshi` (21 stubs, per SPEC.md table) — as a smoke test BEFORE rolling out the remaining 5 songs. Sequence: apply that slug's patch file (replace + append modes per Plan 03), push to Neon via `05-insert-db.ts --slug=yellow-moon-akeboshi`, then perform a manual Vercel play-through eyeballing every verse for `(untranslated)` renders. Per D-09 + D-10, only proceed to Plan 08 (catalog rollout) AFTER smoke verification passes.

Purpose: D-09 mandates a smoke-test before scaling. The 21 stubs in yellow-moon-akeboshi are the irreducible MVP cut — if the pipeline ships clean for one song, it ships clean for the rest. CONTEXT.md `<specifics>` locks this slug as the preferred smoke target with `name-of-love-cinema-staff` as the fallback if yellow-moon turns out to have other data issues.

Output: 1 patched cache file, 1 row in Neon song_versions updated, 1 visual confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11.3-fix-untranslated-jp-verses/11.3-SPEC.md
@.planning/phases/11.3-fix-untranslated-jp-verses/11.3-CONTEXT.md
@.planning/verse-patches/yellow-moon-akeboshi.json

<interfaces>
<!-- Plan 03 extended apply-verse-patch.ts contract: -->
<!--   npx tsx scripts/seed/apply-verse-patch.ts <slug>  → applies replace-mode entries first (in-place), then append-mode (legacy) -->

<!-- Plan 02 audit-untranslated-verses.ts contract: -->
<!--   npx tsx scripts/seed/audit-untranslated-verses.ts --verify --slugs=<csv>  → exits 0 only if cache + DB both have zero stubs across the listed slugs -->

<!-- Existing 05-insert-db.ts contract (commit 7f1dc95): -->
<!--   npx tsx scripts/seed/05-insert-db.ts --slug=<slug>  → pushes the slug's lesson JSONB to Neon song_versions.lesson -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply yellow-moon-akeboshi patch + push to Neon + cache-side verify</name>
  <files>data/lessons-cache/yellow-moon-akeboshi.json</files>
  <read_first>
    - .planning/verse-patches/yellow-moon-akeboshi.json (confirm 21 replace_verse_number entries exist after Plan 06; sum with any prior append-mode entries should be ≥ 21)
    - data/lessons-cache/yellow-moon-akeboshi.json (snapshot pre-patch verse count + spot-check 1-2 stub verses to know the before-state)
    - scripts/seed/apply-verse-patch.ts (Plan 03 extended file — confirm replace-mode wired)
    - scripts/seed/audit-untranslated-verses.ts (Plan 02 file — confirm --verify mode wired)
    - scripts/seed/05-insert-db.ts (lines 300–340 — confirm --slug= filter)
  </read_first>
  <action>
    Run, in this exact order, capturing stdout for each step:

    1. **Pre-snapshot:** Run a cache-side stub count for yellow-moon-akeboshi only:
       ```bash
       node -e "const l=require('./data/lessons-cache/yellow-moon-akeboshi.json');const stubs=l.verses.filter(v=>v.tokens.length===1&&/[぀-ゟ゠-ヿ一-鿿]/.test(v.tokens[0].surface)&&['en','pt-BR','es'].some(loc=>(v.translations?.[loc]??'').startsWith('(')));console.log('pre-patch stubs:',stubs.length)"
       ```
       Expected output: `pre-patch stubs: 21` (matches SPEC table). If different, abort and investigate before continuing.

    2. **Apply patch:**
       ```bash
       npx tsx scripts/seed/apply-verse-patch.ts yellow-moon-akeboshi
       ```
       Expected stdout: `[ok] yellow-moon-akeboshi: <before> → <after> verses` where `before` is the original verse count and `after` is `before + (count of after_original entries — replace entries do not change verse count)`. If skipped count > 0 it means partial prior application — that's fine (idempotency working).

    3. **Cache-side post-patch stub count:**
       ```bash
       node -e "const l=require('./data/lessons-cache/yellow-moon-akeboshi.json');const stubs=l.verses.filter(v=>v.tokens.length===1&&/[぀-ゟ゠-ヿ一-鿿]/.test(v.tokens[0].surface)&&['en','pt-BR','es'].some(loc=>(v.translations?.[loc]??'').startsWith('(')));console.log('post-patch stubs:',stubs.length)"
       ```
       Expected output: `post-patch stubs: 0`. If non-zero, the replace-mode applier failed silently for some entries — abort and debug Plan 03's applier before proceeding.

    4. **Push to Neon:**
       ```bash
       npx tsx scripts/seed/05-insert-db.ts --slug=yellow-moon-akeboshi
       ```
       Expected: ok status reported by 05-insert-db's existing logging. The lesson JSONB column in song_versions for that slug now matches the patched cache file.

    5. **DB-side verify (single-slug):**
       ```bash
       npx tsx scripts/seed/audit-untranslated-verses.ts --verify --slugs=yellow-moon-akeboshi
       ```
       Expected: stdout shows `[verify cache] yellow-moon-akeboshi: 0 stubs` AND `[verify db] yellow-moon-akeboshi: 0 stubs`, and exit code 0.
       If exit non-zero: identify whether cache or DB side flagged stubs and back-trace.

    Document the captured stdout for each of the 5 steps in the SUMMARY artifact (not the plan body).

    Do NOT proceed to Plan 08 if step 5 exit code is not 0 — Plan 08's `depends_on: 11.3-07` will not allow it, and the smoke-gate D-09 is the human-verifiable equivalent.
  </action>
  <verify>
    <automated>npx tsx scripts/seed/audit-untranslated-verses.ts --verify --slugs=yellow-moon-akeboshi</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/seed/apply-verse-patch.ts yellow-moon-akeboshi` exits 0.
    - Post-patch cache-side stub count is exactly 0.
    - `npx tsx scripts/seed/05-insert-db.ts --slug=yellow-moon-akeboshi` exits 0.
    - `npx tsx scripts/seed/audit-untranslated-verses.ts --verify --slugs=yellow-moon-akeboshi` exits 0 with both `[verify cache]: 0 stubs` and `[verify db]: 0 stubs`.
    - `data/lessons-cache/yellow-moon-akeboshi.json` is git-modified (the cache file changed).
  </acceptance_criteria>
  <done>Cache + DB both clean for yellow-moon-akeboshi via the audit gate; the smoke pipeline is mechanically green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual Vercel play-through of /songs/yellow-moon-akeboshi (D-10)</name>
  <files>(visual verification only — no files written)</files>
  <action>Operator opens /songs/yellow-moon-akeboshi on production Vercel deployment and eyeballs every previously-stub verse for: (a) no "(untranslated)" or "(auto-coverage)" text, (b) pt-BR + es toggles render proper translations, (c) tokens.length > 1 with non-empty meaning.en. Detailed steps in <how-to-verify>. Per D-09, Plan 08 is BLOCKED until operator confirms clean.</action>
  <verify><automated>npx tsx scripts/seed/audit-untranslated-verses.ts --verify --slugs=yellow-moon-akeboshi</automated></verify>
  <done>Operator types "approved" with smoke verdict; audit gate exits 0; Plan 08 unblocked.</done>
  <what-built>
    Cache and Neon DB both report zero stubs for yellow-moon-akeboshi. The cache file was updated, pushed to Neon via 05-insert-db.ts, and the audit gate passed. The deployed song page on Vercel SHOULD now render every verse with proper en/pt-BR/es translations.
  </what-built>
  <how-to-verify>
    Per D-10 (manual Vercel play-through; ~5 min per song):

    1. Open https://kitsubeat.vercel.app (or whatever the production deployment URL is — check `vercel.json` or recent deploy logs if unsure).
    2. Navigate to the song detail page for yellow-moon-akeboshi (URL: `/songs/yellow-moon-akeboshi`).
    3. For EACH of the 21 verses that were previously stubs (Plan 06's review pass added entries for them), confirm:
       a. The English translation does NOT contain the literal text `(untranslated)`, `(auto-coverage)`, or any other paren-prefixed placeholder.
       b. The Portuguese (pt-BR) translation toggle (if accessible from the player UI) renders proper Portuguese text — not English fallback or paren-prefix.
       c. The Spanish (es) translation toggle renders proper Spanish text.
       d. The token-level word breakdown shows >1 token (the stub signature was `tokens.length === 1` — patched verses should have multiple tokenized words).
       e. Tooltips on individual tokens show non-empty `meaning.en` (no `?`, no `unknown`, no empty string — these were Plan 05 D-08 flag triggers).
    4. Scroll through the entire song from start to finish (the player exposes a verse list — you don't need to actually play it).
    5. If even ONE verse shows `(untranslated)` or any other regression: STOP. Note the verse_number and surface. The most likely cause is a stale Vercel deployment cache (Vercel may serve an older bundle). Force a refresh / hard reload (Ctrl+Shift+R). If still broken after a hard reload, report the regression — Plan 08 (catalog rollout) is BLOCKED until the smoke song renders clean.

    6. Reply with one of:
       - `approved — yellow-moon-akeboshi renders clean across all 21 patched verses; pt-BR + es toggles confirmed; Plan 08 is unblocked.`
       - `regression at verse <N> (<surface>) — <description>; Plan 08 BLOCKED.`

    The blocking aspect of D-09 is real: per CONTEXT.md, "Only proceed to the remaining 5 songs after smoke verification passes."
  </how-to-verify>
  <resume-signal>Type "approved" with the smoke verdict, or describe the regression.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| apply-verse-patch.ts → cache file | File rewrite; idempotency-guarded |
| 05-insert-db.ts --slug=<slug> → Neon song_versions JSONB | Existing channel; no new SQL paths |
| Operator browser → Vercel deployment | Read-only verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.3-20 | Tampering | apply-verse-patch.ts mis-applies replace-mode (e.g. on wrong verse_number) | mitigate | Plan 03 Vitest fixture covered the contract; the cache-side post-patch stub count check in Task 1 step 3 is the runtime gate. Catches at-most a handful of malformed entries before DB push. |
| T-11.3-21 | Tampering | 05-insert-db.ts pushes the wrong lesson | mitigate | The script's existing --slug= filter is well-trodden (commit 7f1dc95, 2026-04-26). Audit-gate step 5 cross-checks DB content against the same predicate as cache. |
| T-11.3-22 | Information disclosure | Vercel deployment exposes lesson content | accept | Public catalog song; lesson content is the product, not a secret. |
| T-11.3-23 | Denial of service | Vercel cache stale at smoke verify time | mitigate | Step 5's hard-reload instruction; if persistent, rollout (Plan 08) waits — does not silently ship over a bad smoke. |
</threat_model>

<verification>
- audit --verify exits 0 for yellow-moon-akeboshi.
- Operator confirms zero `(untranslated)` renders on the deployed song page.
- D-09 smoke gate explicitly passed before any further songs are processed.
</verification>

<success_criteria>
- The smoke song (yellow-moon-akeboshi) ships clean: cache, DB, and deployed UI all confirm zero stubs.
- The pipeline (audit → patch apply → DB push → audit-verify) is end-to-end green for one slug.
- Plan 08's catalog rollout is unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/11.3-fix-untranslated-jp-verses/11.3-07-SUMMARY.md` recording: pre/post stub counts, apply-patch stdout, 05-insert-db stdout, --verify exit code, Vercel verification status, any spot-checked verse_numbers + their post-patch translations.
</output>
