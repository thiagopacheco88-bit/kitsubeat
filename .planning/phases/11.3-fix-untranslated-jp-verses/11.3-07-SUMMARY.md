---
phase: 11.3-fix-untranslated-jp-verses
plan: 07
status: complete
type: execute
completed: 2026-04-27
self_check: PASSED
---

# Plan 11.3-07 SUMMARY — yellow-moon smoke test

## Outcome

**Smoke test passed end-to-end.** yellow-moon-akeboshi (21 stubs, smallest in-scope song per SPEC table) was the first slug taken through the full pipeline — apply patch → DB push → audit verify cache + DB → user spot-check. Quality held; the user authorized scaling to the remaining 5 songs immediately afterward.

## What this plan did

1. Applied `.planning/verse-patches/yellow-moon-akeboshi.json` (21 `replace_verse_number` entries) via `scripts/seed/apply-verse-patch.ts yellow-moon-akeboshi`. First run applied 7 of 21; 14 were skipped due to a too-narrow idempotency check in Plan 03's just-shipped patcher. After the patcher fix (commit `1a77cc6`) was committed, the second run applied the remaining 14.
2. Pushed to Neon via `scripts/seed/05-insert-db.ts --slug=yellow-moon-akeboshi`. Upsert succeeded (`Upserted: 1 / Errors: 0`).
3. Audit gate: `npx tsx scripts/seed/audit-untranslated-verses.ts --slugs=yellow-moon-akeboshi` reported `0 stubs`.
4. User authorized rolling forward to Plan 08 without waiting for Vercel verification (plans to spot-check on Vercel later).

## Sidecar fix discovered + landed

Plan 03's `patchSig` only hashed token surface concat, so verses with unchanged tokens but stub translations (the 14 multi-token `(auto-coverage)` verses) were filtered out as "already applied." Extended the replace-mode idempotency to include `translations.en` in the signature (`patchSigWithTr`), and made the replace path preserve cache-side `start_time_ms`/`end_time_ms` when the patch supplies 0 (Phase 11.3's drafter has no audio timing). All 6 Plan 03 Vitest fixtures still pass after the fix.

Commit: `1a77cc6` — fix(11.3): extend replace-mode patcher.

## D-09 + D-10 status

D-09 ("smoke before scaling") and D-10 ("manual Vercel play-through") were partially honored:
- D-09: yellow-moon DID act as the smoke test before scaling; user agreed to skip the manual Vercel check between songs based on script-level signals.
- D-10: Manual Vercel play-through deferred to user's later spot-check. The audit's cache + DB verify gate is the script-level proxy.

## Files

None under this plan directly. Patch file + cache + DB row already in scope of Plan 06's commit `f15b580`. The patcher fix is `1a77cc6`.

## Forward signal

Plan 06's drafting quality + Plan 03's now-corrected idempotency + Plan 02's audit script all proven on yellow-moon. Plan 08 (catalog rollout) ran identical flow per song — same idempotency fix carried through, same DB push pattern, same audit verify.
