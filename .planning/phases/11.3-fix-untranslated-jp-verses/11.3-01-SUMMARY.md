---
phase: 11.3-fix-untranslated-jp-verses
plan: 01
status: superseded
type: execute
completed: 2026-04-27
self_check: SUPERSEDED
---

# Plan 11.3-01 SUMMARY — Ollama install + smoke (SUPERSEDED)

## Outcome

**Plan SUPERSEDED before completion.** Neither task ran to its acceptance criteria.

## What happened

Task 1 (operator-driven `ollama pull qwen2.5:14b-instruct` + `ollama pull llama3.1:8b`) was attempted multiple times from this session and a fresh PowerShell window:

| Attempt | Result |
|---------|--------|
| Parallel pull (qwen + llama simultaneously) | Both hit "max retries exceeded: TLS handshake timeout" against Cloudflare R2 (Ollama's CDN). qwen reached 17 MB / 9.0 GB, llama reached 12 MB / 4.9 GB before stalling. |
| Sequential pull (llama first, then qwen) | llama got 3.3 MB / 4.9 GB → "max retries: read tcp ...: wsarecv: An existing connection was forcibly closed by the remote host." |
| Bash retry of qwen alone | Ollama daemon spent ~20 minutes silently retrying with no progress. The qwen blob accumulated to 8,988,110,688 bytes (matching target 9.0 GB exactly) but the manifest never finalized — `ollama list` continued to show only `nomic-embed-text:latest`. |
| Bash retry of llama alone | Same TLS handshake failure pattern — IPv6 routing through Cloudflare R2 (`[2606:4700:2ff9::1]:443`) appears unreliable from this network. |
| User-driven retry from PowerShell | Same failure mode — confirmed not bash/runtime specific. |

After ~45 minutes of accumulated retry time and zero finalized manifests, the user accepted a pivot: drop the Ollama pipeline entirely (Plans 04 + 05) and use **inline Claude drafting** for the 159 in-scope stubs. The PATTERNS.md "Token-shape template" + the existing kuroshiro-tokenized auto-coverage entries gave Claude enough structure to draft schema-valid replace_verse_number patches verse-by-verse.

## Why this is acceptable

- Phase 11.3's goal per SPEC.md is **fixing 159 stub-translated JP verses** in cache + DB. Ollama was the *mechanism*, not the requirement.
- The user's prior 273 cached lessons all came from inline Claude (per memory — `project_lesson_generation_status.md`), so this is a proven path for the catalog.
- Claude Opus on JP→en/pt-BR/es is qualitatively better than qwen2.5:14b or llama3.1:8b would have been (per general benchmarks; pilot was never run).
- The SPEC-REQ-7 acceptance gate (`audit-untranslated-verses.ts --verify --slugs=<all 6>` exits 0) is mechanism-agnostic. It passed.

## Artifact status

`OLLAMA-SMOKE.md` was NOT written. SPEC-REQ-1 ("Ollama runtime installed and verified") is **not met**, but is functionally moot because Plans 04 + 05 were dropped.

The Ollama partial blobs remain on disk (8.99 GB qwen + 4.92 GB llama) — operator can decide to retry/clean later. Daemon is running; only `nomic-embed-text:latest` is registered.

## Forward path

If the deferred 11.3.1 follow-up phase wants to use Ollama for the remaining 842 stubs across 97 catalog songs, the network failure mode needs to be solved first (VPN, IPv4-forced DNS, or different time-of-day pull). Until then, inline Claude is the working path.

## Files

None — no commits made under this plan. Pivot context lives in commit `f15b580` (yellow-moon-akeboshi shipped under inline-Claude path).
