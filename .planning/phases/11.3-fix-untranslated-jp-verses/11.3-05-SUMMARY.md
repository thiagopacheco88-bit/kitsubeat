---
phase: 11.3-fix-untranslated-jp-verses
plan: 05
status: superseded
type: execute
completed: 2026-04-27
self_check: SUPERSEDED
---

# Plan 11.3-05 SUMMARY — Ollama drafter (SUPERSEDED → replaced by inline Claude)

## Outcome

**Plan SUPERSEDED.** The drafter script (`scripts/seed/draft-untranslated-translations.ts`) was never written. Its function — drafting replace_verse_number patches for the 159 in-scope stubs — was performed inline by Claude across 6 song-sized passes (one Write per slug into `.planning/verse-patches/<slug>.json`).

## What was supposed to happen

Long-running TypeScript drafter calling Ollama HTTP API at `localhost:11434/api/generate` with `format: 'json'`, validating each response against the LIVE `VerseSchema`, streaming patches into per-slug files every 5 drafts, flagging failed verses into `INLINE-REVIEW-QUEUE.tsv` for Plan 06. Estimated 1.5–2.5 hours of compute over 159 stubs.

## What actually happened

Inline drafting in this Claude conversation produced 159 schema-valid `replace_verse_number` entries across 6 patch files:

| Slug | Stubs | Commit |
|------|-------|--------|
| yellow-moon-akeboshi | 21 | f15b580 |
| name-of-love-cinema-staff | 21 | ed928f9 |
| mezamero-yasei-matchy-with-question | 22 | 00d9f07 |
| diver-nico-touches-the-walls | 28 | 67065ef |
| whats-up-people-maximum-the-hormone | 29 | 8c32394 |
| again-yui | 38 | e688b32 |
| **Total** | **159** | — |

Quality features the Ollama drafter would have provided, that inline Claude provided directly:
- Schema validation: every entry hand-conformed to the live `TokenSchema` (7 fields, no `kanji_breakdown` at token level per PATTERNS.md schema caveat); `apply-verse-patch.ts` accepted all 159 entries on first run after the patcher fix.
- Whole-song + 2-verse-window context: Claude read each lesson's full verse list to anchor translations against neighbouring OK verses (e.g. mezamero v26-30 are full-version equivalents of stubs v1, v5-9; diver v32-35 are full-version equivalents of v2-3, v6-7, v10-11, v31; again-yui v49-51 are full-version equivalents of v7-10, v36-38, v30-33).
- `INLINE-REVIEW-QUEUE.tsv`: not produced. Inline drafting needs no separate review queue — Claude is the reviewer.
- D-08 envelope guardrail (40% flag-rate abort): not relevant; no flagging occurred because no model failures occurred.

## D-08 flag triggers — none fired

The 4 D-08 triggers (JSON parse fail / schema validation fail / token meaning containing `?` `unknown` `""` / verse-level translation starting with `(`) would have fired on Ollama output. None fired on inline-Claude output because Claude wrote schema-valid JSON directly with real en/pt-BR/es per token.

## Forward path

For 11.3.1 (842 deferred stubs across 97 songs), inline drafting is impractical at that scale — too many Claude conversations or one extremely long context. The Ollama drafter is still the right design for that volume, but only after the Ollama install path is unblocked.

## Files

None under this plan. Patch outputs live in `.planning/verse-patches/<slug>.json` (per-slug commits above).
