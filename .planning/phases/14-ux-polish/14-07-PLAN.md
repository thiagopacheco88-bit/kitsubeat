---
phase: 14
plan: 07
type: execute
wave: 3
depends_on: ["14-05", "14-06"]
files_modified:
  - src/app/review/UpsellModal.tsx
  - src/app/review/ReviewLanding.tsx
  - src/app/review/ReviewSession.tsx
  - src/app/review/page.tsx
  - src/app/vocabulary/page.tsx
  - src/app/vocabulary/FilterControls.tsx
  - src/app/vocabulary/JlptGapSummary.tsx
  - src/app/vocabulary/SeenInExpander.tsx
  - src/app/profile/page.tsx
  - src/app/profile/ProfileForm.tsx
  - src/app/components/LevelUpTakeover.tsx
  - src/app/components/UpsellModal.tsx
  - tests/e2e/mobile-parity.spec.ts
autonomous: true
requirements: [1, 2, 3, 4, 5]

must_haves:
  truths:
    - "All review surface files (review/UpsellModal, ReviewLanding, ReviewSession, page.tsx) use only token-driven CSS vars"
    - "All vocabulary surface files (page.tsx, FilterControls, JlptGapSummary, SeenInExpander) use only token-driven CSS vars"
    - "All profile surface files (page.tsx, ProfileForm) use only token-driven CSS vars (Plan 14-03 already wired theme radio; this plan migrates remaining palette utilities)"
    - "Cross-cutting modals (review/UpsellModal, components/UpsellModal, components/LevelUpTakeover) use Modal primitive instead of inline fixed inset-0 shells"
    - "JlptGapSummary uses Badge variant=jlpt for the per-tier badges"
    - "Lint + audit gates report 0 violations under review + vocabulary + profile surfaces"
    - "mobile-parity tests for /review, /vocabulary, /profile are enabled and pass"
    - "canvas-confetti suppression verified at LevelUpTakeover.tsx fire site (disableForReducedMotion: true is preserved per existing pattern)"
  artifacts:
    - path: "src/app/review/UpsellModal.tsx"
      provides: "Modal primitive consumer (replaces inline shell)"
      contains: "Modal"
    - path: "src/app/components/LevelUpTakeover.tsx"
      provides: "Modal primitive consumer + tokenized layout (canvas-confetti preserved)"
      contains: "Modal\\|disableForReducedMotion"
  key_links:
    - from: "src/app/review/UpsellModal.tsx"
      to: "src/components/ui/Modal.tsx"
      via: "import { Modal, ModalContent, ModalTitle }"
      pattern: 'from\s+"@/components/ui/Modal"'
    - from: "src/app/components/LevelUpTakeover.tsx"
      to: "src/components/ui/Modal.tsx"
      via: "import { Modal, ModalContent, ModalTitle }"
      pattern: 'from\s+"@/components/ui/Modal"'
---

<objective>
Migrate the 3 mid-traffic surfaces: `/review` (cross-song SRS — Phase 11-05), `/vocabulary` (cross-song dashboard — Phase 11-04), `/profile` (settings + HUD — Phase 8.4-05 + 12-05). Plus migrate the 2 cross-cutting modals NOT in `/songs/[slug]`: `src/app/review/UpsellModal.tsx` (premium-gate for cross-song review) and `src/app/components/UpsellModal.tsx` (generic upsell — verify which surfaces consume it).

Plus migrate `src/app/components/LevelUpTakeover.tsx` (Phase 12-06 level-up celebration). This component:
1. Has its own inline modal shell — replace with Modal primitive
2. Uses `bg-orange-600` for the Continue button (palette utility — replace with `var(--color-accent)` since orange is the level-up brand color, OR add a new --color-level-up token; PER PLANNER JUDGMENT: reuse --color-accent since the modal is full-screen takeover and the accent red carries the same celebratory weight)
3. Uses canvas-confetti — DO NOT modify the confetti call site; just verify `disableForReducedMotion: true` is still present (per CONTEXT existing pattern)

Per D-21 sequencing: these 3 surfaces are mid-traffic (logged-in users only). Wave 3 because they depend on Wave 2's Modal pattern being proven out at /songs/[slug].

Purpose: After this plan, all logged-in surfaces (review queue, vocabulary dashboard, profile settings) match the design system. Plus cross-cutting modals (the upsell + level-up takeover) use the Modal primitive — completing the inline-modal migration started in Plan 14-05 (AdvancedDrillsUpsellModal).

Output: ~13 file modifications. Net code reduction: 2 inline modal shells removed (~40 LOC each) and replaced with primitive consumption (~15 LOC each).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-ux-polish/14-SPEC.md
@.planning/phases/14-ux-polish/14-CONTEXT.md
@.planning/phases/14-ux-polish/14-PATTERNS.md
@./CLAUDE.md
@./kitsubeat/CLAUDE.md
@src/app/globals.css
@src/components/ui/Modal.tsx
@src/components/ui/Card.tsx
@src/components/ui/Badge.tsx
@src/components/ui/Button.tsx
@src/components/ui/EmptyState.tsx
@src/components/ui/Skeleton.tsx

<interfaces>
Modal migration template (from Plan 14-05 + PATTERNS Modal section):

```typescript
// BEFORE (typical inline modal — review/UpsellModal.tsx:31-66 shape)
<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/70 p-4" role="dialog" aria-modal="true" onClick={onClose}>
  <div className="w-full max-w-sm rounded-xl border border-gray-700 bg-gray-900 p-6" onClick={(e) => e.stopPropagation()}>
    <h2>...</h2>
  </div>
</div>

// AFTER (Modal primitive)
<Modal open={open} onOpenChange={(o) => { if (!o) onClose(); }}>
  <ModalContent className="max-w-sm">
    <ModalTitle>Cross-song review is premium</ModalTitle>
    {/* body */}
  </ModalContent>
</Modal>
```

JlptGapSummary uses 10 sites per RESEARCH §2 — likely 5 JLPT tier pills (N5 through N1) × 2 columns or row variants. Each becomes Badge variant=jlpt level=Nx.

LevelUpTakeover preserves the existing star-shine + level-pop keyframes (D-27); only the modal shell + button styling change. canvas-confetti at line ~39 keeps `disableForReducedMotion: true` (CONTEXT existing pattern, per VALIDATION.md "Reduced-motion guard for canvas-confetti").

Mapping table (same as Plans 14-05 + 14-06):
- bg-gray-N00 / bg-orange-N00 / bg-red-N00 → token references
- text-gray-N00 / text-orange-N00 → token references  
- border-gray-N00 → border-[var(--color-border)]
- bg-green-N00 (correct feedback) → bg-[var(--color-grammar-adjective)] (existing #22c55e green)
- bg-red-N00 (wrong feedback) → bg-[var(--color-accent)] OR bg-[var(--color-grammar-verb)] (both = #ef4444)
- text-violet-N00 → text-[var(--color-grammar-expression)]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate /review surface (UpsellModal + ReviewLanding + ReviewSession + page.tsx) — Modal primitive consumer</name>
  <files>
    src/app/review/UpsellModal.tsx,
    src/app/review/ReviewLanding.tsx,
    src/app/review/ReviewSession.tsx,
    src/app/review/page.tsx
  </files>
  <read_first>
    - src/app/review/UpsellModal.tsx (existing inline modal — full file, verify ESC handler + aria-modal pattern)
    - src/app/review/ReviewLanding.tsx (Phase 11-05 — landing page; 2 button-shape sites per RESEARCH)
    - src/app/review/ReviewSession.tsx (Phase 11-05 — active review session UI)
    - src/app/review/page.tsx (server component shell)
    - src/components/ui/Modal.tsx (Plan 14-02 primitive)
    - .planning/phases/14-ux-polish/14-PATTERNS.md "src/components/ui/Modal.tsx" Wave 2 transformation block
  </read_first>
  <action>
    Step 1 — Migrate `src/app/review/UpsellModal.tsx`. Same pattern as Plan 14-05's AdvancedDrillsUpsellModal:
    - Replace outer fixed-inset div with Modal + ModalContent
    - Replace inline `<h2>` with ModalTitle
    - Replace body text with ModalDescription if it's a one-liner
    - Replace Continue/Maybe-later buttons with `<Button variant="primary"|"secondary">` from Plan 14-02
    - Remove the inline ESC handler (Radix handles it)
    - Remove explicit aria-modal/role="dialog" (Radix sets them)
    - Preserve the onClose callback contract — wrap with `onOpenChange={(o) => { if (!o) onClose(); }}`
    - Preserve any existing data-testid attributes for E2E specs

    Step 2 — Migrate `ReviewLanding.tsx`. Apply mapping table. The 2 button-shape sites per RESEARCH §2 likely include "Start review" CTA (`<Button variant="primary">`) and a secondary "View vocabulary" link (`<Button variant="ghost" asChild><Link>...`). Free-tier message + premium-gate banner uses tokens.

    Step 3 — Migrate `ReviewSession.tsx`. The active-session UI has feedback panels (correct/wrong colors), card containers, "Submit" / "Next" buttons. Apply tokens uniformly. Correct-answer feedback = `bg-[var(--color-grammar-adjective)]/12 border-[var(--color-grammar-adjective)]`; wrong-answer feedback = `bg-[var(--color-grammar-verb)]/12 border-[var(--color-grammar-verb)]`.

    Step 4 — Migrate `src/app/review/page.tsx`. Server component shell — likely thin wrapper around ReviewLanding/ReviewSession. Palette → token swap if any inline styling exists.

    Step 5 — Lint + build:
    ```bash
    npm run lint src/app/review/
    npm run build
    ```
    Run any existing review tests:
    ```bash
    npx playwright test --grep "review"
    ```
    Verify the existing /review E2E spec (Phase 11-05) still passes — modal interactions especially.
  </action>
  <verify>
    <automated>(! grep -lE "(bg-gray-[0-9]+|text-gray-[0-9]+|border-gray-[0-9]+|bg-red-[0-9]+|text-red-[0-9]+|bg-green-[0-9]+|text-green-[0-9]+)" src/app/review/UpsellModal.tsx src/app/review/ReviewLanding.tsx src/app/review/ReviewSession.tsx src/app/review/page.tsx 2&gt;/dev/null) &amp;&amp; grep -q 'from "@/components/ui/Modal"' src/app/review/UpsellModal.tsx &amp;&amp; ! grep -q '"fixed inset-0' src/app/review/UpsellModal.tsx &amp;&amp; npm run build 2&gt;&amp;1 | grep -E "(error|Error)" | grep -v "warning" | wc -l | grep -q '^0$'</automated>
  </verify>
  <done>
    - All 4 /review files have zero palette utilities
    - review/UpsellModal.tsx imports + uses Modal primitive (no inline fixed inset-0 shell)
    - Existing review E2E specs still pass
    - `npm run lint src/app/review/` reports 0 violations
    - `npm run build` succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate /vocabulary surface (page + FilterControls + JlptGapSummary + SeenInExpander) — Badge primitive consumer</name>
  <files>
    src/app/vocabulary/page.tsx,
    src/app/vocabulary/FilterControls.tsx,
    src/app/vocabulary/JlptGapSummary.tsx,
    src/app/vocabulary/SeenInExpander.tsx
  </files>
  <read_first>
    - src/app/vocabulary/page.tsx (Phase 11-04 — dashboard server component; tier-grouped list)
    - src/app/vocabulary/FilterControls.tsx (Phase 11-04 — search + filters; 3 button sites per RESEARCH)
    - src/app/vocabulary/JlptGapSummary.tsx (Phase 12-05 — JLPT gap analysis; 10 sites per RESEARCH = likely 5 JLPT pills × 2 contexts)
    - src/app/vocabulary/SeenInExpander.tsx (Phase 11-03 — "Seen in: X songs" expander)
    - src/components/ui/Badge.tsx (Plan 14-02 — JLPT variant)
  </read_first>
  <action>
    Step 1 — Migrate `JlptGapSummary.tsx` first (densest file in this surface, 10 sites). Each JLPT tier pill (N5..N1) × likely "mastered" + "remaining" columns. Use `<Badge variant="jlpt" level={tier}>` for each. The numeric counter ("87/180 N5 words mastered") becomes a `<Badge variant="mono">`. Preserve all existing logic (gap calculation, percentage display).

    Step 2 — Migrate `FilterControls.tsx`. 3 button-shape sites per RESEARCH = likely filter chips (e.g., "All / Started / Mastered" toggle) + a clear-filter button. Use `<Button variant="ghost" size="sm">` for chips with active state via className override.

    Step 3 — Migrate `SeenInExpander.tsx`. Likely a small expandable panel — palette → token swap. The chevron icon stays (inline SVG, no palette colors).

    Step 4 — Migrate `vocabulary/page.tsx`. Server component shell — apply mapping table. Likely renders JlptGapSummary + FilterControls + tier-grouped list. Free-tier 20-row preview gate (Phase 11-04) uses an EmptyState-style "Upgrade for full list" panel — wrap with `<EmptyState variant="default" heading="Free tier preview" body="..." ctaLabel="Upgrade" ctaHref="/profile" />` from Plan 14-02.

    Step 5 — Lint + build:
    ```bash
    npm run lint src/app/vocabulary/
    npm run build
    ```
  </action>
  <verify>
    <automated>(! grep -lE "(bg-gray-[0-9]+|text-gray-[0-9]+|border-gray-[0-9]+|bg-blue-[0-9]+|text-blue-[0-9]+|bg-green-[0-9]+|bg-amber-[0-9]+)" src/app/vocabulary/page.tsx src/app/vocabulary/FilterControls.tsx src/app/vocabulary/JlptGapSummary.tsx src/app/vocabulary/SeenInExpander.tsx 2&gt;/dev/null) &amp;&amp; grep -q 'from "@/components/ui/Badge"' src/app/vocabulary/JlptGapSummary.tsx &amp;&amp; npm run build 2&gt;&amp;1 | grep -E "(error|Error)" | grep -v "warning" | wc -l | grep -q '^0$'</automated>
  </verify>
  <done>
    - All 4 /vocabulary files have zero palette utilities
    - JlptGapSummary uses Badge primitive for all 5 JLPT tier displays
    - FilterControls uses Button primitive for chips/toggles
    - vocabulary/page.tsx uses EmptyState for free-tier preview message
    - `npm run build` succeeds
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate /profile + cross-cutting modals (LevelUpTakeover, UpsellModal) + enable mobile-parity tests</name>
  <files>
    src/app/profile/page.tsx,
    src/app/profile/ProfileForm.tsx,
    src/app/components/LevelUpTakeover.tsx,
    src/app/components/UpsellModal.tsx,
    tests/e2e/mobile-parity.spec.ts
  </files>
  <read_first>
    - src/app/profile/page.tsx (Phase 8.4-05 + 12-05 — server component; HUD + level progress)
    - src/app/profile/ProfileForm.tsx (Phase 8.4-05 + 12-05 — client form; Plan 14-03 added Appearance radio; this task migrates remaining palette utilities)
    - src/app/components/LevelUpTakeover.tsx (Phase 12-06 — full-screen level-up celebration; uses canvas-confetti at line ~39 + level-pop keyframe at globals.css:53-60)
    - src/app/components/UpsellModal.tsx (generic upsell — verify which surface consumes it; pattern matches review/UpsellModal)
    - src/components/ui/Modal.tsx (primitive)
    - tests/e2e/mobile-parity.spec.ts (Plan 14-00 shell — flip /review, /vocabulary, /profile fixme to real)
    - .planning/phases/14-ux-polish/14-PATTERNS.md "Reduced-motion guard for canvas-confetti" + LevelUpTakeover analog
  </read_first>
  <action>
    Step 1 — Migrate `LevelUpTakeover.tsx`. CRITICAL — three things to preserve verbatim:
    1. The `level-pop` keyframe class on the headline (`className="level-pop ..."` — D-27 retains the keyframe; only the surrounding palette utilities change)
    2. The canvas-confetti call at line ~39 with `disableForReducedMotion: true` — DO NOT modify (per VALIDATION.md "Reduced-motion guard for canvas-confetti" and CONTEXT existing pattern at LevelUpTakeover.tsx:39, RowUnlockModal.tsx:14, StarDisplay.tsx:36)
    3. The `data-testid="level-up-continue"` attribute on the Continue button (E2E test contract from Phase 12)

    Migration:
    - Replace outer fixed-inset div with `<Modal open={true} onOpenChange={(o) => { if (!o) onDismiss(); }}>` + `<ModalContent>`
    - Replace inline headline with `<ModalTitle className="text-3xl font-bold level-pop">` (preserve level-pop class)
    - Replace `bg-orange-600` Continue button with `<Button variant="primary" data-testid="level-up-continue" onClick={onDismiss}>Continue</Button>`. Per planner judgment: reuse `--color-accent` (red) — the takeover is celebratory full-screen and the accent red carries the same weight as orange. If user feedback says orange feels distinctly different, add `--color-level-up-orange` to globals.css later (Phase 14.1).
    - Apply mapping table for any remaining palette utilities (text-orange-N00 → text-[var(--color-text)]; bg-gradient-to-* with orange/yellow → use --color-accent or --shadow-hero-glow)
    - Add a comment above the canvas-confetti call: `/* motion-catalog: confetti milestone — disableForReducedMotion already applied */`

    Step 2 — Migrate `src/app/components/UpsellModal.tsx`. Same Modal primitive pattern as Plans 14-05 (AdvancedDrillsUpsellModal) and 14-07 Task 1 (review/UpsellModal). If this file is a near-duplicate of review/UpsellModal, consider whether to consolidate them into a single shared modal OR keep separate per their distinct copy. PER PLANNER JUDGMENT: keep separate — the per-surface upsell copy is the variation, the shell is now shared via the Modal primitive.

    Step 3 — Migrate `profile/page.tsx`. Server component — palette → token swap. HUD + level progress sections likely use Card primitive — wrap with `<Card variant="elevated" size="lg">` if appropriate.

    Step 4 — Migrate `profile/ProfileForm.tsx`. Plan 14-03 already added the Appearance radio group with token-only styling. This task migrates the OTHER form sections (skip_learning toggle, new_card_cap input — Phase 8.4-05). Apply the mapping table. Form inputs (`<input type="checkbox">`, `<input type="number">`) get `accent-[var(--color-accent)]` and `border-[var(--color-border)]`. Labels get `text-[var(--color-text)]`. Per D-28 verify the cosmetic-system labels (Avatar theme — Phase 12) say "Avatar theme" NOT "Color theme" to avoid collision with Appearance.

    Step 5 — Enable mobile-parity tests for /review, /vocabulary, /profile in `tests/e2e/mobile-parity.spec.ts`. Replace 3 fixme blocks with real tests (same pattern as Plan 14-06 Task 2).

    Step 6 — Run the full sequence:
    ```bash
    npm run lint
    npm run build
    npm run dev &
    sleep 5
    npx playwright test mobile-parity.spec.ts -g "(/review|/vocabulary|/profile)"
    npx playwright test theme-toggle.spec.ts  # ProfileForm radio writes cookie — should still work
    kill %1
    ```

    Step 7 — Verify reduced-motion E2E (Plan 14-04) still passes. Confetti should still fire under non-reduced-motion (manual check) and skip under reduced-motion (already-passing test from Plan 14-04).
  </action>
  <verify>
    <automated>(! grep -lE "(bg-gray-[0-9]+|text-gray-[0-9]+|border-gray-[0-9]+|bg-orange-[0-9]+|text-orange-[0-9]+|bg-red-[0-9]+|text-red-[0-9]+)" src/app/profile/page.tsx src/app/profile/ProfileForm.tsx src/app/components/LevelUpTakeover.tsx src/app/components/UpsellModal.tsx 2&gt;/dev/null) &amp;&amp; grep -q 'from "@/components/ui/Modal"' src/app/components/LevelUpTakeover.tsx &amp;&amp; grep -q 'disableForReducedMotion: true' src/app/components/LevelUpTakeover.tsx &amp;&amp; grep -q 'data-testid="level-up-continue"' src/app/components/LevelUpTakeover.tsx &amp;&amp; grep -q 'level-pop' src/app/components/LevelUpTakeover.tsx &amp;&amp; grep -q 'test\("/review — no horizontal scroll"' tests/e2e/mobile-parity.spec.ts &amp;&amp; grep -q 'test\("/vocabulary — no horizontal scroll"' tests/e2e/mobile-parity.spec.ts &amp;&amp; grep -q 'test\("/profile — no horizontal scroll"' tests/e2e/mobile-parity.spec.ts &amp;&amp; npm run build 2&gt;&amp;1 | grep -E "(error|Error)" | grep -v "warning" | wc -l | grep -q '^0$'</automated>
  </verify>
  <done>
    - profile/page.tsx + ProfileForm.tsx zero palette utilities; existing Phase 12 + 8.4 form logic preserved
    - LevelUpTakeover.tsx uses Modal primitive; preserves level-pop keyframe class, disableForReducedMotion guard, data-testid
    - components/UpsellModal.tsx uses Modal primitive; preserves onClose contract
    - mobile-parity.spec.ts has 3 new real tests for /review, /vocabulary, /profile (all pass)
    - theme-toggle.spec.ts (Plan 14-03) still passes (ProfileForm radio writes cookie)
    - reduced-motion.spec.ts (Plan 14-04) still passes
    - `npm run build` succeeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Modal portal target | Inherited from Plan 14-02 Modal primitive |
| canvas-confetti at LevelUpTakeover | Existing surface — disableForReducedMotion is the only reduced-motion guard for JS-driven motion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07-01 | Tampering | Modal substitution losing the level-pop keyframe class | mitigate | Verify task acceptance: grep for `level-pop` in LevelUpTakeover.tsx (must remain). The keyframe class lives on the inner ModalTitle, NOT on a removed wrapper |
| T-14-07-02 | Denial of Service | Reduced-motion bypass at canvas-confetti site | mitigate | Verify task acceptance: grep for `disableForReducedMotion: true` in LevelUpTakeover.tsx (must remain). The lib-level guard is the ONLY suppression for JS-driven motion; the global CSS @media override does not reach canvas-confetti |
| T-14-07-03 | Information Disclosure | Profile form Appearance radio leaking userId via uncontrolled input | mitigate | The Appearance radio uses Plan 14-03's optimistic+server-action pattern. The setThemePreference action validates userId server-side. No new attack surface introduced |
| T-14-07-04 | Authorization | Premium-gate Modal logic disturbed | accept | Plan 14-07 only touches className strings + modal shell substitution. The premium-gate logic in review/UpsellModal + components/UpsellModal is unchanged. Existing tests guard it |

Threats explicitly out of scope for Plan 14-07:
- New auth surface
- Theme cookie security (Plan 14-03)
- Bundle budget (covered by Phase 13 D-23 carry-forward)
</threat_model>

<verification>
After all 3 tasks complete:

```bash
# 1. Zero palette utilities under all 13 modified files
grep -rE "(bg-gray-[0-9]+|text-gray-[0-9]+|border-gray-[0-9]+|bg-red-[0-9]+|bg-orange-[0-9]+|bg-green-[0-9]+|bg-blue-[0-9]+|bg-amber-[0-9]+)" src/app/review/ src/app/vocabulary/ src/app/profile/ src/app/components/LevelUpTakeover.tsx src/app/components/UpsellModal.tsx 2>/dev/null | wc -l  # 0

# 2. Modal primitive consumer count
grep -lE 'from "@/components/ui/Modal"' src/app/ -r | wc -l  # >= 4 (advanced-drills + 2 upsells + level-up)

# 3. Critical preservations in LevelUpTakeover
grep -c "level-pop" src/app/components/LevelUpTakeover.tsx  # >= 1
grep -c "disableForReducedMotion: true" src/app/components/LevelUpTakeover.tsx  # >= 1
grep -c 'data-testid="level-up-continue"' src/app/components/LevelUpTakeover.tsx  # >= 1

# 4. Lint + audit
npm run lint
npx tsx scripts/audit/token-compliance.ts 2>&1 | grep -E "(review|vocabulary|profile|LevelUpTakeover|UpsellModal)" | wc -l  # 0

# 5. Mobile-parity for these 3 routes
npm run dev & sleep 5
npx playwright test mobile-parity.spec.ts -g "(/review|/vocabulary|/profile)"
kill %1

# 6. Existing tests still green
npx playwright test theme-toggle.spec.ts reduced-motion.spec.ts
npx playwright test --grep "review|vocabulary|level-up"
```
</verification>

<success_criteria>
- All 13 source files have zero palette utilities
- 2 cross-cutting modals (review/UpsellModal + components/UpsellModal) use Modal primitive
- LevelUpTakeover uses Modal primitive AND preserves level-pop keyframe + disableForReducedMotion + data-testid
- JlptGapSummary uses Badge primitive for all 5 JLPT tiers
- FilterControls uses Button primitive
- vocabulary/page.tsx uses EmptyState for free-tier preview gate
- mobile-parity.spec.ts has 3 new real tests (/review, /vocabulary, /profile — all pass)
- Existing Phase 11 + 12 tests still pass (theme-toggle, reduced-motion, review E2E, etc.)
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/14-ux-polish/14-07-SUMMARY.md` documenting:
- Per-file palette violation count BEFORE vs AFTER
- Modal primitive consumer count (now 4: AdvancedDrillsUpsellModal, review/UpsellModal, components/UpsellModal, LevelUpTakeover)
- Critical preservations checked (level-pop keyframe, disableForReducedMotion, data-testid)
- mobile-parity test results (3 routes pass)
- Confirmation: theme-toggle + reduced-motion E2E specs still green (no regressions from this plan)
</output>
