# 14.3-02 PLAN: Practice Game Loop

## Intent

Move the Practice tab from "quiz config" to "game lobby + run loop" while preserving all existing exercise generation, persistence, premium, and mastery behavior.

## Files

- `src/app/songs/[slug]/components/ExerciseTab.tsx`
- `src/app/songs/[slug]/components/ExerciseSession.tsx`
- `src/app/songs/[slug]/components/QuestionCard.tsx`
- `src/app/songs/[slug]/components/FeedbackPanel.tsx`
- `src/app/songs/[slug]/components/SessionSummary.tsx`
- `tests/e2e/lesson-redesign.spec.ts`

## Tasks

1. Restyle the Practice config screen as a game lobby with track cards, compact length selectors, and explicit start actions.
2. Tokenize the Advanced Drills lock state and preserve the 80%-per-track gate.
3. Restyle session progress as a stage header with a progress bar and question counter.
4. Restyle generic question options so idle/correct/wrong states feel like selectable game tiles.
5. Keep feedback and summary mastery moments intact.

## Verification

- Component tests for song lesson components.
- E2E smoke for gear-shift into Practice.
- Mobile no-horizontal-scroll on `/songs/again-yui`.
- Build and route size budget.

