# 14.3-02 SUMMARY: Practice Game Loop

## Completed

- Restyled `ExerciseTab` as a Practice lobby with run cards, compact length selectors, and tokenized Advanced Drills lock state.
- Restyled `ExerciseSession` progress into a stage header with current round, rounds remaining, and a tokenized progress bar.
- Restyled `QuestionCard` as a game-card surface with prompt well and larger answer tiles.
- Restyled `FeedbackPanel` surface while preserving `VerseDominatedAnimation`, mastery popovers, image fallback behavior, and Continue flow.
- Token-swept supporting lesson panels used by 14.3:
  - `VerseBlock`
  - `VocabularySection`
  - `GrammarSection`
  - `LearnCard`
  - `KanjiBreakdownSection`
  - `TokenPopup`
  - `TokenSpan`
  - `GrammarWriteCard`
  - `VocabTypedCard`
- Updated the roadmap to point Phase 14.3 at the locked spec and completed plans.

## Verification

- `npx eslint --no-warn-ignored ...` — passed with existing `<img>` warnings only.
- `npx vitest run 'src/app/songs/[slug]/components/__tests__'` — 5 files / 26 tests passed.
- Playwright home + lesson pass:
  - `tests/e2e/lesson-redesign.spec.ts` gear-shift smoke — passed.
  - `tests/e2e/mobile-parity.spec.ts` `/` no horizontal scroll — passed.
  - `tests/e2e/mobile-parity.spec.ts` `/songs/again-yui` strict no horizontal scroll — passed.
- `npm run build` — passed.
- `npm run size` — passed at 12.22 kB gzipped / 50 kB.

## Notes

- The top/mobile menu overflow issue is fixed on the verified home and lesson routes. The `/songs/again-yui` assertion is now strict (`<= 0` overflow).
- Remaining lint warnings are inherited plain `<img>` guidance in exercise image cards; those files intentionally use raw `<img loading="lazy">` patterns from earlier image-fallback plans.

