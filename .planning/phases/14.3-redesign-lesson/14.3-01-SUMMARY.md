# 14.3-01 SUMMARY: Lesson Shell And Gear Shift

## Completed

- Created the Phase 14.3 lesson redesign spec and first implementation plan.
- Converted the song header into a compact "Now playing" session header using Phase 14 primitives.
- Added `lesson-practice-gearshift`, an in-page Practice gear-shift button that switches the existing tab state without route navigation.
- Restyled `SongLayout` as a unified player stage with video and synced-lyrics panel hooks:
  - `song-player-stage`
  - `song-video-panel`
  - `song-lyrics-panel`
- Tokenized the player controls, version toggle, tab strip, and Practice Suspense fallback.
- Tightened the `/songs/again-yui` mobile no-horizontal-scroll assertion to strict no overflow.
- Added a 14.3 Playwright smoke test for the gear-shift behavior.

## Verification

- `npx eslint --no-warn-ignored ...` — passed.
- `npx vitest run 'src/app/songs/[slug]/components/__tests__'` — 5 files / 26 tests passed.
- `npx playwright test tests/e2e/mobile-parity.spec.ts -g "/songs/again-yui.*no horizontal scroll" --workers=1 --timeout=90000 --reporter=line` — passed after restarting the stale port 7000 dev server.
- `npx playwright test tests/e2e/lesson-redesign.spec.ts --workers=1 --timeout=90000 --reporter=line` — passed.
- `npm run build` — passed.
- `npm run size` after production build — passed at 12.36 kB gzipped / 50 kB.

## Notes

- The first Playwright retry failed because an existing port 7000 dev server served a stale/missing `.next` manifest. Stopping the process bound to port 7000 and letting Playwright start a fresh server fixed it.
- Running Playwright after `next build` rewrites `.next` into dev chunks; `npm run size` must be run immediately after a production build.

