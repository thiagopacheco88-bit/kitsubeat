---
phase: 14.1-redesign-path
verified: 2026-05-03T00:00:00Z
status: human_needed
score: 8/8 must-haves verified (automated); 5 items require human/runtime verification
overrides_applied: 0
human_verification:
  - test: "Run W-2 pre-baseline pixeldiff gate: `node scripts/debug/path-baseline-pixeldiff.mjs` (requires `npm run dev` on localhost:7000)"
    expected: "Script exits 0 — live /path within 5% pixel diff of _temp/path-redesign/demo-CA-hybrid.png"
    why_human: "Requires live dev server; verifies visual fidelity against the locked CA-hybrid design prototype before locking Playwright snapshots"
  - test: "Lock visual baselines and run visual diff specs: `npx playwright test path-visual-dark path-visual-light --update-snapshots` then re-run without flag"
    expected: "Both dark and light specs pass within 2% pixel diff tolerance (AC #2 and AC #3)"
    why_human: "Screenshot diff requires a live dev server + first-run snapshot capture; cannot be verified statically"
  - test: "Run axe-core a11y gate: `RUN_A11Y=1 npx playwright test path-a11y.spec.ts`"
    expected: "Zero serious/critical axe violations in both dark and light themes (AC #5, AC #6); note WR-02 ARIA list nesting in PathMap may consume minor/moderate budget"
    why_human: "axe-core requires a rendered DOM in a real browser; WR-02 (nested role=list without role=listitem children in PathMap.tsx:89) is a live screen-reader bug that will be caught here"
  - test: "Run seeded-DB Playwright tests: set TEST_DATABASE_URL, then `npx playwright test path-starter-bug-repair path-continue-anchor`"
    expected: "StarterPick modal or EmptyState renders for null slug user (AC #1); ContinueAnchor suppressed on StarterPick branch (AC #10)"
    why_human: "Seeded-DB tests require TEST_DATABASE_URL — they skip cleanly without it; smoke test (non-seeded) verifies 200 OK only"
  - test: "Measure exact bundle delta: fix pre-existing Clerk/ThemeToggle server-only import issue, then run `npm run build` and compare /path First Load JS to 118 kB pre-baseline"
    expected: "Delta <= +10 kB (soft cap per D-12); estimated ~5-8 kB from 2 new client islands (KanaCheckpointNode + ContinueAnchor)"
    why_human: "Exact build fails due to pre-existing Clerk/ThemeToggle server-only import conflict unrelated to Phase 14.1; estimated delta is PASS but requires clean build for confirmation"
---

# Phase 14.1: Redesign /path — Verification Report

**Phase Goal:** Apply the locked CA-hybrid design language (cover-art-as-background nodes + lantern-streak header + hero-progress block + visual-only locked treatment + Japanese-co-headed tier dividers + sticky Continue CTA + first-class kana checkpoints) to the /path surface, using only the Phase 14 token system extended with 2 new tokens (--mist-fill + --aura-color; --shadow-cta-red already authored in Phase 14-01) — and unblock visual verification by repairing the existing getStarterSongs() runtime error.
**Verified:** 2026-05-03T00:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `/path` renders without runtime error for null-slug users (starter-bug repair) | VERIFIED | `getStarterSongs()` uses filter+warn (starter-songs.ts:100-122); `StarterPick` has EmptyState fallback (StarterPick.tsx:49-59); no throw present |
| 2 | PathHeader renders KitsuBeat wordmark + LanternStreak, replacing PathHud | VERIFIED | `PathHeader.tsx` exists with `data-testid="path-header"`, fox-mark SVG + "KitsuBeat" wordmark + `<LanternStreak count={streakCurrent} />`; `PathHud.tsx` absent from `src/app/path/components/`; `page.tsx` imports PathHeader directly |
| 3 | LanternStreak replaces fire emoji with SVG lantern + 3-tier flame opacity | VERIFIED | `LanternStreak.tsx` has `flameOpacity()` returning 0.45/0.70/1.0 for <7/7-29/>=30; flame `<ellipse>` has `opacity={opacity}` as JSX number + `className="ka-flicker"`; `aria-label="{N}-day streak"`; no `🔥` emoji in `src/app/path/` (confirmed by grep) |
| 4 | HeroProgress block shows Level + XP bar + Now Learning callout + next-reward chip | VERIFIED | `HeroProgress.tsx` has `data-testid="hero-progress"`, 28px tabular-nums level, XP `<progress>` bar using `xpWithinCurrentLevel()`, conditional "Now Learning" callout, next-reward chip (D-09); passed as props from `page.tsx` |
| 5 | PathNode is 84px cover-art-as-background with 4 states and M1 invariant preserved | VERIFIED | `PathNode.tsx` rewritten: `height: "84px"`, 38% cover panel + 62% info panel, YouTube CDN `maxresdefault.jpg`, states mastered/current/locked, mist overlay with `pointerEvents: "none"`, `<CardLink>` root in all states, no `disabled` attr on link |
| 6 | KanaCheckpointNode renders 2 first-class checkpoints (hiragana + katakana) linked to /kana | VERIFIED | `KanaCheckpointNode.tsx` reads `useKanaProgress`, 3 states (mastered/in-progress/locked), `href="/kana?script={script}"`, 64px height, dashed border, kana glyph badge; `PathMap.tsx` inserts row at basic tier start; `checkpoint-state.ts` helper with 90% mastery threshold; `kana/page.tsx` accepts `?script=` param |
| 7 | TierDivider has bilingual co-heading + tier SVG icon + 1px rule | VERIFIED | `TierDivider.tsx` renders "Side A · 基礎 · Beginner" / "Side B · 中級 · Intermediate" / "Side C · 上級 · Advanced"; hand-authored bamboo/torii/mountain SVG icons with `currentColor`; `aria-label` with full bilingual label; 1px border-t rule |
| 8 | ContinueAnchor is sticky bottom CTA routing to /songs/{slug}, suppressed on StarterPick branch | VERIFIED | `ContinueAnchor.tsx`: `position: fixed`, `bottom: 0`, `min-h-14` (56px), `--color-accent` background, `--shadow-cta-red` glow, guard `if (!currentSongSlug || !currentSongTitle) return null`; links to `/songs/${currentSongSlug}` |

**Score:** 8/8 truths verified (automated static checks)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/app/path/page.tsx` | Composes PathHeader + HeroProgress + PathMap/StarterPick + ContinueAnchor | VERIFIED | PathHud import removed; all 5 components composed correctly |
| `src/app/path/components/PathHud.tsx` | Deleted (D-06) | VERIFIED | Not present in `src/app/path/components/` directory |
| `src/app/path/components/PathHeader.tsx` | NEW — KitsuBeat wordmark + LanternStreak | VERIFIED | 107-line server component, data-testid="path-header" |
| `src/app/path/components/LanternStreak.tsx` | NEW — SVG lantern, 3-tier flame opacity, ka-flicker | VERIFIED | 183-line component, flameOpacity() function, ka-flicker class on flame ellipse |
| `src/app/path/components/HeroProgress.tsx` | NEW — Level + XP + Now Learning + next-reward chip | VERIFIED | 110-line server component, xpWithinCurrentLevel(), data-testid="hero-progress" |
| `src/app/path/components/PathNode.tsx` | REWRITTEN — 84px cover-art-as-background, M1 invariant | VERIFIED | 184-line client component, CardLink root, 3 states, mist overlay pointer-events:none |
| `src/app/path/components/KanaCheckpointNode.tsx` | NEW — useKanaProgress, 3 states, /kana?script= link | VERIFIED | 150-line client component, Skeleton pre-hydration, computeCheckpointState() |
| `src/app/path/components/TierDivider.tsx` | NEW — bilingual label + SVG icons | VERIFIED | 183-line server component, bamboo/torii/mountain SVGs, full aria-label |
| `src/app/path/components/ContinueAnchor.tsx` | NEW — sticky bottom CTA, 56px min-height | VERIFIED | 115-line component (note: IN-01 advisory — `"use client"` may be unnecessary) |
| `src/app/path/components/PathMap.tsx` | UPDATED — delegates to TierDivider, adds kana row | VERIFIED | Imports TierDivider + KanaCheckpointNode; inserts kana row at basic tier start |
| `src/lib/gamification/starter-songs.ts` | Filter+warn replaces throw (D-01) | VERIFIED | Lines 100-122: filter logic, per-slug warn, all-drift warn, no throw |
| `src/lib/kana/checkpoint-state.ts` | NEW helper — mastered/in-progress/locked derivation | VERIFIED | 107-line pure function, 90% mastery ratio, MASTERY_STAR_THRESHOLD=7 |
| `src/app/kana/page.tsx` | Accepts ?script=hiragana|katakana query param (D-05) | VERIFIED | Lines 31-40: isValidScriptParam() guard, initialMode set from searchParams |
| `src/app/globals.css` | --mist-fill, --aura-color (dark+light); --shadow-cta-red (pre-existing); ka-pulse, ka-flicker, ka-aura keyframes | VERIFIED | --shadow-cta-red: lines 97+157; --mist-fill: lines 105+163; --aura-color: lines 111+164; keyframes: lines 237-272; global reduced-motion override: lines 280-287 |
| `docs/motion-catalog.md` | 3 new entries: ka-pulse, ka-flicker, ka-aura | VERIFIED | Lines 95-117: all 3 entries with trigger/duration/easing/target/reduced-motion fallback |
| `tests/e2e/path-visual-dark.spec.ts` | AC #2 screenshot diff | VERIFIED (authored) | Spec file exists; requires `--update-snapshots` + live dev server |
| `tests/e2e/path-visual-light.spec.ts` | AC #3 screenshot diff | VERIFIED (authored) | Spec file exists; requires `--update-snapshots` + live dev server |
| `tests/e2e/path-m1-invariant.spec.ts` | AC #8 M1 invariant | VERIFIED (authored) | 3 tests: PathNode no-disabled, mist pointer-events:none, KanaCheckpointNode clickable |
| `tests/e2e/path-reduced-motion.spec.ts` | AC #7 ka-* 0ms under reduced-motion | VERIFIED (authored) | Tests ka-flicker unconditionally; ka-pulse/ka-aura conditional on state |
| `tests/e2e/path-continue-anchor.spec.ts` | AC #10 ContinueAnchor sticky + nav | VERIFIED (authored) | position:fixed, bottom:0, min-height>=56, nav test; StarterPick suppression gated on HAS_TEST_DB |
| `tests/e2e/path-kana-checkpoint-nav.spec.ts` | AC #9 >=2 checkpoints, /kana?script= nav | VERIFIED (authored) | 3 tests: count>=2, hrefs correct, click-nav to /kana?script=hiragana/katakana |
| `tests/e2e/path-starter-bug-repair.spec.ts` | AC #1 null-slug branch | VERIFIED (authored) | Seeded test gated on HAS_TEST_DB; unconditional smoke test verifies 200 OK |
| `tests/e2e/path-a11y.spec.ts` | AC #5/#6 axe-core >=95 | VERIFIED (authored) | Gated on RUN_A11Y=1; dark+light themes |
| `tests/integration/getStarterSongs.test.ts` | Integration test for filter+warn behavior | VERIFIED (authored) | Gated on HAS_TEST_DB; Test 5 live DB happy path |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `page.tsx` | `PathHeader` | import + render | WIRED | `import { PathHeader }` + `<PathHeader streakCurrent={state.streak_current} />` |
| `page.tsx` | `HeroProgress` | import + render | WIRED | `import { HeroProgress }` + `<HeroProgress state={...} currentSongTitle={...} nextReward={...} />` |
| `page.tsx` | `ContinueAnchor` | import + render | WIRED | `import { ContinueAnchor }` + `<ContinueAnchor currentSongSlug={...} currentSongTitle={...} />` |
| `page.tsx` | `getStarterSongs()` | conditional call | WIRED | `state.current_path_node_slug === null ? await getStarterSongs() : null` |
| `PathHeader` | `LanternStreak` | import + render | WIRED | `import { LanternStreak }` + `<LanternStreak count={streakCurrent} />` |
| `PathMap` | `TierDivider` | import + render per-tier | WIRED | `import { TierDivider }` + `<TierDivider key={...} tier={tier} />` on tier change |
| `PathMap` | `KanaCheckpointNode` | import + 2 instances at basic tier | WIRED | `import { KanaCheckpointNode }` + `<KanaCheckpointNode script="hiragana" />` + katakana |
| `KanaCheckpointNode` | `useKanaProgress` store | `useKanaProgress((s) => ...)` | WIRED | Reads both `_hasHydrated` + script mastery map; triggers Skeleton pre-hydration |
| `KanaCheckpointNode` | `computeCheckpointState` | import + call | WIRED | `import { computeCheckpointState }` + `computeCheckpointState(map, script)` |
| `ContinueAnchor` | `/songs/{slug}` | `<Link href={...}>` | WIRED | `href={\`/songs/${currentSongSlug}\`}` |
| `KanaCheckpointNode` | `/kana?script={script}` | `<CardLink href={href}>` | WIRED | `href = \`/kana?script=${script}\`` |
| `PathNode` | `<CardLink>` | import + root element | WIRED | Root is always `<CardLink href={/songs/${song.slug}} ...>` in all 3 states |
| `globals.css` `--mist-fill` | `PathNode.tsx` mist div | `style={{ background: "var(--mist-fill)" }}` | WIRED | Token consumed in `PathNode.tsx:176` |
| `globals.css` `--aura-color` | `PathNode.tsx` StarAura | `style={{ background: "var(--aura-color)" }}` | WIRED | Token consumed in `PathNode.tsx:161` |
| `globals.css` `--shadow-cta-red` | `ContinueAnchor.tsx` | `shadow-[var(--shadow-cta-red)]` | WIRED | Token consumed in `ContinueAnchor.tsx:60` |
| `globals.css` `.ka-flicker` | `LanternStreak.tsx` flame ellipse | `className="ka-flicker"` | WIRED | `LanternStreak.tsx:145` |
| `globals.css` `.ka-pulse` | `PathNode.tsx` play overlay | `className="ka-pulse ..."` | WIRED | `PathNode.tsx:117` |
| `globals.css` `.ka-aura` | `PathNode.tsx` StarAura | `className="ka-aura ..."` | WIRED | `PathNode.tsx:159` |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `PathHeader` | `streakCurrent` | `state.streak_current` from `getUserGamificationState()` in page.tsx | Yes — real DB query | FLOWING |
| `HeroProgress` | `state.level`, `state.xp_total`, `currentSongTitle` | `getUserGamificationState()` + song lookup in page.tsx | Yes — real DB query | FLOWING |
| `PathMap` / `PathNode` | `songs` array | `getAllSongs(userId)` in page.tsx | Yes — real DB query | FLOWING |
| `KanaCheckpointNode` | kana mastery map | `useKanaProgress` zustand + localStorage | Yes — real localStorage store, Skeleton on pre-hydration | FLOWING |
| `ContinueAnchor` | `currentSongSlug`, `currentSongTitle` | `state.current_path_node_slug` + songs lookup in page.tsx | Yes — real DB data | FLOWING |
| `StarterPick` | `candidates` | `getStarterSongs()` — DB query with filter+warn | Yes — real DB query, defensively filtered | FLOWING |

---

### Behavioral Spot-Checks

Step 7b: SKIPPED for full E2E run (requires live dev server at localhost:7000). Static checks below confirm key behaviors at the code level.

| Behavior | Check | Result | Status |
|----------|-------|--------|--------|
| `flameOpacity(3) === 0.45` | Code trace: `if (count >= 30) ... if (count >= 7) ... return 0.45` | count=3 → returns 0.45 | PASS |
| `flameOpacity(15) === 0.7` | Code trace: count=15, >=7 branch | returns 0.70 | PASS |
| `flameOpacity(60) === 1.0` | Code trace: count=60, >=30 branch | returns 1.0 | PASS |
| `getStarterSongs()` filter replaces throw | Grep for `throw` in starter-songs.ts:67-143 | No throw found | PASS |
| `ContinueAnchor` returns null for null slug | `if (!currentSongSlug || !currentSongTitle) return null` at line 41 | Guard present | PASS |
| `KanaCheckpointNode` href format | `href = \`/kana?script=${script}\`` | Matches SPEC-REQ-6 acceptance criterion | PASS |
| No `🔥` emoji in `src/app/path/` | Grep result: 0 matches | AC #11 PASS | PASS |
| `ka-pulse`, `ka-flicker`, `ka-aura` in globals.css | Lines 237-272 confirmed | All 3 keyframes present | PASS |
| `--mist-fill`, `--aura-color` in both themes | Lines 105+163 (mist-fill), 111+164 (aura-color) | Both dark+light scoped | PASS |
| `PathHud.tsx` deleted | Directory listing: not present | D-06 honored | PASS |
| Motion catalog has 3 new entries | Lines 95-117 in `docs/motion-catalog.md` | ka-pulse + ka-flicker + ka-aura entries | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| SPEC-REQ-1 | 14.1-00 (prereq) | Starter-bug repair — filter+warn, EmptyState fallback | SATISFIED | starter-songs.ts filter logic; StarterPick EmptyState branch |
| SPEC-REQ-2 | 14.1-05/06 | PathHeader — wordmark + LanternStreak replaces PathHud | SATISFIED | PathHeader.tsx; page.tsx composition; PathHud.tsx deleted |
| SPEC-REQ-3 | 14.1-04/05 | LanternStreak — SVG lantern, 3-tier flame, ka-flicker | SATISFIED | LanternStreak.tsx; flameOpacity() tiers; ka-flicker className |
| SPEC-REQ-4 | 14.1-06/07 | HeroProgress — Level + XP + Now Learning + next-reward chip | SATISFIED | HeroProgress.tsx; xpWithinCurrentLevel(); nextReward prop (D-09) |
| SPEC-REQ-5 | 14.1-09 | PathNode cover-art-as-background — 84px, 3 states, M1 invariant | SATISFIED | PathNode.tsx rewrite; CardLink root; mist pointer-events:none; in_progress deferred per CONTEXT |
| SPEC-REQ-6 | 14.1-02/03/08 | KanaCheckpointNode — first-class kana milestones, /kana?script= | SATISFIED | KanaCheckpointNode.tsx; PathMap inserts at basic tier; checkpoint-state.ts; kana/page.tsx ?script= param |
| SPEC-REQ-7 | 14.1-08 | TierDivider — bilingual co-heading + SVG icon + 1px rule | SATISFIED | TierDivider.tsx; TIER_META map; TierIcon; aria-label |
| SPEC-REQ-8 | 14.1-10/11 | ContinueAnchor — sticky bottom CTA, 56px, brand-red, /songs/{slug} | SATISFIED | ContinueAnchor.tsx; fixed+bottom-0 layout; min-h-14; --shadow-cta-red |

**Upstream REQUIREMENTS.md (v2.0):** Reviewed. No EXER-*, STAR-*, KANA-*, CROSS-*, PATH-*, or other requirement IDs from REQUIREMENTS.md map to Phase 14.1. This phase is cross-cutting visual/structural work on the /path surface with no new feature rows. The CONTEXT.md explicitly notes: "not directly mapped to 14.1 (this is cross-cutting visual work, not a feature row)." No orphaned upstream requirements.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `PathMap.tsx` | 89 | `className="flex flex-col gap-2 space-y-1"` — conflicting spacing systems (WR-03) | Warning | Non-uniform 12px effective spacing (8px gap + 4px margin); pick one system |
| `PathMap.tsx` | 89 | `role="list"` outer container without `role="listitem"` children (WR-02) | Warning | axe-core `aria-required-children` violation; VoiceOver/NVDA list count incorrect |
| `PathMap.tsx` | 61 | Nested `role="list"` (kana row) also without `role="listitem"` children (WR-02) | Warning | Doubles the ARIA list nesting violation |
| `PathMap.tsx` | 34-35 | Magic number `999999` for null popularity_rank fallback (IN-03) | Info | Use `Number.POSITIVE_INFINITY` for self-documenting sentinel |
| `PathMap.tsx` | 30-37 | Secondary sort by `popularity_rank` is dead code — field not selected in getAllSongs() (WR-01) | Warning | Works by accident (stable sort + DB pre-sort); silently breaks if pre-sort order changes |
| `ContinueAnchor.tsx` | 1 | `"use client"` directive unnecessary — no client hooks used (IN-01) | Info | Adds component to client bundle unnecessarily; remove to keep as server component |
| `KanaCheckpointNode.tsx` | 81-82, 98-99 | `pillClass` identical for in-progress and locked states — copy-paste duplication (IN-04) | Info | Hoist to NEUTRAL_PILL constant |
| `src/lib/gamification/starter-songs.ts` | 100-122 | All-DB-missing case masked by all-`has_lesson:null` path — missing Test 5 coverage (WR-04) | Warning | The all-drift warn fires correctly but with misleading detail when rows is empty; add Test 5 |

**Blocker anti-patterns:** None — all are warnings or info items. WR-02 (nested ARIA list) is the most consequential: it will consume the axe-core minor/moderate violation budget in the path-a11y.spec.ts gate (threshold `<= 5`) and is a real screen-reader regression. It should be fixed before or immediately after the a11y gate runs.

---

### Human Verification Required

#### 1. W-2 Pre-Baseline Pixel Diff Gate

**Test:** Start `npm run dev` (localhost:7000), then run `node scripts/debug/path-baseline-pixeldiff.mjs`
**Expected:** Script exits 0 — live /path at 390x844 is within 5% pixel diff of `_temp/path-redesign/demo-CA-hybrid.png` (the locked CA-hybrid design prototype)
**Why human:** Requires a live running dev server; verifies visual fidelity before Playwright snapshots are locked. Must run BEFORE `--update-snapshots`.

#### 2. Visual Screenshot Diff — Dark Theme (AC #2)

**Test:** After W-2 passes, run `npx playwright test path-visual-dark --update-snapshots` to lock baseline, then re-run without flag
**Expected:** `/path` at 390x844 in dark theme matches `_temp/path-redesign/demo-CA-hybrid.png` within 2% pixel diff tolerance (masking lantern-streak-count, song titles, kana counts)
**Why human:** Requires live dev server + first-run snapshot capture; visual regression cannot be verified statically.

#### 3. Visual Screenshot Diff — Light Theme (AC #3)

**Test:** Run `npx playwright test path-visual-light --update-snapshots` then re-run without flag
**Expected:** `/path` at 390x844 in light theme matches `_temp/path-redesign/demo-CA-hybrid-light.png` within 2% pixel diff tolerance
**Why human:** Same as AC #2 — requires live dev server and snapshot capture.

#### 4. axe-core A11y Audit — Dark + Light Themes (AC #5, AC #6)

**Test:** `RUN_A11Y=1 npx playwright test path-a11y.spec.ts`
**Expected:** Zero serious/critical violations in both themes; minor/moderate count <= 5. Note: WR-02 (nested `role="list"` in PathMap without `role="listitem"` children) will likely surface here — fix is a 2-line change (add `role="listitem"` to child wrappers or replace `role="list"` with semantic `<ul>`)
**Why human:** axe-core requires a rendered DOM in a real browser context.

#### 5. Seeded-DB Playwright Tests (AC #1 full seeded path, AC #10 StarterPick suppression)

**Test:** Set `TEST_DATABASE_URL`, run `npx playwright test path-starter-bug-repair path-continue-anchor`
**Expected:** StarterPick modal or EmptyState renders for null-slug user without console errors; ContinueAnchor absent on StarterPick branch
**Why human:** Requires a seeded test database with `current_path_node_slug = null` for the test user; the unconditional smoke test only verifies 200 OK.

---

### Gaps Summary

No blocking implementation gaps found. All 8 SPEC-REQs are implemented with substantive, wired, and data-flowing code. The 4 review warnings (WR-01 through WR-04) are advisory and do not prevent the goal from being achieved, though WR-02 (ARIA list nesting) should be addressed before or immediately after the a11y gate to avoid consuming the axe-core violation budget.

The 5 human verification items are infrastructure-dependent (live dev server, Playwright snapshot locking, test DB) and represent the standard final-mile checks for a visual redesign phase. They are not gaps in the implementation.

**Pre-existing non-blocking issues noted:**
- `ContinueAnchor.tsx` has `"use client"` unnecessarily (IN-01) — advisory, remove to reduce bundle
- `npm run build` fails due to pre-existing Clerk/ThemeToggle server-only import unrelated to Phase 14.1; exact bundle delta measurement requires this to be resolved first (estimated delta ~5-8 kB is within the +10 kB soft cap)

---

_Verified: 2026-05-03T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
