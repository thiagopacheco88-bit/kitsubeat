---
phase: 14.1-redesign-path
plan: "01"
subsystem: ui
tags: [css, tokens, keyframes, animation, design-system, reduced-motion]

# Dependency graph
requires:
  - phase: 14-ux-polish
    provides: "Phase 14 token system in globals.css (--shadow-cta-red already existed at lines 97+145)"
provides:
  - "--mist-fill token (both themes): dark rgba(15,15,18) gradient, light rgba(255,255,255) gradient"
  - "--aura-color token (both themes): dark 0.35 alpha amber, light 0.45 alpha amber (+30% per D-04)"
  - "ka-pulse keyframe + .ka-pulse class: 1.6s scale+opacity loop for PathNode current state"
  - "ka-flicker keyframe + .ka-flicker class: 1.8s opacity sweep for LanternStreak flame"
  - "ka-aura keyframe + .ka-aura class: 2.4s box-shadow halo using --aura-color"
  - "3 motion-catalog.md entries (ka-pulse, ka-flicker, ka-aura) in existing 12-entry format"
affects:
  - PathNode.tsx (Plan 09) — mist overlay background (var(--mist-fill)) + StarAura halo (.ka-aura, var(--aura-color))
  - LanternStreak.tsx (Plan 03) — flame ellipse animation (.ka-flicker)
  - ContinueAnchor.tsx (Plan 10) — CTA glow (var(--shadow-cta-red), already existed)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "New Phase 14.1 tokens inserted after --shadow-focus-ring in both @theme and :root[data-theme=light] blocks"
    - "New keyframes inserted between verse-dominated-pulse and global reduced-motion @media block"
    - "Global reduced-motion override at file end handles all new keyframes — no per-keyframe media query"
    - "Motion catalog entries follow 5-bullet format: Trigger/Duration/Easing/Target/Reduced-motion fallback"

key-files:
  created: []
  modified:
    - src/app/globals.css
    - docs/motion-catalog.md

key-decisions:
  - "2-token correction confirmed: --shadow-cta-red already existed in Phase 14-01 (lines 97+145); only --mist-fill and --aura-color are new"
  - "Global reduced-motion override at globals.css line 239+ handles all 3 new keyframes; no per-keyframe @media blocks needed (CONTEXT D-15)"
  - "Light-theme --aura-color uses 0.45 alpha vs dark 0.35 (+30% per CONTEXT D-04 shadow opacity convention)"

patterns-established:
  - "Wave 2 components consume --mist-fill, --aura-color, --shadow-cta-red via var() tokens"
  - "Wave 2 components apply .ka-pulse / .ka-flicker / .ka-aura classes; reference entries in motion-catalog.md via comments"

requirements-completed:
  - SPEC-REQ-3
  - SPEC-REQ-5
  - SPEC-REQ-8

# Metrics
duration: 3min
completed: 2026-05-03
---

# Phase 14.1 Plan 01: Foundation Tokens + Keyframes Summary

**Two new CSS design tokens (--mist-fill, --aura-color) and three new keyframes (ka-pulse, ka-flicker, ka-aura) landed in globals.css for both light/dark themes; motion catalog extended to 15 entries**

## Performance

- **Duration:** 3 min
- **Started:** 2026-05-03T09:06:18Z
- **Completed:** 2026-05-03T09:09:00Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments

- Authored `--mist-fill` and `--aura-color` tokens in both `@theme` (dark) and `:root[data-theme="light"]` scopes — light-theme aura alpha is 0.45 vs dark 0.35 (+30% per CONTEXT D-04 visibility convention)
- Added `ka-pulse` (1.6s scale+opacity), `ka-flicker` (1.8s opacity sweep), and `ka-aura` (2.4s box-shadow halo) keyframes; each has a consumer class; global reduced-motion override at file end suppresses all three to 0ms with no per-keyframe `@media` blocks
- Appended 3 motion catalog entries (ka-pulse, ka-flicker, ka-aura) in the existing 12-entry format — total Trigger bullets now 15; existing 12 entries untouched (append-only diff confirmed)

## Token Placement (exact line numbers in final file)

- `--mist-fill` (dark): inside `@theme` block, after `--shadow-focus-ring` at ~line 101
- `--aura-color` (dark): inside `@theme` block, at ~line 108
- `--mist-fill` (light): inside `:root[data-theme="light"]` block, after `--shadow-focus-ring` at ~line 152
- `--aura-color` (light): inside `:root[data-theme="light"]` block, at ~line 155
- `ka-pulse` keyframe block: after `verse-dominated-pulse` @media block, before global reduced-motion override
- `ka-flicker` keyframe block: after `ka-pulse`
- `ka-aura` keyframe block: after `ka-flicker`, immediately before global override (which remains last)

## Task Commits

Each task was committed atomically:

1. **Task 1: Add --mist-fill + --aura-color tokens to globals.css (both themes)** - `2bace0b` (feat)
2. **Task 2: Add ka-pulse + ka-flicker + ka-aura keyframes to globals.css** - `199a8a2` (feat)
3. **Task 3: Append ka-pulse, ka-flicker, ka-aura entries to docs/motion-catalog.md** - `ed563d8` (docs)

## Files Created/Modified

- `src/app/globals.css` — 2 new tokens × 2 themes (4 declarations) + 3 keyframes + 3 consumer classes (58 new lines total)
- `docs/motion-catalog.md` — 3 new entries appended (24 new lines; existing 12 entries untouched)

## Decisions Made

- **2-token correction confirmed (CONTEXT D-13):** `--shadow-cta-red` already existed at lines 97 (dark) and 145 (light) from Phase 14-01. Only `--mist-fill` and `--aura-color` are genuinely new. No duplication introduced.
- **Global reduced-motion suppression (CONTEXT D-15):** The existing `@media (prefers-reduced-motion: reduce) { *, *::before, *::after { animation-duration: 0ms !important; ... } }` at the file end handles all 3 new keyframes. No per-keyframe `@media` blocks were authored.
- **Light-theme alpha convention (CONTEXT D-04):** `--aura-color` light-theme uses 0.45 (+30% relative to dark 0.35) following the established shadow opacity convention for light-background visibility.

## Deviations from Plan

None — plan executed exactly as written. The demo HTML file at `_temp/path-redesign/demo-CA-hybrid.html` was not present in the worktree, but the plan's action section provided all authoritative CSS values directly so no reference to the demo was required.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

Wave 2 components (Plans 02–08) can now:
- Reference `var(--mist-fill)` for locked-node mist overlays
- Reference `var(--aura-color)` for 3-star halo radial-gradients
- Reference `var(--shadow-cta-red)` for Continue CTA glow (token already existed; confirmed)
- Apply `.ka-pulse` to PathNode current-state PLAY overlays
- Apply `.ka-flicker` to LanternStreak flame `<ellipse>` elements
- Apply `.ka-aura` to PathNode StarAura halo elements
- Reference catalog entry names in `/* motion-catalog: ka-* */` comments per Phase 14 D-14 convention

---
*Phase: 14.1-redesign-path*
*Completed: 2026-05-03*
