# Phase 14.1: Redesign /path — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in [14.1-CONTEXT.md](./14.1-CONTEXT.md) — this log preserves the alternatives considered.

**Date:** 2026-05-02
**Phase:** 14.1-redesign-path
**Areas discussed:** Starter-bug repair strategy, Kana checkpoint data source + links, Component composition shape, Verification gates

---

## Starter-bug repair strategy

### Q1: How should getStarterSongs() handle missing/lessonless approved slugs?

| Option | Description | Selected |
|--------|-------------|----------|
| Filter + warn | Drop missing/lessonless slugs, log dev warn, render whatever remains. EmptyState fallback if all 3 drift. | ✓ |
| Try-catch at the call site | Keep throw inside the helper, wrap in page.tsx, fall back to PathMap branch. | |
| DB-fix + keep throw | Investigate which slug drifted, restore in DB or update STARTER_SONG_SLUGS. Keep throw. | |
| Filter + EmptyState + replacement-list | Filter as in option 1, but fall back to ~10 'beginner-OK' slugs to always return 3 cards. | |

**User's choice:** Filter + warn (recommended)
**Notes:** Locked → CONTEXT D-01. Logger mechanism (Pino vs console.warn) deferred to plan-phase as Claude's Discretion.

### Q2: Should the StarterPick visual restyle be folded into 14.1 scope?

| Option | Description | Selected |
|--------|-------------|----------|
| No — bug-fix only | Repair the bug, leave existing token-migrated card layout alone. Defer restyle to follow-up. | ✓ |
| Yes — restyle as a bonus task | Apply cover-art-as-background to StarterPick cards in 14.1. | |
| Defer fully — separate phase 14.1.1 | Don't even fix the bug as part of 14.1; spin into its own micro-phase. | |

**User's choice:** No — bug-fix only (recommended)
**Notes:** Locked → CONTEXT D-02. Visual restyle moves to deferred ideas — separate plan after 14.1 lands.

---

## Kana checkpoint data source + links

### Q1: How should KanaCheckpointNode read kana progress?

| Option | Description | Selected |
|--------|-------------|----------|
| Client island reads zustand store | 'use client' component reads useKanaProgress directly. Skeleton on SSR, real progress on hydration. | ✓ |
| Cookie-bridged SSR | Mirror progress to kb_kana_progress cookie. Read via next/headers in page.tsx. | |
| New server-side kana_progress table | Hand-written migration, server query, sync from zustand on session-summary. | |
| Aggregate stats only | Single % per script, not per-character. | |

**User's choice:** Client island reads zustand store (recommended)
**Notes:** Locked → CONTEXT D-03. Skeleton variant choice deferred to plan-phase (Claude's Discretion).

### Q2: Where do KanaCheckpoint links route?

| Option | Description | Selected |
|--------|-------------|----------|
| All states → /kana?script={script} | Mastered + in-progress + locked all link to /kana grid with query param. | ✓ |
| Mastered → /kana/session?script={s}&mode=review, others → /kana | One-tap drill into review for mastered users. Requires extending /kana/session selection logic. | |
| All states → /kana (no query param) | Simplest — every checkpoint navigates to /kana, user picks script there. | |

**User's choice:** All states → /kana?script={script} (recommended)
**Notes:** Locked → CONTEXT D-05. Plan-phase verifies whether /kana page already accepts the param or needs minor handling.

### Q3 (follow-up): What threshold defines a kana checkpoint as 'mastered' vs 'in-progress' vs 'locked'?

| Option | Description | Selected |
|--------|-------------|----------|
| Per-char ≥7 stars; 90% of basic row = mastered | 41/46 chars at 7+ stars earns the mastered pill. Tolerates 5 weak chars. | ✓ |
| Per-char ≥5 stars; 100% of basic row = mastered | Stricter — every char at 5+ stars (half-mastered tier). | |
| Per-char ≥10 stars; 100% = mastered | Strictest — every char at full mastery. Probably unreachable. | |
| You decide | Let Claude pick during planning based on demo + axe constraints. | |

**User's choice:** Per-char ≥7 stars; 90% of basic row = mastered (recommended)
**Notes:** Locked → CONTEXT D-04. Plan-phase verifies semantics in src/lib/kana/mastery.ts; extracts logic into src/lib/kana/checkpoint-state.ts helper.

---

## Component composition shape

### Q1: What happens to <PathHud>?

| Option | Description | Selected |
|--------|-------------|----------|
| Delete + replace with HeroProgress | PathHud.tsx removed entirely. page.tsx composes PathHeader + HeroProgress directly. | ✓ |
| Thin shell wrapping HeroProgress + PathHeader | Keep PathHud.tsx as a parent. | |
| Refactor in place — PathHud becomes HeroProgress | Rename + gut + add Now Learning callout. | |

**User's choice:** Delete + replace with HeroProgress (recommended)
**Notes:** Locked → CONTEXT D-06.

### Q2: What happens to streak_best (the 'Best: N' sub-line currently in PathHud)?

| Option | Description | Selected |
|--------|-------------|----------|
| Drop entirely from /path | LanternStreak shows current only. 'Best: N' moves to /profile (or already lives there). | ✓ |
| Keep as a tooltip on LanternStreak | Hover/long-press shows 'Current 15 · Best 60'. | |
| Move to HeroProgress as a third metric | HeroProgress shows Level | Now Learning | Best Streak. | |

**User's choice:** Drop entirely from /path (recommended)
**Notes:** Locked → CONTEXT D-07. Plan-phase verifies whether /profile already surfaces streak_best.

### Q3: Where does the dropped avatar go?

| Option | Description | Selected |
|--------|-------------|----------|
| Nowhere on /path; defer relocation | Drop entirely. Avatar belongs to /profile. SPEC out-of-scope says no avatar redesign. | ✓ |
| Move to PathHeader as 'fox-mark' replacement | Replace static fox SVG in wordmark with user's equipped avatar. | |
| Bottom-right floating badge near ContinueAnchor | Small avatar badge anchored bottom-right. | |

**User's choice:** Nowhere on /path; defer relocation (recommended)
**Notes:** Locked → CONTEXT D-08. Equipped-cosmetic-frame users lose visual reward on /path until /profile redesign (Phase 14.5+).

### Q4 (follow-up): Where does the next-reward chip live?

| Option | Description | Selected |
|--------|-------------|----------|
| Inside HeroProgress, below the XP bar | Third row: 'Next reward at Lv {threshold} → {label}' chip. | ✓ |
| Inside PathHeader, between wordmark and LanternStreak | Second persistent header chip. | |
| Drop from 14.1, relocate later | Don't render on /path this phase. | |

**User's choice:** Inside HeroProgress, below the XP bar (recommended)
**Notes:** Locked → CONTEXT D-09.

---

## Verification gates

### Q1: Pixel-diff tolerance for the Playwright screenshot gate?

| Option | Description | Selected |
|--------|-------------|----------|
| 2% with smart masking | toHaveScreenshot maxDiffPixelRatio: 0.02 + mask regions for dynamic content. | ✓ |
| 1% strict, no masking | Hard gate, deterministic-state fixture infra needed. | |
| 3% lenient, no masking | Loose tolerance; defeats the gate. | |
| Drop pixel diff, rely on axe + manual visual verify | Skip Playwright screenshot gate entirely. | |

**User's choice:** 2% with smart masking (recommended)
**Notes:** Locked → CONTEXT D-10. Plan-phase enumerates 6-8 mask selectors during test authoring (Claude's Discretion).

### Q2: How to obtain the light-theme reference screenshot?

| Option | Description | Selected |
|--------|-------------|----------|
| Capture during plan-phase from demo's light frame | Re-run screenshot script with [data-theme='light']. ~15min plan-phase task. | ✓ |
| Hand-author from current /path migration | Build the implementation, screenshot it as reference. Circular. | |
| Drop light-theme pixel diff, rely on axe + dual-theme manual review | SPEC AC #3 amendment required. | |

**User's choice:** Capture during plan-phase from demo's light frame (recommended)
**Notes:** Locked → CONTEXT D-11. First plan-phase task captures _temp/path-redesign/demo-CA-hybrid-light.png.

### Q3: Bundle delta target for /path First Load JS?

| Option | Description | Selected |
|--------|-------------|----------|
| Soft cap +10KB, capture pre/post | Match SPEC's 'should not regress beyond ~10KB' as soft target. Reporting only. | ✓ |
| Hard cap +5KB, fail CI on overage | size-limit entry for /path. Likely blocks legitimate islands. | |
| Monitor only, no target | Capture pre/post but no specific gate. | |

**User's choice:** Soft cap +10KB, capture pre/post (recommended)
**Notes:** Locked → CONTEXT D-12. NOT a size-limit entry; reporting target only.

---

## Claude's Discretion

(Areas where user said "you decide" or deferred to Claude — see CONTEXT.md ## Claude's Discretion subsection.)

- Skeleton variant for kana-checkpoint pre-hydration paint
- Exact mask selectors for the screenshot diff
- getStarterSongs() warn-log mechanism (Pino vs console.warn)
- /kana script-param wiring (extend page.tsx or rely on store default)
- Tier-icon SVG sourcing (hand-author vs Lucide alternates)
- ka-flicker animation timing
- Kana basic-row count constants (reuse existing or hand-author)

## Deferred Ideas

(See CONTEXT.md `<deferred>` section for the full list.)

- StarterPick cover-art-as-background visual restyle (separate follow-up plan)
- Cross-surface PathHeader promotion to RootLayout (future polish phase)
- Avatar relocation to /profile (Phase 14.5+)
- streak_best historical-best surfacing on /profile if missing
- Mastered → /kana/session?mode=review one-tap drill
- Server-side kana_progress table (cross-device sync)
- Visible social activity (Phase 14.4)
- Streak behavioral systems (Phase 14.4)
- True progression gating (future-phase)
- New avatar tokens
- Backend / data-shape changes beyond starter + kana wiring
- PathHeader second header chip slots
- Hard /path bundle gate
- Custom tier-icon set across all surfaces
