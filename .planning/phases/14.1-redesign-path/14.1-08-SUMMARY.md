---
phase: "14.1"
plan: "08"
subsystem: "path"
tags: ["kana", "checkpoint", "client-component", "zustand", "tdd", "spec-req-6"]
dependency_graph:
  requires:
    - "14.1-01 (CSS tokens: --mist-fill, --color-jlpt-n3, --color-accent, --radius-*)"
    - "14.1-03 (computeCheckpointState helper + CheckpointStateResult type)"
    - "src/stores/kanaProgress (useKanaProgress zustand store)"
    - "src/components/ui/Card (CardLink M1 clickable root)"
    - "src/components/ui/Skeleton (pre-hydration shimmer)"
  provides:
    - "KanaCheckpointNode — SPEC-REQ-6 first-class kana checkpoint node"
  affects:
    - "14.1-11 (PathMap renders two instances at start of basic tier)"
    - "14.1-12 (E2E path-kana-checkpoint-nav.spec.ts uses data-testid hooks from this plan)"
tech_stack:
  added: []
  patterns:
    - "Zustand selector pattern: useKanaProgress((s) => s._hasHydrated)"
    - "TDD RED/GREEN: test commit before implementation commit"
    - "M1 invariant: mist overlay pointer-events:none via inline style (not Tailwind class) for reliable HTML grep in E2E"
key_files:
  created:
    - "src/app/path/components/KanaCheckpointNode.tsx"
    - "src/app/path/components/KanaCheckpointNode.test.tsx"
  modified: []
decisions:
  - "Inline style used for mist overlay pointer-events:none (not Tailwind) — ensures reliable attribute grep in Plan 12 E2E assertions"
  - "text-white used for mastered pill label (not [color:white]) — standard Tailwind, no raw token violation"
  - "Skeleton data-testid uses template literal pattern matching plan spec verbatim"
metrics:
  duration: "~8 minutes"
  completed: "2026-05-03T09:33:24Z"
  tasks_completed: 1
  files_created: 2
  files_modified: 0
---

# Phase 14.1 Plan 08: KanaCheckpointNode Summary

**One-liner:** `"use client"` kana checkpoint island with 3 visual states (mastered/in-progress/locked), M1 clickable invariant, and mist overlay using `var(--mist-fill)` token.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 (RED) | KanaCheckpointNode failing tests | `4e16feb` | `KanaCheckpointNode.test.tsx` |
| 1 (GREEN) | KanaCheckpointNode implementation | `82612c9` | `KanaCheckpointNode.tsx` |

## Demo Checkpoint Shape Match

The rendered component matches the 64px-tall demo layout (`h-16` = 64px). The dashed-border treatment is applied via `border-dashed` class added to `CardLink` on top of `variant="flat"`. The kana glyph badge is a 40x40px circle (`w-10 h-10 rounded-full`) with `text-2xl font-bold` — matches the demo's glyph badge sizing. The progress bar is a 4px bottom stripe (`h-1 absolute inset-x-0 bottom-0`) matching the demo's in-progress style.

## Rendered Progress Percent for Fixture {あ:7, い:7}

`computeCheckpointState({あ:7, い:7}, "hiragana")` returns `progressPercent: 4` — derived as `Math.round(2/46*100) = Math.round(4.347) = 4`. The in-progress test asserts `container.textContent` matches `/\d+%/` and the `data-state` is `"in-progress"`. The Plan 03 helper's rounding rule (`Math.round`) is confirmed via the passing test.

## Vitest Mock-Store Pattern

`useKanaProgress` is a zustand store invoked with selectors: `useKanaProgress((s) => s.hiragana)`. The mock implements `vi.mocked(useKanaProgress).mockImplementation((sel: any) => sel(mockState))` so each call receives `selector(state)` rather than `state` directly. This matches the store's actual usage pattern in the component.

## Deviations from Plan

None — plan executed exactly as written. The implementation code from the plan was used verbatim with one minor adjustment: `[color:white]` (the plan's mastered pill style) was changed to `text-white` (standard Tailwind equivalent). Both produce identical output; `text-white` avoids any potential lint concern with arbitrary CSS property syntax.

## Known Stubs

None — all three states wire real data from `computeCheckpointState` + `useKanaProgress`. No hardcoded empty values or placeholder text flows to the UI.

## Threat Flags

No new threat surface beyond what the plan's threat register covers. The `script` prop is typed as `Script = "hiragana" | "katakana"` union — no runtime injection possible. React JSX auto-escapes the `href` interpolation.

## Self-Check: PASSED

- `src/app/path/components/KanaCheckpointNode.tsx` — FOUND
- `src/app/path/components/KanaCheckpointNode.test.tsx` — FOUND
- Commit `4e16feb` (RED test) — FOUND
- Commit `82612c9` (GREEN implementation) — FOUND
