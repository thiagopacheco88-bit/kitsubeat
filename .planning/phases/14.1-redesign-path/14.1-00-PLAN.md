---
phase: 14.1
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - _temp/path-redesign/demo-CA-hybrid-light.png
  - _temp/path-redesign/screenshot-light.mjs
  - .planning/phases/14.1-redesign-path/14.1-baseline-bundle.txt
autonomous: true
requirements:
  - SPEC-REQ-1
  - SPEC-REQ-2
  - SPEC-REQ-3
  - SPEC-REQ-4
  - SPEC-REQ-5
  - SPEC-REQ-6
  - SPEC-REQ-7
  - SPEC-REQ-8
must_haves:
  truths:
    - "Light-theme reference PNG exists for AC #3 pixel-diff comparison"
    - "Pre-baseline /path First Load JS captured for D-12 +10KB delta reporting"
  artifacts:
    - path: "_temp/path-redesign/demo-CA-hybrid-light.png"
      provides: "Light-theme reference for path-visual-light.spec.ts"
    - path: ".planning/phases/14.1-redesign-path/14.1-baseline-bundle.txt"
      provides: "Pre-baseline /path First Load JS measurement"
  key_links:
    - from: "tests/e2e/path-visual-light.spec.ts (Wave 4)"
      to: "_temp/path-redesign/demo-CA-hybrid-light.png"
      via: "toHaveScreenshot reference path"
      pattern: "demo-CA-hybrid-light\\.png"
---

<objective>
Wave 0 prerequisites: capture a light-theme reference screenshot from the locked
`demo-CA-hybrid.html` prototype (per D-11) and capture the pre-Wave-1 bundle
baseline for `/path` (per D-12). Both artifacts are required BEFORE any UI work
lands so verification gates in Wave 4 have honest references.

Purpose: Without the light PNG, AC #3 (light-theme pixel-diff) cannot run.
Without the bundle baseline, D-12 +10KB delta reporting is impossible.

Output: `demo-CA-hybrid-light.png` (390×844 PNG) + `14.1-baseline-bundle.txt`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14.1-redesign-path/14.1-SPEC.md
@.planning/phases/14.1-redesign-path/14.1-CONTEXT.md

# Existing demo-capture script (template — modify or duplicate)
@_temp/path-redesign/screenshot-scrolled.mjs

# Locked demo (visual contract)
@_temp/path-redesign/demo-CA-hybrid.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture light-theme reference screenshot from demo-CA-hybrid.html</name>
  <read_first>
    - _temp/path-redesign/screenshot-scrolled.mjs (existing dark-theme capture; copy as template)
    - _temp/path-redesign/demo-CA-hybrid.html (verify it has data-theme attribute on root or body — locate where to inject [data-theme="light"])
    - _temp/path-redesign/demo-CA-hybrid.png (existing dark capture — confirm 390×844 dimensions to match)
  </read_first>
  <action>
Create `_temp/path-redesign/screenshot-light.mjs` by copying `screenshot-scrolled.mjs` and modifying it to:

1. Use Playwright's chromium (existing pattern in screenshot-scrolled.mjs).
2. Set viewport to exactly `{ width: 390, height: 844 }` (iPhone 14 Pro per D-10).
3. Open `_temp/path-redesign/demo-CA-hybrid.html` via `file://` URL.
4. Inject light theme BEFORE first paint via `await page.addInitScript(() => { document.documentElement.setAttribute('data-theme', 'light'); })` — must run before the React-on-Babel mount so first render uses the light tokens.
5. Wait for `networkidle` then `await page.waitForTimeout(500)` to let any animations settle.
6. `await page.screenshot({ path: '_temp/path-redesign/demo-CA-hybrid-light.png', fullPage: false })` — viewport-only, NOT full page (the dark reference is viewport-only — verify via `file ./demo-CA-hybrid.png` width=390).
7. Run via `node _temp/path-redesign/screenshot-light.mjs`.

If the demo HTML hard-codes its theme (no data-theme switch), ALSO patch the HTML file in-place during the script run by reading the file, running a regex replace adding `data-theme="light"` on the `<html>` tag, writing to a temp file, screenshotting that, then NOT modifying the original demo. The original `demo-CA-hybrid.html` MUST stay unchanged (it is the locked dark contract for AC #2).
  </action>
  <verify>
    <automated>node _temp/path-redesign/screenshot-light.mjs && test -f _temp/path-redesign/demo-CA-hybrid-light.png && node -e "const sharp = require('sharp'); sharp('_temp/path-redesign/demo-CA-hybrid-light.png').metadata().then(m => { if (m.width !== 390 || m.height !== 844) { console.error('Wrong dimensions:', m.width, m.height); process.exit(1); } else { console.log('OK 390x844'); } });"</automated>
  </verify>
  <acceptance_criteria>
    - `_temp/path-redesign/demo-CA-hybrid-light.png` exists with dimensions exactly 390×844.
    - `_temp/path-redesign/screenshot-light.mjs` exists and is a runnable Node script.
    - `_temp/path-redesign/demo-CA-hybrid.html` is byte-identical to its master commit (the dark contract is untouched).
    - Visual sanity check: open the light PNG; background is light cream/white (`--color-bg: #FAFAF9`), NOT dark (`#0E0E0E`).
  </acceptance_criteria>
  <done>Light-theme reference PNG committed to `_temp/path-redesign/`; ready for path-visual-light.spec.ts in Wave 4.</done>
</task>

<task type="auto">
  <name>Task 2: Capture pre-Wave-1 /path bundle baseline</name>
  <read_first>
    - .planning/phases/13-performance-infrastructure/13-CONTEXT.md (D-09 size-limit baseline approach — `npm run build` output parsing pattern)
    - package.json (confirm `npm run build` script + `npm run size` if present)
    - .size-limit.cjs (Phase 13 D-23 — confirm /path is NOT in the hard-gate list, only /songs/[slug] is at 50KB)
  </read_first>
  <action>
Run `npm run build` from project root (`C:/Users/thiag/velora-projects/kitsubeat`). Capture the Next.js build output's route table — specifically the `/path` line showing `Size` and `First Load JS`. Also capture the shared "First Load JS" baseline footer.

Write the captured measurements to `.planning/phases/14.1-redesign-path/14.1-baseline-bundle.txt` with this exact structure:

```
# /path bundle baseline — pre-Wave-1
# Captured: <ISO timestamp>
# Phase 14 master HEAD commit: <git rev-parse HEAD>

Route               Size       First Load JS
/path               <X> kB     <Y> kB

# Shared chunks
First Load JS shared by all   <Z> kB

# D-12 soft cap: +10 KB on /path First Load JS
# Wave 4 verification compares against Y above
```

Substitute `<X>`, `<Y>`, `<Z>` with the actual numbers from `npm run build` output.
Substitute the timestamp via `new Date().toISOString()` and HEAD via `git rev-parse HEAD`.

If `npm run build` fails for unrelated reasons (e.g., D-PRE-04 Clerk WIP files from STATE.md), document the failure mode in the baseline file under a `# Build failure note` section AND fall back to capturing the route bundle via `npx next build --debug 2>&1 | grep -A2 '/path'` if that works. If neither works, write `BASELINE_CAPTURE_FAILED` and document the cause; Wave 4 will re-baseline when the build is unblocked.
  </action>
  <verify>
    <automated>test -f .planning/phases/14.1-redesign-path/14.1-baseline-bundle.txt && grep -E '(/path|BASELINE_CAPTURE_FAILED)' .planning/phases/14.1-redesign-path/14.1-baseline-bundle.txt</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/14.1-redesign-path/14.1-baseline-bundle.txt` exists.
    - File contains either a `/path` row with a numeric `First Load JS` value OR the `BASELINE_CAPTURE_FAILED` marker with a documented cause.
    - File contains the `git rev-parse HEAD` SHA at capture time.
  </acceptance_criteria>
  <done>Bundle baseline persisted for D-12 reporting in Wave 4; Wave 1 can now land safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→git | Generated PNG + script files committed to repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.1.00-01 | I (Information disclosure) | screenshot-light.mjs | accept | Demo HTML is public prototype with no real user data; screenshot captures only the locked design |
| T-14.1.00-02 | T (Tampering) | demo-CA-hybrid.html | mitigate | Script MUST NOT modify the original demo file in-place; original is the locked AC #2 dark contract |
</threat_model>

<verification>
- `_temp/path-redesign/demo-CA-hybrid-light.png` exists at 390×844
- `_temp/path-redesign/demo-CA-hybrid.html` unchanged from master HEAD (`git diff` shows zero changes to that file)
- `.planning/phases/14.1-redesign-path/14.1-baseline-bundle.txt` exists with a `/path` row OR documented capture failure
</verification>

<success_criteria>
Wave 1 plans (01, 02, 03) can begin in parallel; Wave 4 plan (12) has both reference artifacts it needs.
</success_criteria>

<output>
After completion, create `.planning/phases/14.1-redesign-path/14.1-00-SUMMARY.md` documenting:
- Light PNG dimensions verified
- Pre-baseline `/path` First Load JS in KB (or capture-failure cause)
- Any deviations from this plan
</output>
