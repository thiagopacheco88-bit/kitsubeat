---
phase: 14.1-redesign-path
plan: "00"
subsystem: infra
tags: [playwright, screenshot, bundle-baseline, next-js, wave-0, ca-hybrid]

# Dependency graph
requires:
  - phase: 14.0-design-tokens
    provides: "Phase 14 design tokens (light/dark theme CSS vars, demo-CA-hybrid.html locked prototype)"
  - phase: 13-performance-infrastructure
    provides: "NEXT_PUBLIC_APP_ENV=production build pattern for consistent bundle measurement"
provides:
  - "Light-theme reference PNG at 390x844 for path-visual-light.spec.ts (Wave 4)"
  - "Pre-Wave-1 /path First Load JS baseline: 118 kB (D-12 soft cap breach threshold: 128 kB)"
  - "screenshot-light.mjs: reusable Playwright script targeting IosFrame screen element"
affects:
  - "14.1-12-PLAN.md (Wave 4) — path-visual-light.spec.ts consumes demo-CA-hybrid-light.png"
  - All Wave 1-3 plans (can now land; pre-baseline captured before UI changes)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "IosFrame screen element targeting: locator('div[style*=\"width: 390px\"][style*=\"height: 844px\"]').nth(1) captures light phone from side-by-side demo"
    - "NEXT_PUBLIC_APP_ENV=production for consistent Next.js production bundle measurement (plain npm run build fails on kana/session/summary prerender)"

key-files:
  created:
    - "_temp/path-redesign/screenshot-light.mjs — Playwright script capturing light IosFrame at 390x844"
    - "_temp/path-redesign/demo-CA-hybrid-light.png — Light-theme reference PNG (390x844, #FAFAF9 background)"
    - ".planning/phases/14.1-redesign-path/14.1-baseline-bundle.txt — Pre-Wave-1 /path bundle baseline"
  modified: []

key-decisions:
  - "Targeted the 2nd IosFrame screen div (390x844) instead of [data-theme=light] content div (756px height) to capture full phone screen including status bar"
  - "Used NEXT_PUBLIC_APP_ENV=production to reproduce Phase 13 measurement conditions; plain npm run build fails on kana/session/summary webpack TypeError (pre-existing WIP issue)"

patterns-established:
  - "Wave 0 artifact commits before any UI work: ensures Wave 4 verification gates have honest pre-change references"

requirements-completed: [SPEC-REQ-1, SPEC-REQ-2, SPEC-REQ-3, SPEC-REQ-4, SPEC-REQ-5, SPEC-REQ-6, SPEC-REQ-7, SPEC-REQ-8]

# Metrics
duration: 17min
completed: 2026-05-03
---

# Phase 14.1 Plan 00: Wave 0 Prerequisites Summary

**Light-theme reference PNG (390x844) and pre-Wave-1 /path First Load JS baseline (118 kB) captured before any UI work lands**

## Performance

- **Duration:** 17 min
- **Started:** 2026-05-03T09:05:58Z
- **Completed:** 2026-05-03T09:22:55Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Created `screenshot-light.mjs` using Playwright to target the 2nd IosFrame screen div (the light phone) in the side-by-side demo, capturing it at exactly 390x844 (iPhone 14 Pro dimensions)
- Committed `demo-CA-hybrid-light.png` at 390x844 with verified #FAFAF9 (light cream) background; `demo-CA-hybrid.html` left byte-identical (locked dark contract for AC #2, T-14.1.00-02 mitigated)
- Captured `/path First Load JS = 118 kB` via `NEXT_PUBLIC_APP_ENV=production npm run build`; D-12 soft cap breach threshold = 128 kB; `First Load JS shared by all = 102 kB`

## Task Commits

Each task was committed atomically:

1. **Task 1: Capture light-theme reference screenshot** - `f02f060` (feat)
2. **Task 2: Capture pre-Wave-1 /path bundle baseline** - `fbedd99` (chore)

**Plan metadata:** (docs commit follows this SUMMARY)

## Files Created/Modified

- `_temp/path-redesign/screenshot-light.mjs` - Playwright script; locates 2nd IosFrame screen div (390x844), screenshots it, saves to demo-CA-hybrid-light.png
- `_temp/path-redesign/demo-CA-hybrid-light.png` - Light-theme reference PNG, 390x844, background pixel at (50,50) = rgb(250,250,249) = #FAFAF9
- `.planning/phases/14.1-redesign-path/14.1-baseline-bundle.txt` - Pre-Wave-1 bundle baseline with /path size (3.22 kB) and First Load JS (118 kB)

## Decisions Made

- **IosFrame screen targeting strategy:** The demo shows both dark and light phones side-by-side. `[data-theme="light"]` content div is 390x756 (excludes status bar). Targeting the 2nd `div[style*="width: 390px"][style*="height: 844px"]` captures the full IosFrame screen at the correct 390x844 dimensions.
- **Production env build required:** `npm run build` fails with `TypeError: Cannot read properties of undefined (reading 'call')` at `kana/session/summary` prerender — a pre-existing webpack issue from uncommitted WIP (ThemeToggle.tsx Clerk import, git status shows `M src/lib/user-prefs.ts`). `NEXT_PUBLIC_APP_ENV=production` reproduces the exact build conditions used in Phase 13 Plan 03 and produces the route table successfully.

## Deviations from Plan

None - plan executed exactly as written.

The build failure on plain `npm run build` was anticipated by the plan ("If `npm run build` fails for unrelated reasons (e.g., D-PRE-04 Clerk WIP files from STATE.md), document..."). The plan's fallback path led to `NEXT_PUBLIC_APP_ENV=production npm run build` which succeeded and produced accurate measurements.

## Issues Encountered

- `_temp/path-redesign/` did not exist in the worktree (reset to base commit 4f62edf which predates the untracked `_temp/` directory). Resolved by copying from main project working tree before running the screenshot script.
- Plain `npm run build` failed on kana/session/summary prerender — pre-existing issue in main project working tree. Resolved using `NEXT_PUBLIC_APP_ENV=production` per Phase 13 measurement pattern.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Wave 1 plans (14.1-01, 14.1-02, 14.1-03) can now begin in parallel — pre-baseline captured.
- Wave 4 plan (14.1-12) has both reference artifacts needed:
  - `_temp/path-redesign/demo-CA-hybrid-light.png` → for `path-visual-light.spec.ts`
  - `.planning/phases/14.1-redesign-path/14.1-baseline-bundle.txt` → D-12 +10KB delta reporting

## Self-Check: PASSED

- `_temp/path-redesign/screenshot-light.mjs` - FOUND
- `_temp/path-redesign/demo-CA-hybrid-light.png` - FOUND (390x844 verified)
- `.planning/phases/14.1-redesign-path/14.1-baseline-bundle.txt` - FOUND (contains /path row)
- `f02f060` (Task 1 commit) - FOUND in git log
- `fbedd99` (Task 2 commit) - FOUND in git log

---
*Phase: 14.1-redesign-path*
*Completed: 2026-05-03*
