---
status: partial
phase: 14.1-redesign-path
source: [14.1-VERIFICATION.md]
started: 2026-05-03T11:30:00Z
updated: 2026-05-03T11:30:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. W-2 pre-baseline pixeldiff gate
expected: `node scripts/debug/path-baseline-pixeldiff.mjs` produces a diff PNG showing /path matches the locked baseline (`_temp/path-redesign/demo-CA-hybrid-light.png`) within tolerance. Confirms the new design hasn't drifted before baselines are locked.
result: [pending]

### 2. Visual diff dark theme (AC #2)
expected: Run `npx playwright test path-visual-dark --update-snapshots` to lock the dark-theme baseline screenshot, then re-run `npx playwright test path-visual-dark` and observe ≤2% pixel-diff tolerance. W-5 mask should hide only the `lantern-streak-count` text, leaving the SVG lantern visible.
result: [pending]

### 3. Visual diff light theme (AC #3)
expected: Same protocol as Test 2 but for `path-visual-light`. Confirms light-theme tokens render correctly with the +30% alpha convention from CONTEXT D-04.
result: [pending]

### 4. axe-core accessibility audit (AC #5 + AC #6)
expected: `RUN_A11Y=1 npx playwright test path-a11y.spec.ts` returns 0 serious + 0 critical violations and ≤5 minor violations across both dark and light themes.
note: Code review WR-02 (PathMap.tsx nested `role="list"` without `role="listitem"` children at line 89) will likely surface here and consume the minor-violations budget. Recommend fixing WR-02 before running.
result: [pending]

### 5. Seeded-DB integration tests (AC #1 full path + AC #10 StarterPick)
expected: With `TEST_DATABASE_URL` set and seed data loaded, run `npm run test -- tests/integration/getStarterSongs.test.ts` and observe the 3 starter songs are returned. AC #10 (StarterPick suppression) requires the same seeded DB.
note: This currently fails because the test database is empty. Run `npm run test:seed` first.
result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps

(none — all SPEC-REQ-1..8 verified statically per 14.1-VERIFICATION.md)
